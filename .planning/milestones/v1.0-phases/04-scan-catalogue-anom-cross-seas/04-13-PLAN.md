---
phase: 04-scan-catalogue-anom-cross-seas
plan: 13
type: execute
wave: 9
gap_closure: true
depends_on:
  - 04-11
  - 04-12
files_modified:
  - crates/miner-core/src/engine/mod.rs
  - crates/miner-core/src/reader.rs
  - crates/miner-core/src/scan/anom/adf/mod.rs
  - crates/miner-core/src/scan/anom/adf/kernel.rs
  - crates/miner-core/src/scan/anom/arch_lm/mod.rs
  - crates/miner-core/src/scan/anom/arch_lm/kernel.rs
  - crates/miner-core/src/scan/anom/jarque_bera/kernel.rs
  - crates/miner-core/src/scan/anom/kpss/mod.rs
  - crates/miner-core/src/scan/anom/kpss/kernel.rs
  - crates/miner-core/src/scan/anom/variance_ratio/mod.rs
  - crates/miner-core/src/scan/anom/variance_ratio/kernel.rs
  - crates/miner-core/src/scan/cross/lead_lag/mod.rs
  - crates/miner-core/src/scan/cross/lead_lag/kernel.rs
  - crates/miner-core/src/scan/ljung_box/mod.rs
  - crates/miner-core/src/scan/seas/mod.rs
  - crates/miner-core/src/scan/seas/anova_kw/mod.rs
  - crates/miner-core/src/scan/seas/anova_kw/kernel.rs
  - crates/miner-core/src/scan/seas/eom_som/mod.rs
  - crates/miner-core/src/scan/seas/event_window/mod.rs
  - crates/miner-core/src/scan/seas/event_window/kernel.rs
autonomous: true
requirements: []
success_criteria:
  - "CI Gate 2 (`cargo clippy --workspace --all-targets -- -D warnings`) exits 0 on a clean working tree. Currently fails with 88 clippy::pedantic errors in miner-core (lib). All errors resolved either by mechanical fix (docstring backticks, or_default, let-else, mechanical-iter, range-contains, if-let) or by `#[allow(clippy::<lint>, reason = \"<rationale>\")]` with a behaviour-preserving rationale for the small set of intentional patterns (large-sample usize→f64 casts in stats kernels, ≥100-line closed-form regression bodies)."
  - "cargo test --workspace --no-fail-fast continues to pass at the same count as before Plan 04-13 (modulo Plan 04-11's three #[ignore]d goldens which stay ignored). No new test runs, no test changes — this plan does not touch test files except where a public-surface rename (e.g., binding `s` → `samp`) requires it."
  - "cargo run -p xtask -- gen-schema && git diff --exit-code schemas/ exits 0. This plan touches NO schemars-derived types, so the schema diff must stay clean."
  - "cargo fmt --all -- --check exits 0 after the changes (the formatter is the tiebreaker for any layout question)."
  - "STATE.md is updated to reflect that Plan 04-13 closes the CI Gate 2 redness that STATE.md itself called out as deferred to Phase 7; the 'cargo clippy -D warnings workspace cleanup deferred to Phase 7 hardening' note is reversed for the miner-core lib (other crates are not pedantic-dirty per Plan 04-13 inventory, but Phase 7 still owns the deny-warnings audit across binaries and tests)."
tags:
  - rust
  - gap-closure
  - clippy
  - ci-unblock
  - hygiene

must_haves:
  truths:
    - "User pushes any commit and the CI build succeeds at Gate 2 (`cargo clippy --workspace --all-targets -- -D warnings`). Before this plan, CI has been red on Gate 2 across every Phase-4 commit; the redness is a hard blocker for Phase 5 because phase plans assume green CI as the safety net for atomic-per-task commits."
    - "User runs `cargo clippy --workspace --all-targets -- -D warnings` locally on a fresh checkout and observes zero errors and zero warnings."
    - "User reads `crates/miner-core/src/scan/anom/arch_lm/kernel.rs:76`, `crates/miner-core/src/scan/seas/anova_kw/mod.rs:113`, `crates/miner-core/src/scan/anom/kpss/kernel.rs:81`/`117`, and `crates/miner-core/src/scan/cross/lead_lag/kernel.rs:150` and finds an `#[allow(clippy::<lint>, reason = \"...\")]` annotation explaining why the pattern is intentional. No `#[allow]` annotation is added without a `reason = ...` field (matches the `reader.rs:198-258`-style discipline already used in `clippy.toml`)."
    - "User runs cargo test --workspace and observes the SAME number of passing tests as before Plan 04-13 (modulo the existing 3 #[ignore]d goldens which remain unchanged). No behaviour drift from the lint cleanup."
  artifacts:
    - path: "crates/miner-core/src/engine/mod.rs"
      provides: "9 or_insert_with(Foo::default) call-sites converted to or_default(); 4 doc_markdown backtick fixes in the RunOutcome/preflight docs; needless_pass_by_value on `cancel: Arc<AtomicBool>` resolved (either &Arc or `Arc::clone(&cancel)` at the call site — Task 6 picks)."
      contains: "or_default"
    - path: "crates/miner-core/src/scan/anom/arch_lm/kernel.rs"
      provides: "`#[allow(clippy::too_many_lines, reason = \"closed-form Engle 1982 derivation\")]` on the kernel body that runs 113 lines; `#[allow(clippy::many_single_char_names)]` on the regression locals (y, x, β, Σ, e — the canonical Engle notation); doc_markdown backticks; manual_let_else rewrite for `Ok(fit)`."
      contains: "too_many_lines"
    - path: "crates/miner-core/src/scan/anom/kpss/kernel.rs"
      provides: "`#[allow(clippy::cast_precision_loss, reason = \"sample size fits in f64 mantissa for n ≤ 2^53 ≈ 9e15\")]` on the two `n as f64` sites (lines 81 and 117); two `needless_range_loop`s rewritten to `for (i, &yi) in y.iter().enumerate()` (lines 162 and 181 retain index-and-value access)."
      contains: "cast_precision_loss"
    - path: "crates/miner-core/src/scan/cross/lead_lag/kernel.rs"
      provides: "`#[allow(clippy::cast_possible_truncation, reason = \"lag k is bounded by max_lag (CLI-validated <= n/4 << usize::MAX)\")]` on the `k.unsigned_abs() as usize` cast at line 150."
      contains: "cast_possible_truncation"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "crates/miner-core/src/**"
      via: "Gate 2 `cargo clippy --workspace --all-targets -- -D warnings` — after Plan 04-13 lands, every push gets a green Gate 2 on miner-core"
      pattern: "cargo clippy --workspace --all-targets -- -D warnings"
    - from: "Cargo.toml [workspace.lints.clippy]"
      to: "all clippy::pedantic lints"
      via: "`pedantic = { level = \"warn\", priority = -1 }` propagates to every workspace crate; combined with CI's `-D warnings` every pedantic finding is a hard error"
      pattern: "pedantic"
---

<objective>
Resolve the 88 `clippy::pedantic` errors that have been failing CI Gate 2 (`cargo clippy --workspace --all-targets -- -D warnings`) since Phase 4 implementation began. The Phase 4 plans deferred this cleanup to Phase 7 hardening (per `.planning/STATE.md` "cargo clippy -D warnings workspace cleanup deferred to Phase 7 hardening"), but CI red is a hard blocker for Phase 5: the per-task atomic-commit discipline that GSD relies on is unsafe when the baseline build is failing — there is no signal to confirm a commit didn't introduce a new regression because every commit shows the same 88-error fingerprint.

The 88 errors are all in `miner-core` lib (zero in `miner-cli`, `miner-mcp`, `miner-http`, `miner-reader-dukascopy`, `miner-bench`, or any tests). The distribution:

| Lint | Count | Disposition |
|------|-------|-------------|
| `clippy::doc_markdown` | 54 | Mechanical: wrap identifiers in backticks. |
| `clippy::unwrap_or_default` (formerly `or_insert_with(Foo::default)`) | 9 | Mechanical: `.or_default()`. |
| `clippy::similar_names` | 5 | Rename one binding (e.g. `g` → `grp`) or `#[allow]` with rationale for canonical statistical notation. |
| `clippy::manual_let_else` | 4 | Mechanical: `let Ok(x) = ... else { continue };` |
| `clippy::many_single_char_names` | 2 | `#[allow]` with rationale for canonical statistical notation (Engle ARCH-LM uses y, x, β, Σ, e; ADF uses the same). |
| `clippy::too_many_lines` | 2 | `#[allow]` with rationale for closed-form derivation bodies (ARCH-LM kernel + ANOVA-KW Scan::run). |
| `clippy::cast_precision_loss` | 2 | `#[allow]` with rationale: sample sizes fit in f64 mantissa for n ≤ 2^53. |
| `clippy::needless_range_loop` | 3 | Mechanical: `for (i, &x) in slice.iter().enumerate()`. |
| `clippy::explicit_iter_loop` | 2 | Mechanical: drop the `.iter()`. |
| `clippy::cast_possible_truncation` | 1 | `#[allow]` with rationale: lag index is CLI-bounded. |
| `clippy::single_match` | 1 | Mechanical: `if let`. |
| `clippy::needless_pass_by_value` | 1 | Mechanical: `&Arc<AtomicBool>` parameter type. |
| `clippy::manual_range_contains` | 1 | Mechanical: `(1..=MAX).contains(&v)`. |
| `clippy::items_after_statements` | 1 | Mechanical: hoist the `const` to the function head. |
| **TOTAL** | **88** | |

`#[allow]` is used for SIX errors (across four sites) where the pattern is intentional: closed-form regression bodies that exceed clippy's 100-line cap (refactoring would obscure the math), `usize → f64` casts on sample sizes (statistically safe for any series we'd ever scan), `u64 → usize` cast on a CLI-bounded lag index, and the canonical Engle/Dickey-Fuller single-character notation. Every `#[allow]` carries a `reason = "..."` field per the existing `clippy.toml` discipline.

The remaining 82 errors are mechanical fixes that do not change runtime behaviour.

Out of scope (deferred to Phase 7 hardening):
- `clippy::pedantic` audit across `miner-cli`, `miner-mcp`, `miner-http`, `miner-reader-dukascopy`, `miner-bench` — none of those crates currently trip a pedantic lint per Plan 04-13's inventory, but Phase 7's deny-warnings sweep formalises the audit.
- `cargo deny` advisory checks (CLAUDE.md mentions these as Phase 7 hygiene tools).
- Walking back `[workspace.lints.clippy] pedantic = warn` to a curated allowlist — explicitly rejected; the team's quality bar is `pedantic` and Plan 04-13 commits to meeting it.
- WR-01..WR-08 from 04-REVIEW.md (envelope-shape concerns) — those are real REVIEW findings tracked separately, not lint cleanup.
</objective>

<context>
@.planning/STATE.md   # "cargo clippy -D warnings workspace cleanup deferred to Phase 7 hardening; only 3 in-scope LN_2 lints in drawdown/kernel.rs fixed."
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-11-PHASE-VERIFICATION.md   # Phase 4 sign-off; CI gate 2 status not explicitly verified
@.github/workflows/ci.yml   # Gate 2 specification (line 42-43)
@Cargo.toml   # [workspace.lints.clippy] pedantic = warn at workspace level
@clippy.toml   # disallowed-macros stays as-is; pedantic config is in Cargo.toml
@crates/miner-core/src/engine/mod.rs   # 13 errors (4 doc + 9 or_default + 1 needless_pass_by_value)
@crates/miner-core/src/scan/anom/adf/   # 18 errors (15 doc + 3 let-else + 1 single_match + 1 many-char)
@crates/miner-core/src/scan/anom/arch_lm/   # 9 errors (4 doc + 1 too_many_lines + 1 let-else + 1 many-char + 1 similar)
@crates/miner-core/src/scan/anom/kpss/   # 16 errors (10 doc + 2 cast_precision + 2 range_loop + 2 similar)
@crates/miner-core/src/scan/anom/variance_ratio/   # 5 errors (3 doc + 1 range_loop + 1 similar)
@crates/miner-core/src/scan/cross/lead_lag/   # 3 errors (2 doc + 1 cast_truncation)
@crates/miner-core/src/scan/seas/anova_kw/   # 6 errors (4 doc + 1 too_many_lines + 1 explicit_iter_loop)
@crates/miner-core/src/scan/seas/eom_som/   # 2 errors (1 items_after_statements + 1 manual_range_contains)
@crates/miner-core/src/scan/seas/event_window/   # 3 errors (1 doc + 1 similar + 1 explicit_iter_loop)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: doc_markdown — wrap identifiers in backticks (54 errors, 16 files)</name>
  <files>
    crates/miner-core/src/engine/mod.rs,
    crates/miner-core/src/reader.rs,
    crates/miner-core/src/scan/anom/adf/mod.rs,
    crates/miner-core/src/scan/anom/adf/kernel.rs,
    crates/miner-core/src/scan/anom/arch_lm/mod.rs,
    crates/miner-core/src/scan/anom/arch_lm/kernel.rs,
    crates/miner-core/src/scan/anom/jarque_bera/kernel.rs,
    crates/miner-core/src/scan/anom/kpss/mod.rs,
    crates/miner-core/src/scan/anom/kpss/kernel.rs,
    crates/miner-core/src/scan/anom/variance_ratio/mod.rs,
    crates/miner-core/src/scan/anom/variance_ratio/kernel.rs,
    crates/miner-core/src/scan/cross/lead_lag/mod.rs,
    crates/miner-core/src/scan/ljung_box/mod.rs,
    crates/miner-core/src/scan/seas/mod.rs,
    crates/miner-core/src/scan/seas/anova_kw/mod.rs,
    crates/miner-core/src/scan/seas/event_window/mod.rs
  </files>
  <read_first>
    - /tmp/clippy-all.txt (full enumeration of 54 doc_markdown sites with file:line:col)
    - Each of the 16 files at the cited line numbers; the fix is always wrapping a bare identifier (e.g. `RawArray`, `param_schema`, `hour_of_day`, `MacKinlay`, `LjungBox`, `series_kind`, `event_timestamps`, `par_iter`, `cutoff_n=3`, `N_eff`, `anova_kw`, `eom_som`, `day_of_week`) in backticks.
  </read_first>
  <behavior>
    - For each of the 54 doc_markdown errors, locate the docstring line and wrap the offending identifier in backticks.
    - DO NOT change docstring content beyond the backtick wrap — no rewording, no link expansion, no shortening.
    - DO NOT touch non-docstring lines.
    - After all 54 edits, run `cargo clippy --workspace --all-targets -- -D warnings 2>&1 | grep "missing backticks" | wc -l` and confirm the count is zero. The total clippy error count should drop from 88 to 34.
    - Verify with `cargo build --workspace` clean compile (docstrings can't break compilation but the build confirms no accidental code edits).
  </behavior>
  <commit_message>
    style(04-13): wrap identifiers in backticks in miner-core doc comments

    Resolves 54 clippy::doc_markdown errors across 16 files in miner-core
    lib. Pure docstring edits — no behaviour change, no API change. Knocks
    CI gate 2 (cargo clippy -D warnings) error count from 88 → 34.

    Identifiers backticked: RawArray, param_schema, hour_of_day,
    day_of_week, eom_som, anova_kw, event_timestamps, series_kind, N_eff,
    LjungBox, MacKinlay, par_iter, and the rest enumerated in
    /tmp/clippy-all.txt at plan time.

    Part of Plan 04-13 (CI clippy::pedantic unblock).
  </commit_message>
</task>

<task type="auto" tdd="false">
  <name>Task 2: or_default — replace 9 or_insert_with(Foo::default) call-sites in engine/mod.rs</name>
  <files>crates/miner-core/src/engine/mod.rs</files>
  <read_first>
    - crates/miner-core/src/engine/mod.rs at lines 678, 695, 749, 792, 824, 853, 862, 887, 906 (all 9 `or_insert_with(...)` sites)
  </read_first>
  <behavior>
    - Each of the 9 sites uses `.or_insert_with(SomeType::default)` or `.or_insert_with(|| Default::default())` to materialise a default value on first map access (the `BTreeMap<String, PerScanCounts>` `per_scan` counter map).
    - Replace each with `.or_default()` — semantically identical, idiomatic, clippy-clean.
    - DO NOT touch other call-sites or the surrounding map manipulation.
    - After the 9 edits, run `cargo clippy --workspace --all-targets -- -D warnings 2>&1 | grep "or_insert_with" | wc -l` and confirm zero.
    - Confirm tests still pass: `cargo test -p miner-core --test engine_summary_aggregation 2>&1 | tail -5` (or whichever engine summary test exercises per_scan counters; check for any test that touches `PerScanCounts` aggregation).
  </behavior>
  <commit_message>
    style(04-13): replace or_insert_with(Foo::default) with or_default() in engine

    Resolves 9 clippy::unwrap_or_default errors in
    crates/miner-core/src/engine/mod.rs. Mechanical: every site is a
    BTreeMap<String, PerScanCounts> insertion path; the rewrite is
    semantically identical and idiomatic.

    Part of Plan 04-13 (CI clippy::pedantic unblock). Drops clippy error
    count from 34 → 25.
  </commit_message>
</task>

<task type="auto" tdd="false">
  <name>Task 3: manual_let_else — rewrite 4 fallible matches as let-else</name>
  <files>
    crates/miner-core/src/scan/anom/adf/kernel.rs,
    crates/miner-core/src/scan/anom/arch_lm/kernel.rs
  </files>
  <read_first>
    - crates/miner-core/src/scan/anom/adf/kernel.rs at lines 165, 203, 340 (3 sites — two `fit_adf_regression` loop heads + one terminal site)
    - crates/miner-core/src/scan/anom/arch_lm/kernel.rs at line 154 (1 site)
  </read_first>
  <behavior>
    - Each site currently has the shape:
      ```rust
      let fit = match fit_adf_regression(y, k, regression) {
          Ok(f) => f,
          Err(_) => continue,
      };
      ```
    - Rewrite as:
      ```rust
      let Ok(fit) = fit_adf_regression(y, k, regression) else { continue };
      ```
    - Preserve the exact error-arm action (`continue` for the loop sites; the terminal site at adf/kernel.rs:340 may use `return` — check carefully).
    - DO NOT change behaviour: the `let-else` arm must take the SAME branch (continue / return / break) as the original `Err(_) =>` arm.
    - After the 4 edits, run `cargo clippy ... -- -D warnings 2>&1 | grep "let\\.\\.\\.else" | wc -l` and confirm zero.
    - Run `cargo test -p miner-core --tests adf 2>&1 | tail` and `cargo test -p miner-core --tests arch_lm 2>&1 | tail` (or the closest equivalent test names) to confirm ADF AIC lag selection still picks the same lag and ARCH-LM stats still match.
  </behavior>
  <commit_message>
    refactor(04-13): rewrite manual match-and-continue as let-else in adf/arch_lm kernels

    Resolves 4 clippy::manual_let_else errors:
    - scan/anom/adf/kernel.rs lines 165, 203, 340 (fit_adf_regression callers)
    - scan/anom/arch_lm/kernel.rs line 154

    Semantically identical: `match ... { Ok(x) => x, Err(_) => continue }`
    becomes `let Ok(x) = ... else { continue }`. The fallible-call arms
    keep their original control flow (continue / return).

    Part of Plan 04-13 (CI clippy::pedantic unblock). Drops error count
    from 25 → 21.
  </commit_message>
</task>

<task type="auto" tdd="false">
  <name>Task 4: similar_names + many_single_char_names — rename or allow with rationale (7 errors)</name>
  <files>
    crates/miner-core/src/scan/anom/adf/kernel.rs,
    crates/miner-core/src/scan/anom/arch_lm/kernel.rs,
    crates/miner-core/src/scan/anom/kpss/kernel.rs,
    crates/miner-core/src/scan/anom/variance_ratio/mod.rs,
    crates/miner-core/src/scan/seas/event_window/kernel.rs
  </files>
  <read_first>
    - crates/miner-core/src/scan/anom/adf/kernel.rs at line 264 (`5 bindings with single-character names in scope` — the canonical Dickey-Fuller regression locals)
    - crates/miner-core/src/scan/anom/arch_lm/kernel.rs at line 84 (`5 bindings with single-character names in scope` — Engle 1982 ARCH-LM regression locals) and line 153 (`binding's name is too similar to existing binding`)
    - crates/miner-core/src/scan/anom/kpss/kernel.rs at lines 161, 172 (`binding's name is too similar to existing binding`)
    - crates/miner-core/src/scan/anom/variance_ratio/mod.rs at line 154 (`binding's name is too similar to existing binding`)
    - crates/miner-core/src/scan/seas/event_window/kernel.rs at line 97 (`binding's name is too similar to existing binding`)
  </read_first>
  <behavior>
    - **`many_single_char_names` (2 sites — adf/kernel.rs:264, arch_lm/kernel.rs:84):** Both are closed-form OLS regression kernels using the CANONICAL statistical notation (y, x, β, Σ, e or y, x, k, t, n — names that match the literature reference cited in the file's module docstring). Renaming would obscure the formula. Add a function-level `#[allow(clippy::many_single_char_names, reason = "canonical Dickey-Fuller / ARCH-LM regression notation matches Said-Dickey 1984 / Engle 1982 derivation; renaming obscures the formula")]`. Place ABOVE the `fn` signature, NOT at the binding sites.
    - **`similar_names` (5 sites):** For each, prefer a short clarifying rename over `#[allow]`:
      - `kpss/kernel.rs:161` and `:172` — likely a tight `s` / `sk` or similar pair; rename the inner one to a more descriptive name (e.g. `partial_sum` → `partial_sum_k`).
      - `arch_lm/kernel.rs:153` — likely `e` / `ee` (residual / residual-squared); rename to `resid_sq` or similar.
      - `variance_ratio/mod.rs:154` — check context; rename if cheap or `#[allow]` with rationale.
      - `event_window/kernel.rs:97` — check context; rename or `#[allow]` with rationale.
    - For each `#[allow]` added, include a `reason = "..."` field per `clippy.toml` discipline.
    - DO NOT touch the actual numerical kernel — only rename local bindings or add `#[allow]` attributes.
    - After edits, run `cargo clippy ... -- -D warnings 2>&1 | grep -E "similar|single-character" | wc -l` and confirm zero.
    - Run the affected scan tests (`cargo test -p miner-core --tests adf arch_lm kpss variance_ratio event_window 2>&1 | tail`) to confirm renamed bindings still resolve correctly. If a renamed binding is part of a struct field name or a `pub use`, revert that rename and add `#[allow(clippy::similar_names, reason = "...")]` instead.
  </behavior>
  <commit_message>
    refactor(04-13): rename ambiguous bindings + allow canonical stat notation

    Resolves 7 clippy errors (5x similar_names + 2x many_single_char_names)
    in miner-core stat kernels. Two dispositions:

    1. Renames where the original binding was genuinely ambiguous
       (kpss/kernel.rs lines 161, 172; arch_lm/kernel.rs line 153;
       variance_ratio/mod.rs line 154; event_window/kernel.rs line 97).
    2. `#[allow(clippy::many_single_char_names, reason = "canonical
       Dickey-Fuller / ARCH-LM regression notation")]` on adf and arch_lm
       kernels — renaming y, x, β, Σ, e would obscure the formula that
       matches the cited literature.

    Every #[allow] carries a `reason = ...` field per clippy.toml discipline.
    Part of Plan 04-13 (CI clippy::pedantic unblock). Drops error count
    from 21 → 14.
  </commit_message>
</task>

<task type="auto" tdd="false">
  <name>Task 5: cast and too_many_lines — allow with rationale (5 errors)</name>
  <files>
    crates/miner-core/src/scan/anom/arch_lm/kernel.rs,
    crates/miner-core/src/scan/anom/kpss/kernel.rs,
    crates/miner-core/src/scan/cross/lead_lag/kernel.rs,
    crates/miner-core/src/scan/seas/anova_kw/mod.rs
  </files>
  <read_first>
    - crates/miner-core/src/scan/anom/arch_lm/kernel.rs at line 76 (`this function has too many lines (113/100)` — the ARCH-LM regression body)
    - crates/miner-core/src/scan/seas/anova_kw/mod.rs at line 113 (`this function has too many lines (113/100)` — the `Scan::run` body for ANOVA-KW)
    - crates/miner-core/src/scan/anom/kpss/kernel.rs at lines 81 and 117 (two `casting usize to f64 causes a loss of precision` — sample size cast)
    - crates/miner-core/src/scan/cross/lead_lag/kernel.rs at line 150 (`casting u64 to usize may truncate` — `k.unsigned_abs() as usize` for lag indexing)
  </read_first>
  <behavior>
    - For each of the 5 sites, add a function-level (or expression-level) `#[allow]` attribute with a `reason = "..."` field. NEVER suppress a lint without a rationale comment.
    - **`too_many_lines` (2 sites):**
      - `arch_lm/kernel.rs:76`: `#[allow(clippy::too_many_lines, reason = "closed-form Engle 1982 ARCH-LM derivation: residual fit → squared-residual regression → LM statistic in one body matches the cited derivation; splitting obscures the formula")]`
      - `anova_kw/mod.rs:113`: `#[allow(clippy::too_many_lines, reason = "ANOVA + Kruskal-Wallis bundled Scan::run: bucket-key dispatch → group materialisation → F-stat → KW-stat → effect.extra construction is a single envelope-emission body and splitting it would not improve clarity")]`
    - **`cast_precision_loss` (2 sites, kpss/kernel.rs:81 and :117):** `#[allow(clippy::cast_precision_loss, reason = "n is the bar-count sample size; an OHLCV time series with n > 2^53 ≈ 9e15 bars is physically impossible for any historical data set the miner targets")]`
    - **`cast_possible_truncation` (1 site, lead_lag/kernel.rs:150):** `#[allow(clippy::cast_possible_truncation, reason = "k is bounded by max_lag which is CLI-validated to <= n/4; for any real n, max_lag fits in usize on every supported target (we have no 16-bit targets)")]`
    - Place each `#[allow]` immediately above the offending function or expression, NEVER at the crate level — keep the suppression scope as narrow as possible.
    - After edits, run `cargo clippy ... -- -D warnings 2>&1 | grep -E "too_many_lines|precision|truncate" | wc -l` and confirm zero.
    - DO NOT refactor the function bodies. The `#[allow]` is the deliberate decision; refactoring is out of scope.
  </behavior>
  <commit_message>
    style(04-13): allow intentional cast and too_many_lines patterns with rationale

    Resolves 5 clippy errors that should NOT be mechanically fixed:

    - arch_lm/kernel.rs:76 + anova_kw/mod.rs:113 (too_many_lines, 113/100):
      closed-form statistical derivations that mirror the cited literature;
      splitting them would obscure the formula. #[allow] with reason.
    - kpss/kernel.rs:81 + :117 (cast_precision_loss, usize→f64): the
      truncated value is a bar-count sample size, n > 2^53 is physically
      impossible. #[allow] with reason.
    - lead_lag/kernel.rs:150 (cast_possible_truncation, u64→usize): k is
      CLI-bounded to <= n/4; fits in usize on every supported target.
      #[allow] with reason.

    Every #[allow] carries a `reason = ...` field per clippy.toml discipline.
    Part of Plan 04-13. Drops error count from 14 → 9.
  </commit_message>
</task>

<task type="auto" tdd="false">
  <name>Task 6: remaining mechanical lints (9 errors)</name>
  <files>
    crates/miner-core/src/engine/mod.rs,
    crates/miner-core/src/scan/anom/adf/mod.rs,
    crates/miner-core/src/scan/anom/kpss/kernel.rs,
    crates/miner-core/src/scan/anom/variance_ratio/kernel.rs,
    crates/miner-core/src/scan/seas/anova_kw/kernel.rs,
    crates/miner-core/src/scan/seas/eom_som/mod.rs,
    crates/miner-core/src/scan/seas/event_window/kernel.rs
  </files>
  <read_first>
    - crates/miner-core/src/engine/mod.rs at line 657 (`needless_pass_by_value` — `cancel: Arc<AtomicBool>` should be `&Arc<AtomicBool>`)
    - crates/miner-core/src/scan/anom/adf/mod.rs at line 291 (`single_match` — `match X { Some(_) => ..., None => () }` rewrite to `if let`)
    - crates/miner-core/src/scan/anom/kpss/kernel.rs at lines 162, 181 (`needless_range_loop` — `for i in 0..n { use y[i] }` to `for (i, &yi) in y.iter().enumerate()`)
    - crates/miner-core/src/scan/anom/variance_ratio/kernel.rs at line 106 (`needless_range_loop` — `for i in 0..k { use centred[i] }` to `for &val in centred.iter().take(k)`)
    - crates/miner-core/src/scan/seas/anova_kw/kernel.rs at line 180 (`explicit_iter_loop` — `for &v in g.iter() { ... }` to `for &v in g { ... }`)
    - crates/miner-core/src/scan/seas/eom_som/mod.rs at line 150 (`items_after_statements` — hoist `const CANCEL_POLL_CADENCE: usize = 4096;` to function head) and line 293 (`manual_range_contains` — `if v < 1 || v > MAX_CUTOFF_N` to `if !(1..=MAX_CUTOFF_N).contains(&v)`)
    - crates/miner-core/src/scan/seas/event_window/kernel.rs at line 82 (`explicit_iter_loop` — drop `.iter()` from `event_timestamps_ms.iter()`)
  </read_first>
  <behavior>
    - **`needless_pass_by_value` (engine/mod.rs:657):** Change the helper-function signature from `cancel: Arc<AtomicBool>` to `cancel: &Arc<AtomicBool>`. Update the SINGLE call site (the function is internal — check `mod.rs` for callers). If the call site already has `Arc::clone(&cancel)` or `cancel.clone()`, simplify to `&cancel` after the signature change. If the parameter is part of a public surface, fall back to `#[allow(clippy::needless_pass_by_value, reason = "Arc<AtomicBool> is cheap-to-clone; keeping the by-value signature matches the scan::Scan trait convention")]`.
    - **`single_match` (adf/mod.rs:291):** Rewrite `match X { Some(_) => Y, None => () }` (or `match X { Pat => Y, _ => () }`) as `if let Pat = X { Y }`. Preserve the exact branch body.
    - **`needless_range_loop` (kpss/kernel.rs:162 + :181):** Both loops use both `i` and `y[i]`. Rewrite as `for (i, &yi) in y.iter().enumerate() { ... }` and replace `y[i]` references with `yi`. Confirm the index `i` is still used (KPSS partial-sum kernel reads both the running index and the value).
    - **`needless_range_loop` (variance_ratio/kernel.rs:106):** Per clippy's hint, this loop uses ONLY the index to access `centred[i]` (does not use the index in arithmetic). Rewrite as `for &val in centred.iter().take(k)` per the clippy suggestion. Confirm `val` is the only use of `centred[i]` in the loop body.
    - **`explicit_iter_loop` (anova_kw/kernel.rs:180):** `for &v in g.iter()` → `for &v in g`. The IDE may rewrite to `for &v in &*g` if `g` is `&Vec<_>`; pick whichever clippy accepts.
    - **`items_after_statements` (eom_som/mod.rs:150):** Move `const CANCEL_POLL_CADENCE: usize = 4096;` from its current position (after statements) to immediately after the function's opening brace.
    - **`manual_range_contains` (eom_som/mod.rs:293):** `if v < 1 || v > MAX_CUTOFF_N { return Err(...) }` → `if !(1..=MAX_CUTOFF_N).contains(&v) { return Err(...) }`. Preserve the surrounding control flow.
    - **`explicit_iter_loop` (event_window/kernel.rs:82):** `for &event_ts in event_timestamps_ms.iter()` → `for &event_ts in event_timestamps_ms`.
    - After all 9 edits, run `cargo clippy --workspace --all-targets -- -D warnings` and confirm ZERO errors (the running total should drop from 9 → 0).
    - Run `cargo test --workspace --no-fail-fast 2>&1 | tail -3` and confirm the test count and pass-rate are unchanged from before Plan 04-13. Particular attention to the KPSS, variance-ratio, ANOVA-KW, EOM-SOM, and event-window kernel tests since those touched the inner loops.
  </behavior>
  <commit_message>
    refactor(04-13): clean up remaining clippy::pedantic warnings in miner-core

    Resolves the final 9 clippy errors with mechanical rewrites:

    - engine/mod.rs:657 (needless_pass_by_value): cancel: Arc<AtomicBool>
      → &Arc<AtomicBool> (internal helper, single caller).
    - scan/anom/adf/mod.rs:291 (single_match): match → if let.
    - scan/anom/kpss/kernel.rs:162, 181 (needless_range_loop): use
      enumerate() for index+value access.
    - scan/anom/variance_ratio/kernel.rs:106 (needless_range_loop): use
      .iter().take(k).
    - scan/seas/anova_kw/kernel.rs:180 (explicit_iter_loop): drop .iter().
    - scan/seas/eom_som/mod.rs:150 (items_after_statements): hoist const
      to function head.
    - scan/seas/eom_som/mod.rs:293 (manual_range_contains): use
      (1..=MAX_CUTOFF_N).contains(&v).
    - scan/seas/event_window/kernel.rs:82 (explicit_iter_loop): drop
      .iter().

    All behaviour-preserving. Part of Plan 04-13. Brings clippy error
    count to ZERO — CI gate 2 is now green.
  </commit_message>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Verify CI gates locally + write SUMMARY.md + update STATE.md</name>
  <files>
    .planning/phases/04-scan-catalogue-anom-cross-seas/04-13-SUMMARY.md,
    .planning/STATE.md
  </files>
  <read_first>
    - .github/workflows/ci.yml (the 5 gates)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-12-SUMMARY.md (template for the SUMMARY.md format)
    - .planning/STATE.md (the "cargo clippy -D warnings workspace cleanup deferred to Phase 7 hardening" line that needs amending)
  </read_first>
  <behavior>
    - **Run all 5 CI gates locally to confirm green:**
      ```bash
      export PATH="$HOME/.cargo/bin:$PATH"
      cargo build --workspace --all-targets                                    # Gate 1
      cargo clippy --workspace --all-targets -- -D warnings                    # Gate 2 (THE one we fixed)
      cargo fmt --all -- --check                                               # fmt gate
      cargo test --workspace --no-fail-fast                                    # tests
      # Gate 3 — tokio-free miner-core
      PROHIBITED='^(tokio|tokio-[^ ]+|async-std|async-std-[^ ]+|smol|smol-[^ ]+|async-trait|async-io|async-channel|async-executor|async-task)$'
      cargo tree -p miner-core --edges normal,build --prefix none | awk '{print $1}' | sort -u | grep -E "$PROHIBITED" && echo FAIL || echo OK
      # Gate 4 — schema sync
      cargo run -p xtask -- gen-schema && git diff --exit-code schemas/findings-v1.schema.json && echo OK
      ```
      Each must exit clean. If ANY gate fails, do NOT write the SUMMARY — return to the relevant earlier task and fix.

    - **Write `04-13-SUMMARY.md`** following the same template as `04-12-SUMMARY.md`. Sections:
      - **Status**: Complete, all 5 CI gates green locally.
      - **Goal**: Resolve 88 clippy::pedantic errors blocking CI gate 2.
      - **What landed** (per-task summary table, error counts before/after):
        | Task | Errors fixed | Mechanism |
        |------|--------------|-----------|
        | 1 | 54 | doc_markdown backticks |
        | 2 | 9  | or_default |
        | 3 | 4  | let-else |
        | 4 | 7  | renames + 2 #[allow] for canonical notation |
        | 5 | 5  | 5x #[allow] with reason for cast / too_many_lines |
        | 6 | 9  | mechanical rewrites |
        | **TOTAL** | **88** | |
      - **#[allow] inventory** (6 attributes added across 4 sites, all with `reason = "..."`):
        - arch_lm/kernel.rs:76 — `clippy::too_many_lines`
        - arch_lm/kernel.rs:~ — `clippy::many_single_char_names`
        - adf/kernel.rs:~ — `clippy::many_single_char_names`
        - anova_kw/mod.rs:113 — `clippy::too_many_lines`
        - kpss/kernel.rs:81, :117 — `clippy::cast_precision_loss` (single attribute scope)
        - lead_lag/kernel.rs:150 — `clippy::cast_possible_truncation`
      - **Verification evidence**: paste the actual output of the 5 CI gates run locally (clippy "no warnings emitted", test summary line, schema-sync no diff, fmt clean, tokio-free OK).
      - **Out of scope (deferred)**: Phase 7 still owns the deny-warnings audit across binaries and tests; Plan 04-13 is the miner-core lib slice.
      - **Lessons / why this slipped**: the workspace-level `clippy::pedantic = warn` was set in Phase 1 but never enforced in the per-task TDD loops. Phase 4 plans landed code that compiled cleanly under `cargo build` but not under `cargo clippy -- -D warnings`. Future plans should include a clippy-clean gate in their per-task verification (and Plan 04-13's SUMMARY recommends amending the gsd-executor task verification ritual to include `cargo clippy -- -D warnings` for any task touching `miner-core` source).

    - **Update `.planning/STATE.md`**:
      - Amend the `decisions` list entry that reads "Plan 04-11: cargo clippy -D warnings workspace cleanup deferred to Phase 7 hardening; only 3 in-scope LN_2 lints in drawdown/kernel.rs fixed." to add a postscript: "Plan 04-13 (2026-05-20) reversed this deferral for miner-core lib: all 88 pedantic errors resolved, CI gate 2 now green. Phase 7 still owns the deny-warnings audit across binaries and tests."
      - Update `stopped_at` to "Phase 04 verified pass + CI gate 2 green after Plan 04-13; ready for Phase 5".
      - Update `last_activity` to "2026-05-20 -- Plan 04-13 CI clippy::pedantic unblock complete".
      - Update `last_updated` to the current UTC ISO timestamp.
      - Increment `completed_plans` from 32 → 33 in the `progress` block; `total_plans` stays 32 because Plan 04-13 is a gap-closure that increases the denominator too — set `total_plans: 33` as well (matches the 04-12 precedent where total_plans counted gap-closure plans).

    - **Update ROADMAP.md plan progress**:
      ```bash
      gsd-sdk query roadmap.update-plan-progress 04 04-13 complete 2>/dev/null || true
      ```
      If the SDK helper is unavailable, edit ROADMAP.md directly — flip the `- [ ] 04-13-PLAN.md` line under Phase 4 to `- [x] 04-13-PLAN.md`.

    - **Commit** with all three docs in one atomic commit (SUMMARY + STATE + ROADMAP plan-progress edit, since they all describe the same milestone: Plan 04-13 complete).
  </behavior>
  <commit_message>
    docs(04-13): complete CI clippy::pedantic unblock plan

    Plan 04-13 lands clean: all 88 clippy::pedantic errors in miner-core lib
    resolved across 6 atomic-per-category commits (Tasks 1-6). CI gate 2
    (cargo clippy --workspace --all-targets -- -D warnings) is now green
    for the first time since Phase 4 implementation began.

    Mechanical fixes: 54 doc_markdown, 9 or_default, 4 let-else, 5 renames,
    9 misc (single_match, range_loop, iter_loop, items_after_statements,
    manual_range_contains, needless_pass_by_value) = 81 errors.
    #[allow] with reason: 7 errors across 4 sites (closed-form regression
    bodies + sample-size casts + canonical statistical notation).

    SUMMARY.md documents the per-task breakdown, the #[allow] inventory,
    and amends STATE.md to reverse the prior "deferred to Phase 7"
    decision for miner-core lib (Phase 7 still owns the deny-warnings
    audit across binaries and tests).

    Ready for Phase 5 discuss/plan.
  </commit_message>
</task>

</tasks>

<verification>
After all 7 tasks land:

```bash
export PATH="$HOME/.cargo/bin:$PATH"

# Gate 1 — build
cargo build --workspace --all-targets

# Gate 2 — clippy (THE gate this plan fixes)
cargo clippy --workspace --all-targets -- -D warnings

# Gate 3 — tokio-free miner-core
PROHIBITED='^(tokio|tokio-[^ ]+|async-std|async-std-[^ ]+|smol|smol-[^ ]+|async-trait|async-io|async-channel|async-executor|async-task)$'
cargo tree -p miner-core --edges normal,build --prefix none \
    | awk '{print $1}' | sort -u | grep -E "$PROHIBITED" && exit 1 || echo OK

# Gate 4 — schema sync
cargo run -p xtask -- gen-schema && git diff --exit-code schemas/findings-v1.schema.json

# fmt + test
cargo fmt --all -- --check
cargo test --workspace --no-fail-fast
```

Expected:
- `cargo build` clean.
- `cargo clippy` clean (the headline assertion of Plan 04-13).
- `cargo tree` shows no async-runtime leak.
- `cargo run -p xtask -- gen-schema` produces zero diff.
- `cargo fmt --check` clean.
- `cargo test --workspace` shows the SAME test count as before Plan 04-13 (Phase 4 baseline of >= 787 passing + 3 ignored). No new tests, no removed tests, no test failures.

Then push and confirm GitHub Actions CI gate 2 turns green.
</verification>

<deferred>
The following are intentionally deferred and NOT addressed by Plan 04-13:

- **clippy::pedantic audit across `miner-cli`, `miner-mcp`, `miner-http`, `miner-reader-dukascopy`, `miner-bench`** — none of those crates currently trip a pedantic lint per Plan 04-13's inventory, but Phase 7's deny-warnings sweep formalises the audit (and may discover lints in test/integration files that the workspace lint config doesn't propagate to).
- **`cargo deny` advisory checks** — Phase 7 hardening per CLAUDE.md (`cargo-deny — license + advisory checks`).
- **Walking back `[workspace.lints.clippy] pedantic = warn` to a curated allowlist** — explicitly rejected; the team's quality bar is `clippy::pedantic` and Plan 04-13 commits to meeting it.
- **WR-01..WR-08 from `04-REVIEW.md`** (envelope-shape concerns: effect_extra_keys ordering, event_window doc/impl mismatch, eom_som EOM-precedence, RawArray F64-for-UTF8 lies, etc.) — those are real REVIEW findings tracked separately; they belong in their own gap-closure plans or Phase 7 hardening.
- **ADF reconciliation between `cross/engle_granger` and `anom/adf`** — Phase 5 / HYG-01 (carry-forward from Plan 04-11).
- **Pinned-venv goldens regen** — user setup recipe in `04-11-SUMMARY.md` §"User Setup Required"; the 3 `#[ignore]`d tests stay ignored until the user runs that recipe.
- **Amending the `gsd-executor` task verification ritual to include `cargo clippy -- -D warnings`** — recommended in Plan 04-13's SUMMARY but tracked as a separate workflow change; not a code change in this repo.
</deferred>
