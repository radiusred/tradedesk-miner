---
phase: 04-scan-catalogue-anom-cross-seas
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - crates/miner-core/src/scan/anom/mod.rs
  - crates/miner-core/src/scan/anom/returns/mod.rs
  - crates/miner-core/src/scan/anom/returns/kernel.rs
  - crates/miner-core/src/scan/anom/summary/mod.rs
  - crates/miner-core/src/scan/anom/summary/kernel.rs
  - crates/miner-core/src/scan/anom/vol/mod.rs
  - crates/miner-core/src/scan/anom/vol/kernel.rs
  - crates/miner-core/tests/scan_returns_profile.rs
  - crates/miner-core/tests/scan_summary_welford.rs
  - crates/miner-core/tests/scan_vol_rolling.rs
  - crates/miner-core/tests/shuffled_future_regression.rs
autonomous: true
requirements:
  - ANOM-01
  - ANOM-02
  - ANOM-03
tags:
  - rust
  - scan
  - anom

must_haves:
  truths:
    - "User can run `miner scan stats.returns.profile@1 --instrument EURUSD:bid --timeframe 15m --window ... --params variant=log` and receive a Finding::Result with effect.metric == \"returns_log_mean\" and effect.extra.returns_vector + effect.extra.variant_label (ANOM-01)"
    - "User can run `miner scan stats.summary.welford@1 ...` and receive a Finding::Result whose effect.value matches scipy.stats.describe.mean within 1e-10 for the same input (ANOM-02)"
    - "User can run `miner scan stats.vol.rolling@1 --params window=20` and receive a Finding::Result whose effect.value is the last-window vol and effect.extra.values is the full rolling-vol vector (ANOM-03)"
    - "User triggers Finding::ScanError with InsufficientData for N=0 or N=1 inputs on every scan in this plan"
  artifacts:
    - path: "crates/miner-core/src/scan/anom/returns/mod.rs"
      provides: "ReturnsProfileScan with id stats.returns.profile@1, arity Single, variant=log|simple|intraday|overnight param (ANOM-01)"
      contains: "stats.returns.profile"
    - path: "crates/miner-core/src/scan/anom/summary/mod.rs"
      provides: "SummaryWelfordScan with id stats.summary.welford@1 (ANOM-02)"
      contains: "stats.summary.welford"
    - path: "crates/miner-core/src/scan/anom/vol/mod.rs"
      provides: "VolRollingScan with id stats.vol.rolling@1 (ANOM-03)"
      contains: "stats.vol.rolling"
  key_links:
    - from: "crates/miner-core/src/scan/anom/returns/mod.rs"
      to: "crates/miner-core/src/scan/primitives/returns.rs"
      via: "imports log_returns, simple_returns, intraday_returns, overnight_returns; dispatches by params.variant (D4-05)"
      pattern: "primitives::returns"
    - from: "crates/miner-core/src/scan/anom/mod.rs::register_anom_scans"
      to: "ReturnsProfileScan + SummaryWelfordScan + VolRollingScan"
      via: "three new r.register(Box::new(...)) lines appended to the per-family register_anom_scans helper created in Plan 04-02 (alphabetical by scan_id); registry.rs::bootstrap() is NOT touched by this plan"
      pattern: "register_anom_scans"
---

<objective>
Implement three single-leg ANOM scans following the Phase 3 LjungBoxScan gold-standard pattern (Pattern A): ANOM-01 stats.returns.profile@1 (callable scan with variant=log|simple|intraday|overnight via params), ANOM-02 stats.summary.welford@1 (Welford running-moments), ANOM-03 stats.vol.rolling@1 (rolling std + vol-of-vol with vector-output emission per Pattern D).

Each scan: (mod.rs + kernel.rs) split, statrs CDFs only, BTreeMap-only Serialize maps, cancel polling at run entry (+ inside the vol rolling-window loop), look-ahead-safety via ctx.bars_up_to where applicable (ANOM-03 uses partition_point on the closing-bar timestamp), registration via the per-family `register_anom_scans` helper established in Plan 04-02 — NO direct edits to `crates/miner-core/src/scan/registry.rs::bootstrap()` in this plan (that file is owned by Plan 04-02 in Phase 4 and the per-family registrar pattern means subsequent plans only append to `scan/anom/mod.rs::register_anom_scans`; Plan 04-11 owns the registry-count assertion for the full 22-scan catalogue).

Purpose: ANOM-01 unlocks the kernel-utility consumers (every other ANOM/CROSS scan in subsequent plans imports primitives::returns); ANOM-02 establishes the Welford pattern; ANOM-03 establishes the vector-output / rolling-window pattern that CROSS-02/03/04 and SEAS bucketed scans replicate.
Output: 3 registered scans + 3 integration-test files with deterministic synthetic-fixture happy-path tests. Goldens for ANOM-02 (scipy.stats.describe) and ANOM-03 (pandas.rolling.std) live in goldens/ but are AUTHORED in Plan 04-11. This plan ships the scans + happy-path tests; the golden cross-checks land in 04-11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-CONTEXT.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-VALIDATION.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-02-SUMMARY.md
@crates/miner-core/src/scan/ljung_box/mod.rs
@crates/miner-core/src/scan/ljung_box/kernel.rs
@crates/miner-core/src/scan/primitives/returns.rs
@crates/miner-core/src/scan/primitives/raw_array.rs
@crates/miner-core/tests/scan_ljung_box.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ANOM-01 stats.returns.profile@1 — callable scan + register via per-family helper + integration test</name>
  <files>crates/miner-core/src/scan/anom/returns/mod.rs, crates/miner-core/src/scan/anom/returns/kernel.rs, crates/miner-core/src/scan/anom/mod.rs, crates/miner-core/tests/scan_returns_profile.rs</files>
  <read_first>
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md (Pattern A full skeleton; Pattern B kernel-file shape; Pattern E registry-via-per-family-helper pattern established in Plan 04-02; 13-named-tests block)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md (§1.4 ANOM-01 surface — ONE scan with variant param; §Section 2 row for stats.returns.profile@1: effect.value = mean log-return, effect.extra keys = returns_vector / variant_label / mean / std / n)
    - crates/miner-core/src/scan/ljung_box/mod.rs (analog — full Scan impl + 13 named tests)
    - crates/miner-core/src/scan/ljung_box/kernel.rs (analog — pure-kernel discipline)
    - crates/miner-core/src/scan/primitives/returns.rs (the four lifted return functions ANOM-01 dispatches over)
    - crates/miner-core/src/scan/primitives/raw_array.rs (f64_slice_to_raw_array helper)
    - crates/miner-core/src/scan/anom/mod.rs (the per-family `register_anom_scans(r: &mut Registry)` helper stub created in Plan 04-02 Task 1 — this task APPENDS one `r.register(Box::new(ReturnsProfileScan));` line to that helper's body)
    - crates/miner-core/tests/scan_ljung_box.rs (analog integration test — 8-step Pattern J)
    - crates/miner-core/tests/common/ (shared BufferSink + helpers location)
  </read_first>
  <behavior>
    - Test (`returns_profile_id_and_version`): id == "stats.returns.profile", version == 1
    - Test (`returns_profile_arity_is_single`): arity() == ScanArity::Single
    - Test (`returns_profile_param_schema`): param_schema() includes a "variant" property with enum ["log", "simple", "intraday", "overnight"] and default "log"
    - Test (`returns_profile_default_variant_is_log`): omitting `variant` in resolved_params dispatches to primitives::returns::log_returns
    - Test (`returns_profile_explicit_variant_simple`): variant=simple dispatches to primitives::returns::simple_returns
    - Test (`returns_profile_invalid_variant_rejected`): variant="garbage" returns ScanError::Kernel mentioning "variant must be log|simple|intraday|overnight"
    - Test (`returns_profile_emits_one_result`): a deterministic LCG-seeded BarFrame produces exactly one Finding::Result in the sink
    - Test (`returns_profile_result_envelope_shape`): effect.metric == "returns_log_mean" (or "returns_<variant>_mean") + effect.value is the arithmetic mean of returns + effect.n is returns.len() as u64 + effect.extra carries keys {returns_vector, variant_label, mean, std, n} + raw.series carries {returns, timestamps_ms}
    - Test (`returns_profile_raw_new_enforces_timestamps_ms`): omitting timestamps_ms in raw.series triggers Raw::new error (D-03 invariant)
    - Test (`returns_profile_cancellation`): cancel.store(true, Ordering::Relaxed) before run produces zero findings
    - Test (`returns_profile_n_zero_emits_scan_error_insufficient_data`): BarFrame with close.len() == 0 emits Finding::ScanError with code InsufficientData/ComputeError
    - Test (`returns_profile_n_one_emits_scan_error_insufficient_data`): BarFrame with close.len() == 1 emits Finding::ScanError (need ≥2 closes for any returns variant)
    - Integration test (`scan_returns_profile_happy_path`): build deterministic LCG-seeded BarFrame, run scan via run_one_with_registry, parse the emitted JSONL, assert envelope shape via insta snapshot with common::mask_volatile_fields
  </behavior>
  <action>
    Create `crates/miner-core/src/scan/anom/returns/` directory with `mod.rs` and `kernel.rs`. Follow Pattern A verbatim:

    `mod.rs`:
    - SCAN_ID = "stats.returns.profile"
    - SCAN_VERSION = 1
    - EFFECT_METRIC builds dynamically as `format!("returns_{}_mean", variant)` — variant string from params
    - `pub struct ReturnsProfileScan;`
    - `impl Scan for ReturnsProfileScan` declares arity() => ScanArity::Single
    - param_schema returns a hand-rolled JSON Schema with `variant` enum + default "log" + `additionalProperties: false`
    - finding_fields returns ScanFindingShape { effect_extra_keys: &["returns_vector", "variant_label", "mean", "std", "n"], raw_series_keys: &["returns", "timestamps_ms"] }
    - run body: cancel check at entry; resolve variant param via helper analogous to resolve_lags in ljung_box; call the dispatch helper in kernel.rs that switches on variant and calls the appropriate primitives::returns::* function; compute mean + std (Welford or simple two-pass); construct Effect { metric, value: mean, p_value: None, n: Some(returns.len() as u64), ci95: None, extra: BTreeMap with five keys }; construct Raw with BTreeMap of two keys via primitives::raw_array::f64_slice_to_raw_array (timestamps_ms come from ctx.bars.ts_open_utc.iter().map(|t| t.timestamp_millis()).collect()); construct ResultFinding and write via sink.write_envelope(&Finding::Result(...))

    `kernel.rs`:
    - File header + clippy gate (verbatim from ljung_box/kernel.rs lines 1-19)
    - `#[inline] pub(super) fn dispatch_variant(closes: &[f64], ts: &[DateTime<Utc>], variant: ReturnsVariant) -> Vec<f64>` — internal enum ReturnsVariant { Log, Simple, Intraday, Overnight } that switches into primitives::returns::* calls
    - `#[inline] pub(super) fn mean_and_std(values: &[f64]) -> (f64, f64)` — two-pass mean + sample-std (ddof=1). debug_assert!(values.len() >= 2, "mean_and_std: need ≥2 samples").
    - Constant-input branch: if all values identical, std = 0.0 (no NaN)
    - Pure-kernel discipline (Pattern B): #[inline], pub(super), statrs unused for this kernel (no CDFs), sequential summation order, debug_assert invariants

    Update `crates/miner-core/src/scan/anom/mod.rs` to declare `pub mod returns;` and `pub use returns::ReturnsProfileScan;`.

    Register `ReturnsProfileScan` by APPENDING the line `r.register(Box::new(ReturnsProfileScan));` to the body of the `register_anom_scans(r: &mut Registry)` helper inside `crates/miner-core/src/scan/anom/mod.rs` (the helper stub created in Plan 04-02 Task 1 has an empty body sealed with `let _ = r;` — replace the placeholder with one register line). Maintain alphabetical-by-scan-id order inside the helper as subsequent ANOM plans append more lines. DO NOT modify `crates/miner-core/src/scan/registry.rs::bootstrap()` in this plan — that file is OWNED by Plan 04-02; Plans 04-03..04-10 only append to their family's `register_<family>_scans` helper. (The existing `bootstrap_registers_ljung_box_scan` count test and `registry_iter_lex_order` ordering test land their final assertions in Plan 04-11 once all 22 scans are registered; transient updates during 04-03..04-10 are scoped per-plan to keep counts consistent — see acceptance criteria.)

    Create `crates/miner-core/tests/scan_returns_profile.rs` per Pattern J 8-step walk. Pattern analog: crates/miner-core/tests/scan_ljung_box.rs. The integration test builds a deterministic LCG-seeded BarFrame via the existing fixture builder (`ar1_bar_frame_seeded` or equivalent shared helper), runs ReturnsProfileScan, parses the captured envelope, asserts effect.value + effect.extra.variant_label, and pins the envelope shape via `insta::assert_json_snapshot!("returns_profile_happy_path", masked)` with common::mask_volatile_fields. NO statsmodels golden in this plan — Plan 04-11 handles golden cross-checks if needed; for ANOM-01 the kernel is hand-derivable and no statsmodels reference is needed (returns primitive is project-defined).

    All 13 named tests live in `#[cfg(test)] mod tests` block inside mod.rs (analog: scan_ljung_box.rs tests 323-693). Lift `ar1_bar_frame_seeded` to `tests/common/synthetic_cache.rs` if it isn't already there (so subsequent plans don't duplicate).
  </action>
  <acceptance_criteria>
    - `cargo build -p miner-core --all-targets` exits 0
    - `grep -nE 'stats\.returns\.profile' crates/miner-core/src/scan/anom/returns/mod.rs` finds ≥ 1 match (the SCAN_ID constant)
    - `grep -n 'ScanArity::Single' crates/miner-core/src/scan/anom/returns/mod.rs` finds ≥ 1 match
    - `grep -n 'ReturnsProfileScan' crates/miner-core/src/scan/anom/mod.rs` finds ≥ 2 matches (the `pub use` re-export AND the `r.register(Box::new(ReturnsProfileScan))` line inside `register_anom_scans`)
    - `grep -nE 'fn register_anom_scans' crates/miner-core/src/scan/anom/mod.rs` finds exactly 1 match (the helper signature established in Plan 04-02)
    - `git diff --stat crates/miner-core/src/scan/registry.rs` shows ZERO changes from this plan's commits (registry.rs is NOT touched in 04-03)
    - `cargo test -p miner-core --lib scan::anom::returns::tests` — all 12 unit tests pass
    - `cargo test -p miner-core --test scan_returns_profile` — integration test passes (insta snapshot matches or is newly created and accepted)
    - `cargo run -p miner-cli -- scans | grep '"id":"stats.returns.profile"' | grep '"arity":"single"'` succeeds (catalogue lists the new scan)
    - The Phase 3 LjungBox golden still passes: `cargo test -p miner-core --test scan_ljung_box`
    - `cargo clippy -p miner-core --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p miner-core --all-targets && cargo test -p miner-core --lib scan::anom::returns::tests && cargo test -p miner-core --test scan_returns_profile --test scan_ljung_box && cargo run -p miner-cli --quiet -- scans 2>/dev/null | grep -q '"id":"stats.returns.profile"'</automated>
  </verify>
  <done>stats.returns.profile@1 is a registered Single-arity scan dispatching on variant param to the four primitives::returns::* functions; registration appends to `register_anom_scans` (registry.rs untouched); the happy-path integration test passes; the Phase 3 LjungBox regression continues to pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ANOM-02 stats.summary.welford@1 — Welford running-moments scan + register via per-family helper + integration test</name>
  <files>crates/miner-core/src/scan/anom/summary/mod.rs, crates/miner-core/src/scan/anom/summary/kernel.rs, crates/miner-core/src/scan/anom/mod.rs, crates/miner-core/tests/scan_summary_welford.rs</files>
  <read_first>
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md (Pattern A full skeleton; Pattern B kernel-file shape; Pattern E per-family registrar)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md (§Section 2 row for stats.summary.welford@1: effect.value = mean; effect.extra keys = std, skew, excess_kurtosis, iqr, min, max, n; reference = scipy.stats.describe + scipy.stats.iqr; tolerance 1e-10)
    - crates/miner-core/src/scan/anom/returns/mod.rs (sibling analog created in Task 1)
    - crates/miner-core/src/scan/ljung_box/mod.rs (gold-standard)
    - crates/miner-core/src/scan/primitives/returns.rs (uses log_returns for the input)
    - crates/miner-core/src/scan/anom/mod.rs (the per-family `register_anom_scans` helper — append one register line)
  </read_first>
  <behavior>
    - Test (`summary_welford_id_and_version`): id == "stats.summary.welford", version == 1
    - Test (`summary_welford_arity_is_single`)
    - Test (`summary_welford_param_schema`): includes optional `series` param with enum ["close", "log_returns"] default "log_returns"
    - Test (`summary_welford_emits_one_result`)
    - Test (`summary_welford_result_envelope_shape`): effect.metric == "summary_welford_mean", effect.value == arithmetic mean of returns, effect.extra contains all 7 keys (std, skew, excess_kurtosis, iqr, min, max, n)
    - Test (`summary_welford_skew_matches_hand_derived`): for a hand-derived input (e.g., [1,2,3,4,5]) the computed skew is within 1e-12 of the known closed-form value
    - Test (`summary_welford_excess_kurtosis_matches_hand_derived`): same for excess kurtosis
    - Test (`summary_welford_iqr_matches_hand_derived`): IQR computed as P75 - P25 via linear interpolation matches the manually computed value for an odd-length input within 1e-12
    - Test (`summary_welford_cancellation`)
    - Test (`summary_welford_n_zero_emits_scan_error`)
    - Test (`summary_welford_n_one_emits_trivial_result`): n=1 emits a Result where std/skew/kurtosis are NaN-safe (e.g., 0.0 or explicit handling — kernel decides; pin via assertion)
    - Integration test (`scan_summary_welford_happy_path`): insta snapshot of the envelope on a deterministic LCG-seeded input
  </behavior>
  <action>
    Create `crates/miner-core/src/scan/anom/summary/` directory with `mod.rs` and `kernel.rs`. Pattern A clone of ANOM-01.

    `mod.rs`:
    - SCAN_ID = "stats.summary.welford", SCAN_VERSION = 1, EFFECT_METRIC = "summary_welford_mean"
    - Single arity
    - param_schema: optional `series` enum ["close", "log_returns"] default "log_returns"
    - finding_fields: effect_extra_keys = &["std", "skew", "excess_kurtosis", "iqr", "min", "max", "n"]; raw_series_keys = &["returns", "timestamps_ms"] (or &["closes", "timestamps_ms"] when series=close)
    - run body: cancel-at-entry; resolve series param; either use primitives::returns::log_returns(&closes) or ctx.bars.close directly; call kernel::welford_pass + kernel::iqr; construct envelope

    `kernel.rs`:
    - `#[inline] pub(super) fn welford_pass(values: &[f64]) -> WelfordStats { mean, m2, m3, m4 accumulators; final pass returns mean, std (ddof=1), skew via m3/(n*sigma^3) bias-corrected, excess_kurtosis via m4/(n*sigma^4) - 3 bias-corrected }` — closed-form bias corrections per scipy.stats.describe bias-corrected formulas
    - `#[inline] pub(super) fn iqr(values: &[f64]) -> f64` — sort a copy, linear interpolation between adjacent indices at 0.25*(n-1) and 0.75*(n-1), match scipy.stats.iqr default behavior
    - `#[inline] pub(super) fn min_max(values: &[f64]) -> (f64, f64)` — NaN-aware (skip NaN or return NaN if any NaN — decision: error on NaN input upstream in mod.rs::run)
    - Sequential summation order for Welford pass — critical for cross-platform determinism (Pitfall 4)
    - debug_assert!(values.len() >= 2, "welford_pass: need ≥2 samples")
    - Unit tests: hand-derived skew/kurtosis/iqr known-input tests within 1e-12

    Update anom/mod.rs to declare `pub mod summary;` + `pub use summary::SummaryWelfordScan;` re-export. APPEND `r.register(Box::new(SummaryWelfordScan));` to the existing `register_anom_scans` helper body — the scan_id "stats.summary.welford" sorts AFTER "stats.returns.profile" (alphabetical-by-id ordering inside the helper). DO NOT touch `crates/miner-core/src/scan/registry.rs` in this task.

    Create integration test `crates/miner-core/tests/scan_summary_welford.rs` per Pattern J. The happy-path test uses a deterministic seeded BarFrame and pins envelope via insta. Golden cross-check against scipy.stats.describe is deferred to Plan 04-11 — for this plan, the hand-derived kernel tests give correctness coverage.
  </action>
  <acceptance_criteria>
    - `cargo build -p miner-core --all-targets` exits 0
    - `grep -nE 'stats\.summary\.welford' crates/miner-core/src/scan/anom/summary/mod.rs` finds ≥ 1 match
    - `grep -n 'fn welford_pass' crates/miner-core/src/scan/anom/summary/kernel.rs` finds 1 match
    - `grep -n 'fn iqr' crates/miner-core/src/scan/anom/summary/kernel.rs` finds 1 match
    - `grep -n 'SummaryWelfordScan' crates/miner-core/src/scan/anom/mod.rs` finds ≥ 2 matches (re-export + register line)
    - `git diff --stat crates/miner-core/src/scan/registry.rs` shows ZERO changes from this task's commits
    - `cargo test -p miner-core --lib scan::anom::summary::tests` — all named tests pass (≥11 tests)
    - The kernel hand-derived skew/kurtosis/iqr unit tests pass within 1e-12 tolerance
    - `cargo test -p miner-core --test scan_summary_welford` integration test passes
    - `cargo run -p miner-cli --quiet -- scans | grep -q '"id":"stats.summary.welford"'`
    - The Phase 3 LjungBox golden + new ANOM-01 scan continue to pass
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p miner-core --all-targets && cargo test -p miner-core --lib scan::anom::summary::tests && cargo test -p miner-core --test scan_summary_welford --test scan_returns_profile --test scan_ljung_box && cargo run -p miner-cli --quiet -- scans 2>/dev/null | grep -q '"id":"stats.summary.welford"'</automated>
  </verify>
  <done>stats.summary.welford@1 is registered via `register_anom_scans`; Welford pass + IQR kernels are hand-derivation-tested within 1e-12; integration test pins envelope shape; registry.rs untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ANOM-03 stats.vol.rolling@1 — rolling vol + vol-of-vol vector-output scan + register via per-family helper + integration test + shuffled-future extension</name>
  <files>crates/miner-core/src/scan/anom/vol/mod.rs, crates/miner-core/src/scan/anom/vol/kernel.rs, crates/miner-core/src/scan/anom/mod.rs, crates/miner-core/tests/scan_vol_rolling.rs, crates/miner-core/tests/shuffled_future_regression.rs</files>
  <read_first>
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md (Pattern A; Pattern D vector-output effect.extra layout; Pattern M shuffled-future regression extension; Pattern E per-family registrar)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md (§Section 2 row for stats.vol.rolling@1: effect.value = last_window_vol; effect.extra = values, vol_of_vol, window_starts_ms, window_length; one finding per invocation with vectors in extra; reference = pandas.Series.rolling(W).std(ddof=1); tolerance 1e-10; Pitfall 1 "Per-window finding storm" — REJECTED, one envelope per invocation only)
    - crates/miner-core/tests/shuffled_future_regression.rs (existing Phase 3 file lines 1-80 — the extension point + LjungBox proptest; the in-file documentation pins it as the Phase 4 extension target). NOTE: this task is the ONLY Wave-3 plan that modifies this file; Plan 04-07 (also Wave 3 — Pair-arity rolling correlation/OLS) does NOT touch shuffled_future_regression.rs in Wave 3 (those proptest extensions land in Plan 04-08 Wave 4 to avoid same-wave file-write conflict).
    - crates/miner-core/src/scan/primitives/returns.rs + raw_array.rs (kernel inputs)
    - crates/miner-core/src/scan/ljung_box/mod.rs (gold-standard envelope construction)
  </read_first>
  <behavior>
    - Test (`vol_rolling_id_and_version`): id == "stats.vol.rolling", version == 1
    - Test (`vol_rolling_arity_is_single`)
    - Test (`vol_rolling_param_schema`): includes `window: integer` (required, ≥2), optional `min_periods` (default = window), optional `series` enum ["log_returns", "close"] default "log_returns"
    - Test (`vol_rolling_default_params_dispatches_log_returns`)
    - Test (`vol_rolling_invalid_window_low`): window=1 rejected with ScanError::Kernel
    - Test (`vol_rolling_invalid_window_high`): window > returns.len() rejected
    - Test (`vol_rolling_emits_one_result`): produces exactly 1 Finding::Result (NOT one-per-window; Pitfall 1)
    - Test (`vol_rolling_result_envelope_shape`): effect.metric == "vol_rolling_last", effect.value == values.last(), effect.n == values.len() as u64, effect.p_value == None, effect.extra keys = {values, vol_of_vol, window_starts_ms, window_length}, raw.series carries {returns, timestamps_ms}
    - Test (`vol_rolling_extras_have_correct_lengths`): values.len() == window_starts_ms.len() == n_returns - window + 1
    - Test (`vol_rolling_known_input_matches_hand_derived`): for [1,1,1,1,1] closes with window=2, log-returns are all 0, vol is all 0
    - Test (`vol_rolling_cancellation_during_window_loop`): cancel.store(true) mid-loop terminates promptly (poll cancel inside the per-window loop per RESEARCH.md §Cancel polling discussion)
    - Test (`vol_rolling_raw_new_enforces_timestamps_ms`)
    - Integration test (`scan_vol_rolling_happy_path`): insta envelope snapshot
    - Shuffled-future proptest (`vol_rolling_shuffled_future_invariant` added to crates/miner-core/tests/shuffled_future_regression.rs per Pattern M): build closes via lcg_closes(N, seed); run vol scan on prefix [0..T]; shuffle [T..N]; run again on full array; the per-window values vector prefix-aligned at index T MUST be byte-identical (look-ahead-safety pin)
  </behavior>
  <action>
    Create `crates/miner-core/src/scan/anom/vol/` directory with `mod.rs` and `kernel.rs`. Pattern A + Pattern D.

    `mod.rs`:
    - SCAN_ID = "stats.vol.rolling", SCAN_VERSION = 1, EFFECT_METRIC = "vol_rolling_last"
    - Single arity
    - param_schema: required `window: integer ≥2`; optional `min_periods` default = window; optional `series` enum default "log_returns"
    - finding_fields: effect_extra_keys = &["values", "vol_of_vol", "window_starts_ms", "window_length"]; raw_series_keys = &["returns", "timestamps_ms"]
    - run body: cancel-at-entry; resolve window/series params; compute returns via primitives::returns; call kernel::rolling_std(&returns, window) returning Vec<f64> of length n-window+1; call kernel::vol_of_vol(&values, window_vov) where window_vov defaults to half the primary window (or kept = window); window_starts_ms = ctx.bars.ts_open_utc[window-1..].iter().map(timestamp_millis); poll cancel inside the rolling loop (every K iterations is fine; mandatory per RESEARCH §Cancel polling + Pitfall 1 note)

    `kernel.rs`:
    - `#[inline] pub(super) fn rolling_std(returns: &[f64], window: usize) -> Vec<f64>` — naive O(n*window) is acceptable for v1; do NOT use Welford incremental (the rolling-Welford variant is numerically tricky and ANOM-03 is not on the hot path of throughput claims; profile in Phase 7 if needed)
    - `#[inline] pub(super) fn vol_of_vol(rolling_vols: &[f64], window: usize) -> Vec<f64>` — same shape, applies rolling_std to the vol series
    - Pure-kernel discipline: #[inline], pub(super), no IO, no serde, sequential summation, constant-input branch → 0.0
    - Unit tests with hand-derived expectations (a few small inputs)

    Update anom/mod.rs to declare `pub mod vol;` + `pub use vol::VolRollingScan;` re-export. APPEND `r.register(Box::new(VolRollingScan));` to the existing `register_anom_scans` helper body — "stats.vol.rolling" sorts after "stats.summary.welford" (alphabetical-by-id). DO NOT touch `crates/miner-core/src/scan/registry.rs` in this task.

    Create `crates/miner-core/tests/scan_vol_rolling.rs` per Pattern J. The happy-path test pins the envelope via insta on a deterministic LCG-seeded BarFrame.

    Extend `crates/miner-core/tests/shuffled_future_regression.rs` per Pattern M: add a new proptest function `vol_rolling_shuffled_future_invariant` following the existing LjungBox proptest shape (lines ~50-80 of the existing file). The function:
    1. Builds closes via the existing `lcg_closes(N, seed)` helper.
    2. Runs VolRollingScan on the prefix [0..T] only (build a BarFrame from the prefix, call ScanRequest with the prefix window, capture findings).
    3. Shuffles [T..N] (use a deterministic seeded shuffle, NOT std::sync::Mutex<rand>, to keep proptest deterministic).
    4. Runs VolRollingScan again on the full array [0..N].
    5. Decodes the per-window `values` vector from both runs; asserts the prefix of the second-run vector that ends at index T is byte-identical (within 0.0 — exact equality) to the first run's vector.

    This is the ONLY Wave-3 plan that modifies `tests/shuffled_future_regression.rs`. Plans 04-07 (Pearson/Spearman/OLS rolling shuffled-future proptests) defer those extensions to Plan 04-08 Wave 4 to avoid same-wave file-write conflict.
  </action>
  <acceptance_criteria>
    - `cargo build -p miner-core --all-targets` exits 0
    - `grep -nE 'stats\.vol\.rolling' crates/miner-core/src/scan/anom/vol/mod.rs` finds ≥ 1 match
    - `grep -n 'fn rolling_std' crates/miner-core/src/scan/anom/vol/kernel.rs` finds 1 match
    - `grep -n 'fn vol_of_vol' crates/miner-core/src/scan/anom/vol/kernel.rs` finds 1 match
    - `grep -n 'VolRollingScan' crates/miner-core/src/scan/anom/mod.rs` finds ≥ 2 matches (re-export + register line)
    - `git diff --stat crates/miner-core/src/scan/registry.rs` shows ZERO changes from this task's commits
    - `cargo test -p miner-core --lib scan::anom::vol::tests` passes
    - `cargo test -p miner-core --test scan_vol_rolling` integration test passes
    - `cargo test -p miner-core --test shuffled_future_regression` passes (incl. the new vol_rolling proptest + existing LjungBox proptest)
    - `cargo run -p miner-cli --quiet -- scans | grep -q '"id":"stats.vol.rolling"'`
    - The Phase 3 LjungBox + ANOM-01 + ANOM-02 regressions continue to pass
    - The emitted envelope contains EXACTLY ONE Finding::Result (NOT N findings for N windows — Pitfall 1)
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p miner-core --all-targets && cargo test -p miner-core --lib scan::anom::vol::tests && cargo test -p miner-core --test scan_vol_rolling --test shuffled_future_regression --test scan_returns_profile --test scan_summary_welford --test scan_ljung_box && cargo run -p miner-cli --quiet -- scans 2>/dev/null | grep -q '"id":"stats.vol.rolling"'</automated>
  </verify>
  <done>stats.vol.rolling@1 emits a single-envelope vector-output finding (Pattern D, Pitfall 1); registration via `register_anom_scans` (registry.rs untouched); the shuffled-future regression test extension pins look-ahead-safety; the integration test pins envelope shape via insta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/MCP/HTTP → ScanRequest.resolved_params | Untrusted JSON param values cross here; integer overflow / DoS via huge `window` |
| Scan kernel → BarFrame.close | Untrusted bar count from upstream cache; rolling kernel iterates O(n*window) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Denial of Service | vol.rolling param `window` | mitigate | Tasks validate `window ≥ 2` AND `window ≤ returns.len()` via ScanError::Kernel — rejected at run-entry before any allocation. |
| T-04-03-02 | Information Disclosure | vol.rolling look-ahead | mitigate | Shuffled-future proptest extension (Pattern M) gates merge: future bars (post cutoff T) must not affect rolling-vol values for windows ending ≤ T. Behavior test pins byte-identicality across the cut. |
| T-04-03-03 | Tampering | NaN in returns | mitigate | Kernel debug_assert + sequential summation pin determinism; constant-input branch (zero variance → vol = 0.0) prevents NaN propagation. All-NaN input upstream — kernels return ScanError::Kernel before envelope construction. |
| T-04-03-SC | Tampering | No new packages | accept | This plan installs no new packages; relies on Plan 04-01's audited deps. |
</threat_model>

<verification>
- All three scans compile, register via `register_anom_scans`, and appear in `miner scans` JSONL output with arity=single
- `crates/miner-core/src/scan/registry.rs` is NOT modified by this plan (verified via `git diff --stat registry.rs` showing zero changes)
- All happy-path integration tests pass against deterministic synthetic fixtures
- The Phase 3 LjungBox golden + Phase 2/3 facade tests continue to pass byte-identically
- The shuffled-future proptest passes for ANOM-03 (and continues to pass for LjungBox)
- `cargo clippy -p miner-core --all-targets -- -D warnings` clean
- ZERO new Finding::Result envelopes per-window (Pitfall 1) — ANOM-03 emits exactly one envelope per invocation with vector arrays in effect.extra
</verification>

<success_criteria>
- stats.returns.profile@1 registered via `register_anom_scans` with Single arity + variant param + four primitive dispatches (ANOM-01)
- stats.summary.welford@1 registered with hand-derivation-tested Welford + IQR (ANOM-02)
- stats.vol.rolling@1 registered with vector-output emission per Pattern D + shuffled-future regression proptest (ANOM-03)
- `crates/miner-core/src/scan/registry.rs` untouched in this plan (per-family registrar pattern owns all Phase 4 additions)
- All three scans listed in `miner scans` JSONL with arity=single
- N=0 and N=1 inputs trigger Finding::ScanError on every scan
- Phase 3 LjungBox golden continues to pass byte-identically
</success_criteria>

<output>
Create `.planning/phases/04-scan-catalogue-anom-cross-seas/04-03-SUMMARY.md` documenting per-scan: SCAN_ID + SCAN_VERSION, effect.metric/value/extra keys actually shipped, any deviations from RESEARCH.md §Section 2 table (none expected), insta snapshot file names committed, and an explicit "registry.rs untouched — registration via register_anom_scans" line confirming the per-family registrar pattern.
</output>
