---
phase: 04-scan-catalogue-anom-cross-seas
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - crates/miner-core/src/scan/anom/mod.rs
  - crates/miner-core/src/scan/anom/ljung_box_sq/mod.rs
  - crates/miner-core/src/scan/anom/ljung_box_sq/kernel.rs
  - crates/miner-core/src/scan/anom/outliers/mod.rs
  - crates/miner-core/src/scan/anom/outliers/kernel.rs
  - crates/miner-core/src/scan/anom/drawdown/mod.rs
  - crates/miner-core/src/scan/anom/drawdown/kernel.rs
  - crates/miner-core/tests/scan_ljung_box_squared.rs
  - crates/miner-core/tests/scan_outliers.rs
  - crates/miner-core/tests/scan_drawdown.rs
autonomous: true
requirements:
  - ANOM-04
  - ANOM-10
  - ANOM-11
tags:
  - rust
  - scan
  - anom

must_haves:
  truths:
    - "User can run `miner scan stats.autocorr.ljung_box_sq@1 --instrument EURUSD:bid --timeframe 15m --window ... --params lags=20` and receive a Finding::Result whose effect.value is the Q-stat on SQUARED returns (ANOM-04 squared variant per RESEARCH §2)"
    - "User can run `miner scan stats.outliers.z_and_mad@1 --params z_threshold=3.0` and receive a Finding::Result whose effect.extra.outlier_indices identifies bars exceeding both z and modified-z thresholds (ANOM-10)"
    - "User can run `miner scan stats.drawdown.profile@1` and receive a Finding::Result whose effect.value is max_drawdown and effect.extra contains peaks, troughs, drawdown_durations_ms, time_to_recover_ms, dd_distribution_p50_p95_p99 (ANOM-11)"
    - "User running the existing Phase 3 LjungBox golden test sees byte-identical output (ANOM-04 returns variant — Pitfall 9 invariant)"
  artifacts:
    - path: "crates/miner-core/src/scan/anom/ljung_box_sq/mod.rs"
      provides: "LjungBoxSqScan with id stats.autocorr.ljung_box_sq@1, arity Single (ANOM-04 squared-returns variant)"
      contains: "stats.autocorr.ljung_box_sq"
    - path: "crates/miner-core/src/scan/anom/outliers/mod.rs"
      provides: "OutliersZAndMadScan with id stats.outliers.z_and_mad@1 (ANOM-10)"
      contains: "stats.outliers.z_and_mad"
    - path: "crates/miner-core/src/scan/anom/drawdown/mod.rs"
      provides: "DrawdownProfileScan with id stats.drawdown.profile@1 (ANOM-11)"
      contains: "stats.drawdown.profile"
  key_links:
    - from: "crates/miner-core/src/scan/anom/ljung_box_sq/mod.rs"
      to: "crates/miner-core/src/scan/ljung_box/kernel.rs::{biased_acf, ljung_box_q_and_p}"
      via: "reuses Phase 3 kernel functions; input is returns² instead of returns"
      pattern: "ljung_box::kernel"
    - from: "crates/miner-core/src/scan/anom/drawdown/mod.rs"
      to: "crates/miner-core/src/scan/primitives/returns.rs::log_returns"
      via: "cumulative log-returns build the synthetic equity curve"
      pattern: "primitives::returns::log_returns"
---

<objective>
Implement three single-shot pure-arithmetic ANOM scans following Pattern A: ANOM-04 squared variant (Ljung-Box on returns²), ANOM-10 outlier detection (z-score + modified-z / MAD), ANOM-11 drawdown profile (peak-trough scan on cumulative log-returns equity curve). All three have tight numerical tolerances (1e-10 to 1e-12) since they are pure arithmetic — no statistical distribution interpolation.

Purpose: complete the "easy" half of ANOM (the half not requiring AIC lag selection / KPSS / VR / ARCH-LM / JB). After this plan, ANOM-04 (both variants), ANOM-10, ANOM-11 are shipped; Plans 04-05/04-06 handle the five hand-derived heavyweight tests.
Output: 3 new registered scans + 3 integration tests + Pattern M shuffled-future extension is NOT needed (none of these three is rolling/causal — all are full-series scans).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-CONTEXT.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-VALIDATION.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-03-SUMMARY.md
@crates/miner-core/src/scan/ljung_box/mod.rs
@crates/miner-core/src/scan/ljung_box/kernel.rs
@crates/miner-core/src/scan/anom/returns/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ANOM-04 stats.autocorr.ljung_box_sq@1 — Ljung-Box on squared returns + register + integration test</name>
  <files>crates/miner-core/src/scan/anom/ljung_box_sq/mod.rs, crates/miner-core/src/scan/anom/ljung_box_sq/kernel.rs, crates/miner-core/src/scan/anom/mod.rs, crates/miner-core/tests/scan_ljung_box_squared.rs</files>
  <read_first>
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md (Pattern A; "Scan implementations" row for `crates/miner-core/src/scan/anom/ljung_box/` — squared-returns variant: "refactor: factor returns out, register `_sq` variant")
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md (§Section 2 row for stats.autocorr.ljung_box_sq@1: same as ljung_box but input = returns²; effect.metric "ljung_box_q_squared"; effect.extra carries `series_kind="squared_returns"`; tolerance 1e-10 / 1e-8)
    - crates/miner-core/src/scan/ljung_box/mod.rs (the EXISTING Phase 3 scan — full Scan impl; will be REUSED for the kernel calls; the new ljung_box_sq scan is a near-clone with squared-returns preprocessing)
    - crates/miner-core/src/scan/ljung_box/kernel.rs (biased_acf + ljung_box_q_and_p — reusable kernel functions; visibility may need adjustment from pub(super) to pub(crate))
    - crates/miner-core/src/scan/primitives/returns.rs (log_returns for the input)
  </read_first>
  <behavior>
    - Test (`ljung_box_sq_id_and_version`): id == "stats.autocorr.ljung_box_sq", version == 1
    - Test (`ljung_box_sq_arity_is_single`)
    - Test (`ljung_box_sq_param_schema`): same shape as ljung_box (lags, etc.)
    - Test (`ljung_box_sq_kernel_input_is_returns_squared`): runs scan and asserts the returns vector in raw.series is the squared log-returns, NOT raw log-returns (kernel.rs has a `square_returns(&returns) -> Vec<f64>` helper that's verified by unit test)
    - Test (`ljung_box_sq_effect_extra_series_kind`): effect.extra contains key "series_kind" with string value "squared_returns"
    - Test (`ljung_box_sq_invalid_params`)
    - Test (`ljung_box_sq_emits_one_result`)
    - Test (`ljung_box_sq_cancellation`)
    - Test (`ljung_box_sq_q_stat_on_iid_returns_below_critical`): for an IID Gaussian-seeded BarFrame (lcg_closes), squared returns are independent → Q-stat is small (a smoke check; the actual statsmodels golden lives in Plan 04-11)
    - Integration test (`scan_ljung_box_squared_happy_path`): insta envelope snapshot on a deterministic seed
  </behavior>
  <action>
    Open crates/miner-core/src/scan/ljung_box/kernel.rs. Change the visibility of `biased_acf` and `ljung_box_q_and_p` from `pub(super)` to `pub(crate)` so the new ljung_box_sq scan can call them. Add `#[inline] pub(crate) fn square_returns(returns: &[f64]) -> Vec<f64> { returns.iter().map(|r| r * r).collect() }` to ljung_box/kernel.rs OR to a new ljung_box_sq/kernel.rs (decision: put in ljung_box_sq/kernel.rs since it's specific to the squared variant).

    Create `crates/miner-core/src/scan/anom/ljung_box_sq/` directory with `mod.rs` + `kernel.rs`. Pattern A near-clone of the existing ljung_box scan.

    `mod.rs`:
    - SCAN_ID = "stats.autocorr.ljung_box_sq", SCAN_VERSION = 1, EFFECT_METRIC = "ljung_box_q_squared"
    - Single arity
    - param_schema: identical to ljung_box (lags param)
    - finding_fields.effect_extra_keys = &["lags", "q_stats", "p_values", "acf", "series_kind"]; raw_series_keys = &["returns_squared", "timestamps_ms"]
    - run body: cancel-at-entry; compute returns via primitives::returns::log_returns; compute returns_squared via kernel::square_returns; reuse ljung_box::kernel::biased_acf + ljung_box::kernel::ljung_box_q_and_p; construct envelope with effect.extra inserting "series_kind" with String value "squared_returns"; raw.series.returns_squared is the squared series; raw.series.timestamps_ms is the timestamp tail aligned with returns_squared

    `kernel.rs`: just the `pub(super) fn square_returns(returns: &[f64]) -> Vec<f64>` and a unit test on a small known input (e.g., [1.0, -2.0, 3.0] → [1.0, 4.0, 9.0]).

    Update anom/mod.rs to declare `pub mod ljung_box_sq;` + re-export. Append a register line to crates/miner-core/src/scan/anom/mod.rs::register_anom_scans — alphabetically "stats.autocorr.ljung_box_sq" sorts immediately after "stats.autocorr.ljung_box" (which is the existing LjungBoxScan). Update existing registry tests' expected scan counts.

    Create `crates/miner-core/tests/scan_ljung_box_squared.rs` per Pattern J. Happy-path: build deterministic LCG-seeded BarFrame; run LjungBoxSqScan; assert envelope shape via insta snapshot; verify series_kind == "squared_returns".

    The Phase 3 LjungBox golden (`cargo test -p miner-core --test scan_ljung_box`) MUST continue to pass byte-identically — D4-06 / Pitfall 9 invariant. The new ljung_box_sq scan does not touch the existing LjungBox kernel computation path on RETURNS (only on RETURNS²); the visibility change from pub(super) → pub(crate) is non-behavioural.
  </action>
  <acceptance_criteria>
    - `cargo build -p miner-core --all-targets` exits 0
    - `grep -n 'pub\\(crate\\) fn biased_acf\\|pub\\(crate\\) fn ljung_box_q_and_p' crates/miner-core/src/scan/ljung_box/kernel.rs` finds ≥ 2 matches (visibility widened)
    - `grep -n 'stats.autocorr.ljung_box_sq' crates/miner-core/src/scan/anom/ljung_box_sq/mod.rs` finds ≥ 1 match
    - `grep -n 'fn square_returns' crates/miner-core/src/scan/anom/ljung_box_sq/kernel.rs` finds 1 match
    - `grep -n 'LjungBoxSqScan\\|LjungBoxSquaredScan' crates/miner-core/src/scan/anom/mod.rs` finds ≥ 1 match
    - `cargo test -p miner-core --lib scan::anom::ljung_box_sq::tests` passes
    - `cargo test -p miner-core --test scan_ljung_box_squared` integration test passes
    - The Phase 3 LjungBox golden test `cargo test -p miner-core --test scan_ljung_box` continues to pass BYTE-IDENTICALLY
    - `cargo run -p miner-cli --quiet -- scans | grep -q '"id":"stats.autocorr.ljung_box_sq"'`
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p miner-core --all-targets && cargo test -p miner-core --lib scan::anom::ljung_box_sq::tests && cargo test -p miner-core --test scan_ljung_box_squared --test scan_ljung_box && cargo run -p miner-cli --quiet -- scans 2>/dev/null | grep -q '"id":"stats.autocorr.ljung_box_sq"'</automated>
  </verify>
  <done>stats.autocorr.ljung_box_sq@1 is a registered Single-arity scan reusing the existing Phase 3 ACF + Q-stat kernels on squared returns; the Phase 3 LjungBox golden continues to pass byte-identically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ANOM-10 stats.outliers.z_and_mad@1 — z-score + modified-z outlier detection + register + integration test</name>
  <files>crates/miner-core/src/scan/anom/outliers/mod.rs, crates/miner-core/src/scan/anom/outliers/kernel.rs, crates/miner-core/src/scan/anom/mod.rs, crates/miner-core/tests/scan_outliers.rs</files>
  <read_first>
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md (Pattern A; Pattern B kernel discipline)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md (§Section 2 row for stats.outliers.z_and_mad@1: reference = scipy.stats.zscore + hand-rolled 0.6745*(x-median)/MAD per Iglewicz-Hoaglin; effect.value = count of outliers at z>3; effect.extra keys = z_threshold, mad_threshold, outlier_indices, outlier_values_z, outlier_values_modified_z, mad, median; tolerance 1e-12)
    - crates/miner-core/src/scan/anom/summary/kernel.rs (created in Plan 04-03 — provides hand-derivable mean/std helpers usable here, OR re-roll a small two-pass for clarity)
  </read_first>
  <behavior>
    - Test (`outliers_id_and_version`)
    - Test (`outliers_arity_is_single`)
    - Test (`outliers_param_schema`): `z_threshold: number default 3.0`, `modified_z_threshold: number default 3.5`, `series: enum["log_returns","close"] default "log_returns"`
    - Test (`outliers_modified_z_iglewicz_hoaglin`): for a known input where median=0, MAD=1, x=4 → modified_z = 0.6745 * 4 / 1 = 2.698 → NOT outlier at threshold 3.5; x=10 → modified_z = 6.745 → IS outlier
    - Test (`outliers_z_score_basic`): for input [1,2,3,4,100] the last index is clearly an outlier under both z (~2.0 * std) and modified-z
    - Test (`outliers_indices_match`): outlier_indices Vec is the union of z-outliers and modified-z-outliers (Plan choice: report both vectors separately AND a union vector; pin via behavior test)
    - Test (`outliers_emits_one_result`)
    - Test (`outliers_result_envelope_shape`): effect.metric == "outliers_count", effect.value == count of outliers (as f64), effect.extra carries all 7 documented keys, raw.series.{returns, timestamps_ms}
    - Test (`outliers_cancellation`)
    - Test (`outliers_zero_variance_emits_scan_error`): if MAD == 0.0 (constant input) → ScanError::Kernel (modified-z divides by MAD)
    - Test (`outliers_n_zero_n_one`): both produce ScanError
    - Integration test (`scan_outliers_happy_path`): insta envelope snapshot
  </behavior>
  <action>
    Create `crates/miner-core/src/scan/anom/outliers/` with `mod.rs` + `kernel.rs`. Pattern A clone.

    `mod.rs`:
    - SCAN_ID = "stats.outliers.z_and_mad", SCAN_VERSION = 1, EFFECT_METRIC = "outliers_count"
    - Single arity
    - param_schema: z_threshold (default 3.0), modified_z_threshold (default 3.5), series (enum default "log_returns")
    - finding_fields.effect_extra_keys = &["z_threshold", "mad_threshold", "outlier_indices", "outlier_values_z", "outlier_values_modified_z", "mad", "median"]
    - run body: cancel-at-entry; resolve params; compute series; call kernel::z_scores + kernel::modified_z_scores; build outlier index vector (union); construct envelope with effect.value = outlier_indices.len() as f64

    `kernel.rs`:
    - `#[inline] pub(super) fn z_scores(values: &[f64]) -> (Vec<f64>, f64, f64)` returning (z_scores, mean, std)
    - `#[inline] pub(super) fn modified_z_scores(values: &[f64]) -> (Vec<f64>, f64, f64)` returning (modified_z, median, mad). Uses 0.6745 constant per Iglewicz-Hoaglin. debug_assert!(values.len() >= 1).
    - `#[inline] fn median(values: &[f64]) -> f64` — sort a copy, return median element (linear interpolation for even-length)
    - `#[inline] fn mad(values: &[f64], median: f64) -> f64` — median absolute deviation = median(|x - median|)
    - Constant-input branch: if MAD == 0.0 → return MAD = 0.0 and let mod.rs::run convert to ScanError::Kernel
    - Unit tests: hand-derived expectations on [1,2,3,4,5] (median=3, MAD = median([2,1,0,1,2]) = 1, modified_z values predictable)

    Update anom/mod.rs to declare `pub mod outliers;` + re-export. Update register_anom_scans alphabetical position: "stats.outliers.z_and_mad" sorts after "stats.drawdown.profile" alphabetically — wait, drawdown is Task 3 of this plan and may not exist yet. Order depends on which task runs first; the engine doesn't care about registration order — only the `registry_iter_lex_order` test does, and that test sorts by id internally. Just register in the order this plan creates them.

    Create `crates/miner-core/tests/scan_outliers.rs` per Pattern J.
  </action>
  <acceptance_criteria>
    - `cargo build -p miner-core --all-targets` exits 0
    - `grep -nE 'stats\.outliers\.z_and_mad' crates/miner-core/src/scan/anom/outliers/mod.rs` finds ≥ 1 match
    - `grep -n 'fn z_scores\\|fn modified_z_scores' crates/miner-core/src/scan/anom/outliers/kernel.rs` finds ≥ 2 matches
    - `grep -nE '0\\.6745' crates/miner-core/src/scan/anom/outliers/kernel.rs` finds ≥ 1 match (the Iglewicz-Hoaglin constant)
    - `grep -n 'OutliersZAndMadScan' crates/miner-core/src/scan/anom/mod.rs` finds ≥ 1 match
    - `cargo test -p miner-core --lib scan::anom::outliers::tests` passes
    - `cargo test -p miner-core --test scan_outliers` integration test passes
    - `cargo run -p miner-cli --quiet -- scans | grep -q '"id":"stats.outliers.z_and_mad"'`
    - The Iglewicz-Hoaglin modified-z hand-derived unit test passes within 1e-12
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p miner-core --all-targets && cargo test -p miner-core --lib scan::anom::outliers::tests && cargo test -p miner-core --test scan_outliers && cargo run -p miner-cli --quiet -- scans 2>/dev/null | grep -q '"id":"stats.outliers.z_and_mad"'</automated>
  </verify>
  <done>stats.outliers.z_and_mad@1 emits outlier indices + values for both z-score and modified-z criteria; hand-derived Iglewicz-Hoaglin unit tests pass within 1e-12.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ANOM-11 stats.drawdown.profile@1 — peak-trough drawdown profile + register + integration test</name>
  <files>crates/miner-core/src/scan/anom/drawdown/mod.rs, crates/miner-core/src/scan/anom/drawdown/kernel.rs, crates/miner-core/src/scan/anom/mod.rs, crates/miner-core/tests/scan_drawdown.rs</files>
  <read_first>
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md (Pattern A; Pattern B)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md (§Section 2 row for stats.drawdown.profile@1: hand-derived — no statsmodels reference; effect.value = max_drawdown; effect.extra = equity_curve, peaks, troughs, drawdown_durations_ms, time_to_recover_ms, dd_distribution_p50_p95_p99; tolerance 1e-12)
    - crates/miner-core/src/scan/primitives/returns.rs (log_returns for building the cumulative equity curve)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md "No Analog Found" — "ANOM-11 drawdown is hand-derived against scipy/statsmodels references during plan-phase. The kernel-FILE shape still matches ljung_box/kernel.rs exactly."
  </read_first>
  <behavior>
    - Test (`drawdown_id_and_version`)
    - Test (`drawdown_arity_is_single`)
    - Test (`drawdown_param_schema`): optional `series` enum["log_returns", "close"] default "log_returns"
    - Test (`drawdown_known_input_monotonic_increasing`): for closes [1,2,3,4,5] (monotonic uptrend), max_drawdown == 0.0, peaks.len() == 1 (or 0 depending on convention), troughs.len() == 0
    - Test (`drawdown_known_input_v_shape`): for closes [10, 5, 10] (50% dd then recover), max_drawdown == ln(5/10) ≈ -0.6931, drawdown_durations_ms has 1 entry, time_to_recover_ms[0] is the gap between trough and recovery
    - Test (`drawdown_dd_distribution_percentiles`): the p50/p95/p99 vector has length 3 in that order
    - Test (`drawdown_emits_one_result`)
    - Test (`drawdown_result_envelope_shape`): effect.metric == "max_drawdown", effect.value <= 0.0 always (drawdown is negative or zero), effect.extra has all 7 documented keys
    - Test (`drawdown_cancellation`)
    - Test (`drawdown_n_zero_n_one`): both produce ScanError
    - Test (`drawdown_raw_new_enforces_timestamps_ms`)
    - Integration test (`scan_drawdown_happy_path`): insta envelope snapshot
  </behavior>
  <action>
    Create `crates/miner-core/src/scan/anom/drawdown/` with `mod.rs` + `kernel.rs`. Pattern A clone.

    `mod.rs`:
    - SCAN_ID = "stats.drawdown.profile", SCAN_VERSION = 1, EFFECT_METRIC = "max_drawdown"
    - Single arity
    - param_schema: optional series enum default "log_returns"
    - finding_fields.effect_extra_keys = &["equity_curve", "peaks", "troughs", "drawdown_durations_ms", "time_to_recover_ms", "dd_distribution_p50_p95_p99"]; raw_series_keys = &["returns", "timestamps_ms"]
    - run body: cancel-at-entry; resolve series param; compute log_returns; call kernel::cumulative_log_equity (computes equity_curve = log(close/close[0]) effectively, or cumulative sum of log_returns starting at 0); call kernel::compute_drawdown_profile(&equity_curve, &ts) returning a struct with all the fields; construct envelope

    `kernel.rs`:
    - `pub struct DrawdownProfile { pub max_dd: f64, pub equity_curve: Vec<f64>, pub peaks: Vec<usize>, pub troughs: Vec<usize>, pub durations_ms: Vec<i64>, pub time_to_recover_ms: Vec<i64>, pub dd_dist_percentiles: [f64; 3] }`
    - `#[inline] pub(super) fn cumulative_log_equity(log_returns: &[f64]) -> Vec<f64>` — sequential cumulative sum; length n
    - `#[inline] pub(super) fn compute_drawdown_profile(equity_curve: &[f64], ts: &[DateTime<Utc>]) -> DrawdownProfile` — single-pass: track running peak; at each index compute dd = equity[i] - peak; if dd < current_min, update; if equity[i] >= last_peak, close the drawdown episode (record duration + time_to_recover); finally compute p50/p95/p99 of the recorded drawdown magnitudes (or zeros if no episodes)
    - debug_assert! invariants
    - Hand-derived unit tests for [10, 5, 10] (single V) and [10, 8, 5, 7, 10] (more complex)

    Update anom/mod.rs to declare `pub mod drawdown;` + re-export. append register line to register_anom_scans().

    Create `crates/miner-core/tests/scan_drawdown.rs` per Pattern J with insta envelope snapshot on a deterministic LCG-seeded BarFrame (the LCG-seeded series naturally has drawdowns since returns are in (1.0, 2.0)).
  </action>
  <acceptance_criteria>
    - `cargo build -p miner-core --all-targets` exits 0
    - `grep -nE 'stats\.drawdown\.profile' crates/miner-core/src/scan/anom/drawdown/mod.rs` finds ≥ 1 match
    - `grep -n 'fn cumulative_log_equity\\|fn compute_drawdown_profile' crates/miner-core/src/scan/anom/drawdown/kernel.rs` finds ≥ 2 matches
    - `grep -n 'DrawdownProfileScan' crates/miner-core/src/scan/anom/mod.rs` finds ≥ 1 match
    - `cargo test -p miner-core --lib scan::anom::drawdown::tests` passes
    - `cargo test -p miner-core --test scan_drawdown` integration test passes
    - `cargo run -p miner-cli --quiet -- scans | grep -q '"id":"stats.drawdown.profile"'`
    - The V-shape known-input test passes within 1e-12 tolerance
    - The all-prior tests in `cargo test -p miner-core` continue to pass
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p miner-core --all-targets && cargo test -p miner-core --lib scan::anom::drawdown::tests && cargo test -p miner-core --test scan_drawdown --test scan_outliers --test scan_ljung_box_squared --test scan_ljung_box && cargo run -p miner-cli --quiet -- scans 2>/dev/null | grep -q '"id":"stats.drawdown.profile"'</automated>
  </verify>
  <done>stats.drawdown.profile@1 emits a deterministic drawdown profile with peaks/troughs/durations/percentiles for a synthetic equity curve; hand-derived V-shape unit test passes within 1e-12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scan kernel → BarFrame.close | Untrusted close array; outlier detection iterates O(n) plus a sort for median |
| Drawdown kernel state | Cumulative state machine over n bars; bounded memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | Outlier kernel with constant input | mitigate | MAD == 0.0 explicitly converted to ScanError::Kernel; no NaN propagation. Behavior test `outliers_zero_variance_emits_scan_error` gates this. |
| T-04-04-02 | Denial of Service | drawdown kernel state machine | mitigate | Single O(n) pass with O(k) recorded drawdown episodes where k ≤ n. No quadratic behavior. |
| T-04-04-03 | Tampering | LjungBox kernel visibility widening (pub(super) → pub(crate)) | mitigate | Visibility change is non-behavioural; the Phase 3 LjungBox golden integration test gates that the kernel behaves identically. |
| T-04-04-SC | Tampering | No new packages | accept | This plan installs no new packages. |
</threat_model>

<verification>
- cargo build -p miner-core --all-targets exits 0
- cargo test -p miner-core --lib (every unit test including the three new submodules passes)
- cargo test -p miner-core --test scan_ljung_box_squared --test scan_outliers --test scan_drawdown (all three integration tests pass)
- The Phase 3 LjungBox golden test (cargo test -p miner-core --test scan_ljung_box) continues to pass byte-identically
- cargo run -p miner-cli -- scans emits arity=single for all three new scans
- cargo clippy -p miner-core --all-targets -- -D warnings clean
</verification>

<success_criteria>
- stats.autocorr.ljung_box_sq@1 reuses Phase 3 ACF + Q-stat kernels on squared returns (ANOM-04 squared variant)
- stats.outliers.z_and_mad@1 emits z-score + Iglewicz-Hoaglin modified-z outlier vectors (ANOM-10)
- stats.drawdown.profile@1 emits peak-trough drawdown profile with percentiles (ANOM-11)
- All three appear in `miner scans` JSONL with arity=single
- Phase 3 LjungBox golden continues to pass byte-identically (Pitfall 9 invariant preserved)
- Hand-derived kernel unit tests pass within 1e-12 (pure arithmetic kernels)
</success_criteria>

<output>
Create `.planning/phases/04-scan-catalogue-anom-cross-seas/04-04-SUMMARY.md` documenting the three new scans' canonical envelope fields, kernel hand-derivation references (Iglewicz-Hoaglin for ANOM-10, drawdown V-shape examples for ANOM-11), and any visibility-widening changes to the existing ljung_box/kernel.rs module.
</output>
