---
phase: 04-scan-catalogue-anom-cross-seas
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - crates/miner-core/src/scan/mod.rs
  - crates/miner-core/src/scan/primitives/mod.rs
  - crates/miner-core/src/scan/primitives/returns.rs
  - crates/miner-core/src/scan/primitives/time_alignment.rs
  - crates/miner-core/src/scan/primitives/raw_array.rs
  - crates/miner-core/src/scan/anom/mod.rs
  - crates/miner-core/src/scan/cross/mod.rs
  - crates/miner-core/src/scan/seas/mod.rs
  - crates/miner-core/src/scan/registry.rs
  - crates/miner-core/src/engine/preflight.rs
  - crates/miner-core/src/engine/gap_policy.rs
  - crates/miner-core/src/engine/mod.rs
  - crates/miner-core/src/gap.rs
  - crates/miner-cli/src/scan_args.rs
  - crates/miner-cli/src/main.rs
  - crates/miner-core/tests/two_leg_facade.rs
  - crates/miner-core/tests/arity_preflight.rs
  - crates/miner-core/tests/gap_intersect_cross.rs
  - crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md
  - crates/miner-core/tests/goldens/python-requirements.lock
  - crates/miner-core/tests/goldens/.gitkeep
autonomous: true
requirements:
  - ANOM-01
  - CROSS-01
tags:
  - rust
  - engine
  - cli
  - primitives

must_haves:
  truths:
    - "User can run `miner scan stats.autocorr.ljung_box@1 --instrument EURUSD:bid --timeframe 15m --window ...` with the new repeatable --instrument SYMBOL:side flag (D4-02)"
    - "User triggers PreflightCode::WrongInstrumentArity by passing two --instrument flags to a Single-arity scan, or one to a Pair-arity scan"
    - "User can call ctx.bars_up_to(ts) on rolling scans and receive a borrow-only BarFrameView truncated at or before ts (look-ahead-safety enforcement)"
    - "User imports primitives::returns::{log_returns, simple_returns, intraday_returns, overnight_returns} and gets a byte-identical move of Phase 3 ljung_box::kernel::log_returns (D4-06 + Pitfall 9)"
    - "User imports primitives::time_alignment::inner_join + intersect_gaps for CROSS scan plumbing (CROSS-01 / D4-04)"
    - "User reads crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md and finds pinned statsmodels + scipy + arch + python versions"
    - "User receives byte-identical Phase 3 LjungBox golden after primitives refactor (D4-06; Pitfall 9 prevented)"
  artifacts:
    - path: "crates/miner-core/src/scan/primitives/returns.rs"
      provides: "log_returns, simple_returns, intraday_returns, overnight_returns kernel functions (ANOM-01 primitive)"
      contains: "pub fn log_returns"
    - path: "crates/miner-core/src/scan/primitives/time_alignment.rs"
      provides: "inner_join(BarFrame, BarFrame) -> AlignedPair + intersect_gaps(GapManifest, GapManifest) -> GapManifest (CROSS-01 primitive + D4-04 helper)"
      contains: "pub fn inner_join"
    - path: "crates/miner-core/src/engine/preflight.rs"
      provides: "validate_arity(scan, &instruments) -> Result<(), WireError>"
      contains: "fn validate_arity"
    - path: "crates/miner-cli/src/scan_args.rs"
      provides: "repeatable --instrument SYMBOL:side parser"
      contains: "instruments: Vec<InstrumentSpec>"
    - path: "crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md"
      provides: "Pinned statsmodels 0.14.6 + scipy 1.14.x + arch 7.x + numpy versions for golden reproducibility"
      min_lines: 25
  key_links:
    - from: "crates/miner-cli/src/scan_args.rs"
      to: "ScanRequest.instruments: Vec<InstrumentSpec>"
      via: "clap value-parser parse_instrument_spec applied to repeatable --instrument SYMBOL:side flag (D4-02)"
      pattern: "value_parser = parse_instrument_spec"
    - from: "crates/miner-core/src/engine/mod.rs::run_one_with_registry"
      to: "crates/miner-core/src/engine/preflight.rs::validate_arity"
      via: "engine adds an arity preflight step in the 7-step algorithm between resolve_scan and parse_params (D4-02)"
      pattern: "validate_arity"
    - from: "crates/miner-core/src/scan/ljung_box/kernel.rs"
      to: "crates/miner-core/src/scan/primitives/returns.rs::log_returns"
      via: "D4-06: LjungBoxScan refactor calls the primitive instead of its private log_returns body — MUST be byte-identical move (Pitfall 9)"
      pattern: "primitives::returns::log_returns"
---

<objective>
Wire the engine, CLI, primitives, and per-family namespace stubs that every Phase 4 scan plan depends on. Specifically: (a) add the ANOM-01 returns primitive + CROSS-01 time_alignment primitive (both kernel-only); (b) extend engine::preflight with validate_arity; (c) extend engine::gap_policy + gap.rs with the two-leg intersection helper; (d) extend ScanCtx with bars_pair + bars_up_to(ts) look-ahead-safety view; (e) extend the CLI ScanArgs with repeatable --instrument SYMBOL:side; (f) create empty anom/cross/seas namespace mod.rs files and per-family register helpers so subsequent plans can append scans without touching the central registry.rs::bootstrap(); (g) seed goldens/REFERENCE-VERSIONS.md and python-requirements.lock; (h) seed integration-test scaffolds (two_leg_facade.rs, arity_preflight.rs, gap_intersect_cross.rs) so per-scan plans append without re-laying scaffolding.

Refactor LjungBoxScan (D4-06) to call primitives::returns::log_returns instead of its private body — MUST be byte-identical move per Pitfall 9.

Task structure: the foundational work is split into Task 1a (primitives + ljung_box refactor + byte-identical-golden gate) and Task 1b (namespace stubs + goldens scaffolding) to keep each task at 6-8 files modified (originally Task 1 touched 14 files in one task — split for clarity per checker recommendation). Task 2 wires the engine + scan-ctx; Task 3 wires the CLI.

Purpose: every scan plan downstream consumes these primitives, the arity preflight, and the per-family register helpers. Without this wave, the scan plans would either duplicate primitive logic or serialize on registry.rs::bootstrap().
Output: complete engine + CLI + primitives + namespace scaffolding. ZERO new scan registrations beyond LjungBoxScan rewire (per-family registrations land in 04-03..04-10). The Phase 3 LjungBox golden test must continue to pass byte-identically (D4-06 / D3-23 invariant).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-CONTEXT.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-VALIDATION.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-01-SUMMARY.md

<interfaces>
<!-- Established by Plan 04-01 -->
From crates/miner-core/src/scan/mod.rs (post Plan 04-01):
- pub struct InstrumentSpec { pub symbol: String, pub side: Side } (impl Display + FromStr)
- pub enum ScanArity { Single, Pair } (serde snake_case + as_str)
- Scan trait now has `fn arity(&self) -> ScanArity`
- ScanRequest carries `pub instruments: Vec<InstrumentSpec>`
- ScanCtx still carries `pub bars: &'a BarFrame` (single-leg fallback)

From crates/miner-core/src/findings/mod.rs (post Plan 04-01):
- DataSlice carries either `pub sources: Vec<Source>` (D4-03) or `pub source: Source` + `pub peer_sources: Vec<Source>` (D4-03-ALT)

From crates/miner-core/src/error/codes.rs (post Plan 04-01):
- PreflightCode::WrongInstrumentArity exists, as_str returns "wrong_instrument_arity"

From crates/miner-core/src/aggregator.rs (Phase 2, unchanged):
- pub struct BarFrame { pub source_id: String, pub symbol: String, pub side: Side, pub tf: Timeframe, pub ts_open_utc: Vec<DateTime<Utc>>, pub open: Vec<f64>, pub high: Vec<f64>, pub low: Vec<f64>, pub close: Vec<f64>, pub tick_volume: Vec<f64> }

From crates/miner-core/src/gap.rs (Phase 2, unchanged):
- pub struct GapManifest { pub spans: Vec<GapSpan>, ... }
- pub struct GapSpan { pub start: DateTime<Utc>, pub end: DateTime<Utc>, pub reason: GapReason }

From crates/miner-core/src/scan/ljung_box/kernel.rs (Phase 3, lines 25-28):
- pub(super) fn log_returns(closes: &[f64]) -> Vec<f64> — MUST be moved verbatim (Pitfall 9)

From crates/miner-core/src/engine/preflight.rs (Phase 3):
- fn resolve_scan_id_at_version(...) -> Result<(...), WireError>
- fn resolve_scan(...) -> Result<&dyn Scan, WireError>

From crates/miner-core/src/engine/mod.rs::run_one_with_registry (Phase 3, ~line 211+):
- 7-step facade: resolve_scan -> parse_params -> build_framing -> dry_run -> gap_policy::dispatch -> Scan::run -> RunSummary

From crates/miner-core/src/engine/gap_policy.rs (Phase 3):
- pub enum GapDispatch { ... }
- pub fn dispatch(manifest, policy, window) -> GapDispatch

From crates/miner-cli/src/scan_args.rs (Phase 3):
- pub struct ScanArgs { pub scan: String, pub instrument: String, pub side: String, pub timeframe: ..., pub window: String, ... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1a: Primitives (returns + time_alignment + raw_array helper) + LjungBoxScan refactor + Phase 3 golden byte-identical gate</name>
  <files>crates/miner-core/src/scan/primitives/mod.rs, crates/miner-core/src/scan/primitives/returns.rs, crates/miner-core/src/scan/primitives/time_alignment.rs, crates/miner-core/src/scan/primitives/raw_array.rs, crates/miner-core/src/scan/mod.rs, crates/miner-core/src/gap.rs, crates/miner-core/src/scan/ljung_box/kernel.rs, crates/miner-core/src/scan/ljung_box/mod.rs</files>
  <read_first>
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md (Pattern A imports block; the `f64_slice_to_raw_array` helper lift recommendation in Pattern A — "Plan-research §3 recommends lifting to scan::primitives::raw_array"; Pitfall 9 / D4-06 "move, do not rewrite")
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md (§1.4 ANOM-01 surface; §Section 1.6 two-leg fetch ordering; the "Two-leg time alignment via inner-join" code block at lines 596-628)
    - crates/miner-core/src/scan/ljung_box/kernel.rs (the existing log_returns function lines 25-28 — the byte-identical source to move; also the file's header + clippy gate + statrs import block lines 1-19 for the kernel-file convention)
    - crates/miner-core/src/scan/ljung_box/mod.rs (the existing f64_slice_to_raw_array helper lines ~298-312 — the source to lift)
    - crates/miner-core/src/scan/mod.rs (current shape post Plan 04-01 — for the new `pub mod primitives;` declaration)
    - crates/miner-core/src/aggregator.rs (BarFrame columnar shape — the input to inner_join)
    - crates/miner-core/src/gap.rs (GapManifest + GapSpan + GapReason — the input to intersect_gaps)
    - crates/miner-core/tests/fixtures/ (existing fixture builders — for inspecting what a synthetic BarFrame builder looks like)
  </read_first>
  <behavior>
    - Test 1 (`primitives::returns::log_returns_matches_ljung_box_kernel`): for the AR(1) seeded series produced by Phase 3's `ar1_bar_frame_seeded`, primitives::returns::log_returns(&closes) returns the same Vec<f64> (length n-1) as the old ljung_box::kernel::log_returns(&closes) within 0.0 (byte-exact — D4-06 requires byte-identical move)
    - Test 2 (`primitives::returns::simple_returns_basic`): `simple_returns(&[1.0, 1.1, 1.21])` returns `[0.1, 0.1]` within 1e-12
    - Test 3 (`primitives::returns::log_returns_empty`): empty input returns empty Vec
    - Test 4 (`primitives::returns::log_returns_singleton`): single-element input returns empty Vec
    - Test 5 (`primitives::time_alignment::inner_join_disjoint_returns_empty`): two BarFrames with zero overlapping ts_open_utc return an AlignedPair with len 0
    - Test 6 (`primitives::time_alignment::inner_join_partial_overlap`): two BarFrames where bars_a covers t∈[1,2,3] and bars_b covers t∈[2,3,4] produce AlignedPair { timestamps_ms: [2,3].map(to_ms), close_a: [a[1], a[2]], close_b: [b[0], b[1]] }
    - Test 7 (`primitives::time_alignment::intersect_gaps_no_overlap_is_empty`): two GapManifests with non-overlapping spans intersect to an empty manifest
    - Test 8 (`primitives::time_alignment::intersect_gaps_partial_overlap`): manifests with overlapping spans intersect to the overlap region
    - Test 9 (existing `scan_ljung_box` golden test): MUST pass byte-identically after the LjungBoxScan refactor (D4-06 — Pitfall 9 invariant)
  </behavior>
  <action>
    Create the `primitives` namespace module under crates/miner-core/src/scan/primitives/ with mod.rs declaring `pub mod returns;`, `pub mod time_alignment;`, `pub mod raw_array;`. Wire `pub mod primitives;` into crates/miner-core/src/scan/mod.rs.

    Create `primitives/returns.rs` with four pub functions: `log_returns`, `simple_returns`, `intraday_returns`, `overnight_returns`. Each is `#[inline] pub fn <name>(closes: &[f64]) -> Vec<f64>`. The `log_returns` body MUST be a BYTE-IDENTICAL move of crates/miner-core/src/scan/ljung_box/kernel.rs's pub(super) fn log_returns (lines 25-28 of that file). The `simple_returns` body computes `windows(2).map(|w| (w[1] - w[0]) / w[0]).collect()`. `intraday_returns` and `overnight_returns` follow the same shape but accept additionally the timestamps slice (signature: `pub fn intraday_returns(closes: &[f64], ts: &[DateTime<Utc>]) -> Vec<f64>`); intraday_returns computes returns BETWEEN bars within the same UTC date; overnight_returns computes returns ACROSS the date boundary. Document in a doc comment that "intraday/overnight returns are defined relative to UTC date — caller is responsible for any timezone-shifting if needed."

    Move the existing pub(super) fn log_returns OUT of ljung_box/kernel.rs (i.e., delete the body there). Update crates/miner-core/src/scan/ljung_box/mod.rs to import `use crate::scan::primitives::returns::log_returns;` and call it. This is the D4-06 refactor. The byte-identicality test (Behavior Test 1) is the regression guard, plus the Phase 3 golden (Behavior Test 9 = `cargo test -p miner-core --test scan_ljung_box`) MUST pass unchanged.

    Create `primitives/raw_array.rs` with `#[inline] pub fn f64_slice_to_raw_array(s: &[f64]) -> RawArray`. This is the helper currently duplicated inline in ljung_box/mod.rs lines ~298-312. Lift it once; ljung_box/mod.rs imports from this primitives module. Body is verbatim from Pattern A.

    Create `primitives/time_alignment.rs` with two pub functions:
    1. `pub struct AlignedPair { pub timestamps_ms: Vec<i64>, pub close_a: Vec<f64>, pub close_b: Vec<f64> }`
    2. `pub fn inner_join(bars_a: &BarFrame, bars_b: &BarFrame) -> AlignedPair` — implementation per RESEARCH.md "Two-leg time alignment via inner-join" code block at lines 596-628. Two-pointer sweep over both sorted `ts_open_utc` vectors; when timestamps match, push to all three output vectors. ts_open_utc is `Vec<DateTime<Utc>>`; convert to milliseconds via `.timestamp_millis()` for the output.
    3. `pub fn intersect_gaps(a: &GapManifest, b: &GapManifest) -> GapManifest` — interval-intersection on the two manifests' spans (treat each span as a half-open or closed interval per the existing GapSpan convention). The output GapManifest's spans cover the union of timestamps that are missing in EITHER leg. Plan home decision per PATTERNS.md Pattern I: place in primitives/time_alignment.rs (co-located with the inner-join primitive that CROSS-01 owns). Add `GapReason::EitherLeg` only if needed (which lives in gap.rs); otherwise reuse the existing variants and pick the more conservative reason per span.

    Add the 9 behavior tests above. Tests 1-8 live in `#[cfg(test)] mod tests` blocks within their respective primitive files (returns.rs, time_alignment.rs). Test 9 is the EXISTING Phase 3 integration test — no new file, just verify it still passes after the refactor.
  </action>
  <acceptance_criteria>
    - `cargo build -p miner-core --all-targets` exits 0 after the refactor
    - `grep -n 'pub fn log_returns' crates/miner-core/src/scan/primitives/returns.rs` finds exactly 1 match
    - `grep -nE 'pub\(super\) fn log_returns|fn log_returns' crates/miner-core/src/scan/ljung_box/kernel.rs | grep -v '^#'` finds 0 matches (the body was MOVED, not duplicated)
    - `grep -n 'use crate::scan::primitives::returns::log_returns' crates/miner-core/src/scan/ljung_box/mod.rs` finds ≥ 1 match (the import that consumes the lifted primitive)
    - `grep -n 'pub fn inner_join' crates/miner-core/src/scan/primitives/time_alignment.rs` finds exactly 1 match with signature `(bars_a: &BarFrame, bars_b: &BarFrame) -> AlignedPair`
    - `grep -n 'pub fn intersect_gaps' crates/miner-core/src/scan/primitives/time_alignment.rs` finds exactly 1 match
    - `grep -n 'pub fn f64_slice_to_raw_array' crates/miner-core/src/scan/primitives/raw_array.rs` finds exactly 1 match
    - All 8 new primitive unit tests pass: `cargo test -p miner-core --lib scan::primitives::returns::tests scan::primitives::time_alignment::tests`
    - The Phase 3 LjungBox golden test passes BYTE-IDENTICALLY: `cargo test -p miner-core --test scan_ljung_box`
    - `cargo clippy -p miner-core --all-targets -- -D warnings` exits 0 (no new lints)
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p miner-core --all-targets && cargo test -p miner-core --lib scan::primitives && cargo test -p miner-core --test scan_ljung_box && cargo clippy -p miner-core --all-targets -- -D warnings</automated>
  </verify>
  <done>Primitives namespace exists with returns + time_alignment + raw_array; LjungBoxScan calls the lifted primitive (D4-06); the Phase 3 golden passes byte-identically (Pitfall 9 invariant pinned).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b: Per-family namespace stubs (anom/cross/seas with register_*_scans helpers) + registry.rs bootstrap wiring + goldens directory scaffolding (REFERENCE-VERSIONS.md + python-requirements.lock)</name>
  <files>crates/miner-core/src/scan/anom/mod.rs, crates/miner-core/src/scan/cross/mod.rs, crates/miner-core/src/scan/seas/mod.rs, crates/miner-core/src/scan/mod.rs, crates/miner-core/src/scan/registry.rs, crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md, crates/miner-core/tests/goldens/python-requirements.lock, crates/miner-core/tests/goldens/.gitkeep</files>
  <read_first>
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md (Pattern E per-family registrar pattern; the contract that Plans 04-03..04-10 only append to `register_<family>_scans`, never to registry.rs::bootstrap)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md (§1.2 reference-implementation pinning policy; §Section 7 Schemars Regen Recipe context)
    - crates/miner-core/src/scan/registry.rs (current bootstrap() body — for the extension point)
    - crates/miner-core/src/scan/mod.rs (post Task 1a — for the new `pub mod anom; pub mod cross; pub mod seas;` declarations)
    - .planning/phases/03-*/03-*-SUMMARY.md (Phase 3 LjungBoxScan golden + statsmodels 0.14.6 pin — the precedent for REFERENCE-VERSIONS.md content)
  </read_first>
  <behavior>
    - Test 1 (`scan::anom::register_anom_scans_is_noop_initially`): the helper is callable with `&mut Registry` and adds zero entries; the empty-body stub is intentional (Plans 04-03..04-10 will append register lines)
    - Test 2 (sibling for cross and seas): same shape; empty body, zero entries added on call
    - Test 3 (`scan::registry::bootstrap_invokes_all_three_family_registrars`): a unit test or fakelistener verifies bootstrap() calls register_anom_scans + register_cross_scans + register_seas_scans (Plan 04-11 will tighten this with the full 22-count assertion once all families are populated)
    - Test 4 (filesystem assertion via the verify step): the three goldens-scaffold files exist with the expected pinned versions
  </behavior>
  <action>
    Create per-family namespace stubs at crates/miner-core/src/scan/anom/mod.rs, crates/miner-core/src/scan/cross/mod.rs, crates/miner-core/src/scan/seas/mod.rs. Each file contains: a doc comment describing the family, plus a single pub function named `register_anom_scans` / `register_cross_scans` / `register_seas_scans` with signature `(r: &mut Registry) -> ()` and an EMPTY body that suppresses unused-variable warning via `let _ = r;`. Subsequent Phase 4 plans (04-03..04-10) append `r.register(Box::new(<NewScan>));` lines inside their own family's `register_<family>_scans` function, alphabetically by scan_id. NO plan after 04-02 touches `crates/miner-core/src/scan/registry.rs::bootstrap()` — that contract is established here and enforced by the per-plan `git diff --stat` acceptance criteria in 04-03..04-10.

    Update crates/miner-core/src/scan/registry.rs::bootstrap() to call the three new helpers in order: after the existing `r.register(Box::new(LjungBoxScan))` line, add three lines: `crate::scan::anom::register_anom_scans(&mut r);`, `crate::scan::cross::register_cross_scans(&mut r);`, `crate::scan::seas::register_seas_scans(&mut r);`. This is the LAST modification to bootstrap() in Phase 4 (Plan 04-11 only updates count-assertions in tests, not bootstrap() itself).

    Wire `pub mod anom; pub mod cross; pub mod seas;` into crates/miner-core/src/scan/mod.rs.

    Create the goldens directory scaffolding:
    - `crates/miner-core/tests/goldens/.gitkeep` (empty file)
    - `crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md` per RESEARCH.md §1.2: pin `statsmodels==0.14.6`, `scipy==1.14.x` (lock to specific 1.14.X version available at research date), `arch==7.x` (for ANOM-07 Lo-MacKinlay reference), `numpy==1.26.x` (statsmodels/scipy transitive), `python==3.11.x`. Include a "Regeneration" section noting: "Run `pip install --no-deps -r python-requirements.lock` in a venv, then `python crates/miner-core/tests/goldens/generate_<scan>.py > crates/miner-core/tests/goldens/<scan_id>.jsonl` for each golden." Include a Phase 3 cross-reference: "Phase 3 LjungBoxScan golden already uses statsmodels 0.14.6; Phase 4 keeps the same version pin (the path of least resistance per RESEARCH.md §1.2)." Target length ≥ 25 lines.
    - `crates/miner-core/tests/goldens/python-requirements.lock` — pip freeze output for the venv recipe in REFERENCE-VERSIONS.md. At minimum: statsmodels==0.14.6, scipy==1.14.1, arch==7.2.0, numpy==1.26.4, pandas==2.2.x, plus transitive hash-pinned entries. NOTE: The exact lockfile content may need to be generated by running pip install in a venv; the task delivers a documented placeholder file IF live pip is not available — note clearly at the top "REGENERATE WITH: python -m venv /tmp/venv && /tmp/venv/bin/pip install statsmodels==0.14.6 scipy==1.14.1 arch==7.2.0 && /tmp/venv/bin/pip freeze > crates/miner-core/tests/goldens/python-requirements.lock".

    Clippy hygiene: `#![allow(dead_code)]` is forbidden on primitives; if the namespace stubs (anom/cross/seas/mod.rs) trip dead-code warnings, prefer `#[allow(dead_code)]` at the empty-module level with a comment "// Populated by Plans 04-03..04-10". The primitives are pub (consumed downstream) and won't warn.
  </action>
  <acceptance_criteria>
    - `cargo build -p miner-core --all-targets` exits 0
    - `grep -nE 'pub fn register_anom_scans|pub fn register_cross_scans|pub fn register_seas_scans' crates/miner-core/src/scan/` (recursive) finds exactly 3 matches (one per family)
    - `grep -nE 'register_anom_scans|register_cross_scans|register_seas_scans' crates/miner-core/src/scan/registry.rs` finds 3 call-site matches in bootstrap()
    - `test -d crates/miner-core/src/scan/anom && test -d crates/miner-core/src/scan/cross && test -d crates/miner-core/src/scan/seas` (namespace dirs exist)
    - `test -f crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md` AND `wc -l < crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md` returns ≥ 25
    - `grep -q 'statsmodels==0.14.6' crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md` succeeds
    - `grep -q 'scipy' crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md` succeeds
    - `test -f crates/miner-core/tests/goldens/python-requirements.lock` (placeholder OR populated)
    - `test -f crates/miner-core/tests/goldens/.gitkeep`
    - `cargo test -p miner-core --lib scan::anom::tests scan::cross::tests scan::seas::tests` — registrar stub tests pass (empty body = zero entries added)
    - `cargo test -p miner-core --test scan_ljung_box` — Phase 3 golden continues to pass (registrar wiring did not regress the existing LjungBox registration)
    - `cargo run -p miner-cli --quiet -- scans 2>/dev/null | wc -l` returns 1 (only LjungBoxScan is registered at this point; the three family registrars are no-ops until Plans 04-03..04-10 populate them)
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p miner-core --all-targets && cargo test -p miner-core --lib scan::anom scan::cross scan::seas && cargo test -p miner-core --test scan_ljung_box && test -f crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md && grep -q 'statsmodels==0.14.6' crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md && test -f crates/miner-core/tests/goldens/python-requirements.lock</automated>
  </verify>
  <done>Per-family namespace stubs (anom/cross/seas) exist with `register_*_scans(r: &mut Registry)` helpers; registry.rs::bootstrap() invokes all three; goldens directory scaffolding (REFERENCE-VERSIONS.md + python-requirements.lock + .gitkeep) is in place; Phase 3 LjungBox golden continues to pass; the per-family registrar pattern is now the SOLE registration path for Plans 04-03..04-10 (registry.rs is locked).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Engine validate_arity + two-leg gap dispatch + ScanCtx::bars_pair + ScanCtx::bars_up_to(ts) + facade wiring</name>
  <files>crates/miner-core/src/engine/preflight.rs, crates/miner-core/src/engine/gap_policy.rs, crates/miner-core/src/engine/mod.rs, crates/miner-core/src/scan/mod.rs, crates/miner-core/tests/arity_preflight.rs, crates/miner-core/tests/two_leg_facade.rs, crates/miner-core/tests/gap_intersect_cross.rs</files>
  <read_first>
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md (Pattern H validate_arity helper full code block; Pattern I two-leg gap-policy intersection; Pattern L look-ahead-safe ctx.bars_up_to(ts) API)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md (§1.5 look-ahead-safety API shape — `bars_up_to(ts) -> BarFrameView<'_>` with partition_point cutoff; §1.6 two-leg fetch ordering + cancel poll; §Validation Architecture lines 1054-1057 for the integration-test scaffold names)
    - crates/miner-core/src/engine/preflight.rs (existing resolve_scan_id_at_version lines 51-73 + resolve_scan lines 79+ — the analog WireError::preflight construction pattern)
    - crates/miner-core/src/engine/mod.rs (the run_one_with_registry 7-step algorithm — the insertion point between resolve_scan and parse_params)
    - crates/miner-core/src/engine/gap_policy.rs (existing GapDispatch enum line 99 + dispatch fn line 136 — the analog for the new Pair branch or dispatch_pair sibling)
    - crates/miner-core/src/scan/mod.rs (current ScanCtx struct — for the bars_pair field addition and bars_up_to method)
    - crates/miner-core/src/aggregator.rs (BarFrame columnar layout — for the BarFrameView field list)
    - crates/miner-core/tests/dry_run.rs (analog integration test pattern — for two_leg_facade.rs structure)
    - crates/miner-core/tests/gap_policy.rs (analog for gap_intersect_cross.rs)
    - crates/miner-core/src/scan/primitives/time_alignment.rs (created in Task 1a — provides intersect_gaps that this task consumes)
  </read_first>
  <behavior>
    - Test 1 (`engine::preflight::tests::validate_arity_accepts_correct_single`): a scan with arity Single + instruments.len() == 1 returns Ok(())
    - Test 2 (`engine::preflight::tests::validate_arity_rejects_two_instruments_for_single`): a scan with arity Single + instruments.len() == 2 returns Err(WireError::preflight(WrongInstrumentArity, ...)) with `.context["expected_arity"] == 1` and `.context["supplied_arity"] == 2`
    - Test 3 (`engine::preflight::tests::validate_arity_accepts_pair`): a scan with arity Pair + instruments.len() == 2 returns Ok(())
    - Test 4 (`engine::preflight::tests::validate_arity_rejects_one_instrument_for_pair`): a scan with arity Pair + instruments.len() == 1 returns the WrongInstrumentArity error
    - Test 5 (integration in `crates/miner-core/tests/arity_preflight.rs`): submitting a single-leg request to a Pair-arity stub scan via run_one_with_registry produces a Finding::ScanError envelope on stdout with `error_code == "wrong_instrument_arity"` and a non-zero exit code via the four-tier router
    - Test 6 (integration in `crates/miner-core/tests/two_leg_facade.rs`): a stub Pair-arity Scan implemented inline in the test that reads ctx.bars_pair, asserts (a, b) length parity, and emits one Finding::Result through run_one_with_registry — the test verifies the envelope's data_slice.sources (D4-03) or data_slice.peer_sources (D4-03-ALT) has length 2 in leg order
    - Test 7 (`scan_ctx_bars_up_to_partitions_at_cutoff` — unit in scan/mod.rs tests): given a BarFrame with ts_open_utc = [t0, t1, t2, t3], `ctx.bars_up_to(t1)` returns a BarFrameView with ts_open_utc.len() == 2 (only [t0, t1] inclusive); `ctx.bars_up_to(t0 - 1m)` returns len 0; `ctx.bars_up_to(t3 + 1d)` returns len 4 (no entries past t3)
    - Test 8 (integration in `crates/miner-core/tests/gap_intersect_cross.rs`): synthetic two-leg request where leg_a has a gap [10:00, 11:00) and leg_b has a gap [10:30, 11:30) — under strict policy, the facade emits one Finding::GapAborted carrying both legs' manifests; under continuous_only, the facade partitions on intervals where BOTH legs are continuous (one Result per partition)
  </behavior>
  <action>
    Add `pub fn validate_arity(scan: &dyn Scan, instruments: &[InstrumentSpec]) -> Result<(), WireError>` to crates/miner-core/src/engine/preflight.rs per PATTERNS.md Pattern H verbatim. The body matches `scan.arity()` against `instruments.len()` (Single -> 1, Pair -> 2), returning `WireError::preflight(PreflightCode::WrongInstrumentArity, format!(...)).with_context("scan_id", ...).with_context("expected_arity", ...).with_context("supplied_arity", ...)` on mismatch.

    Wire `validate_arity` into `engine::run_one_with_registry` between `resolve_scan` and `parse_params` (the natural ordering: id-resolve → version-check → arity-check → params-parse, per RESEARCH.md §1.10). The call pattern matches the existing `preflight::resolve_scan(...).map_err(MinerError::Preflight)?` chain. On error, the engine emits a Finding::ScanError via the existing emit_scan_error path (Phase 3 D3-03 / D3-07 / 03-07-PLAN's gap-closure) and the CLI router yields exit code 1.

    Extend `engine::gap_policy::dispatch` (or add a sibling `dispatch_pair`) for the two-leg case. Pattern I recommends either: (a) extend `dispatch` with an internal branch that handles both single-manifest and intersected-manifest cases, or (b) add `pub fn dispatch_pair(manifest_a, manifest_b, policy, window) -> GapDispatch` calling `primitives::time_alignment::intersect_gaps` and then the existing `dispatch` on the joint manifest. Prefer (b) for minimal blast radius on Phase 3 tests.

    Extend `ScanCtx<'a>` (in crates/miner-core/src/scan/mod.rs) with a new field `pub bars_pair: Option<(&'a BarFrame, &'a BarFrame)>`. Single-leg scans see `bars_pair: None`, `bars` populated. Pair-leg scans see `bars: <first leg>` (for code that still wants a default — and as a borrow-anchor for the lifetime), `bars_pair: Some((&'a leg_a, &'a leg_b))`. Helper method `pub fn bars_pair(&self) -> Option<(&'a BarFrame, &'a BarFrame)> { self.bars_pair }` for ergonomic access from CROSS scan bodies.

    Add `impl<'a> ScanCtx<'a> { pub fn bars_up_to(&self, ts: DateTime<Utc>) -> BarFrameView<'_> }` per PATTERNS.md Pattern L + RESEARCH.md §1.5. Implementation:
    - cutoff = `self.bars.ts_open_utc.partition_point(|t| *t <= ts)`
    - return `BarFrameView { source_id: &self.bars.source_id, symbol: &self.bars.symbol, side: self.bars.side, tf: self.bars.tf, ts_open_utc: &self.bars.ts_open_utc[..cutoff], close: &self.bars.close[..cutoff], open: &self.bars.open[..cutoff], high: &self.bars.high[..cutoff], low: &self.bars.low[..cutoff], tick_volume: &self.bars.tick_volume[..cutoff] }`
    - Add the BarFrameView struct in the same file (pub) with `&'a` borrows on every column. Document in a doc-comment that this is the look-ahead-safety enforcement surface.

    Extend `run_one_with_registry` for the Pair branch: when `scan.arity() == ScanArity::Pair`, fetch BarFrame for `req.instruments[0]` via BarCache::get_or_build, poll `cancel.load(Ordering::Relaxed)` and return early if set, fetch BarFrame for `req.instruments[1]`, poll cancel again, then call `dispatch_pair(manifest_a, manifest_b, req.gap_policy, req.window)`. Use the intersection helper for the manifest intersection. Wire the resulting `GapDispatch` through the existing partition / abort handling.

    Update `Finding::GapAborted` construction in the Pair branch: the envelope's data_slice carries the JOINT manifest in `data_slice.gap_manifest` (existing field) AND data_slice.sources (D4-03) has length 2, parallel to req.instruments.

    Add Test 1-4 in `mod tests` block within preflight.rs. Test 7 in `mod tests` block within scan/mod.rs (next to existing ScanCtx tests if any).

    Create `crates/miner-core/tests/arity_preflight.rs` integration test scaffold (~50 lines) implementing Test 5. Pattern analog: tests/gap_policy.rs (which calls preflight helpers directly). The test inlines a stub Pair-arity Scan (`struct StubPair; impl Scan for StubPair { fn arity(&self) -> ScanArity { ScanArity::Pair } /* minimal id/version/param_schema/finding_fields/run impls */ }`), registers it in a fresh Registry, builds a ScanRequest with `instruments: vec![InstrumentSpec { symbol: "EURUSD".into(), side: Side::Bid }]` (length 1 — wrong arity), runs `engine::run_one_with_registry`, and asserts the emitted JSONL contains a Finding::ScanError with `error_code == "wrong_instrument_arity"`.

    Create `crates/miner-core/tests/two_leg_facade.rs` integration test scaffold (~80 lines) implementing Test 6. Pattern analog: tests/dry_run.rs. The test inlines a stub Pair-arity Scan that reads `ctx.bars_pair().expect("pair")`, asserts the two legs are non-empty, and emits a Finding::Result with two-element data_slice.sources. The test verifies the envelope's data_slice.sources.len() == 2 (D4-03) or data_slice.peer_sources.len() == 1 (D4-03-ALT) per the 04-01-SCHEMA-DIFF.md decision.

    Create `crates/miner-core/tests/gap_intersect_cross.rs` integration test scaffold (~100 lines) implementing Test 8. Pattern analog: tests/gap_policy.rs. Use synthetic BarFrames with known gap spans; verify strict + intersection produces GapAborted and continuous_only + intersection produces N Results.
  </action>
  <acceptance_criteria>
    - `cargo build -p miner-core --all-targets` exits 0
    - `grep -n 'fn validate_arity' crates/miner-core/src/engine/preflight.rs` finds exactly 1 match with signature `(scan: &dyn Scan, instruments: &[InstrumentSpec]) -> Result<(), WireError>`
    - `grep -n 'validate_arity' crates/miner-core/src/engine/mod.rs` finds ≥ 1 call site (the integration into run_one_with_registry)
    - `grep -n 'fn bars_up_to' crates/miner-core/src/scan/mod.rs` finds exactly 1 method with signature `(&self, ts: DateTime<Utc>) -> BarFrameView<'_>`
    - `grep -n 'pub struct BarFrameView' crates/miner-core/src/scan/mod.rs` finds exactly 1 match
    - `grep -nE 'dispatch_pair|fn dispatch.*pair' crates/miner-core/src/engine/gap_policy.rs` finds ≥ 1 match (the new Pair branch helper)
    - `grep -n 'bars_pair' crates/miner-core/src/scan/mod.rs` finds ≥ 2 matches (field declaration + accessor method)
    - `test -f crates/miner-core/tests/arity_preflight.rs && test -f crates/miner-core/tests/two_leg_facade.rs && test -f crates/miner-core/tests/gap_intersect_cross.rs` (all three scaffold files exist)
    - `cargo test -p miner-core --lib engine::preflight::tests::validate_arity_accepts_correct_single engine::preflight::tests::validate_arity_rejects_two_instruments_for_single engine::preflight::tests::validate_arity_accepts_pair engine::preflight::tests::validate_arity_rejects_one_instrument_for_pair` — all four unit tests pass
    - `cargo test -p miner-core --lib scan::tests::scan_ctx_bars_up_to_partitions_at_cutoff` passes
    - `cargo test -p miner-core --test arity_preflight --test two_leg_facade --test gap_intersect_cross` — all three integration tests pass
    - The Phase 3 LjungBox golden test continues to pass: `cargo test -p miner-core --test scan_ljung_box`
    - The Phase 3 facade tests continue to pass: `cargo test -p miner-core --test gap_policy --test dry_run --test scan_facade_determinism`
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p miner-core --all-targets && cargo test -p miner-core --lib engine::preflight::tests scan::tests::scan_ctx_bars_up_to && cargo test -p miner-core --test arity_preflight --test two_leg_facade --test gap_intersect_cross --test scan_ljung_box --test gap_policy --test dry_run --test scan_facade_determinism</automated>
  </verify>
  <done>validate_arity gates the facade preflight, two-leg gap intersection works end-to-end, ScanCtx::bars_up_to enforces look-ahead-safety structurally, and the three integration-test scaffolds pin the D4-01/D4-02/D4-04 contract for downstream plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CLI repeatable --instrument SYMBOL:side parser + arity-aware ScanArgs::to_scan_request + scans catalogue arity field</name>
  <files>crates/miner-cli/src/scan_args.rs, crates/miner-cli/src/main.rs</files>
  <read_first>
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md (Pattern K CLI repeatable --instrument flag; the existing parse_window value-parser at scan_args.rs:64 as the analog)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md (§Architectural Responsibility Map — Catalogue surface row: adds `arity` field to each scans catalogue line)
    - crates/miner-cli/src/scan_args.rs (current ScanArgs lines 46-100 — the single-instrument shape to replace)
    - crates/miner-cli/src/main.rs (current `miner scans` subcommand body — for the catalogue arity-field addition)
    - crates/miner-core/src/scan/mod.rs (post Plans 04-01/04-02 — for InstrumentSpec::from_str + Scan::arity())
    - crates/miner-core/src/reader.rs (Side::from_str for use within parse_instrument_spec)
  </read_first>
  <behavior>
    - Test 1 (`scan_args::tests::parse_instrument_spec_basic`): `parse_instrument_spec("EURUSD:bid")` returns Ok(InstrumentSpec { symbol: "EURUSD".into(), side: Side::Bid })
    - Test 2 (`scan_args::tests::parse_instrument_spec_ask`): `parse_instrument_spec("GBPUSD:ask")` returns Ok(InstrumentSpec { symbol: "GBPUSD".into(), side: Side::Ask })
    - Test 3 (`scan_args::tests::parse_instrument_spec_invalid_side`): `parse_instrument_spec("EURUSD:both")` returns Err with a message that mentions "side" or "bid|ask"
    - Test 4 (`scan_args::tests::parse_instrument_spec_missing_colon`): `parse_instrument_spec("EURUSD")` returns Err with a message that mentions "SYMBOL:side"
    - Test 5 (`scan_args::tests::scan_args_round_trip_two_instruments` — clap integration via try_parse_from): `ScanArgs::try_parse_from(["miner", "scan", "cross.corr.pearson_rolling@1", "--instrument", "EURUSD:bid", "--instrument", "GBPUSD:bid", "--timeframe", "15m", "--window", "2024-01-01:2024-12-31"])` succeeds and produces a ScanArgs with `instruments.len() == 2` in leg order
    - Test 6 (integration via `cargo test -p miner-core --test scans_catalogue` extension OR a new test asserting catalogue emission): `miner scans` JSONL output gains an `arity` field per scan; for LjungBoxScan the value is "single"
  </behavior>
  <action>
    Replace `pub instrument: String` and `pub side: String` in ScanArgs (crates/miner-cli/src/scan_args.rs lines 53 + 57) with a single repeatable Vec field per PATTERNS.md Pattern K:

    `#[arg(long = "instrument", action = clap::ArgAction::Append, value_parser = parse_instrument_spec)] pub instruments: Vec<InstrumentSpec>`

    Add a free function `parse_instrument_spec(s: &str) -> Result<InstrumentSpec, String>` (clap value-parser convention: return String error). Body: split `s` on ':' once; reject if zero or more than one colon; the left part is the symbol (uppercase normalisation if InstrumentSpec doesn't already enforce); the right part parses via `Side::from_str` (or hand-parse "bid"/"ask" lowercase); return Err with a message mentioning the expected `SYMBOL:side` format and the offending input.

    Add `#[arg(long = "min-instruments", default_value = "1")]` and `#[arg(long = "max-instruments", default_value = "2")]` ONLY if you want runtime guards; otherwise rely on engine::preflight::validate_arity to reject. Default: no extra guards in the CLI — let preflight handle it. Document this in the doc-comment on `instruments`.

    Update `ScanArgs::to_scan_request(code_revision: &str) -> ScanRequest` (or main.rs build-request path) to construct `ScanRequest { instruments: args.instruments.clone(), ... }` instead of `instruments: vec![InstrumentSpec { ... }]`-from-singleton.

    Update `miner scans` subcommand in main.rs to extend each emitted JSONL line with a new `arity` field. The current emission (per Phase 3 03-02-PLAN / 03-05-PLAN scans-catalogue.json schema) carries `name`, `version`, `param_schema`, `finding_fields`. Phase 4 adds `arity` as a sibling field — value is `scan.arity().as_str()` (i.e., `"single"` or `"pair"`). Update schemas/scans-catalogue-v1.schema.json regen path (already handled in Plan 04-01 Task 3's gen-schema run if `arity` is on the Scan trait; if not, run `cargo run -p xtask -- gen-schema` again as part of this task's verify).

    Add Tests 1-5 in `#[cfg(test)] mod tests` block within scan_args.rs (analog: existing parse_window tests). Test 6 either extends crates/miner-core/tests/scans_catalogue.rs (if it exists) or is a new assertion against the `miner scans` stdout in a CLI integration test under crates/miner-cli/tests/.

    Maintain backward-compat sugar if convenient: optional `--side bid|ask` flag is REMOVED (no sugar — clean break per CONTEXT.md D4-02 "repeatable --instrument SYMBOL:side"). Users of single-leg scans pass `--instrument EURUSD:bid` once.
  </action>
  <acceptance_criteria>
    - `grep -n 'fn parse_instrument_spec' crates/miner-cli/src/scan_args.rs` finds exactly 1 match returning `Result<InstrumentSpec, String>`
    - `grep -n 'instruments: Vec<InstrumentSpec>' crates/miner-cli/src/scan_args.rs` finds exactly 1 match
    - `grep -nE 'pub instrument: String|pub side: String' crates/miner-cli/src/scan_args.rs | grep -v '^#'` finds 0 matches (legacy singleton fields removed)
    - `grep -n 'ArgAction::Append' crates/miner-cli/src/scan_args.rs` finds ≥ 1 match (the repeatable flag wiring)
    - `cargo build -p miner-cli` exits 0
    - `cargo test -p miner-cli --lib scan_args::tests::parse_instrument_spec_basic scan_args::tests::parse_instrument_spec_ask scan_args::tests::parse_instrument_spec_invalid_side scan_args::tests::parse_instrument_spec_missing_colon scan_args::tests::scan_args_round_trip_two_instruments` — all 5 tests pass
    - `cargo run -p miner-cli -- scans` invocation produces JSONL where each line contains a `"arity"` key with value `"single"` (every Phase 3 scan is Single until 04-07/08 land Pair scans): `cargo run -p miner-cli -- scans 2>/dev/null | head -1 | grep -q '"arity":"single"'`
    - The Phase 3 scans_catalogue test continues to pass (extended with the arity field): `cargo test -p miner-core --test scans_catalogue` OR equivalent CLI integration
    - End-to-end CLI smoke: `cargo run -p miner-cli -- scan stats.autocorr.ljung_box@1 --instrument EURUSD:bid --timeframe 15m --window 2024-01-01:2024-01-31` exits with the expected code (likely 1 due to missing fixture; the key check is the CLI parses without error and reaches engine preflight)
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p miner-cli && cargo test -p miner-cli --lib scan_args::tests && cargo run -p miner-cli --quiet -- scans 2>/dev/null | head -1 | grep -q '"arity"'</automated>
  </verify>
  <done>The CLI accepts repeatable --instrument SYMBOL:side with InstrumentSpec parsing; ScanArgs maps cleanly to ScanRequest.instruments: Vec<InstrumentSpec>; `miner scans` emits an arity field per scan; the Phase 3 catalogue regression continues to pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI `--instrument` flag → parse_instrument_spec → InstrumentSpec | Untrusted symbol + side strings from CLI; integer overflow / DoS via huge instrument count |
| Engine run_one_with_registry → validate_arity | Untrusted ScanRequest.instruments cross here before any compute |
| Two-leg cache fetch → intersect_gaps | Manifest intersection on untrusted gap span counts (DoS if either manifest has 10⁶ spans) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Denial of Service | parse_instrument_spec | mitigate | clap's ArgAction::Append caps total instrument count implicitly via shell argv limits (~10k on Linux); preflight validate_arity rejects anything ≠ scan.arity() (Single or Pair). No quadratic growth. |
| T-04-02-02 | Tampering | InstrumentSpec parse | mitigate | parse_instrument_spec rejects non-SYMBOL:side patterns with a clear error message (Behavior Tests 3-4). No regex / dynamic eval — pure split on `':'`. |
| T-04-02-03 | Denial of Service | intersect_gaps two-leg manifest | mitigate | The intersection helper is O(n+m) two-pointer sweep over sorted span vectors (input is already sorted per Phase 2 GapDetector invariant). No quadratic blowup. Add a debug_assert that input manifests are sorted. |
| T-04-02-04 | Information Disclosure | bars_up_to look-ahead leakage | mitigate | partition_point cutoff is `t <= ts` — the structural guarantee that the returned view never contains a bar with ts > ts. Behavior Test 7 pins this. Subsequent rolling scans MUST use this API or pass shuffled-future regression (enforced in Plan 04-11). |
| T-04-02-05 | Tampering | LjungBox refactor (D4-06) | mitigate | The primitive lift is a BYTE-IDENTICAL move (Pitfall 9); Behavior Test 1 (byte-exact equality, tolerance 0.0) AND the Phase 3 golden integration test passing both gate the merge. |
| T-04-02-SC | Tampering | python-requirements.lock | accept | The lock file is REGENERATION INPUT only — never executed by the Rust build or CI. Golden regeneration is a manual developer task. No supply-chain attack surface. |
</threat_model>

<verification>
- cargo build -p miner-core --all-targets && cargo build -p miner-cli exits 0
- cargo test -p miner-core --lib (all unit tests pass — primitives, engine, scan ctx, family registrar stubs)
- cargo test -p miner-core --test arity_preflight --test two_leg_facade --test gap_intersect_cross (new scaffolds pass)
- cargo test -p miner-core --test scan_ljung_box (Phase 3 golden continues to pass byte-identically after D4-06 refactor)
- cargo test -p miner-cli --lib (CLI parser tests pass)
- cargo run -p miner-cli -- scans emits JSONL with arity field
- `cargo clippy --workspace --all-targets -- -D warnings` exits 0 (no new lints)
</verification>

<success_criteria>
- primitives::returns::log_returns is the byte-identical lifted body from Phase 3; LjungBoxScan imports it (D4-06)
- primitives::time_alignment::inner_join + intersect_gaps exist with unit tests
- engine::preflight::validate_arity wired into run_one_with_registry between resolve_scan and parse_params
- ScanCtx gains bars_pair (Option<(&BarFrame, &BarFrame)>) + bars_up_to(ts) -> BarFrameView<'_>
- gap_policy gains a Pair branch (dispatch_pair or extension to dispatch) calling intersect_gaps
- CLI ScanArgs.instruments: Vec<InstrumentSpec> via repeatable --instrument SYMBOL:side
- `miner scans` JSONL output gains arity field per scan
- crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md pins statsmodels 0.14.6, scipy 1.14.x, arch 7.x, numpy/python versions
- crates/miner-core/tests/arity_preflight.rs + two_leg_facade.rs + gap_intersect_cross.rs scaffolds exist and pass
- Per-family namespace stubs (anom/cross/seas/mod.rs) with `register_<family>_scans` helpers established; registry.rs::bootstrap() invokes all three; Plans 04-03..04-10 are now contractually locked to per-family registrars only
</success_criteria>

<output>
Create `.planning/phases/04-scan-catalogue-anom-cross-seas/04-02-SUMMARY.md` documenting: the primitives API surface, the per-family registrar contract (registry.rs::bootstrap() invokes three helpers; Plans 04-03..04-10 only append to their family's helper), the engine wiring deltas to run_one_with_registry, the CLI flag form, and the locked decisions on look-ahead-safety API shape + two-leg cancel-poll position.
</output>
