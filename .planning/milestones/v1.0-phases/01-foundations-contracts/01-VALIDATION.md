---
phase: 01
slug: foundations-contracts
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-15
updated: 2026-05-16
---

# Phase 01 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (built-in) + optional `cargo-nextest` 0.9+ for parallel local runs |
| **Config file** | none — workspace `Cargo.toml` test layout is conventional |
| **Quick run command** | `cargo test -p miner-core` |
| **Full suite command** | `cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings` |
| **Estimated runtime** | ~10–30 seconds (greenfield workspace, no fixture I/O) |

See `01-RESEARCH.md §Validation Architecture` for the full per-requirement test map.

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p <crate-being-touched>` (or `cargo test --workspace` if cross-crate)
- **After every plan wave:** Run `cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings`
- **Before `/gsd:verify-work`:** Full CI gates from FOUND-01..05 / OUT-01..03 must be green (build + clippy + tokio-tree grep + schema-sync diff)
- **Max feedback latency:** 30 seconds (target — workspace is small in Phase 1)

---

## Per-Task Verification Map

> Populated by the planner from `<verification>` / `<verify>` / `<automated>` blocks in each `*-PLAN.md`. Do NOT edit by hand — re-run `/gsd:plan-phase 1 --gaps` if rows are missing.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-01-T1 | 01 | 1 | FOUND-01 | T-01-04 | workspace pin + serde_json non-preserve_order | build | `test -f Cargo.toml && test -f rust-toolchain.toml && test -f .cargo/config.toml && grep -q 'resolver = "3"' Cargo.toml && grep -q 'edition = "2024"' Cargo.toml && grep -q 'rust-version = "1.85"' Cargo.toml && grep -q 'channel = "1.85"' rust-toolchain.toml && grep -q 'xtask = "run --package xtask' .cargo/config.toml && ! grep -E 'serde_json.*preserve_order' Cargo.toml` | ❌ Wave 0 (config files created in this task) | ⬜ pending |
| 01-01-T2 | 01 | 1 | FOUND-01, FOUND-04 | T-01-04 | tokio-free miner-core + serde_json preserve_order off | build | `cargo build --workspace 2>&1 \| tee /tmp/build.log; grep -q "Compiling miner-core" /tmp/build.log && grep -q "Compiling miner-cli" /tmp/build.log && grep -q "Compiling xtask" /tmp/build.log && ! grep -qE "(error\|warning: unused)" /tmp/build.log && ! cargo tree -e features --workspace 2>&1 \| grep -F 'serde_json feature ' \| grep -F preserve_order` | ❌ Wave 0 (crate scaffolds created in this task) | ⬜ pending |
| 01-01-T3 | 01 | 1 | FOUND-01 | T-01-SC | seven-crate workspace coherence | build | `test -f Cargo.lock && cargo metadata --format-version 1 --no-deps 2>/dev/null \| python3 -c 'import json,sys; meta=json.load(sys.stdin); names=sorted(p["name"] for p in meta["packages"]); want=["miner-bench","miner-cli","miner-core","miner-http","miner-mcp","miner-reader-dukascopy","xtask"]; assert names==want, f"got {names} want {want}"; print("ok")'` | ✅ (Cargo.lock generated by T2 build) | ⬜ pending |
| 01-02-T1 | 02 | 2 | FOUND-03 | T-01-02 | schemars 1.x Base64Bytes spike — contentEncoding verification | unit | `cargo test -p miner-core --test spike_schema -- --nocapture 2>&1 \| tee /tmp/spike_schema.log; grep -q "spike_emits_content_encoding ... ok" /tmp/spike_schema.log \|\| grep -q "test result: ok" /tmp/spike_schema.log` | ❌ Wave 0 (`crates/miner-core/tests/spike_schema.rs` created in this task) | ⬜ pending |
| 01-02-T2 | 02 | 2 | FOUND-05 | n/a | figment + clap CLI-wins precedence spike (A4) | unit | `cargo test -p miner-core --test spike_figment_precedence -- --test-threads=1 2>&1 \| tee /tmp/spike_fig.log; grep -q "test result: ok" /tmp/spike_fig.log` | ❌ Wave 0 (`crates/miner-core/tests/spike_figment_precedence.rs` created in this task) | ⬜ pending |
| 01-03-T1 | 03 | 3 | FOUND-03, OUT-02, OUT-03 | T-01-02, T-01-04 | locked envelope enum + Base64Bytes + RunId Copy + BTreeMap | unit | `cargo test -p miner-core findings:: 2>&1 \| tee /tmp/findings.log; grep -q "test result: ok" /tmp/findings.log && ! test -f crates/miner-core/src/spike_base64.rs && ! test -f crates/miner-core/src/spike_figment.rs && grep -q 'Debug, Clone, Copy, PartialEq, Eq, Hash' crates/miner-core/src/findings/run_id.rs && grep -q 'Default, Debug, Clone' crates/miner-core/src/findings/mod.rs && grep -q 'pub use findings::' crates/miner-core/src/lib.rs && grep -q 'RunStart,' crates/miner-core/src/lib.rs && grep -q 'RunEnd,' crates/miner-core/src/lib.rs && grep -q 'RunSummary,' crates/miner-core/src/lib.rs && grep -q 'PerScanCounts,' crates/miner-core/src/lib.rs && grep -q 'Base64Bytes,' crates/miner-core/src/lib.rs && grep -q 'RawArray,' crates/miner-core/src/lib.rs && grep -q 'Dtype,' crates/miner-core/src/lib.rs && grep -q 'DataSlice,' crates/miner-core/src/lib.rs && grep -q 'Source,' crates/miner-core/src/lib.rs && grep -q 'Effect,' crates/miner-core/src/lib.rs && grep -q 'Raw,' crates/miner-core/src/lib.rs && grep -q 'RunId,' crates/miner-core/src/lib.rs` | ❌ Wave 0 (`crates/miner-core/src/findings/*` files created in this task) | ⬜ pending |
| 01-03-T2 | 03 | 3 | FOUND-03 | T-01-02 | error model split + FindingSink trait + MinerConfig schema | unit | `cargo test -p miner-core error::tests:: config::tests:: findings::sink::tests:: 2>&1 \| tee /tmp/err_cfg.log; grep -q "test result: ok" /tmp/err_cfg.log && cargo build -p miner-core --release 2>&1 \| grep -qv error && grep -q 'pub use error::' crates/miner-core/src/lib.rs && grep -q 'WireError' crates/miner-core/src/lib.rs && grep -q 'PreflightCode' crates/miner-core/src/lib.rs && grep -q 'MinerConfig' crates/miner-core/src/lib.rs && grep -q 'OutputDest' crates/miner-core/src/lib.rs && grep -q 'FindingSink' crates/miner-core/src/lib.rs` | ❌ Wave 0 (`crates/miner-core/src/error/*` and `config/mod.rs` created in this task) | ⬜ pending |
| 01-04-T1 | 04 | 4 | FOUND-02, OUT-01 | T-01-03 | StdoutSink single-writer + JSONL framing + per-envelope flush | unit | `cargo test -p miner-core findings::sink::tests:: 2>&1 \| tee /tmp/sink.log; grep -q "test result: ok" /tmp/sink.log && ! grep -q 'println!' crates/miner-core/src/findings/sink.rs && ! grep -q '#\[allow(clippy::disallowed_macros)\]' crates/miner-core/src/findings/sink.rs` | ✅ (`crates/miner-core/src/findings/sink.rs` carried forward from Plan 03 T2) | ⬜ pending |
| 01-04-T2 | 04 | 4 | FOUND-02, OUT-01 | T-01-03 | stderr_emit single-writer pre-flight error JSONL | unit | `cargo test -p miner-core error::stderr_emit::tests:: 2>&1 \| tee /tmp/stderr.log; grep -q "test result: ok" /tmp/stderr.log && ! grep -q 'eprintln!' crates/miner-core/src/error/stderr_emit.rs` | ✅ (`crates/miner-core/src/error/stderr_emit.rs` placeholder created in Plan 03 T2; filled here) | ⬜ pending |
| 01-04-T3 | 04 | 4 | FOUND-02 | T-01-03 | clippy workspace gate (disallowed_macros) — D-15 enforcement | lint | `test -f clippy.toml && grep -q 'std::println' clippy.toml && grep -q 'std::eprintln' clippy.toml && cargo clippy --workspace --all-targets -- -D warnings` | ❌ Wave 0 (`clippy.toml` created in this task) | ⬜ pending |
| 01-05-T1 | 05 | 4 | FOUND-05 | T-01-01, T-01-05 | figment builder + .split("__") + figment Kind classification | unit | `cargo test -p miner-core config::tests:: -- --test-threads=1 2>&1 \| tee /tmp/cfgmod.log; grep -q "test result: ok" /tmp/cfgmod.log && ! grep -E '(/opt/\|/home/\|\$HOME\|XDG_CONFIG_HOME\|miner\.toml)' crates/miner-core/src/config/mod.rs && grep -q 'Env::prefixed("MINER_").split("__")' crates/miner-core/src/config/mod.rs` | ✅ (`crates/miner-core/src/config/mod.rs` carried forward from Plan 03 T2) | ⬜ pending |
| 01-05-T2 | 05 | 4 | FOUND-05, FOUND-02 | T-01-01, T-01-05 | end-to-end CLI: tracing→stderr, findings→stdout, classify_figment_error mapper | integration | `cargo build -p miner-cli 2>&1 \| grep -v warning && cargo run -p miner-cli -- emit-fixture 2>/tmp/cli.stderr.log >/tmp/cli.stdout.log; STDOUT_LINES=$(wc -l < /tmp/cli.stdout.log); STDERR_HAS_TRACING=$(grep -c 'emitting fixture' /tmp/cli.stderr.log \|\| true); [ "$STDOUT_LINES" = "2" ] && [ "$STDERR_HAS_TRACING" -ge 1 ] && python3 -c 'import json,sys; lines=open("/tmp/cli.stdout.log").read().splitlines(); a=json.loads(lines[0]); b=json.loads(lines[1]); assert a["kind"]=="run_start", a; assert b["kind"]=="run_end", b; assert a["run_id"]==b["run_id"], "run_ids must match"; print("ok")' && grep -q 'classify_figment_error' crates/miner-cli/src/main.rs && grep -q 'MissingField' crates/miner-cli/src/main.rs && grep -q 'InvalidConfig' crates/miner-cli/src/main.rs` | ❌ Wave 0 (`crates/miner-cli/src/cli.rs` created here, `main.rs` replaced) | ⬜ pending |
| 01-06-T1 | 06 | 5 | FOUND-03 | T-01-02 | xtask gen-schema + twice-run byte-determinism gate | build | `cargo run -p xtask -- gen-schema 2>&1; test -f schemas/findings-v1.schema.json && python3 -c 'import json; s=json.load(open("schemas/findings-v1.schema.json")); assert "oneOf" in str(s) or "anyOf" in str(s) or "$defs" in s, "schema missing expected shape"; assert "contentEncoding" in json.dumps(s), "Base64Bytes contentEncoding missing"; assert "run_start" in json.dumps(s), "RunStart variant missing"; print("ok")' && cargo run -p xtask -- gen-schema /tmp/findings-v1-second.schema.json 2>&1 && cmp schemas/findings-v1.schema.json /tmp/findings-v1-second.schema.json && echo "twice-run determinism: OK"` | ❌ Wave 0 (`schemas/findings-v1.schema.json` and full `xtask/src/main.rs` created in this task) | ⬜ pending |
| 01-06-T2 | 06 | 5 | FOUND-03, FOUND-04 | T-01-02, T-01-03 | CI workflow: build + clippy + tokio-tree + schema-sync gates | ci | `test -f .github/workflows/ci.yml && grep -q 'tokio-free miner-core' .github/workflows/ci.yml && grep -q 'schema sync' .github/workflows/ci.yml && grep -q 'cargo clippy --workspace --all-targets -- -D warnings' .github/workflows/ci.yml && grep -q 'cargo run -p xtask -- gen-schema' .github/workflows/ci.yml && bash -c 'set -euo pipefail; PROHIBITED="^(tokio\|tokio-[^ ]+\|async-std\|async-std-[^ ]+\|smol\|smol-[^ ]+\|async-trait\|async-io\|async-channel\|async-executor\|async-task)$"; LEAKED=$(cargo tree -p miner-core --edges normal,build --prefix none \| awk "{print \$1}" \| sort -u \| grep -E "$PROHIBITED" \|\| true); [ -z "$LEAKED" ]' && cargo run -p xtask -- gen-schema && git diff --exit-code schemas/findings-v1.schema.json` | ❌ Wave 0 (`.github/workflows/ci.yml` created in this task) | ⬜ pending |
| 01-07-T1 | 07 | 6 | FOUND-03, FOUND-05, OUT-02 | T-01-02 | runtime schema validation per variant + production precedence | integration | `cargo test --workspace --no-fail-fast 2>&1 \| tee /tmp/all_tests.log; grep -q "test result: ok" /tmp/all_tests.log && grep -q "all_kinds_validate" /tmp/all_tests.log && grep -q "cli_wins_over_env_and_toml" /tmp/all_tests.log && grep -q "missing_required_yields_err" /tmp/all_tests.log` | ❌ Wave 0 (`crates/miner-core/tests/schema_roundtrip.rs` and `tests/config_precedence.rs` created in this task) | ⬜ pending |
| 01-07-T2 | 07 | 6 | FOUND-02, OUT-01, OUT-02, OUT-03 | T-01-02, T-01-03 | subprocess stdout/stderr split + schema validation + masked twice-run byte-identity (OUT-03 full closure) + figment kind regression | integration | `cargo test -p miner-cli --test cli_streams 2>&1 \| tee /tmp/cli_streams.log; grep -q "test result: ok" /tmp/cli_streams.log && grep -q "emit_fixture_writes_two_jsonl_lines_to_stdout" /tmp/cli_streams.log && grep -q "emit_fixture_validates_against_committed_schema" /tmp/cli_streams.log && grep -q "preflight_missing_config_emits_wireerror_to_stderr_exit_1" /tmp/cli_streams.log && grep -q "preflight_invalid_toml_emits_invalid_config_code" /tmp/cli_streams.log && grep -q "emit_fixture_byte_identical_when_volatile_fields_masked" /tmp/cli_streams.log` | ❌ Wave 0 (`crates/miner-cli/tests/cli_streams.rs` created in this task) | ⬜ pending |
| 01-07-T3 | 07 | 6 | FOUND-01..05, OUT-01..03 | T-01-02, T-01-03 | Phase 1 sign-off ritual: README quickstart + four-gate local CI walk-through | manual+ci | n/a — `checkpoint:human-verify` gate; verification command is the seven-step sign-off ritual documented in the plan (cargo build/clippy/test/fmt/tokio-tree-grep/schema-sync/emit-fixture). No single shell line — see Plan 07 Task 3 `<how-to-verify>`. | ✅ (each underlying command's tooling exists by this wave; README created in this task) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

> **Note on `File Exists` column:** "❌ Wave 0" means the file referenced by the verify block does not exist before this task — the task IS the creation step (greenfield Phase 1). "✅" means the file was created by an earlier task in the same phase and is now being extended. Per the planner's Wave 0 rule (see `## Wave 0 Requirements` below), every `❌ Wave 0` row is satisfied by an explicit predecessor task within the same plan or by a scaffolding task in the prior plan — there are NO unsatisfied references to non-existent test infrastructure.

---

## Wave 0 Requirements

All Phase 1 tests are net-new (greenfield workspace). The planner MUST schedule the following test infrastructure in Wave 0 (or earlier) before any task with `<verification>` claims they exist. Each row in the per-task table above identifies which plan/task creates the referenced file.

- [x] `crates/miner-core/tests/findings_envelope.rs` — round-trip + schema-validation tests for every envelope `kind` (created by Plan 03 Task 1 inline tests + Plan 07 Task 1 `schema_roundtrip.rs`)
- [x] `crates/miner-core/tests/config_precedence.rs` — CLI > env > TOML > default for `cache_root`, `bar_cache_root`, `output` (created by Plan 07 Task 1)
- [x] `crates/miner-cli/tests/cli_streams.rs` — integration test asserting stdout = JSONL, stderr = tracing, schema validation, masked twice-run byte-identity (created by Plan 07 Task 2)
- [x] `xtask/src/gen_schema.rs` — `cargo run -p xtask -- gen-schema` writes `schemas/findings-v1.schema.json` (created by Plan 06 Task 1; implemented inside `xtask/src/main.rs` as `fn gen_schema`)
- [x] `clippy.toml` (workspace root) — `disallowed-macros` config for `println!` / `eprintln!` (created by Plan 04 Task 3)
- [x] `.github/workflows/ci.yml` — four mandatory CI gates from D-21 (created by Plan 06 Task 2)
- [x] Optional: `Cargo.toml` workspace `dev-dependencies` adds `jsonschema` + `tempfile` + `serial_test` (added by Plan 01-01 Task 1 / Plan 07 Task 1)

*Sentinel: any task whose `<automated>` block references a path under `crates/`, `tests/`, `xtask/`, `schemas/`, or `.github/` MUST list one of the rows above (or an explicit predecessor task) in `<depends_on>` or in its `<read_first>` listing. Verified across all 7 plans: every reference is backed by a creator task in an earlier wave or the same plan.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Phase 1 sign-off ritual (README readability + visual confirmation of `cargo run` happy-path output) | n/a (developer-experience gate) | The Quickstart's "does this make sense to a fresh reader?" check is intrinsically subjective | See Plan 07 Task 3 `<how-to-verify>` (6-step manual walk-through) |

*Phase 1 has exactly ONE manual verification: the human-verify checkpoint at the very end (Plan 07 Task 3). Every requirement-level contract (FOUND-01..05, OUT-01..03) is closed by automated tests above; the manual step is purely a developer-experience sanity check on the README.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (verified across plans 01-01 through 01-07; only Plan 07 Task 3 is `checkpoint:human-verify` and that is the designated developer-experience gate, not a behavioural one)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task in Plans 01-06 carries an `<automated>` block; Plan 07 Tasks 1+2 carry one each; Plan 07 Task 3 is the sign-off checkpoint and is preceded by two fully-automated tasks)
- [x] Wave 0 covers all MISSING references (see annotated table above and the Wave 0 Requirements section)
- [x] No watch-mode flags (every `cargo test` / `cargo clippy` invocation runs once and exits; no `--watch` or `cargo-watch`)
- [x] Feedback latency < 30s (workspace is small; per RESEARCH §Validation Architecture and the Test Infrastructure table above)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** populated 2026-05-16 (revision iteration 1, addresses checker BLOCKER on unpopulated map).
