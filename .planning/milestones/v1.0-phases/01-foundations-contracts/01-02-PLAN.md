---
phase: 01
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - crates/miner-core/src/spike_base64.rs
  - crates/miner-core/src/spike_figment.rs
  - crates/miner-core/src/lib.rs
  - crates/miner-core/tests/spike_schema.rs
  - crates/miner-core/tests/spike_figment_precedence.rs
  - crates/miner-core/Cargo.toml
autonomous: true
requirements_addressed: [FOUND-03, FOUND-05]
threats_addressed: [T-01-02]

must_haves:
  truths:
    - "The exact schemars 1.x `try_into` line in `Base64Bytes::json_schema` compiles cleanly (resolves RESEARCH Risk 2 / Assumption A1)"
    - "Generated JSON Schema fragment for a `Base64Bytes` field contains `contentEncoding: \"base64\"` and `contentMediaType: \"application/octet-stream\"`"
    - "figment + clap `Option<T>` + skip_serializing_if pattern resolves to CLI > env > TOML > error precedence as the live A4 verification"
    - "If schemars spike FAILS, the fallback (field-level `#[schemars(schema_with = ...)]`) is documented in SUMMARY.md and Plan 03 is updated before Wave 3 starts"
    - "Spike modules are committed under `crates/miner-core/src/spike_*.rs` and will be DELETED by Plan 03 (they exist solely to validate the pattern before envelope code is written)"
  artifacts:
    - path: "crates/miner-core/src/spike_base64.rs"
      provides: "Minimal `Base64Bytes` newtype with manual `JsonSchema` impl, exercising the exact schemars 1.x API"
    - path: "crates/miner-core/tests/spike_schema.rs"
      provides: "Test that calls `schemars::schema_for!(SpikeFinding)` and asserts `contentEncoding: \"base64\"` appears in the generated schema JSON"
    - path: "crates/miner-core/src/spike_figment.rs"
      provides: "Minimal figment+clap pipeline with CliOverrides struct and `build_figment` function exercising the exact A4 precedence pattern"
    - path: "crates/miner-core/tests/spike_figment_precedence.rs"
      provides: "Test that sets MINER_CACHE_ROOT=/env, passes --cache-root /cli, writes TOML cache_root=/file, asserts resolved is /cli; covers each two-of-three combination"
  key_links:
    - from: "crates/miner-core/tests/spike_schema.rs"
      to: "crates/miner-core/src/spike_base64.rs"
      via: "schemars::schema_for! macro"
      pattern: "schema_for!\\(.*SpikeFinding\\)"
    - from: "crates/miner-core/tests/spike_figment_precedence.rs"
      to: "crates/miner-core/src/spike_figment.rs"
      via: "build_figment function call"
      pattern: "build_figment"
---

<objective>
Resolve the two MEDIUM-confidence risks RESEARCH flagged before any envelope code is written:

1. **Risk 2 / A1**: The exact `serde_json::Value::try_into::<Schema>()` line in the recommended `Base64Bytes::json_schema` impl. RESEARCH says this is consistent with the schemars 1.x changelog but was not compile-tested.
2. **Risk 5 / A4**: The figment + clap `Option<T>` + `skip_serializing_if` pattern with CLI-wins precedence. The pattern is well-documented but worth a 15-minute live verification before committing the envelope and config code to it.

**Purpose:** RESEARCH §"Open Risks" explicitly demands these two spikes happen in Wave 0 before downstream envelope/config plans land. Skipping the spikes risks Plan 03 producing code that compiles but generates the wrong schema (silent schema-correctness bug), or Plan 05 producing a config layering that resolves the wrong value (silent precedence bug). Both are exactly the kind of breakage that's painful to retrofit later, which is the whole reason Phase 1 exists first.

**Output:** Two committed spike modules + two committed integration tests that prove the patterns work. If either spike fails, the SUMMARY.md captures the fallback and Plan 03 / Plan 05 are updated accordingly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations-contracts/01-CONTEXT.md
@.planning/phases/01-foundations-contracts/01-RESEARCH.md
@.planning/phases/01-foundations-contracts/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spike — schemars 1.x base64-with-shape (Risk 2 / A1)</name>
  <files>
    crates/miner-core/Cargo.toml,
    crates/miner-core/src/spike_base64.rs,
    crates/miner-core/src/lib.rs,
    crates/miner-core/tests/spike_schema.rs
  </files>
  <behavior>
    - Test 1: `cargo test -p miner-core --test spike_schema spike_emits_content_encoding` compiles and passes — confirms the `try_into` line compiles against the resolved schemars version.
    - Test 2: The generated schema JSON contains the literal substring `"contentEncoding": "base64"`.
    - Test 3: The generated schema JSON contains the literal substring `"contentMediaType": "application/octet-stream"`.
    - Test 4: The generated schema JSON contains a property named `data` (from the embedding `SpikeRawArray` struct) and a property named `shape`.
  </behavior>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md §"Architecture Patterns" Pattern 2 (the canonical Base64Bytes shape) — note the `try_into().expect("valid schema fragment")` line on line ~404 of RESEARCH.md is what's being verified
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md §"Schemars + base64-with-shape Pattern" — the spike instructions
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md §"Open Risks" Risk 2 + Risk 5 (spike rationale)
    - crates/miner-core/Cargo.toml (just-created in Plan 01-01; confirm `schemars` and `base64` are in `[dependencies]`)
    - crates/miner-core/src/lib.rs (currently has only `pub const CODE_REVISION`; need to add `pub mod spike_base64;`)
  </read_first>
  <action>
    Implement the spike per RESEARCH §"Architecture Patterns" Pattern 2 verbatim. Files:

    **`crates/miner-core/src/spike_base64.rs`**: Define `pub struct SpikeBase64Bytes(pub Vec<u8>)` with hand-rolled `Serialize`, `Deserialize`, and `JsonSchema` impls per the Pattern 2 example. Use `base64::engine::general_purpose::STANDARD` for encode/decode (the current 0.22 API). Compose into a `SpikeRawArray { data: SpikeBase64Bytes, shape: Vec<u64>, dtype: SpikeDtype }` where `SpikeDtype` is `#[derive(JsonSchema, Serialize, Deserialize)] #[serde(rename_all = "snake_case")] enum SpikeDtype { F64 }`. Wrap that in a `pub struct SpikeFinding { array: SpikeRawArray }` derive-everything top-level type so `schema_for!(SpikeFinding)` exercises the embedding chain.

    The critical line to validate is in `impl JsonSchema for SpikeBase64Bytes::json_schema`: build a `serde_json::json!({...})` value with `contentEncoding`/`contentMediaType` fields, then call `.try_into().expect("valid schema fragment")` to convert into `schemars::Schema`. If this line does NOT compile, the fallback is `schemars::Schema::try_from(serde_json::json!{...}).expect("...")` (using the explicit `try_from` rather than `try_into`); if THAT does not compile either, document the actual constructor in SUMMARY.md and use it.

    **`crates/miner-core/src/lib.rs`**: add `pub mod spike_base64;` re-export so the test module can reach it. This is temporary — Plan 03 will delete this re-export and the spike module.

    **`crates/miner-core/tests/spike_schema.rs`**: integration test:
    - `use miner_core::spike_base64::SpikeFinding;`
    - `let schema = schemars::schema_for!(SpikeFinding);`
    - `let s = serde_json::to_string_pretty(&schema).unwrap();`
    - `assert!(s.contains("\"contentEncoding\": \"base64\""), "schema fragment missing contentEncoding; got:\n{s}");`
    - `assert!(s.contains("\"contentMediaType\": \"application/octet-stream\""), "schema fragment missing contentMediaType");`
    - `assert!(s.contains("\"shape\""), "schema missing shape property");`
    - `assert!(s.contains("\"data\""), "schema missing data property");`

    If `cargo test -p miner-core --test spike_schema` PASSES → the recommended Pattern 2 is verified; Plan 03 implements `Base64Bytes` exactly as shown in RESEARCH.

    If it FAILS → in SUMMARY.md document: (a) the exact compile/runtime error, (b) the working alternative (likely field-level `#[schemars(schema_with = "base64_schema_fn")]` paired with `#[serde(with = "base64_serde")]`), (c) the change Plan 03 must apply. Do NOT proceed to Plan 03 until the test passes.

    Mitigates Threat T-01-02 (schema injection / drift) by establishing the schema-derivation pattern under test from day one.

    Honors D-13 (single source of truth for schema).
  </action>
  <verify>
    <automated>cargo test -p miner-core --test spike_schema -- --nocapture 2>&1 | tee /tmp/spike_schema.log; grep -q "spike_emits_content_encoding ... ok" /tmp/spike_schema.log || grep -q "test result: ok" /tmp/spike_schema.log</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/miner-core/src/spike_base64.rs` exists and defines `pub struct SpikeBase64Bytes(pub Vec<u8>)` plus `impl JsonSchema for SpikeBase64Bytes`
    - File `crates/miner-core/tests/spike_schema.rs` exists and contains `schemars::schema_for!(SpikeFinding)` plus assertions on `contentEncoding`/`contentMediaType`
    - `cargo test -p miner-core --test spike_schema` exits 0
    - If the test fails, SUMMARY.md MUST document the fallback strategy and the spike must be re-implemented and pass before Plan 03 is allowed to start
    - `cargo run -p xtask` still works (xtask placeholder unaffected)
  </acceptance_criteria>
  <done>Schemars 1.x base64-with-shape pattern is compile-verified and produces the expected JSON Schema fragment. Risk 2 closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Spike — figment + clap CLI-wins precedence (Risk 5 / A4)</name>
  <files>
    crates/miner-core/src/spike_figment.rs,
    crates/miner-core/src/lib.rs,
    crates/miner-core/tests/spike_figment_precedence.rs,
    crates/miner-core/Cargo.toml
  </files>
  <behavior>
    - Test 1: When TOML sets `cache_root = "/file"`, env sets `MINER_CACHE_ROOT=/env`, and CliOverrides::cache_root = Some("/cli"), the resolved `cache_root` is `/cli`.
    - Test 2: When TOML sets `cache_root = "/file"`, env sets `MINER_CACHE_ROOT=/env`, and CliOverrides::cache_root = None, the resolved `cache_root` is `/env`.
    - Test 3: When TOML sets `cache_root = "/file"`, env is unset, and CliOverrides::cache_root = None, the resolved `cache_root` is `/file`.
    - Test 4: When no source provides `cache_root`, `figment.extract::<SpikeConfig>()` returns `Err` (NOT silently substituting a default).
    - Test 5: Layering for `bar_cache_root` and `output` follows the same precedence pattern.
  </behavior>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md §"Config Precedence Mechanics" — the canonical figment + clap pattern with the `Option<T>` + `skip_serializing_if` workaround
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md §"Common Pitfalls" Pitfall 1 (figment + clap precedence inversion — the most common failure mode)
    - .planning/phases/01-foundations-contracts/01-CONTEXT.md D-16 (TOML format, env prefix `MINER_*`, precedence: defaults < TOML < env < CLI)
    - crates/miner-core/Cargo.toml (confirm `figment` and `serde` are deps; `clap` is NOT a miner-core dep but is fine to add as a `[dev-dependencies]` for the spike since this entire spike module will be deleted by Plan 05)
    - Plan 01-01 SUMMARY (to confirm figment is at workspace.dependencies with `features = ["toml", "env"]`)
  </read_first>
  <action>
    **`crates/miner-core/Cargo.toml`**: add `figment.workspace = true` to `[dependencies]` if not already present (it should be after Plan 01-01). Add `clap.workspace = true` to `[dev-dependencies]` (only for the spike's CliOverrides; production miner-core never depends on clap per D-16).

    **`crates/miner-core/src/spike_figment.rs`**:

    Define a `pub struct SpikeConfig { cache_root: PathBuf, bar_cache_root: PathBuf, output: SpikeOutputDest }` — all non-optional fields (so `figment.extract()` errors on missing — Test 4). `SpikeOutputDest` is an enum `Stdout | File(PathBuf)`. Derive `Serialize`, `Deserialize`.

    Define `pub struct SpikeCliOverrides` with all-`Option<PathBuf>` fields, `#[serde(skip_serializing_if = "Option::is_none")]` on each. Derive `Serialize`, `Default`. (No `clap::Parser` derive in spike — Test 2 constructs the struct programmatically.)

    Define `pub fn build_figment(toml_path: Option<&Path>, cli: SpikeCliOverrides) -> Figment` that:
    - Starts with `Figment::new()`
    - Conditionally merges `Toml::file(path)` if `toml_path.is_some()` and the path exists
    - Merges `Env::prefixed("MINER_")`
    - Merges `Serialized::defaults(cli)` LAST (this is the critical inversion vs. the figment docs example — see Pitfall 1)

    **`crates/miner-core/src/lib.rs`**: add `pub mod spike_figment;`. Plan 05 will delete.

    **`crates/miner-core/tests/spike_figment_precedence.rs`**: integration test (use `#[serial_test::serial]` or wrap each subtest in its own scope that clears env vars — env tests must not interfere with each other; if `serial_test` is not available, use `std::env::set_var`/`remove_var` carefully inside a single sequential test function).

    Build a tempdir, write a `miner.toml` with `cache_root = "/file"`, `bar_cache_root = "/file/bar"`, `output = "stdout"`. Then:

    1. **All three sources set, CLI wins:** `std::env::set_var("MINER_CACHE_ROOT", "/env")`; `cli = SpikeCliOverrides { cache_root: Some(PathBuf::from("/cli")), .. }`; resolve and assert `cfg.cache_root == PathBuf::from("/cli")`.
    2. **CLI omitted, env wins:** `cli.cache_root = None`; resolve, assert `cfg.cache_root == PathBuf::from("/env")`.
    3. **CLI and env omitted, TOML wins:** `remove_var("MINER_CACHE_ROOT")`; resolve, assert `cfg.cache_root == PathBuf::from("/file")`.
    4. **No source supplies:** write a minimal TOML without `cache_root` (or use a fresh tempdir with no file); remove env; CLI empty; assert `figment.extract::<SpikeConfig>().is_err()`.
    5. Repeat per-field for `bar_cache_root` and `output` (one combo each is enough — pattern is the same).

    Remember to `std::env::remove_var("MINER_CACHE_ROOT")` etc. in test teardown so other tests aren't affected.

    If `cargo test -p miner-core --test spike_figment_precedence` PASSES → Plan 05 implements `miner-core::config` with this exact pattern.

    If it FAILS → document the actual behavior in SUMMARY.md, propose a fix (either the figment merge order is different from what RESEARCH analyzed, or `skip_serializing_if` isn't doing what we expect), and re-run.
  </action>
  <verify>
    <automated>cargo test -p miner-core --test spike_figment_precedence -- --test-threads=1 2>&1 | tee /tmp/spike_fig.log; grep -q "test result: ok" /tmp/spike_fig.log</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/miner-core/src/spike_figment.rs` exists with `build_figment` function and `SpikeConfig` struct
    - File `crates/miner-core/tests/spike_figment_precedence.rs` exists with at least 4 sub-asserts covering CLI>env, env>TOML, TOML only, and missing-required
    - `cargo test -p miner-core --test spike_figment_precedence --test-threads=1` exits 0
    - Test verifies missing required config produces `Err` (not silent default fallback)
    - SUMMARY.md notes whether the recommended pattern works as-is or whether Plan 05 needs adjustments
  </acceptance_criteria>
  <done>figment + clap precedence is verified to produce CLI > env > TOML > error. Risk 5 closed. Plan 05 can proceed with the verified pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust source → JSON Schema artifact | The `schemars`-derived JSON Schema is the contract surface for every downstream consumer (Quant agent). A divergence between the Rust types and the generated schema is a silent contract bug. |

## STRIDE Threat Register (Phase 1 scope)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering / Integrity | `schemas/findings-v1.schema.json` derivation pipeline | mitigate | This plan validates that the manual `JsonSchema` impl pattern produces the expected JSON Schema fragment under the resolved schemars 1.x version. Plan 06 closes the loop by adding the CI gate `cargo run -p xtask -- gen-schema && git diff --exit-code schemas/`. Together these mean a contributor cannot quietly drift the Rust types away from the published schema. |
</threat_model>

<verification>
- `cargo test -p miner-core --test spike_schema` exits 0 and validates `contentEncoding: "base64"` in output
- `cargo test -p miner-core --test spike_figment_precedence --test-threads=1` exits 0 and validates CLI > env > TOML precedence
- SUMMARY.md documents either "patterns verified, Plan 03 / Plan 05 proceed as designed" OR the specific fallback adjustment needed
</verification>

<success_criteria>
- Both spikes pass before Wave 3 starts
- Plan 03 receives confirmation that `Base64Bytes` Pattern 2 compiles
- Plan 05 receives confirmation that figment + clap precedence resolves correctly
- Spike modules (`spike_*.rs`) are committed but tagged for removal by Plans 03 and 05
</success_criteria>

<output>
Create `.planning/phases/01-foundations-contracts/01-02-SUMMARY.md` on completion. SUMMARY.md MUST include a "Spike Results" section stating which patterns are verified and any adjustments needed for Plans 03 and 05.
</output>
