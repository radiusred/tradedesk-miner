---
phase: 06-mcp-http-wrappers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - ARCHITECTURE.md
  - docs/.license-footer.md
autonomous: true
requirements:
  - OP-02
  - OP-03
tags:
  - docs
  - architecture
  - scope-amendment

must_haves:
  truths:
    - "OP-02 and OP-03 are reclassified from v1 Phase 6 to v2 platform requirements (PLAT-v2-07, PLAT-v2-08) in REQUIREMENTS.md."
    - "ROADMAP.md Phase 6 Goal + Success Criteria describe the docs-only deliverable (no MCP/HTTP code), and the rmcp research flag is removed."
    - "PROJECT.md Active list demotes MCP + HTTP server line items to designed/deferred with a pointer at docs/future_mcp_http.md."
    - "STATE.md Blockers/Concerns drops the rmcp risk bullet and Deferred Items records the OP-02 + OP-03 carry-over to v2."
    - "Root ARCHITECTURE.md exists, ends with the canonical Apache-2.0 footer, and narrates the one-way crate dependency direction, sync-core + async-edges discipline, stdout=findings/stderr=logs, locked Finding envelope, and gap-policy semantics."
    - "docs/.license-footer.md exists as the single source of the Apache-2.0 footer template re-used by Plans 06-02 and 06-03."
  artifacts:
    - path: "ARCHITECTURE.md"
      provides: "Public-audience system map; ~80-110 lines including license footer."
      contains: ["miner-core", "miner-cli", "miner-reader-dukascopy", "miner-mcp", "miner-http", "spawn_blocking", "FindingSink", "gap_manifest", "Apache License, Version 2.0"]
    - path: "docs/.license-footer.md"
      provides: "Canonical Apache-2.0 footer template (bare URL form) for reuse by Plans 06-02 / 06-03."
      contains: ["Apache License, Version 2.0", "https://www.apache.org/licenses/LICENSE-2.0", "Copyright 2026"]
    - path: ".planning/REQUIREMENTS.md"
      provides: "OP-02 and OP-03 reclassified to v2 (PLAT-v2-07 + PLAT-v2-08); traceability table updated."
      contains: ["PLAT-v2-07", "PLAT-v2-08", "docs/future_mcp_http.md"]
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 block rewritten to describe the docs-only deliverable; rmcp research flag removed."
      contains: ["docs-only", "docs/future_mcp_http.md"]
    - path: ".planning/PROJECT.md"
      provides: "Active list amended per D6-06."
      contains: ["designed", "docs/future_mcp_http.md"]
    - path: ".planning/STATE.md"
      provides: "Blockers/Concerns + Deferred Items amended per D6-07."
      contains: ["docs/future_mcp_http.md", "OP-02", "OP-03"]
  key_links:
    - from: "ARCHITECTURE.md"
      to: "README.md"
      via: "See also: README.md cross-reference"
      pattern: "README\\.md"
    - from: ".planning/REQUIREMENTS.md (PLAT-v2-07 / PLAT-v2-08 rows)"
      to: "docs/future_mcp_http.md (created in Plan 06-03)"
      via: "explicit doc link in row text"
      pattern: "docs/future_mcp_http\\.md"
    - from: ".planning/STATE.md Deferred Items table"
      to: ".planning/REQUIREMENTS.md PLAT-v2-07 / PLAT-v2-08"
      via: "row references both v2 IDs"
      pattern: "PLAT-v2-0[78]"
---

<objective>
Reshape every v1-planning artifact to reflect the docs-only Phase 6 scope and publish the root `ARCHITECTURE.md` that subsequent docs cross-link into. This plan owns the OP-02 + OP-03 reclassification (per D6-05) and the license-footer template (`docs/.license-footer.md`) reused by Plans 06-02 and 06-03.

Purpose: Without this plan, Phase 6 cannot ship — REQUIREMENTS.md / ROADMAP.md / PROJECT.md / STATE.md still promise a working `miner-mcp` + `miner-http` v1 deliverable that contradicts D6-01, and there is no public-audience system map for the docs/ guides to cross-link into.

Output:
- Root `ARCHITECTURE.md` (~80-110 lines incl. footer).
- `docs/.license-footer.md` (single canonical Apache-2.0 footer block for re-use; ~10 lines).
- Updated `.planning/REQUIREMENTS.md` (OP-02 + OP-03 moved to v2 as PLAT-v2-07 + PLAT-v2-08; traceability table rows updated).
- Updated `.planning/ROADMAP.md` (Phase 6 block rewritten + intro paragraph + progress table line + plans list pre-populated).
- Updated `.planning/PROJECT.md` (Active list amended).
- Updated `.planning/STATE.md` (Blockers + Deferred Items amended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-mcp-http-wrappers/06-CONTEXT.md
@.planning/phases/06-mcp-http-wrappers/06-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@/home/darren/projects/radiusred/tradedesk/ARCHITECTURE.md
@/home/darren/projects/radiusred/tradedesk/docs/aggregation_guide.md

<interfaces>
<!-- Key facts the executor needs verbatim. No code interfaces involved -->
<!-- (docs-only phase). All identifiers below are PROSE references the docs cite. -->

Crate graph (one-way dependency direction):
  miner-cli | miner-mcp | miner-http
        |       |          |
        v       v          v
            miner-reader-dukascopy
                    |
                    v
                miner-core

Key public surface items the ARCHITECTURE.md narrates (paths for ground-truth links):
  - crates/miner-core/src/findings/mod.rs  (Finding enum: RunStart, Result, ScanError, GapAborted, RunEnd, DryRun, SweepSummary)
  - crates/miner-core/src/engine/mod.rs    (engine::run_one + engine::run_one_with_registry facade)
  - crates/miner-core/src/sweep/mod.rs     (sweep::run_sweep + sweep::manifest::read_manifest)
  - crates/miner-core/src/scan/registry.rs (Scan trait + Registry + ScanArity)
  - crates/miner-core/src/cache.rs         (derived-bar cache, Arrow IPC, two-axis invalidation)
  - crates/miner-core/src/findings/sink.rs (FindingSink + StdoutSink + FileSink)
  - schemas/findings-v1.schema.json        (locked envelope schema)

Apache-2.0 license footer template (bare URL form per D6-04 + Open Question #6 default):

  ---

  ## License

  Licensed under the Apache License, Version 2.0.
  See: https://www.apache.org/licenses/LICENSE-2.0

  Copyright 2026 [Radius Red Ltd.](https://github.com/radiusred) | [Contact](mailto:opensource@radiusred.uk)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reclassify OP-02 + OP-03 to v2 and rewrite ROADMAP/PROJECT/STATE for docs-only Phase 6</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — must see current OP-02 / OP-03 lines 32-33, PLAT-v2 section line 86+, traceability table line 138+)
    - .planning/ROADMAP.md (full file — must see Phase 6 block lines 139-155, the Research-flag blockquote at line 157, intro paragraph line 5, phase-list line 20, progress table starting line 182 with Phase 6 row at line 189, AND the Phase 7 block lines 159-175 — specifically the bogus duplicate Phase-6-style plan-list at lines 169-174 that warning-2 fix must restore to a TBD placeholder)
    - .planning/PROJECT.md (read to find the Active list, ~lines 35-44, to amend the MCP + HTTP lines per D6-06)
    - .planning/STATE.md (lines 100-118 — Blockers/Concerns + Deferred Items)
    - .planning/phases/06-mcp-http-wrappers/06-CONTEXT.md (decisions D6-01, D6-05, D6-06, D6-07)
    - .planning/phases/06-mcp-http-wrappers/06-PATTERNS.md (sections "./.planning/REQUIREMENTS.md (EXTENDED)" + "./.planning/ROADMAP.md (EXTENDED)" + "./.planning/STATE.md (EXTENDED)" for exact insertion points and recommended replacement text)
  </read_first>
  <action>
    Apply four planning-doc edits in this exact order so cross-references stay coherent.

    1) `.planning/REQUIREMENTS.md` — implement Pattern A from D6-05 (Open Question #5 recommendation):
       - Remove the OP-02 and OP-03 entries from the v1 Operator Surface (OP) section (currently lines 32-33). The OP-04..OP-08 numbering stays; do not renumber.
       - Append two new rows to the `### Platform (PLAT-v2)` subsection (currently lines 107-114) immediately after `PLAT-v2-06`: `PLAT-v2-07: MCP server wrapping miner-core via tokio::task::spawn_blocking (see docs/future_mcp_http.md)` and `PLAT-v2-08: HTTP server wrapping miner-core via tokio::task::spawn_blocking (see docs/future_mcp_http.md)`.
       - Update the traceability table (currently lines 137-180 — header at line 137-138 with the three columns `| Requirement | Phase | Status |`; OP-02 + OP-03 currently at lines 154-155 read `| OP-02 | Phase 6 | Pending |` and `| OP-03 | Phase 6 | Pending |`) — keep the table strictly 3-column for every row (warning-3 fix: do NOT add a fourth column to only some rows; the rest of the table stays 3-column). Replace the OP-02 row with `| OP-02 | v2 (PLAT-v2-07) | Reclassified — design in docs/future_mcp_http.md |` and the OP-03 row with `| OP-03 | v2 (PLAT-v2-08) | Reclassified — design in docs/future_mcp_http.md |`. The doc pointer rides in the Status-cell text rather than in a new column.
       - Update the Coverage footer: `v1 requirements: 50 total` (was 52) and adjust `Mapped to phases: 50`.

    2) `.planning/ROADMAP.md` — rewrite the Phase 6 block (currently lines 139-155 — `### Phase 6` header at 139; Goal at 140; Depends-on at 141; Requirements at 142; Success-Criteria block 143-148; `**Plans**:` count at 149; `Plans:` list at 151-154; `**UI hint**:` at 155) per D6-01 + Open Question #7:
       - Replace the **Goal** line with: `**Goal**: User can read \`./ARCHITECTURE.md\` plus the \`./docs/\` folder and understand miner's system map, locked Finding envelope, 22-scan catalogue, TOML sweep manifest grammar, and the deferred MCP+HTTP design — without needing to read source. The MCP and HTTP server implementations are deferred to v2 (tracked as PLAT-v2-07 + PLAT-v2-08 in REQUIREMENTS.md).`
       - Replace **Requirements** with `**Requirements**: (none; this phase reclassifies OP-02 + OP-03 to v2 — see PLAT-v2-07, PLAT-v2-08)`.
       - Replace the five **Success Criteria** with the five from Open Question #7: (1) docs deliver the system map + envelope + catalogue without reading source; (2) `docs/sweep_manifest.md` documents v1 TOML manifest + effect-size + hygiene knobs; (3) `docs/future_mcp_http.md` carries the planned MCP + HTTP design with pointers into `.planning/research/`; (4) `docs/examples/decode_finding.py` decodes any Finding envelope and re-tests its statistic from raw arrays; (5) every new doc carries the Apache-2.0 license footer matching the `tradedesk/docs/` convention.
       - DELETE the "**Research flag**" blockquote at line 157 (the `> **Research flag:** Before planning this phase, re-run \`gsd-research\` on the \`rmcp\` crate ...` paragraph). No SDK research needed for docs-only.
       - Update the phase-list bullet at line 20 (the `- [ ] **Phase 6: MCP & HTTP Wrappers** ...` line in the top-level `## Phases` enumeration) from `**Phase 6: MCP & HTTP Wrappers**` to `**Phase 6: MCP & HTTP Wrappers (Docs-Only)** — design contract + docs/ folder; implementation deferred to v2`.
       - Update the intro paragraph (line 5 — the single paragraph under `## Overview`) to replace the sentence about Phase 6 with: `Phase 6 publishes the design contract and \`docs/\` folder describing the locked envelope, scan catalogue, sweep manifest, agent-integration guide, and the deferred MCP+HTTP wrapper design; the wrapper implementations move to v2.`
       - Update the Phase 6 progress-table row (currently line 189: `| 6. MCP & HTTP Wrappers | 0/TBD | Not started | - |`) to `| 6. MCP & HTTP Wrappers (Docs-Only) | 0/3 | Planned | - |`. The progress table starts at line 182 and uses the `| Phase | Plans Complete | Status | Completed |` header.

    2a) `.planning/ROADMAP.md` — clean the Phase 7 plan-list pollution (warning-2 fix). The Phase 7 block currently has a Plans-count + plan-list (lines 169-174) that incorrectly duplicates the Phase 6 plan entries:
         ```
         **Plans**: 3 plans

         Plans:
         - [ ] 06-01-PLAN.md — Scope amendments ...
         - [ ] 06-02-PLAN.md — Reference docs triad: ...
         - [ ] 06-03-PLAN.md — agent_integration.md + ...
         ```
         Phase 7 has not been planned yet; the entries are an adjacent planning-doc bug introduced when Phase 6's plan-list was pre-populated. Restore Phase 7's shape: replace the `**Plans**: 3 plans` line with `**Plans**: TBD pending Phase 7 plan-phase`, and replace the `Plans:` block + the three `- [ ] 06-0?-PLAN.md` bullets with a single line `Plans: TBD pending Phase 7 plan-phase` (no bullet list). Keep the surrounding Phase 7 lines (the `### Phase 7:` header at 159, Goal at 160, Depends-on at 161, Requirements at 162, Success-Criteria 163-168, and `**UI hint**:` line at 175) unchanged.

    3) `.planning/PROJECT.md` — amend the Active list per D6-06. Locate the two lines:
       `- [ ] MCP server as a thin wrapper for agent use` and `- [ ] HTTP API as a thin wrapper for remote agent use (the RadiusRed Quant agent runs as a remote Paperclip agent)`. Replace BOTH with:
       `- [x] MCP server interface — designed; implementation deferred to v2 (see docs/future_mcp_http.md)` and `- [x] HTTP API interface — designed; implementation deferred to v2 (see docs/future_mcp_http.md)`.

    4) `.planning/STATE.md` — amend Blockers/Concerns + Deferred Items per D6-07:
       - In `### Blockers/Concerns` (line 102+) strike the entire bullet starting `- **Phase 6 (MCP & HTTP wrappers):** \`rmcp\` is the highest-risk dependency...`. Replace with: `- **Phase 6 deferred (now docs-only):** design documented in docs/future_mcp_http.md; v2 owns the rmcp re-research + implementation (tracked as PLAT-v2-07 + PLAT-v2-08).`
       - In `## Deferred Items` table (line 113-117) replace the `*(none)*` placeholder row with a real row: `| Operator surface | OP-02 (MCP) + OP-03 (HTTP) | Design documented v1 (docs/future_mcp_http.md); implementation deferred to v2 (PLAT-v2-07, PLAT-v2-08) | Phase 6 |`.

    Do NOT touch the OP-04..OP-08 entries, the OP-01 row, or any FOUND / CACHE / ANOM / CROSS / SEAS / HYG / OUT rows. Do NOT renumber existing PLAT-v2-01..06 entries. Do NOT add a fourth column to the traceability table — every row stays 3-column (`Requirement | Phase | Status`); the reclassification pointer rides inside the Status-cell text per Option A.
  </action>
  <verify>
    <automated>grep -c "^- \*\*PLAT-v2-0[78]\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \*\*PLAT-v2-0[78]\*\*" .planning/REQUIREMENTS.md` returns exactly `2`.
    - `grep -E "^- \*\*OP-02\*\*|^- \*\*OP-03\*\*" .planning/REQUIREMENTS.md` returns 0 lines (both removed from v1 section).
    - `grep -c "| OP-02 | v2 (PLAT-v2-07) " .planning/REQUIREMENTS.md` returns `1`; same check for OP-03 / PLAT-v2-08 returns `1`.
    - `grep "v1 requirements: 50 total" .planning/REQUIREMENTS.md` returns 1 match.
    - `grep -c "docs/future_mcp_http.md" .planning/ROADMAP.md` returns `>= 2`.
    - `grep "Research flag" .planning/ROADMAP.md` returns 0 lines (flag removed).
    - `grep -c "Docs-Only" .planning/ROADMAP.md` returns `>= 2` (phase-list + progress-table updates).
    - `grep "0/3 | Planned" .planning/ROADMAP.md` returns 1 match (Phase 6 progress row).
    - `grep -c "06-0[123]-PLAN.md" .planning/ROADMAP.md` returns exactly `3` (Phase 6's three plan-list entries only; the Phase 7 duplicate-pollution rows are gone — warning-2 fix).
    - `grep -c "TBD pending Phase 7 plan-phase" .planning/ROADMAP.md` returns `>= 1` (Phase 7 plan-list restored to placeholder).
    - `grep -c "designed; implementation deferred to v2" .planning/PROJECT.md` returns `>= 2`.
    - `grep "Phase 6 deferred (now docs-only)" .planning/STATE.md` returns 1 match.
    - `grep "OP-02 (MCP) + OP-03 (HTTP)" .planning/STATE.md` returns 1 match (Deferred Items row added).
    - `grep -c "\*(none)\*" .planning/STATE.md` returns 0 (placeholder row gone).
  </acceptance_criteria>
  <done>All four planning files reflect docs-only Phase 6; OP-02 + OP-03 are visible in v2 only; ROADMAP Phase 6 lists the new docs deliverables; STATE Deferred Items has the carry-over row.</done>
</task>

<task type="auto">
  <name>Task 2: Write root ARCHITECTURE.md and docs/.license-footer.md</name>
  <files>ARCHITECTURE.md, docs/.license-footer.md</files>
  <read_first>
    - /home/darren/projects/radiusred/tradedesk/ARCHITECTURE.md (full file — the sibling-repo template; ~47 lines; section layout: Overview / Data Flow (high level) / Live vs Backtest paths / Key design decisions; closing `See also:` line; Apache-2.0 footer with bare URL)
    - /home/darren/projects/radiusred/tradedesk/docs/aggregation_guide.md (tail 15 lines for canonical footer form)
    - .planning/research/ARCHITECTURE.md sections §1 (System Overview), §2 (Workspace/Crate Layout), §7 (Findings Envelope), §11 (Architectural Patterns), §12 (Data Flow) — source material to distill
    - crates/miner-core/src/findings/mod.rs lines 280-553 (Finding enum + variants — to confirm the seven variants and their names)
    - crates/miner-core/src/engine/mod.rs (skim — confirm engine::run_one + engine::run_one_with_registry are the facade entry points)
    - crates/miner-core/src/sweep/mod.rs (skim — confirm sweep::run_sweep is the sweep entry point)
    - crates/miner-core/src/findings/sink.rs (skim — confirm FindingSink + StdoutSink + FileSink are the writer surface)
    - .planning/phases/06-mcp-http-wrappers/06-CONTEXT.md (decision D6-04 for footer text; "What Phase 6 delivers" item 1 for ARCHITECTURE.md scope)
    - .planning/phases/06-mcp-http-wrappers/06-PATTERNS.md section "./ARCHITECTURE.md (root, ~80-110 lines)" for section-mapping
  </read_first>
  <action>
    Create two files.

    File 1: `docs/.license-footer.md` — the single-source canonical Apache-2.0 footer template re-used verbatim by Plans 06-02 / 06-03 (paste-and-go). Content is exactly the block below, no leading blank lines, ending newline only:

      ---

      ## License

      Licensed under the Apache License, Version 2.0.
      See: https://www.apache.org/licenses/LICENSE-2.0

      Copyright 2026 [Radius Red Ltd.](https://github.com/radiusred) | [Contact](mailto:opensource@radiusred.uk)

    Bare URL form (no autolink) per D6-04 + Open Question #6 default + 4-of-5 tradedesk sibling-repo majority. This file is checked-in but starts with a leading-dot filename so README listings (Plan 06-03) skip it naturally.

    File 2: `ARCHITECTURE.md` (root, target ~85-105 lines including the footer). Mirror the tradedesk-sibling layout exactly: H1 title, then four plain-text section labels followed by bulleted lists (NOT `##` H2s — tradedesk's ARCHITECTURE.md uses plain-text section labels like `Overview` / `Data Flow (high level)` / `Live vs Backtest paths` / `Key design decisions`). For miner the four sections are:

    Section 1 — `Overview` (replace tradedesk's domain bullets with miner's six-crate workspace + the one-way dependency direction):
      - tradedesk-miner is a high-performance, agent-operable data-mining engine for historical financial OHLCV data; it scans cached Dukascopy bid/ask CSVs and surfaces statistical candidates for downstream consumers.
      - The codebase is organised into six crates with a strict one-way dependency direction:
        - `miner-core` — the sync + rayon library. Owns the locked `Finding` envelope, the scan registry, the engine facade (`engine::run_one`), the sweep runner (`sweep::run_sweep`), the derived-bar cache, and the 22 v1 scans (ANOM / CROSS / SEAS).
        - `miner-reader-dukascopy` — the reference implementation of the `Reader` trait against the existing tradedesk-dukascopy zstd-CSV cache layout.
        - `miner-cli` — the thin clap-derive wrapper exposing `miner scan` / `miner sweep` / `miner scans` / `miner emit-fixture`.
        - `miner-mcp` and `miner-http` — placeholder binaries. MCP and HTTP server implementations are deferred to v2 — see `docs/future_mcp_http.md`.
        - `miner-bench` + `xtask` — bench harness + workspace tooling (schema regen, etc.).
      - Dependency direction: `miner-cli | miner-mcp | miner-http -> miner-reader-dukascopy -> miner-core`. CI gate 3 (`cargo tree -p miner-core --edges normal,build`) enforces this — `miner-core` must show zero `tokio` / `async` transitive dependencies.

    Section 2 — `Data Flow (high level)`:
      - `Reader::read_day` ingests zstd-CSVs from the tradedesk-dukascopy cache at `<root>/<SYMBOL>/<YYYY>/<MM 00-indexed>/<DD>_<bid|ask>.csv.zst`.
      - The aggregator deterministically materialises higher-timeframe UTC-aligned bars (15m / 1h / 1d) and serves them from the on-disk derived-bar cache (Arrow IPC files, keyed by `(source_id, symbol, side, timeframe)`, two-axis invalidation: `aggregator_version` / `arrow_schema_version` mismatch -> full rebuild; per-day blake3 fingerprint mismatch -> day-splice).
      - `GapDetector` produces a structured `GapManifest` of `(start, end, reason)` tuples before any scan runs.
      - `engine::run_one` is the single facade entry. It runs preflight -> framing -> dry-run -> gap-policy dispatch -> Scan::run -> RunSummary, emitting `Finding` envelopes through the `FindingSink` trait.
      - Scans are sync + rayon-parallel; multi-job sweeps fan out via `sweep::run_sweep` (rayon `par_iter` over `ResolvedJob`s with deterministic-order buffered drain + end-of-sweep BH-FDR aggregation).
      - `FindingSink` (with the two production impls `StdoutSink` for the CLI and `FileSink` for tests) is the single sanctioned writer. Stdout = findings JSONL; stderr = `tracing` structured logs.

    Section 3 — `Sync core + async edges` (this replaces tradedesk's `Live vs Backtest paths` section; per PATTERNS.md the live/backtest distinction is NOT APPLICABLE to miner):
      - `miner-core` is pure sync + rayon. No `tokio`, no `async fn`, no `.await`. This is FOUND-04 and is CI-enforced.
      - Async lives only at the wrapper edges. The MCP and HTTP wrappers (designed but not implemented in v1 — see `docs/future_mcp_http.md`) will bridge to `miner-core` via `tokio::task::spawn_blocking` so the scan engine never blocks an async runtime worker.
      - Stdout is reserved for findings JSONL. Stderr is reserved for structured logs. `clippy::disallowed_macros` rejects `println!` / `eprintln!` outside the single findings sink and the logging adapter (CI gate 2).

    Section 4 — `Key design decisions`:
      - Locked `Finding` envelope. Seven variants (`RunStart` / `Result` / `ScanError` / `GapAborted` / `RunEnd` / `DryRun` / `SweepSummary`) with `schema_version`, `scan@version`, `param_hash`, `code_revision`, `data_slice`, and reserved DSR + FDR-q slots. Schema-additive discipline only; ground truth is `schemas/findings-v1.schema.json` regenerated from the schemars derive on the Rust types.
      - Gap-policy enforcement. No silent scans over gapped data. Callers pick `strict` (abort + emit the gap manifest as a structured error, zero findings) or `continuous_only` (partition into maximal gap-free sub-ranges, one finding per sub-range, gap manifest attached).
      - Reproducibility envelope. `master_seed` propagates through every `ResolvedJob`; `derive_job_seed` (blake3) makes each scan's RNG state deterministic and byte-identical across re-runs.
      - Streaming and stateless. miner emits findings as NDJSON and exits. There is no persistent results store; callers own persistence. The only writable state miner owns is its derived-bar cache.
      - Agent-operability. Every miner capability is reachable from the CLI today and from MCP + HTTP in v2 (see `docs/future_mcp_http.md`). The CLI is the load-bearing v1 surface; MCP + HTTP wrappers add transport ergonomics without changing the engine.
      - Open-source posture. No hardcoded paths. Cache root + derived-bar-cache root + output destination all configurable via CLI flag > env var > config file precedence. Apache-2.0 licensed.

    After Section 4 add a `See also:` paragraph linking the other docs (this matches tradedesk/ARCHITECTURE.md line 37 pattern — single line, comma-separated, with the final `, and ` Oxford form):

      See also: `README.md`, `docs/findings_envelope.md`, `docs/scan_catalogue.md`, `docs/sweep_manifest.md`, `docs/agent_integration.md`, and `docs/future_mcp_http.md`.

    Then the canonical Apache-2.0 footer block (paste the contents of `docs/.license-footer.md` verbatim — the `---` separator + License heading + bare-URL + Copyright line).

    No TOC. No ASCII diagrams beyond the inline `miner-cli | miner-mcp | miner-http -> miner-reader-dukascopy -> miner-core` arrow notation. No mermaid. Target file size: 85-110 lines.

    Do NOT pre-create any other docs/ files — that is Plans 06-02 / 06-03's responsibility. Do NOT touch `miner-mcp` / `miner-http` placeholder main.rs files — Plan 06-03 owns that optional edit.
  </action>
  <verify>
    <automated>test -f ARCHITECTURE.md && test -f docs/.license-footer.md && tail -8 ARCHITECTURE.md | grep -q "apache.org/licenses/LICENSE-2.0" && grep -F "miner-cli | miner-mcp | miner-http" ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ARCHITECTURE.md` succeeds.
    - `test -f docs/.license-footer.md` succeeds.
    - `wc -l ARCHITECTURE.md` returns between `60` and `120` (allows for footer; target ~85-110).
    - `wc -l docs/.license-footer.md` returns between `6` and `12`.
    - `tail -8 ARCHITECTURE.md | grep -c "apache.org/licenses/LICENSE-2.0"` returns `1`.
    - `tail -8 ARCHITECTURE.md | grep -c "Copyright 2026"` returns `1`.
    - `grep -c "miner-cli | miner-mcp | miner-http -> miner-reader-dukascopy -> miner-core" ARCHITECTURE.md` returns `>= 1`.
    - `grep -F "engine::run_one" ARCHITECTURE.md` returns `>= 1` match.
    - `grep -F "sweep::run_sweep" ARCHITECTURE.md` returns `>= 1` match.
    - `grep -F "FindingSink" ARCHITECTURE.md` returns `>= 1` match.
    - `grep -F "spawn_blocking" ARCHITECTURE.md` returns `>= 1` match.
    - `grep -F "schema_version" ARCHITECTURE.md` returns `>= 1` match.
    - `grep -F "gap" ARCHITECTURE.md` returns `>= 2` matches (gap policy + gap manifest).
    - `grep -F "docs/future_mcp_http.md" ARCHITECTURE.md` returns `>= 1` match (v1 deferral pointer).
    - `grep -F "See also:" ARCHITECTURE.md` returns `1` match.
    - `grep -c "^## " ARCHITECTURE.md` returns `<= 2` (only the trailing `## License` heading from the footer is an H2; the four body sections use plain-text section labels per the tradedesk pattern).
    - `diff <(tail -8 ARCHITECTURE.md) <(tail -8 docs/.license-footer.md)` shows the last 8 lines are identical (footer reused verbatim).
    - `cargo build --workspace --all-targets` still passes (no Rust code touched; sanity regression).
  </acceptance_criteria>
  <done>Root `ARCHITECTURE.md` published; the four body sections narrate the one-way dependency direction, data flow, sync core + async edges discipline, and key design decisions; canonical Apache-2.0 footer present and identical to `docs/.license-footer.md`; cargo build still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — docs-only) | This plan adds no runtime code, no new binaries, no new dependencies, and no network surface. Edits are confined to markdown files under `.planning/` and the repo-root `ARCHITECTURE.md` + `docs/.license-footer.md`. No data crosses any trust boundary as a result of this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-DOCS | n/a | docs/markdown content | accept | No new attack surface — Phase 6 ships documentation only; no new dependencies, no executable code paths. The only runtime-adjacent change in this plan set lands in Plan 06-03 (two `tracing::info!` literal-string edits on existing placeholder binaries). |
</threat_model>

<verification>
- `cargo build --workspace --all-targets` still passes (no Rust touched).
- `cargo tree -p miner-core --edges normal,build` still shows zero `tokio` / `async` / `axum` / `rmcp` (regression-prevention; this plan adds no Cargo.toml edits).
- `grep -n "OP-02\|OP-03" .planning/REQUIREMENTS.md` shows entries only in the v2 PLAT-v2-07 / PLAT-v2-08 rows and the two traceability table reclassification rows (no v1 Operator-Surface rows for them).
- `grep -n "rmcp\|Research flag" .planning/ROADMAP.md` returns 0 matches (the research-flag blockquote is gone).
- `head -1 ARCHITECTURE.md` returns `# Architecture`.
- `head -1 docs/.license-footer.md` returns `---` (the divider that precedes the License heading when the footer is appended to a parent doc).
</verification>

<success_criteria>
- The four planning files (REQUIREMENTS / ROADMAP / PROJECT / STATE) describe the docs-only Phase 6 deliverable consistently with one another.
- OP-02 and OP-03 appear in the v2 section as PLAT-v2-07 + PLAT-v2-08; the v1 Operator Surface rows for them are gone.
- Root `ARCHITECTURE.md` exists, is ~85-110 lines, ends with the canonical Apache-2.0 footer, and carries the one-way crate dependency direction, sync core + async edges discipline, locked envelope variants, and gap-policy semantics.
- `docs/.license-footer.md` exists and is byte-identical to the last 8 lines of `ARCHITECTURE.md` (the executors of Plans 06-02 / 06-03 will reuse this file verbatim).
- Workspace `cargo build --workspace --all-targets` still passes.
</success_criteria>

<output>
Create `.planning/phases/06-mcp-http-wrappers/06-01-SUMMARY.md` with:
- Confirmation of the OP-02 + OP-03 reclassification (D6-05 Pattern A applied).
- Confirmation that ROADMAP Phase 6 block + Success Criteria are now docs-only and the research flag is gone.
- Line count of `ARCHITECTURE.md` (target ~85-110).
- Confirmation that the Apache-2.0 footer is byte-identical between `ARCHITECTURE.md` tail and `docs/.license-footer.md`.
- Pointer at Plan 06-02 + 06-03 as next plans (sequential — same wave is impossible because they reuse `docs/.license-footer.md` produced here).
</output>
</content>
</invoke>