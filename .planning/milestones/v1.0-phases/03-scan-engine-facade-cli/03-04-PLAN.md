---
phase: 03-scan-engine-facade-cli
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - crates/miner-core/src/scan/ljung_box/kernel.rs
  - crates/miner-core/src/scan/ljung_box/mod.rs
  - crates/miner-core/src/engine/mod.rs
  - crates/miner-core/tests/common/mod.rs
  - crates/miner-core/tests/common/counting_sink.rs
autonomous: true
requirements: [OP-01, OP-05, OP-06, OP-08, OUT-04]

must_haves:
  truths:
    - "log_returns([1.0, e, e*e]) == vec![1.0, 1.0] (two log-returns from three closes)"
    - "biased_acf on a constant series returns NaN-or-zero at lag>=1 (denom collapses to 0); biased_acf on a fixed AR(1) seed matches a hand-computed reference within 1e-12"
    - "ljung_box_q_and_p with a known input matches the statsmodels 0.14.6 reference Q-stats and p-values within 1e-12 (the Plan 06 golden test pins byte equality on the redacted JSONL; Plan 04 unit-tests the kernel against a hand-computed reference)"
    - "LjungBoxScan::run on a deterministic BarFrame writes exactly one Finding::Result to the sink with effect.metric=='ljung_box_q', effect.value at max-lag, effect.p_value at max-lag, effect.n == Some(returns_length), effect.extra carrying lags/q_stats/p_values/acf RawArrays, raw.series carrying returns + timestamps_ms RawArrays (using the ACTUAL ResultFinding field names: scan_id_at_version via #[serde(rename = \"scan_id@version\")], param_hash: String, source: Source, params: serde_json::Value, raw: Option<Raw>)"
    - "engine::run_one with a Strict policy + GapManifest containing gaps emits exactly one Finding::RunStart, one Finding::GapAborted carrying the full manifest, one Finding::RunEnd; exit-mappable RunOutcome::Ok; results_emitted == 0 in RunEnd.summary (RunSummary fields are results_emitted / scan_errors / gap_aborted — per Plan 02 Warning 9 RunSummary is NOT extended with dry_run_emitted)"
    - "engine::run_one with ContinuousOnly + zero gaps + LjungBoxScan emits exactly one RunStart, one Result (with data_slice.gap_manifest = Some(GapManifest{gaps:vec![]})), one RunEnd; results_emitted == 1"
    - "engine::run_one with req.dry_run == true emits exactly one RunStart, one Finding::DryRun, one RunEnd; results_emitted == 0; the JSON envelope contains no `dry_run_emitted` field (Warning 9 pin); the DryRunFinding carries resolved_params, planned_data_slice, estimated_findings_count"
    - "engine::run_one with cancel.load() == true at entry returns RunOutcome::Ok with zero scan-body emissions (only RunStart/RunEnd framing); the cancel-poll happens before bar-loading per RESEARCH Pattern 4"
    - "Cancellation during scan body: setting cancel = true between the RunStart write and the Scan::run call causes Scan::run's first cancellation check to return Ok(()) early; only RunStart + RunEnd emitted"
    - "engine::cancellation_tests sub-module covers all three documented yield sites for Phase 3's run_one (cancel_at_entry, cancel_before_subrange, cancel_inside_scan_kernel) per SC-5b — VALIDATION.md row updated to reference these names"
    - "Reader errors are wrapped via MinerError::Scan(format!(...)) — MinerError has variants Io / Serialize / Config / Scan(String) / Internal(String) only (no Reader variant exists); pinned at revision time against crates/miner-core/src/error/mod.rs"
    - "LjungBoxScan::run is cancel-aware while sleeping on the test-only sleep_after_first_finding_ms hook — never calls std::thread::sleep(d) directly; polls ctx.cancel in ~10ms increments inside a while-loop"
    - "CountingSink test helper lives at crates/miner-core/tests/common/counting_sink.rs (declared in this plan's files_modified) — re-exported via crates/miner-core/tests/common/mod.rs"
  artifacts:
    - path: "crates/miner-core/src/scan/ljung_box/kernel.rs"
      provides: "log_returns + biased_acf + ljung_box_q_and_p pure kernels with unit tests against hand-computed references"
      contains: "fn ljung_box_q_and_p"
    - path: "crates/miner-core/src/scan/ljung_box/mod.rs"
      provides: "LjungBoxScan: Scan impl with full envelope construction per D3-04, cancel-aware sleep loop honouring Plan 02's cfg-gated ScanCtx.sleep_after_first_finding_ms hook"
      contains: "impl Scan for LjungBoxScan"
    - path: "crates/miner-core/src/engine/mod.rs"
      provides: "run_one facade body — preflight, framing, dry-run short-circuit (req.dry_run from Plan 02), gap-policy dispatch, scan dispatch, cancel polling, RunSummary build + engine::cancellation_tests sub-module covering SC-5b"
      contains: "pub fn run_one"
    - path: "crates/miner-core/tests/common/counting_sink.rs"
      provides: "CountingSink<S> wrapper over a backing FindingSink that increments per-envelope counters for use in run_one + cancellation_tests unit tests (Warning 6 declaration)"
      contains: "impl FindingSink for CountingSink"
    - path: "crates/miner-core/tests/common/mod.rs"
      provides: "Re-export of CountingSink for the engine integration tests"
      contains: "pub mod counting_sink"
  key_links:
    - from: "crates/miner-core/src/engine/mod.rs::run_one"
      to: "crates/miner-core/src/engine/{preflight,framing,param_hash,gap_policy}::*"
      via: "calls helpers in sequence per RESEARCH §System Architecture Diagram lines 142-191"
      pattern: "preflight::|framing::|param_hash|gap_policy::dispatch"
    - from: "crates/miner-core/src/scan/ljung_box/mod.rs::LjungBoxScan::run"
      to: "crates/miner-core/src/scan/ljung_box/kernel.rs"
      via: "log_returns + biased_acf + ljung_box_q_and_p kernel calls"
      pattern: "kernel::"
    - from: "crates/miner-core/src/scan/ljung_box/mod.rs"
      to: "crates/miner-core/src/findings/mod.rs (Finding::Result + ResultFinding + Effect + Raw + RawArray)"
      via: "envelope construction inside Scan::run using the PINNED field names (scan_id_at_version, param_hash: String, source: Source, params: Value, effect: Effect, raw: Option<Raw>)"
      pattern: "Finding::Result\\("
    - from: "crates/miner-core/src/engine/mod.rs::cancellation_tests"
      to: "crates/miner-core/src/scan/ljung_box/mod.rs (sleep_after_first_finding_ms hook from Plan 02)"
      via: "cancel_inside_scan_kernel test sets ctx.sleep_after_first_finding_ms = Some(2000) + flips cancel mid-sleep; asserts first Result emitted, second never emitted"
      pattern: "cancel_inside_scan_kernel"
---

<objective>
Deliver the Ljung-Box scan kernel (matching statsmodels 0.14.6 acorr_ljungbox under adjusted=False, fft=False) and the full `engine::run_one` facade that composes Plan 03's sub-modules into the single library entry point CLI/MCP/HTTP all call. This is the heart of Phase 3 — the byte-identity contract Phase 6 will validate parity against.

Purpose: After Plan 03 lands the engine sub-modules and Plan 02 locks the envelope contract (including `ScanRequest.dry_run` and the cfg-gated `ScanCtx.sleep_after_first_finding_ms` hook), Plan 04 is the "assembly" plan — Ljung-Box becomes a real Scan impl (D3-01..D3-05), and run_one wires preflight → framing → bar loading → gap-policy dispatch → scan dispatch → framing-close into one pure-sync function. Every Phase 4 scan will land by adding a `r.register(Box::new(NewScan));` line to bootstrap() and a new file under `crates/miner-core/src/scan/<family>/<name>/`; no engine changes needed.

This plan also covers SC-5b in full: the `engine::cancellation_tests` sub-module exercises all THREE documented yield sites for Phase 3's `run_one` — `cancel_at_entry`, `cancel_before_subrange`, `cancel_inside_scan_kernel` — keyed to the names in VALIDATION.md.

Output: working LjungBoxScan emitting a fully-populated `Finding::Result` (using the PINNED ResultFinding field names — see Task 2 action); working `run_one` with strict + continuous_only + dry-run + cancel paths; `engine::cancellation_tests` sub-module with 3 cancel_at_* tests; `CountingSink` test helper at `crates/miner-core/tests/common/counting_sink.rs`; >=15 in-process integration-style unit tests against a VecSink across both new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md
@.planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md
@.planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md
@.planning/phases/03-scan-engine-facade-cli/03-VALIDATION.md
@CLAUDE.md
@crates/miner-core/src/aggregator.rs
@crates/miner-core/src/cache.rs
@crates/miner-core/src/gap.rs
@crates/miner-core/src/findings/mod.rs
@crates/miner-core/src/findings/sink.rs
@crates/miner-core/src/error/mod.rs
@crates/miner-core/src/error/codes.rs
@crates/miner-core/src/config/mod.rs
@crates/miner-core/src/reader.rs

<interfaces>
<!-- Existing types this plan composes — PINNED at revision time against the actual source. -->

From Plan 02/03:
- pub trait Scan: Send + Sync { id, version, param_schema, finding_fields, run(&self, &ScanCtx, &ScanRequest, &mut dyn FindingSink) -> Result<(), ScanError> }
- pub struct ScanCtx<'a> { bars: &'a BarFrame, gap_manifest: Option<&'a GapManifest>, run_id: RunId, code_revision: &'a str, cancel: Arc<AtomicBool>, #[cfg(any(test, feature = "test-internal"))] sleep_after_first_finding_ms: Option<u64> }
- pub struct ScanRequest { scan_id: String, version: u32, instrument: String, side: Side, timeframe: Timeframe, window: ClosedRangeUtc, sub_range: TimeRange, gap_policy: GapPolicyKind, resolved_params: serde_json::Value, param_hash: Blake3Hex, #[serde(default)] dry_run: bool, #[cfg(any(test, feature = "test-internal"))] #[serde(default)] sleep_after_first_finding_ms: Option<u64> }
- pub fn build_run_start, build_run_end (Plan 03)
- pub fn param_hash (Plan 03)
- pub fn resolve_scan, parse_params_kv, parse_iso_utc_window (Plan 03)
- pub fn dispatch(manifest, requested, policy) -> GapDispatch (Plan 03)
- pub enum RunOutcome { Ok, HadScanErrors, PreflightFailed } (Plan 01 scaffold; Plan 04 may extend with Aborted/Cancelled variants if needed — DO NOT add a DryRun variant; Plan 02 Warning 9 pins that the dry-run signal lives in req.dry_run + Finding::DryRun ONLY)

From crates/miner-core/src/findings/mod.rs (PINNED — Warning 7 fix):
- pub struct ResultFinding { schema_version: u32, scan_id_at_version: String (#[serde(rename = "scan_id@version")]), param_hash: String, code_revision: String, data_slice: DataSlice, dsr: Option<f64>, fdr_q: Option<f64>, run_id: RunId, produced_at_utc: DateTime<Utc>, source: Source, params: serde_json::Value, effect: Effect, raw: Option<Raw> }
  — NOTE: `param_hash` is `String` (NOT `Blake3Hex`); call `.as_str().to_string()` or `.to_string()` on the Blake3Hex from req.param_hash to populate it.
  — NOTE: `scan_id_at_version` is renamed via `#[serde(rename = "scan_id@version")]`; the Rust field name is `scan_id_at_version` (snake-case Rust identifier; the wire form has the @ symbol).
  — NOTE: `raw` is `Option<Raw>` (NOT bare `Raw`); LjungBoxScan emits Some(Raw::new(...)?).
  — NOTE: `source` is `Source { source_id: String, symbol: String, side: String, timeframe: String }` (string-typed in Phase 1; LjungBoxScan stringifies req.side and req.timeframe).
- pub struct Effect { metric: String, value: f64, #[serde(default)] p_value: Option<f64>, #[serde(default)] n: Option<u64>, #[serde(default)] ci95: Option<[f64; 2]>, #[serde(default)] extra: BTreeMap<String, RawArray> }
  — NOTE: field is `ci95` (NOT `ci_95`); `n` is `Option<u64>` (NOT bare u64); `p_value` is `Option<f64>`.
- pub struct Raw { series: BTreeMap<String, RawArray> } with Raw::new(series) returning Err if `timestamps_ms` key absent (D-03 invariant — must be enforced when emitting).
- pub struct RawArray { data: Base64Bytes, shape: Vec<u64>, dtype: Dtype } — NOT `len`-based; uses `shape: Vec<u64>` (typically `vec![n as u64]` for 1-D arrays).
- pub struct RunSummary { results_emitted: u64, scan_errors: u64, gap_aborted: u64, per_scan: BTreeMap<String, PerScanCounts> } — fields are `scan_errors` and `gap_aborted` (NOT `errors_emitted` / `gap_aborted_emitted`).
- pub struct PerScanCounts { results: u64, errors: u64, gap_aborted: u64 }
- GapAbortedFinding { schema_version, scan_id_at_version, param_hash: String, code_revision: String, data_slice: DataSlice, dsr, fdr_q, run_id, produced_at_utc, source: Source, gap_manifest: serde_json::Value } — note `gap_manifest: serde_json::Value`, not the typed GapManifest.

From crates/miner-core/src/error/mod.rs (PINNED — Warning 8 fix):
- pub enum MinerError { Io(#[from] std::io::Error), Serialize(#[from] serde_json::Error), Config(#[from] figment::Error), Scan(String), Internal(String) }
- There is NO `MinerError::Reader` or `MinerError::ReaderGeneric` variant. Reader-error wrapping uses `MinerError::Scan(format!("reader: {e}"))` (chosen variant — `Scan` is the closest semantic match for "the data-loading step failed"). The selection is documented in run_one's doc-comment.
- impl From<MinerError> for WireError exists and defaults to ScanErrorCode::InternalPanicCaught.

From Phase 1/2 (unchanged):
- BarCache::get_or_build<R: Reader>(&self, reader: &R, params: AggParams) -> Result<BarFrame, CacheError>
- AggParams { symbol, side, tf, range, calendar } shape (read aggregator.rs)
- GapDetector::detect<R: Reader>(reader, symbol, side, range) -> Result<GapManifest, R::Error>
- BarFrame { ts_open_utc: Vec<DateTime<Utc>>, open: Vec<f64>, high: Vec<f64>, low: Vec<f64>, close: Vec<f64>, tick_volume: Vec<f64> }

From statsmodels reference (RESEARCH Code Examples lines 641-687):
- log_returns(close[]) = ln(close[t] / close[t-1])
- biased_acf(x, max_lag): denom = sum((x-mean)^2); out[0]=1; out[k] = sum((x-mean) * (x_shifted-mean)) / denom (NOT (n-k))
- ljung_box_q[h] = n * (n+2) * cumulative_sum(acf[k]^2 / (n-k)) for k in 1..=h
- p_value[h] = 1 - ChiSquared::new(h as f64).cdf(q[h-1])
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ljung-Box pure kernels — log_returns + biased_acf + ljung_box_q_and_p</name>
  <files>crates/miner-core/src/scan/ljung_box/kernel.rs</files>
  <read_first>
    - crates/miner-core/src/scan/ljung_box/kernel.rs (Plan 01 scaffold — signatures present, bodies unimplemented)
    - .planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md (Code Examples lines 641-687 — the EXACT kernel implementations to copy; note the summation order rule at line 687)
    - .planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md (D3-02 — inline log returns; D3-03 — lags default = min(10, n/5); D3-05 — golden tolerance approach)
    - .planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md (lines 240-285 — kernel pattern with #[cfg(test)] mod tests + #[inline] pure-function rule)
    - crates/miner-core/src/aggregator.rs (lines 235-258 — #[inline] private pure-function template kernel mirrors)
  </read_first>
  <behavior>
    - log_returns_basic: log_returns(&[1.0, 2.7182818284590451, 7.3890560989306495]) returns [1.0, 1.0] within 1e-12.
    - log_returns_empty: log_returns(&[]) returns vec![] (empty input → empty output).
    - log_returns_singleton: log_returns(&[42.0]) returns vec![] (one close → zero returns).
    - log_returns_length_invariant: log_returns(&close_of_n_elements) has length n-1.
    - biased_acf_lag0_is_one: biased_acf(&x, max_lag) returns out where out[0] == 1.0 (lag-0 is 1 by construction per RESEARCH line 660).
    - biased_acf_constant_series: biased_acf(&[5.0; 10], 3) — the denom is 0 (constant series has zero variance). The kernel SHOULD return [1.0, 0.0, 0.0, 0.0] (treat 0/0 as 0 to match statsmodels which special-cases constant series). Test the contract; document the choice in the function's doc-comment.
    - biased_acf_known_input: Construct a deterministic 8-element fixture x = [1.0, 2.0, 3.0, 2.0, 1.0, 2.0, 3.0, 2.0] and assert biased_acf(&x, 3) matches a hand-computed reference within 1e-12 (compute the expected values during plan-writing and embed in the test).
    - biased_acf_length_is_max_lag_plus_one: biased_acf(&x, k).len() == k + 1.
    - ljung_box_q_monotone: For a series with positive autocorrelation, ljung_box_q_and_p().0 is non-decreasing across lags (each term is a non-negative contribution).
    - ljung_box_q_and_p_known_input: Using the same 8-element x above, compute biased_acf(&x, 3), then ljung_box_q_and_p(x.len(), &acf, 3) and assert Q-stats + p-values match a statsmodels 0.14.6 reference within 1e-12. (Plan-writing precomputes these expected values via the reference; if unable to precompute at plan-time, the test uses approximate-equality with tolerance 1e-10 and a comment pointing to the Plan 06 golden test for byte-exact verification.)
    - ljung_box_p_in_unit_interval: For any Q >= 0 and df >= 1, p_value is in [0, 1].
    - ljung_box_invalid_lag_panics: ljung_box_q_and_p(n=10, &acf, 0) should panic via debug_assert (max_lag must be >= 1).
  </behavior>
  <action>
    Implement crates/miner-core/src/scan/ljung_box/kernel.rs per RESEARCH §Code Examples lines 641-687 verbatim — these are the EXACT implementations to copy, not "match the pattern":

    1. `#[inline] pub(super) fn log_returns(close: &[f64]) -> Vec<f64>`:
       ```
       close.windows(2).map(|w| (w[1] / w[0]).ln()).collect()
       ```
       Handles len 0 and len 1 naturally (returns empty Vec). Doc-comment: "log_returns[t] = ln(close[t] / close[t-1])".

    2. `#[inline] pub(super) fn biased_acf(x: &[f64], max_lag: usize) -> Vec<f64>`:
       - Verbatim from RESEARCH lines 652-666: n as f64, mean = sum/n, cent = x - mean, denom = sum(cent*cent). out[0] = 1.0. For k in 1..=max_lag: num = sum(cent[i] * cent[i+k]); out.push(num / denom).
       - Special case: if denom == 0.0 (constant series), set every lag>=1 to 0.0 instead of NaN (matches statsmodels' constant-series handling). Document the choice.

    3. `#[inline] pub(super) fn ljung_box_q_and_p(returns_n: usize, acf: &[f64], max_lag: usize) -> (Vec<f64>, Vec<f64>)`:
       - Verbatim from RESEARCH lines 670-684: n = returns_n as f64; q = vec of length max_lag; acc = 0.0. For k in 1..=max_lag: acc += acf[k] * acf[k] / (n - k as f64); q[k-1] = n * (n + 2.0) * acc. p[k-1] = 1.0 - ChiSquared::new((k as f64)).cdf(q[k-1]) via statrs::distribution::{ChiSquared, ContinuousCDF}.
       - debug_assert!(max_lag >= 1).
       - debug_assert!(acf.len() > max_lag) — caller must have provided enough ACF values.
       - Note the summation order is sequential and cumsum-style (RESEARCH line 687) — this is what makes goldens match statsmodels' np.cumsum.

    4. Add a `#[cfg(test)] mod tests { use super::*; ... }` block with the 11+ behavior tests listed above. For the "known_input" tests, EMBED the precomputed reference values as constants in the test source — the Plan 06 byte-exact golden test will catch any drift.

    Annotate the module with `#![cfg_attr(any(test, debug_assertions), allow(clippy::float_cmp))]` if needed for float-equality assertions in tests (acceptable in test context only; production code uses tolerance comparisons).

    NO unsafe; NO println/eprintln/dbg; NO async; the kernel is pure functions over &[f64].
  </action>
  <verify>
    <automated>cargo test -p miner-core --lib scan::ljung_box::kernel::tests</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p miner-core --lib scan::ljung_box::kernel::tests passes (>=11 tests)
    - `grep -c '#\[inline\]' crates/miner-core/src/scan/ljung_box/kernel.rs` returns >= 3 (one per kernel function)
    - `grep -c 'statrs::distribution::ChiSquared' crates/miner-core/src/scan/ljung_box/kernel.rs` returns >= 1
    - `grep -c 'pub(super)' crates/miner-core/src/scan/ljung_box/kernel.rs` returns >= 3 (kernels are crate-private to scan::ljung_box)
    - `grep -c 'unsafe' crates/miner-core/src/scan/ljung_box/kernel.rs` returns 0
    - cargo clippy -p miner-core -- -D warnings is clean for this file
  </acceptance_criteria>
  <done>
    Three pure kernel functions implemented per RESEARCH spec; 11+ unit tests passing including the known-input regression against precomputed references; statrs::ChiSquared::cdf wired correctly; debug-asserts guard the contracts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LjungBoxScan: Scan impl — full envelope construction per D3-04 + cancel-aware sleep loop honouring Plan 02 sleep hook (Blocker 3 step 3)</name>
  <files>crates/miner-core/src/scan/ljung_box/mod.rs</files>
  <read_first>
    - crates/miner-core/src/scan/ljung_box/mod.rs (Plan 01 scaffold — id+version stubs only)
    - crates/miner-core/src/scan/ljung_box/kernel.rs (Task 1 output — log_returns, biased_acf, ljung_box_q_and_p)
    - crates/miner-core/src/findings/mod.rs (lines 60-260 — PINNED at revision time: ResultFinding field set = { schema_version, scan_id_at_version (#[serde(rename = "scan_id@version")]), param_hash: String, code_revision: String, data_slice: DataSlice, dsr: Option<f64>, fdr_q: Option<f64>, run_id, produced_at_utc, source: Source, params: serde_json::Value, effect: Effect, raw: Option<Raw> }; Effect = { metric: String, value: f64, p_value: Option<f64>, n: Option<u64>, ci95: Option<[f64;2]>, extra: BTreeMap<String, RawArray> }; Raw::new(series) returns Err if `timestamps_ms` missing; RawArray = { data: Base64Bytes, shape: Vec<u64>, dtype: Dtype })
    - crates/miner-core/src/findings/sink.rs (FindingSink trait + VecSink for tests)
    - crates/miner-core/src/aggregator.rs (BarFrame columnar shape)
    - .planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md (D3-01 — scan_id@version; D3-02 — inline returns; D3-03 — lags default min(10, n/5); D3-04 — full effect/raw shape lock; D3-22 — cancel polling)
    - .planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md (lines 192-285 — LjungBoxScan impl + algorithm-doc header pattern from gap.rs:157-187)
    - .planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md (Pattern 3 lines 351-411 — Scan impl sketch; Pitfall 5 lines 579-587 — NEVER call sink.flush inside Scan::run; Pitfall 8 lines 612-622 — sleep hook + cancel-aware loop pattern)
  </read_first>
  <behavior>
    - LjungBoxScan_id_and_version: scan.id() == "stats.autocorr.ljung_box"; scan.version() == 1.
    - LjungBoxScan_finding_fields: scan.finding_fields() returns ScanFindingShape { effect_extra_keys: &["lags", "q_stats", "p_values", "acf"], raw_series_keys: &["returns", "timestamps_ms"] }.
    - LjungBoxScan_param_schema: scan.param_schema() returns a JSON-Schema-shaped Value with `properties.lags` documented as integer (the schema is hand-rolled per RESEARCH §Pattern 3 lines 354-365).
    - LjungBoxScan_default_lags: With BarFrame of 256 closes (255 returns) and resolved_params = {}, the scan resolves lags to min(10, 255 / 5) = 10 (D3-03).
    - LjungBoxScan_explicit_lags: With resolved_params = {"lags": 5}, the scan uses 5.
    - LjungBoxScan_invalid_lags_low: With resolved_params = {"lags": 0}, Scan::run returns Err(ScanError::Compute(_)) — the engine MAY translate to PreflightCode::InvalidParameter at the boundary (Plan 04 Task 3 wires this); the scan itself just rejects via ScanError per D3-03.
    - LjungBoxScan_invalid_lags_high: With resolved_params = {"lags": 999} on a 256-close BarFrame (255 returns), Scan::run returns Err(ScanError::Compute(_)) — lags >= n is invalid.
    - LjungBoxScan_emits_one_result: On a deterministic 256-close BarFrame, Scan::run writes EXACTLY ONE Finding::Result to a VecSink — assert sink.bytes() parses into exactly one Finding::Result(_).
    - LjungBoxScan_result_envelope_shape: The emitted ResultFinding uses the PINNED field set (Warning 7 fix): scan_id_at_version == "stats.autocorr.ljung_box@1" (wire form via #[serde(rename)] — note the `@` symbol), param_hash: String matching the hex form of req.param_hash, effect.metric=="ljung_box_q", effect.n == Some(255) (NOT bare 255 — Option<u64>), effect.value == q_stats[lags-1], effect.p_value == Some(p_values[lags-1]), effect.ci95 == None (the field is named `ci95`, not `ci_95`), effect.extra contains exactly the keys {"lags", "q_stats", "p_values", "acf"}, raw == Some(Raw { series }), raw.series contains exactly the keys {"returns", "timestamps_ms"}, dsr == None, fdr_q == None.
    - LjungBoxScan_extras_have_correct_lengths: effect.extra["q_stats"].shape == vec![lags as u64]; effect.extra["p_values"].shape == vec![lags as u64]; effect.extra["acf"].shape == vec![(lags + 1) as u64] (includes lag-0).
    - LjungBoxScan_raw_series_lengths: raw.series["returns"].shape == vec![255]; raw.series["timestamps_ms"].shape == vec![255] (one per return; per-return timestamp == ts_open_utc[t]'s epoch milliseconds for t in 1..n).
    - LjungBoxScan_raw_new_enforces_timestamps_ms: building Raw via Raw::new (NOT new_unchecked) confirms the timestamps_ms invariant — if the scan forgot to include it, the Result envelope cannot be constructed.
    - LjungBoxScan_cancellation: With cancel.load() == true at entry to Scan::run, the scan returns Ok(()) early without writing any finding. Verified by setting an Arc<AtomicBool>(true) and asserting sink.bytes() is empty after the call.
    - LjungBoxScan_no_sink_flush: Scan::run NEVER calls sink.flush() (Pitfall 5). Verified by inspecting the source (`grep -c 'flush' crates/miner-core/src/scan/ljung_box/mod.rs` returns 0).
    - LjungBoxScan_cancel_aware_sleep (Blocker 3 step 3): With cfg(test) active, set `ctx.sleep_after_first_finding_ms = Some(2000)`; spawn a watcher thread that sets `ctx.cancel = true` after ~50ms; assert Scan::run returns within ~100ms (NOT 2000ms — the sleep loop polls cancel at 10ms increments). Source-level check: `grep -c 'while.*cancel\.load' crates/miner-core/src/scan/ljung_box/mod.rs` returns >= 1.
    - LjungBoxScan_sleep_hook_never_uses_blocking_sleep (Blocker 3 step 3): Source-level check: `grep -c 'std::thread::sleep([^,)]*ms[^,)]*)\|sleep(Duration::from_millis' crates/miner-core/src/scan/ljung_box/mod.rs` shows ONLY ~10ms increment sleeps within a while-let; never `sleep(Duration::from_millis(2000))` directly.
  </behavior>
  <action>
    Implement crates/miner-core/src/scan/ljung_box/mod.rs:

    1. `pub struct LjungBoxScan;` (unit struct).

    2. `impl Scan for LjungBoxScan { ... }`:
       - id() returns "stats.autocorr.ljung_box" (the &'static str per D3-17).
       - version() returns 1.
       - param_schema() returns a hand-rolled serde_json::Value per RESEARCH Pattern 3 lines 354-365: `{"$schema": "http://json-schema.org/draft-07/schema#", "type": "object", "properties": {"lags": {"type": "integer", "minimum": 1, "description": "Number of lags for the Q-statistic; defaults to min(10, n/5)"}}, "additionalProperties": false}`.
       - finding_fields() returns `ScanFindingShape { effect_extra_keys: &["lags", "q_stats", "p_values", "acf"], raw_series_keys: &["returns", "timestamps_ms"] }`.
       - run(&self, ctx, req, sink):
         a) Cancel-poll at entry per RESEARCH Pattern 4 line 397: `if ctx.cancel.load(Ordering::Relaxed) { return Ok(()); }`. Use std::sync::atomic::Ordering.
         b) Extract lags from req.resolved_params: if the Value has key "lags", parse as i64; else default to min(10, returns.len() / 5). Validate 1 <= lags < returns.len(); on invalid, return Err(ScanError::Compute(format!("invalid lags={lags} for n={n}"))).
         c) Compute returns = log_returns(&ctx.bars.close); n = returns.len(); abort if n < 2.
         d) Compute acf = biased_acf(&returns, lags); (q_stats, p_values) = ljung_box_q_and_p(n, &acf, lags).
         e) Build RawArray values for the four extra arrays and the two raw.series arrays. RawArray shape per pinned `RawArray { data: Base64Bytes, shape: Vec<u64>, dtype: Dtype }`: for a 1-D `&[f64]` slice, encode the LE-f64 bytes into Base64Bytes; shape = `vec![slice.len() as u64]`; dtype = Dtype::F64. Mirror existing helpers in `findings/mod.rs` if a `RawArray::from_f64_slice` constructor exists; if not, construct manually using the documented byte layout (D-02).
         f) For timestamps_ms: convert ctx.bars.ts_open_utc[1..] (skipping the first bar which has no return) to epoch milliseconds via DateTime::timestamp_millis. Pack into a `&[i64]`; store as RawArray with Dtype::I64 and shape `vec![n as u64]`.
         g) Construct Effect using the PINNED field set: `Effect { metric: "ljung_box_q".to_string(), value: q_stats[lags-1], p_value: Some(p_values[lags-1]), n: Some(n as u64), ci95: None, extra: BTreeMap from the four extras }`. Note: there are NO `effect_size` or `p_value_null` fields (those were placeholders in the old plan; the real Effect type does not declare them).
         h) Construct Raw via `Raw::new(series_btree).map_err(|m| ScanError::Compute(m.to_string()))?` — the `timestamps_ms` invariant is enforced by Raw::new (D-03). Result envelope wraps it as `raw: Some(raw_block)`.
         i) Construct ResultFinding using the PINNED field set: `ResultFinding { schema_version: 1, scan_id_at_version: format!("{}@{}", req.scan_id, req.version), param_hash: req.param_hash.as_str().to_string(), code_revision: ctx.code_revision.to_string(), data_slice: <DataSlice with range=req.sub_range and gap_manifest=ctx.gap_manifest.cloned() per D3-12>, dsr: None, fdr_q: None, run_id: ctx.run_id, produced_at_utc: Utc::now(), source: Source { source_id: <reader source — propagated by run_one via ctx, or via a derived constant — confirm against existing Source construction in tests>, symbol: req.instrument.clone(), side: req.side.as_str().to_string(), timeframe: req.timeframe.as_str().to_string() }, params: req.resolved_params.clone(), effect: <built above>, raw: Some(<built above>) }`.
         j) Call sink.write_envelope(&Finding::Result(result_finding))?
         k) **Cancel-aware sleep hook (Blocker 3 step 3 — Pitfall 8 mitigation)**: AFTER the first emit, IF the cfg-gated `ctx.sleep_after_first_finding_ms` is `Some(total_ms)`, enter a polling sleep loop:
            ```
            #[cfg(any(test, feature = "test-internal"))]
            if let Some(total_ms) = ctx.sleep_after_first_finding_ms {
                let step = std::time::Duration::from_millis(10);
                let mut remaining = std::time::Duration::from_millis(total_ms);
                while !ctx.cancel.load(Ordering::Relaxed) && !remaining.is_zero() {
                    let pause = remaining.min(step);
                    std::thread::sleep(pause);
                    remaining = remaining.saturating_sub(pause);
                }
            }
            ```
            This is cancel-aware: never calls `std::thread::sleep(Duration::from_millis(total_ms))` directly. The acceptance criterion `grep -c 'while.*cancel\.load' crates/miner-core/src/scan/ljung_box/mod.rs >= 1` verifies the pattern.
         l) Return Ok(()).
         m) DO NOT call sink.flush() (Pitfall 5).

    3. Add an algorithm-walk doc-comment at the top of LjungBoxScan per gap.rs:158-186 analog: numbered steps (1) log-returns, (2) biased ACF, (3) Q-stat cumulative sum, (4) chi-squared p-value, (5) envelope construction, (6) cancel-aware test-only sleep.

    4. Add a #[cfg(test)] mod tests block with the 14+ behavior tests listed above. Tests build a deterministic 256-close BarFrame in a helper fixture function `fn ar1_bar_frame_seeded(n: usize, seed: u64) -> BarFrame` (use a simple linear-congruential PRNG seeded by `seed` to produce deterministic closes; document the formula in the helper's doc-comment).

    Remove the #[allow(dead_code, unused_variables)] added in Plan 01.

    NO unsafe; NO println/eprintln/dbg; NO sink.flush; BTreeMap discipline (effect.extra and raw.series are both BTreeMap-typed per Phase 1).
  </action>
  <verify>
    <automated>cargo test -p miner-core --lib scan::ljung_box::tests</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p miner-core --lib scan::ljung_box::tests passes (>=14 tests including the cancel-aware sleep test)
    - `grep -c 'fn flush\|sink.flush' crates/miner-core/src/scan/ljung_box/mod.rs` returns 0 (Pitfall 5 gate)
    - `grep -c 'ctx.cancel.load' crates/miner-core/src/scan/ljung_box/mod.rs` returns >= 1 (entry-time cancel poll)
    - `grep -c 'while.*cancel\.load' crates/miner-core/src/scan/ljung_box/mod.rs` returns >= 1 (Blocker 3 step 3 acceptance — cancel-aware sleep loop pattern)
    - `grep -c '"ljung_box_q"' crates/miner-core/src/scan/ljung_box/mod.rs` returns >= 1
    - `grep -c 'stats.autocorr.ljung_box' crates/miner-core/src/scan/ljung_box/mod.rs` returns >= 1
    - `grep -c 'scan_id_at_version' crates/miner-core/src/scan/ljung_box/mod.rs` returns >= 1 (Warning 7 — PINNED field name used in constructor)
    - `grep -c 'ci95\b' crates/miner-core/src/scan/ljung_box/mod.rs` returns >= 1 (Warning 7 — PINNED field name `ci95`, not `ci_95`)
    - `grep -c 'raw: Some' crates/miner-core/src/scan/ljung_box/mod.rs` returns >= 1 (Warning 7 — `raw` is `Option<Raw>`)
    - `grep -c 'Raw::new' crates/miner-core/src/scan/ljung_box/mod.rs` returns >= 1 (D-03 timestamps_ms invariant enforced)
    - cargo clippy -p miner-core -- -D warnings is clean for this file
  </acceptance_criteria>
  <done>
    LjungBoxScan: Scan impl is complete using the PINNED ResultFinding field names (Warning 7); cancellation polled at entry; cancel-aware sleep loop honours Plan 02's cfg-gated `ScanCtx.sleep_after_first_finding_ms` hook (Blocker 3 step 3) without blocking on monolithic sleeps; lags default + validation per D3-03; full effect/raw envelope per D3-04; no sink.flush (Pitfall 5); 14+ unit tests passing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: engine::run_one facade + engine::cancellation_tests sub-module (SC-5b — Blocker 1) + CountingSink test helper (Warning 6) + MinerError::Scan wrapping (Warning 8)</name>
  <files>crates/miner-core/src/engine/mod.rs, crates/miner-core/tests/common/mod.rs, crates/miner-core/tests/common/counting_sink.rs</files>
  <read_first>
    - crates/miner-core/src/engine/mod.rs (Plan 01 scaffold — signature; Plan 03 added pub use gap_policy::*)
    - crates/miner-core/src/engine/{preflight,framing,param_hash,gap_policy}.rs (Plan 03 — every helper run_one calls)
    - crates/miner-core/src/scan/registry.rs (Plan 02 — Registry::get + bootstrap)
    - crates/miner-core/src/scan/ljung_box/mod.rs (Task 2 — LjungBoxScan working impl + cancel-aware sleep hook)
    - crates/miner-core/src/scan/mod.rs (Plan 02 — ScanRequest with `dry_run: bool` and cfg-gated `sleep_after_first_finding_ms`; ScanCtx with cfg-gated `sleep_after_first_finding_ms`)
    - crates/miner-core/src/cache.rs (BarCache::get_or_build signature + AggParams shape)
    - crates/miner-core/src/gap.rs (GapDetector::detect signature)
    - crates/miner-core/src/findings/mod.rs (lines 60-260 — PINNED at revision time; see Task 2 interfaces; RunSummary fields are `results_emitted` / `scan_errors` / `gap_aborted` — Warning 7)
    - crates/miner-core/src/findings/sink.rs (FindingSink + VecSink for tests)
    - crates/miner-core/src/error/mod.rs (PINNED at revision time — MinerError variants Io / Serialize / Config / Scan(String) / Internal(String); NO Reader variant — Warning 8)
    - crates/miner-core/src/config/mod.rs (MinerConfig — Plan 04 reads cache_root + bar_cache_root)
    - .planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md (D3-08, D3-10, D3-11, D3-12, D3-21, D3-22, D3-23 — every emission rule)
    - .planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md (lines 288-360 — run_one algorithm walk + cache.rs:519-573 analog)
    - .planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md (System Architecture Diagram lines 142-191 — the EXACT 7-step run_one body; Pitfall 8 — cancel + sleep test discipline)
    - .planning/phases/03-scan-engine-facade-cli/03-VALIDATION.md (SC-5b row — `cargo test -p miner-core engine::cancellation_tests::cancel_at_*`)
  </read_first>
  <behavior>
    - run_one_preflight_unknown_scan: With req.scan_id == "nope", req.version == 1, the function returns Err(MinerError::Scan(_)) (preflight failure path — wrapped via the chosen variant Warning 8 pins). Stdout (sink) stays empty: no RunStart, no Result, no RunEnd.
    - run_one_preflight_emits_no_run_start: When preflight fails, run_one does NOT emit a RunStart envelope. Stdout stays empty; the caller (Plan 05 main) maps the MinerError to a WireError on stderr.
    - run_one_strict_with_gaps: Given a GapManifest with one gap intersecting the requested range, gap_policy=Strict, req.scan_id="stats.autocorr.ljung_box@1": run_one emits exactly one RunStart, one Finding::GapAborted carrying the full manifest (its data_slice.range == the user-requested window per CONTEXT D3-11), one RunEnd. RunEnd.summary.gap_aborted == 1, results_emitted == 0. Returns Ok(RunOutcome::Ok).
    - run_one_strict_zero_gaps: Given an empty GapManifest, Strict policy: emits one RunStart, one Result (from LjungBoxScan), one RunEnd. The Result's data_slice.gap_manifest == None per CONTEXT D3-12 ("strict provides no manifest in the success path"). RunEnd.summary.results_emitted == 1.
    - run_one_continuous_only_zero_gaps: ContinuousOnly + empty manifest: emits one RunStart, one Result, one RunEnd. The Result's data_slice.gap_manifest == Some(GapManifest { gaps: vec![] }) per CONTEXT D3-12 fast path.
    - run_one_continuous_only_with_gaps: ContinuousOnly + manifest with one gap splitting the requested range into two sub-ranges: emits one RunStart, TWO Result findings (one per sub-range), one RunEnd. Each Result's data_slice.range is the sub-range; data_slice.gap_manifest is the FULL manifest cloned into each Result. RunEnd.summary.results_emitted == 2.
    - run_one_continuous_only_gap_consumes_range: ContinuousOnly + manifest with a gap covering the requested range entirely: emits one RunStart, ZERO Result findings, one RunEnd. RunEnd.summary.results_emitted == 0. RunOutcome::Ok still (no error; just no findings).
    - run_one_dry_run (Blocker 2 fix; Warning 9 pin): With req.dry_run == true (the canonical field from Plan 02 Task 2): emits one RunStart, one Finding::DryRun, one RunEnd. The DryRunFinding carries resolved_params = req.resolved_params, planned_data_slice = the requested window, estimated_findings_count = 1 (Phase 3 LjungBox is single-shot; Phase 5's sweep dry-run will return a richer estimate). RunEnd.summary.results_emitted == 0 (Pitfall 3 invariant). ADDITIONAL Warning 9 assertion: the raw JSONL output for the run does NOT contain the literal substring `"dry_run_emitted"` — there is no such field on RunSummary; the dry-run signal lives only in `Finding::DryRun` + `RunStart.request.dry_run`.
    - run_one_run_start_request_carries_dry_run (Blocker 2 echo): RunStart.request is a serde_json::Value built by build_run_start that ECHOES req.dry_run alongside scan_id, instrument, etc. Verified by parsing RunStart.request as a Value and asserting `req_value.get("dry_run") == Some(&Value::Bool(true))` when req.dry_run was true.
    - run_one_param_hash_in_result: The emitted Result.param_hash equals param_hash(&req.resolved_params).unwrap().as_str() — verified by computing the hash independently and comparing as strings.
    - run_one_run_id_consistency: RunStart.run_id == every Result's run_id == RunEnd.run_id (same RunId across all envelopes in one run).
    - run_one_clock_isolation: RunStart.started_at_utc <= every Result.produced_at_utc <= RunEnd.ended_at_utc (timestamps come from Utc::now() at framing/per-finding boundaries — never inside Scan::run kernel per CONTEXT D3-23).
    - run_one_reader_error_wraps_via_miner_error_scan (Warning 8 fix): With a FakeReader that returns Err(io::Error::other("disk gone")) from GapDetector::detect, run_one returns Err(MinerError::Scan(msg)) where msg contains "reader" or the underlying error text. Confirms the chosen wrapping variant (Scan(String)) per Warning 8 — there is no MinerError::Reader variant.
    - engine::cancellation_tests::cancel_at_entry (SC-5b — Blocker 1): With cancel = Arc::new(AtomicBool::new(true)) BEFORE run_one is invoked: the function returns immediately without emitting ANY envelope (no RunStart, no Result, no RunEnd). RunOutcome::Ok. Sink remains empty.
    - engine::cancellation_tests::cancel_before_subrange (SC-5b — Blocker 1): Under ContinuousOnly with a manifest producing TWO sub-ranges, set up a CountingSink that flips ctx.cancel = true after observing the FIRST Result emission (between the two sub-ranges). Assert: exactly one Result emitted (for the first sub-range); the second sub-range's Scan::run is not invoked; RunEnd still emitted cleanly. RunSummary.results_emitted == 1.
    - engine::cancellation_tests::cancel_inside_scan_kernel (SC-5b — Blocker 1): Set req.sleep_after_first_finding_ms = Some(2000) (uses Plan 02 cfg-gated field). Spawn a watcher thread that sets cancel = true after ~50ms. Assert: run_one returns within ~150ms (not 2000ms — the LjungBoxScan sleep loop polls cancel at 10ms intervals per Task 2 acceptance); the first Result envelope SURVIVES in the sink (already emitted before the sleep); no second envelope is emitted; RunEnd is emitted cleanly. This is the integration of Task 2's cancel-aware sleep with the engine.
  </behavior>
  <action>
    1. **`crates/miner-core/tests/common/counting_sink.rs` + `crates/miner-core/tests/common/mod.rs` (Warning 6 fix)**:
       - Create `crates/miner-core/tests/common/mod.rs` containing `pub mod counting_sink;`.
       - Create `crates/miner-core/tests/common/counting_sink.rs` defining:
         ```
         use miner_core::findings::{Finding, FindingSink};
         use std::sync::{Arc, atomic::{AtomicUsize, Ordering}};
         pub struct CountingSink<S: FindingSink> {
             pub inner: S,
             pub run_start_count: Arc<AtomicUsize>,
             pub result_count: Arc<AtomicUsize>,
             pub scan_error_count: Arc<AtomicUsize>,
             pub gap_aborted_count: Arc<AtomicUsize>,
             pub run_end_count: Arc<AtomicUsize>,
             pub dry_run_count: Arc<AtomicUsize>,
             pub on_first_result: Option<Box<dyn FnMut() + Send>>,
         }
         impl<S: FindingSink> FindingSink for CountingSink<S> { ... }
         ```
       - The `write_envelope` impl matches on the `Finding` variant, increments the appropriate counter, then forwards to `self.inner.write_envelope`. If the variant is `Result(_)` and `result_count` transitions from 0 to 1, invoke the optional `on_first_result` callback BEFORE forwarding (used by the cancel_before_subrange test to flip the cancel token between the first and second emit).
       - `write_raw_json` and `flush` simply forward to `self.inner`.

    2. **Implement `crates/miner-core/src/engine/mod.rs::run_one`** per RESEARCH §System Architecture Diagram lines 142-191:

       Signature: `pub fn run_one<R: Reader>(req: &ScanRequest, cfg: &MinerConfig, reader: &R, sink: &mut dyn FindingSink, cancel: Arc<AtomicBool>) -> Result<RunOutcome, MinerError>`.

       **Step 0 — Cancel-at-entry yield site (cancellation_tests::cancel_at_entry):**
       Before doing ANY work (not even preflight), check `if cancel.load(Ordering::Relaxed) { return Ok(RunOutcome::Ok); }`. Sink remains untouched.

       **Step 1 — Preflight (no RunStart yet)**:
       - Build the Registry via `scan::registry::bootstrap()`.
       - Call `registry.get(&req.scan_id, req.version).ok_or_else(|| MinerError::Scan(format!("unknown scan: {}@{}", req.scan_id, req.version)))?` per Warning 8 — `MinerError` has no `Reader` variant; the closest semantic match for preflight rejections at this layer is `MinerError::Scan(String)`. On Err, return WITHOUT emitting RunStart. Caller maps to exit 1.
       - Validate resolved_params against scan.param_schema() is OUT OF SCOPE for Phase 3 (jsonschema validation is heavyweight); the typed-fallback parse in parse_params_kv plus LjungBoxScan's internal lags-range check is sufficient — document this in a doc-comment.

       **Step 2 — Framing-open**:
       - run_id = RunId::new(); started = Utc::now() (the FIRST clock read; CONTEXT D3-23).
       - Build the RunStart via `framing::build_run_start(req, run_id, started, miner_core::CODE_REVISION)`. The builder echoes req.dry_run into the request Value per Blocker 2 (Plan 03 Task 1 build_run_start contract).
       - sink.write_envelope(&RunStart_finding)?.

       **Step 3 — Dry-run short-circuit (Blocker 2 fix; Warning 9 pin)**:
       - If `req.dry_run` is true: build a `Finding::DryRun(DryRunFinding { run_id, produced_at_utc: Utc::now(), request: <reuse RunStart.request value>, resolved_params: req.resolved_params.clone(), planned_data_slice: DataSlice { range: req.window.into_time_range(), gap_manifest_ref: None, gap_manifest: None }, estimated_findings_count: 1 })`. sink.write_envelope. Jump to Step 7. RunSummary is NOT extended; `results_emitted` stays at 0 (no `dry_run_emitted` field exists — Warning 9 pin).

       **Step 4 — Gap detection (Warning 8 — MinerError::Scan wrapping)**:
       - `let manifest = GapDetector::detect(reader, &req.instrument, req.side, req.window).map_err(|e| MinerError::Scan(format!("reader: {e}")))?;` — per Warning 8, MinerError has only Io/Serialize/Config/Scan/Internal variants; Scan(String) is the chosen wrapper. Document this choice in a doc-comment above the call site.
       - `let dispatch = gap_policy::dispatch(&manifest, req.window, req.gap_policy);`.

       **Step 5 — Dispatch result**:
       - `GapDispatch::Aborted(m)`: build `Finding::GapAborted(GapAbortedFinding { schema_version: 1, scan_id_at_version: format!("{}@{}", req.scan_id, req.version), param_hash: req.param_hash.as_str().to_string(), code_revision: miner_core::CODE_REVISION.to_string(), data_slice: DataSlice { range: req.window.into_time_range(), gap_manifest_ref: None, gap_manifest: Some(m.clone()) }, dsr: None, fdr_q: None, run_id, produced_at_utc: Utc::now(), source: Source { source_id: reader.source_id().to_string(), symbol: req.instrument.clone(), side: req.side.as_str().to_string(), timeframe: req.timeframe.as_str().to_string() }, gap_manifest: serde_json::to_value(&m).map_err(MinerError::from)? })`. Note: GapAbortedFinding has BOTH `data_slice.gap_manifest: Option<GapManifest>` AND a top-level `gap_manifest: serde_json::Value` (per pinned struct shape). sink.write_envelope. Increment summary.gap_aborted. Jump to Step 6.
       - `GapDispatch::SubRanges(sub_ranges)`: For each sub_range:
         a) **cancel_before_subrange yield site (cancellation_tests::cancel_before_subrange):** `if cancel.load(Ordering::Relaxed) { break; }` — checked BEFORE loading bars for this sub-range. The break-out preserves any sub-range emits that already happened; the run still emits RunEnd cleanly.
         b) Build a per-sub-range ScanRequest::new_for_sub_range(req, sub_range) per Pitfall 4 (or struct-clone with sub_range overridden).
         c) Load bars: `let bars = BarCache::new(&cfg.bar_cache_root).get_or_build(reader, AggParams { symbol: &req.instrument, side: req.side, tf: req.timeframe, range: ClosedRangeUtc { start: sub_range.start, end: sub_range.end }, calendar: <reader.trading_calendar()> }).map_err(|e| MinerError::Scan(format!("cache: {e}")))?` (Warning 8 — wrap CacheError via MinerError::Scan for symmetric handling with reader errors).
         d) **Build ScanCtx via an explicit cfg-safe pattern (Warning 1 polish — Rust does NOT allow per-field cfg gating inside a struct literal, so the previous shorthand was invalid).** Use a `ScanCtx::new(cache, gap_detector_unused_here, run_id, code_revision, cancel)` constructor that returns a `ScanCtx` whose cfg-gated `sleep_after_first_finding_ms` defaults to `None`; then under `#[cfg(any(test, feature = "test-internal"))]` assign `ctx.sleep_after_first_finding_ms = req.sleep_after_first_finding_ms;` (or call a `ScanCtx::with_sleep_after_first_finding_ms(self, Option<u64>) -> Self` chained method that is itself cfg-gated identically). Concretely:
            ```
            #[allow(unused_mut)]
            let mut ctx = ScanCtx::new(&bars, <gap_manifest_per_ContinuousOnly>, run_id, miner_core::CODE_REVISION, cancel.clone());
            #[cfg(any(test, feature = "test-internal"))]
            { ctx.sleep_after_first_finding_ms = req.sleep_after_first_finding_ms; }
            ```
            **Acceptance criterion:** `grep -c 'ScanCtx::new' crates/miner-core/src/engine/mod.rs` returns >= 1. If `ScanCtx::new(...)` is not yet defined (Plan 02 may not have added it), declare a thin constructor in this task that takes the five always-present fields and leaves the cfg-gated `sleep_after_first_finding_ms` defaulting to `None` (no additional cross-plan rework required — the constructor lives next to the type). The two-struct-literal alternative (two cfg-divergent ScanCtx struct literals) is also acceptable but produces noisier code; pick the constructor route to keep the call site clean.
         e) Call scan.run(&ctx, &per_sub_request, sink) — the third **cancel_inside_scan_kernel** yield site lives INSIDE LjungBoxScan::run's cancel-aware sleep loop from Task 2. If it returns Ok(()), increment summary.results_emitted by counting (see f). If Err(ScanError::Compute(_) | Kernel(_)), construct a Finding::ScanError envelope, emit it via the sink, increment summary.scan_errors; CONTINUE the loop (D-05). If Err(ScanError::Cancelled), break out of the loop cleanly.
         f) Counter discipline: per Warning 6, wrap the user-provided `&mut dyn FindingSink` with `CountingSink` (the test helper at `crates/miner-core/tests/common/counting_sink.rs`). For test paths, harvest counts from the CountingSink wrapper; for production paths, run_one tracks counts in its local `summary` variable and increments around each emit. Document the choice: production code maintains an explicit `summary: RunSummary` local and increments fields directly; tests can attach a CountingSink to inspect per-envelope behavior.
       - For ContinuousOnly with sub_ranges, `ctx.gap_manifest = Some(&manifest)`; for Strict success path (zero gaps), `ctx.gap_manifest = None` per CONTEXT D3-12.

       **Step 6 — Framing-close**:
       - ended = Utc::now() (the LAST clock read).
       - Build RunEnd via framing::build_run_end(run_id, started, ended, summary).
       - sink.write_envelope(&run_end_finding)?.
       - sink.flush()? — THIS is the one and only place sink.flush is called (Pitfall 5: scans never flush; facade flushes once).

       **Step 7 — Return**:
       - If summary.scan_errors > 0: Ok(RunOutcome::HadScanErrors).
       - Else: Ok(RunOutcome::Ok).

    3. **`engine::cancellation_tests` sub-module (Blocker 1 — SC-5b)**:
       Add a `#[cfg(test)] mod cancellation_tests { ... }` block at the bottom of `crates/miner-core/src/engine/mod.rs` containing exactly THREE tests with these names (matching VALIDATION.md SC-5b row):
       - `cancel_at_entry` — sets `cancel = Arc::new(AtomicBool::new(true))` BEFORE invoking `run_one`. Asserts: `run_one` returns `Ok(RunOutcome::Ok)`; `sink.bytes()` is empty (no envelope of any kind).
       - `cancel_before_subrange` — under `ContinuousOnly` with a synthetic manifest producing TWO sub-ranges, wraps the user sink in a `CountingSink` with `on_first_result` callback that calls `cancel.store(true, Ordering::SeqCst)`. Asserts: exactly ONE Result envelope emitted (for the first sub-range), the second sub-range's `Scan::run` is never invoked, RunEnd is still emitted (so the cancel break preserves clean framing), `RunSummary.results_emitted == 1`.
       - `cancel_inside_scan_kernel` — sets `req.sleep_after_first_finding_ms = Some(2000)` (uses Plan 02 cfg-gated field). Spawns a watcher thread that calls `cancel.store(true, Ordering::SeqCst)` after `~50ms`. Asserts: `run_one` returns within `~150ms` (NOT 2000ms — Task 2's cancel-aware sleep polls every ~10ms); the first Result envelope is preserved in the sink; no second envelope is emitted; RunEnd is emitted cleanly.

       Document the THREE yield sites in `run_one`'s doc-comment so the test names align 1:1:
       ```
       /// Cancellation yield sites in this function (matched by names in
       /// engine::cancellation_tests::cancel_at_*):
       /// 1. cancel_at_entry  — checked BEFORE preflight and any envelope emit.
       /// 2. cancel_before_subrange — checked at the top of each ContinuousOnly sub-range loop iteration.
       /// 3. cancel_inside_scan_kernel — checked inside LjungBoxScan::run's cancel-aware sleep loop
       ///    (controlled by the cfg-gated ScanCtx.sleep_after_first_finding_ms hook, Plan 02 Task 2).
       ```

    4. Add a separate `#[cfg(test)] mod tests` block with the run_one_* behavior tests (preflight failure, strict-with-gaps, strict-zero-gaps, continuous-only paths, dry_run, param_hash echo, run_id consistency, clock isolation, reader-error wrapping). Tests build:
       - A `FakeReader` (test-only impl of Reader returning a fixed BarFrame and a configurable GapManifest) — declare in #[cfg(test)] mod fakes.
       - The shared `CountingSink` from `tests/common/counting_sink.rs` is imported only by the `cancellation_tests` sub-module; the run_one behavior tests under `mod tests` can use a plain VecSink for simplicity.
       - Deterministic ScanRequest fixtures.

    Remove any #[allow(dead_code)] from Plan 01.

    NO unsafe; NO println/eprintln (use tracing::info/warn at state transitions per cache.rs:592-598 analog); BTreeMap-only in summary.per_scan; clock reads ONLY in run_one (build_run_start and build_run_end accept clock values as parameters per Plan 03 Task 1).
  </action>
  <verify>
    <automated>cargo test -p miner-core --lib engine::tests engine::cancellation_tests</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p miner-core --lib engine::tests passes (>=10 tests covering run_one paths)
    - cargo test -p miner-core --lib engine::cancellation_tests passes ALL THREE named tests (cancel_at_entry, cancel_before_subrange, cancel_inside_scan_kernel — Blocker 1 + SC-5b acceptance)
    - `cargo test -p miner-core engine::cancellation_tests::cancel_at_` (the VALIDATION.md test command) runs all three cancel_at_* tests and reports all-green
    - `test -f crates/miner-core/tests/common/counting_sink.rs && grep -c 'impl<S: FindingSink> FindingSink for CountingSink<S>' crates/miner-core/tests/common/counting_sink.rs` returns >= 1 (Warning 6 — CountingSink lives in Plan 04 files_modified)
    - `test -f crates/miner-core/tests/common/mod.rs && grep -c 'pub mod counting_sink' crates/miner-core/tests/common/mod.rs` returns 1
    - `grep -c 'sink.flush' crates/miner-core/src/engine/mod.rs` returns 1 (exactly one flush, after RunEnd — Pitfall 5 gate)
    - `grep -c 'Utc::now' crates/miner-core/src/engine/mod.rs` returns >= 2 (started + ended; CONTEXT D3-23: clock reads only at framing boundaries inside run_one, NOT inside Scan::run kernel)
    - `grep -c 'cancel.load' crates/miner-core/src/engine/mod.rs` returns >= 2 (Step 0 entry + Step 5 per-sub-range — the documented yield sites; the third site lives in LjungBoxScan::run, not run_one)
    - `grep -c 'preflight::resolve_scan\|registry.get' crates/miner-core/src/engine/mod.rs` returns >= 1
    - `grep -c 'framing::build_run' crates/miner-core/src/engine/mod.rs` returns >= 2 (build_run_start + build_run_end)
    - `grep -c 'gap_policy::dispatch' crates/miner-core/src/engine/mod.rs` returns >= 1
    - `grep -c 'MinerError::Scan' crates/miner-core/src/engine/mod.rs` returns >= 2 (Warning 8 — reader-error wrapping at gap detection + cache load sites)
    - `grep -c 'MinerError::Reader\|MinerError::ReaderGeneric' crates/miner-core/src/engine/mod.rs` returns 0 (Warning 8 — these variants DO NOT exist in miner-core/src/error/mod.rs; using them would be a compile error)
    - `grep -c 'req\.dry_run' crates/miner-core/src/engine/mod.rs` returns >= 1 (Blocker 2 — short-circuit reads the Plan 02 field)
    - `grep -c 'dry_run_emitted' crates/miner-core/src/engine/mod.rs crates/miner-core/src/findings/mod.rs` returns 0 (Warning 9 — RunSummary intentionally NOT extended)
    - `grep -c 'results_emitted: u64\|results_emitted ==' crates/miner-core/src/engine/mod.rs` returns >= 1 (Warning 7 — using the actual RunSummary field name)
    - cargo clippy -p miner-core -- -D warnings is clean
    - cargo build -p miner-core (full crate compiles; Plan 05 picks up from here)
  </acceptance_criteria>
  <done>
    run_one is fully wired with the three documented cancellation yield sites; engine::cancellation_tests sub-module covers SC-5b in full with the three cancel_at_* names matching VALIDATION.md (Blocker 1 fix); CountingSink test helper declared at the correct path under Plan 04's files_modified (Warning 6 fix); ResultFinding constructor uses the PINNED field names from findings/mod.rs (Warning 7 fix); reader errors wrap via MinerError::Scan(String) per the pinned MinerError variant set (Warning 8 fix); RunSummary is not extended with dry_run_emitted (Warning 9 fix); req.dry_run is the canonical dry-run signal (Blocker 2 fix); LjungBoxScan's cancel-aware sleep loop integrates with the cancel_inside_scan_kernel test (Blocker 3 step 4 verification).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scan kernel → FindingSink | Scans control what bytes hit stdout; must respect Pitfall 5 (no flush) |
| Reader → engine | reader errors must propagate via MinerError without leaking unstructured strings; wrapping variant is MinerError::Scan(String) per Warning 8 |
| Cancel token → scan body | cooperative; Scan::run must poll between findings (Phase 4 rolling scans) — Phase 3 polls at entry + during cfg-gated sleep loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | Ljung-Box kernel correctness vs statsmodels | mitigate | Kernel unit tests assert against precomputed reference values within 1e-12; Plan 06 byte-exact golden test pins the envelope-level invariant against statsmodels 0.14.6. |
| T-03-04-02 | Repudiation | run_one summary counters incorrect | mitigate | Behavior tests assert summary.results_emitted / scan_errors / gap_aborted per code path (using the actual RunSummary field names per Warning 7). Pitfall 3 (DryRun does NOT increment results_emitted) covered by run_one_dry_run test. RunSummary intentionally NOT extended with a `dry_run_emitted` field (Warning 9 pin). |
| T-03-04-03 | Denial of Service | scan kernel panics on edge inputs | mitigate | debug_asserts in ljung_box_q_and_p; LjungBoxScan validates lags range and returns ScanError::Compute on out-of-bounds; n<2 returns gracefully without emitting (or returns Err — test pins the choice). |
| T-03-04-04 | Repudiation | DataSlice.gap_manifest inconsistent across policies | mitigate | run_one_continuous_only_with_gaps + run_one_strict_zero_gaps + run_one_continuous_only_zero_gaps tests pin the D3-12 + D3-10 rules separately. |
| T-03-04-05 | Tampering | Clock reads inside scan kernel | mitigate | Test run_one_clock_isolation asserts started_at_utc <= produced_at_utc <= ended_at_utc; Plan 03 Task 1 pinned that framing builders accept clock values as inputs (no internal Utc::now). |
| T-03-04-06 | Information Disclosure | param_hash time-dependent | mitigate | run_one_param_hash_in_result test asserts param_hash matches a freshly-computed hash of req.resolved_params; Pitfall 6 separation pinned. |
| T-03-04-07 | Denial of Service | uncancellable monolithic sleep in test mode | mitigate | LjungBoxScan's cfg-gated sleep loop polls ctx.cancel in 10ms increments — never `std::thread::sleep(Duration::from_millis(total))` in one call (Blocker 3 step 3). The cancellation_tests::cancel_inside_scan_kernel test verifies a 2000ms sleep is interrupted within ~150ms when cancel flips. |
| T-03-04-08 | Repudiation | Cancellation yield sites undocumented | mitigate | run_one's doc-comment enumerates the three yield sites by name; the engine::cancellation_tests sub-module has one test per site keyed to the names in VALIDATION.md SC-5b row (Blocker 1 fix). |
| T-03-04-09 | Tampering | Reader-error wrapping uses non-existent MinerError variant | mitigate | Warning 8 pin: `crates/miner-core/src/error/mod.rs` declares MinerError with five variants only (Io / Serialize / Config / Scan / Internal). run_one uses MinerError::Scan(String) for reader+cache wrapping; `grep -c 'MinerError::Reader' crates/miner-core/src/engine/mod.rs` returns 0. Compile gate prevents drift. |
</threat_model>

<verification>
- cargo test -p miner-core --lib scan::ljung_box (kernel + scan impl tests, ~25 total including cancel-aware sleep test)
- cargo test -p miner-core --lib engine (engine + sub-modules + run_one, ~40 total including Plan 03's contributions)
- cargo test -p miner-core --lib engine::cancellation_tests (three cancel_at_* tests — SC-5b acceptance)
- cargo test -p miner-core --lib (full library test suite green)
- cargo clippy -p miner-core -- -D warnings (clean)
- cargo build -p miner-core (compiles; ready for Plan 05 CLI wiring)
- grep -c 'MinerError::Reader' crates/miner-core/src/engine/mod.rs == 0 (Warning 8 — non-existent variant never referenced)
- grep -c 'dry_run_emitted' crates/miner-core/src/engine/mod.rs crates/miner-core/src/findings/mod.rs == 0 (Warning 9)
- test -f crates/miner-core/tests/common/counting_sink.rs (Warning 6)
</verification>

<success_criteria>
1. Three Ljung-Box pure kernels match statsmodels reference within 1e-12 on hand-computed inputs
2. LjungBoxScan::run produces a fully-populated Finding::Result using the PINNED ResultFinding field names (scan_id_at_version, param_hash: String, source: Source, params: Value, effect.ci95 not ci_95, n: Option<u64>, raw: Option<Raw>) — Warning 7 fix
3. LjungBoxScan's cfg-gated sleep loop is cancel-aware (polls cancel in 10ms increments) — Blocker 3 step 3 acceptance
4. engine::run_one composes preflight + framing + bar-loading + gap-policy + scan-dispatch + cancel-polling + RunSummary build into one synchronous function
5. The 7-step algorithm walk from RESEARCH §System Architecture is implemented step-for-step with THREE documented cancellation yield sites (cancel_at_entry, cancel_before_subrange, cancel_inside_scan_kernel)
6. engine::cancellation_tests sub-module covers SC-5b with one test per yield site (Blocker 1 fix; VALIDATION.md row updated to reference these test names)
7. All five gap-policy emission rules from CONTEXT D3-08..D3-12 produce the expected envelope shapes
8. Pitfall 3 (DryRun does NOT increment results_emitted), Pitfall 4 (sub_range discipline), Pitfall 5 (only facade flushes), Pitfall 6 (param_hash excludes run metadata) all pinned with unit tests
9. Reader+cache error wrapping uses MinerError::Scan(String) — the chosen variant per Warning 8 — and never references a non-existent MinerError::Reader variant
10. RunSummary is NOT extended with a dry_run_emitted field; dry_run test asserts no such field in the JSON envelope (Warning 9 pin)
11. CountingSink test helper declared in this plan's files_modified at `crates/miner-core/tests/common/counting_sink.rs` (Warning 6 fix)
12. >=25 unit tests across the three tasks all green; cargo test -p miner-core --lib passes
</success_criteria>

<output>
Create `.planning/phases/03-scan-engine-facade-cli/03-04-SUMMARY.md` when done. Record: LOC added per file, full `cargo test -p miner-core --lib` output showing all tests green and counting (~60+ total post-Plan-04), the cargo test -p miner-core engine::cancellation_tests output showing the three named cancel_at_* tests passing, one sample emitted ResultFinding JSON (with run_id + timestamps redacted) demonstrating the D3-04 envelope shape with the PINNED field names, the run_one algorithm walk in 7 numbered bullets matching RESEARCH §System Architecture Diagram + the three documented cancellation yield sites.
</output>
