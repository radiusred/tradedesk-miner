---
phase: 03-scan-engine-facade-cli
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - crates/miner-core/Cargo.toml
  - crates/miner-core/src/findings/mod.rs
  - crates/miner-core/src/findings/sink.rs
  - crates/miner-core/src/scan/mod.rs
  - crates/miner-core/src/scan/registry.rs
  - crates/miner-core/src/scan/shape.rs
  - schemas/findings-v1.schema.json
  - schemas/scans-catalogue-v1.schema.json
  - xtask/src/main.rs
autonomous: true
requirements: [OP-07, OP-08, OUT-04]

must_haves:
  truths:
    - "A `DataSlice` value with `gap_manifest: Some(GapManifest { gaps: vec![] })` round-trips through `serde_json::to_string` + `from_str` byte-identically"
    - "A `Finding::DryRun(DryRunFinding)` value serialises with `\"kind\":\"dry_run\"` and round-trips via the existing `all_variants_round_trip` test"
    - "`cargo run -p xtask -- gen-schema` regenerates `schemas/findings-v1.schema.json` with ONLY additive diffs (no `schema_version` bump; no removed properties) AND emits a new `schemas/scans-catalogue-v1.schema.json` describing the `miner scans` line shape"
    - "A `Registry::bootstrap()` instance contains exactly one scan keyed `(\"stats.autocorr.ljung_box\", 1)` and `iter()` returns it"
    - "`FindingSink::write_raw_json(&serde_json::Value)` exists as a method on the trait with implementations on StdoutSink + FileSink + VecSink; used by `miner scans` only (per RESEARCH Pitfall 7 option 3, Open Question 8)"
    - "`ScanRequest.dry_run: bool` is the CANONICAL dry-run signal (per D3-21) — there is NO `RunOutcome::DryRun` enum variant; the dry-run signal is carried in `RunStart.request.dry_run` (echoed by Plan 04's framing) and in `Finding::DryRun`"
    - "`ScanCtx.sleep_after_first_finding_ms: Option<u64>` exists cfg-gated under `#[cfg(any(test, feature = \"test-internal\"))]` (Pitfall 8 mitigation hook — landed in the wave that owns the type, not retroactively in Plan 06)"
    - "Per-task `cargo test` invocations are independent: a failure in Task 2 must NOT roll back Task 1's findings changes (each task targets its own new module via scoped `cargo test --lib`)"
    - "`RunSummary` is NOT extended with `dry_run_emitted` — dry-run runs leave `RunSummary.results_emitted == 0`; the dry-run signal lives in `RunStart.request.dry_run` and in `Finding::DryRun`"
  artifacts:
    - path: "crates/miner-core/Cargo.toml"
      provides: "`test-internal` feature flag (no default features added) gating the Pitfall 8 sleep hook in production builds"
      contains: "test-internal"
    - path: "crates/miner-core/src/findings/mod.rs"
      provides: "DataSlice.gap_manifest: Option<GapManifest> field + Finding::DryRun(DryRunFinding) variant + DryRunFinding struct"
      contains: "pub gap_manifest: Option<GapManifest>"
    - path: "crates/miner-core/src/findings/sink.rs"
      provides: "FindingSink::write_raw_json method + StdoutSink/FileSink/VecSink impls"
      contains: "fn write_raw_json"
    - path: "crates/miner-core/src/scan/mod.rs"
      provides: "Real bodies for Scan trait support types (ScanCtx with cfg-gated sleep hook, ScanRequest with dry_run field, ScanError, ScanFindingShape)"
      contains: "pub struct ScanCtx"
    - path: "crates/miner-core/src/scan/registry.rs"
      provides: "Working Registry with BTreeMap-keyed scans + bootstrap() registering LjungBoxScan"
      contains: "r.register(Box::new"
    - path: "schemas/scans-catalogue-v1.schema.json"
      provides: "Sibling JSON Schema for miner scans catalogue lines (per Open Question 8 resolution)"
      contains: "scan_id"
  key_links:
    - from: "crates/miner-core/src/findings/mod.rs (Finding enum)"
      to: "DryRunFinding struct"
      via: "DryRun(DryRunFinding) tagged-enum variant"
      pattern: "DryRun\\(DryRunFinding\\)"
    - from: "schemas/findings-v1.schema.json"
      to: "DataSlice.gap_manifest property + Finding oneOf dry_run arm"
      via: "xtask gen-schema regen"
      pattern: 'gap_manifest|"const": "dry_run"'
    - from: "crates/miner-core/src/scan/registry.rs::bootstrap"
      to: "crates/miner-core/src/scan/ljung_box/mod.rs::LjungBoxScan"
      via: "register call inside bootstrap()"
      pattern: "Box::new\\(LjungBoxScan\\)"
    - from: "crates/miner-core/src/scan/mod.rs (ScanRequest.dry_run)"
      to: "Plan 04 engine::run_one dry-run short-circuit + Plan 05 ScanArgs.to_scan_request"
      via: "the dry_run field is the canonical signal propagated end-to-end (CLI arg → ScanRequest → run_one branch → Finding::DryRun)"
      pattern: "dry_run: bool"
---

<objective>
Lock the envelope contract Phase 3 introduces (`DataSlice.gap_manifest` additive optional field per D3-10; `Finding::DryRun` additive variant per D3-21), regenerate the JSON Schema additively, fill the `Scan` trait support-type bodies (including the `ScanRequest.dry_run` field that is the canonical dry-run signal end-to-end and the cfg-gated `ScanCtx.sleep_after_first_finding_ms` Pitfall 8 hook), deliver a working `Registry::bootstrap()`, and resolve Open Question 8 (`miner scans` catalogue framing) by adding `FindingSink::write_raw_json` per RESEARCH Pitfall 7 option 3.

Purpose: This plan establishes the WIRE CONTRACT for the rest of Phase 3 and Phase 6's MCP/HTTP wrappers. Plans 03-06 implement behavior; this plan locks the shape they fill. The schema gate (`cargo run -p xtask -- gen-schema && git diff --exit-code schemas/`) becomes the regression guard preventing accidental non-additive drift in later plans.

Two contract items that MUST land in this wave (not retroactively in Plan 06) because every downstream plan implements against them:
1. **`ScanRequest.dry_run: bool` (Blocker fix — per D3-21).** The dry-run signal travels through the same struct every other field does. No separate `RunOutcome::DryRun` variant; the signal is in `RunStart.request.dry_run` (echoed by Plan 04's framing) and in `Finding::DryRun`. Plan 04's run_one branches on `req.dry_run`; Plan 05's `ScanArgs` populates it.
2. **`ScanCtx.sleep_after_first_finding_ms: Option<u64>` (Pitfall 8 hook).** Cfg-gated under `#[cfg(any(test, feature = "test-internal"))]` so production builds pay nothing. Plan 04 reads it inside `LjungBoxScan::run`; Plan 05 surfaces the matching `--sleep-after-first-finding-ms` CLI flag; Plan 06's SIGINT integration test uses it to make the race deterministic.

Output: extended Finding envelope (DryRun variant + DataSlice.gap_manifest field), `ScanRequest.dry_run` field, cfg-gated `ScanCtx.sleep_after_first_finding_ms` field + `test-internal` Cargo feature, new `FindingSink::write_raw_json` method with three impls, complete Registry implementation, regenerated findings schema + new sibling scans-catalogue schema, xtask gen-schema extended to emit the sibling schema. `RunSummary` is NOT extended (decision pinned in Task 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md
@.planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md
@.planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md
@.planning/phases/03-scan-engine-facade-cli/03-VALIDATION.md
@.planning/phases/01-foundations-contracts/01-CONTEXT.md
@CLAUDE.md
@crates/miner-core/Cargo.toml
@crates/miner-core/src/findings/mod.rs
@crates/miner-core/src/findings/sink.rs
@crates/miner-core/src/gap.rs
@schemas/findings-v1.schema.json
@xtask/src/main.rs
@crates/miner-core/tests/schema_roundtrip.rs

<interfaces>
<!-- Existing types this plan extends. Extracted from the codebase. -->

From crates/miner-core/src/findings/mod.rs (current locked envelope shapes — pinned at revision time):
- DataSlice { range: TimeRange, gap_manifest_ref: Option<String> } — Plan 02 adds: `pub gap_manifest: Option<GapManifest>` annotated `#[serde(default)]` (NOT skip_serializing_if — null when absent per `dsr`/`fdr_q` rule)
- Finding tagged-enum with kind discriminator: RunStart | Result | ScanError | GapAborted | RunEnd — Plan 02 adds: DryRun(DryRunFinding)
- RunStart { run_id: RunId, started_at_utc: DateTime<Utc>, miner_version: String, code_revision: String, request: serde_json::Value } — DryRunFinding mirrors this framing shape
- RunSummary { results_emitted: u64, scan_errors: u64, gap_aborted: u64, per_scan: BTreeMap<String, PerScanCounts> } — NOT extended by Plan 02; dry-run leaves all three counters at their defaults (Pitfall 3 / D-09 invariant)
- ResultFinding fields (used by Plan 04 — pinned at revision time so Plan 04's constructor names match): schema_version: u32, scan_id_at_version: String (#[serde(rename = "scan_id@version")]), param_hash: String, code_revision: String, data_slice: DataSlice, dsr: Option<f64>, fdr_q: Option<f64>, run_id: RunId, produced_at_utc: DateTime<Utc>, source: Source, params: serde_json::Value, effect: Effect, raw: Option<Raw>
- Effect fields: metric: String, value: f64, p_value: Option<f64>, n: Option<u64>, ci95: Option<[f64; 2]>, extra: BTreeMap<String, RawArray>

From crates/miner-core/src/findings/sink.rs (FindingSink trait this plan extends):
- pub trait FindingSink: Send with two methods write_envelope + flush; Plan 02 adds fn write_raw_json(&mut self, v: &serde_json::Value) -> std::io::Result<()> with impls on StdoutSink, FileSink, VecSink

From crates/miner-core/src/gap.rs (GapManifest already derives JsonSchema):
- pub struct GapManifest { pub gaps: Vec<GapSpan> }

From CONTEXT D3-21 — DryRunFinding payload (framing-like, not result-like):
- fields: run_id, produced_at_utc, request, resolved_params, planned_data_slice, estimated_findings_count
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Finding envelope — DataSlice.gap_manifest + Finding::DryRun + DryRunFinding + pin RunSummary unchanged</name>
  <files>crates/miner-core/src/findings/mod.rs</files>
  <read_first>
    - crates/miner-core/src/findings/mod.rs (lines 68-72 — current DataSlice; lines 293-301 — current Finding enum; lines 161-171 — RunStart shape DryRunFinding mirrors; lines 189-195 — RunSummary shape NOT modified; lines 209-211 — `dsr`/`fdr_q` null-not-omitted rule; lines 514-548 — existing round-trip test pattern; lines 200-205 — PerScanCounts mirror)
    - .planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md (D3-10 — gap_manifest semantics; D3-21 — DryRun payload shape)
    - .planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md (lines 554-639 — field-additivity + variant-additivity pattern with exact `#[serde(default)]` rules and Pitfall 3 reaffirmation)
    - .planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md (Pattern 1 lines 252-303 — schema-additivity guarantee; Pitfall 3 lines 559-567 — DryRun must NOT increment results_emitted)
    - .planning/phases/01-foundations-contracts/01-CONTEXT.md (D-23 schema_version immutability — additive only)
  </read_first>
  <behavior>
    - Test 1: A DataSlice value with both optional fields None serialises with both fields present as JSON null (NOT omitted) per `dsr`/`fdr_q` rule (PATTERNS line 572).
    - Test 2: A DataSlice value with gap_manifest set to Some(GapManifest { gaps: vec![] }) round-trips byte-identically via serde_json::to_string then from_str.
    - Test 3: Finding::DryRun(_) serialises with the kind discriminator equal to dry_run (snake_case via existing #[serde(rename_all = "snake_case")] attribute on Finding).
    - Test 4: Extend the existing all_variants_round_trip test (line 536-548) to include Finding::DryRun(...) in the iteration list; every variant including DryRun round-trips.
    - Test 5 (Pitfall 3 pin — RunSummary unchanged): RunSummary::default() then accept a DryRun envelope MUST NOT increment results_emitted (counter stays 0). Test name: dry_run_does_not_increment_results_emitted. ALSO assert that the `RunSummary` struct definition has not gained any `dry_run_emitted`-like field — verified structurally by the test only mentioning the three existing fields (results_emitted, scan_errors, gap_aborted).
    - Test 6 (RunSummary field set unchanged — Warning 9 pin): A compile-time assertion that destructures `RunSummary { results_emitted, scan_errors, gap_aborted, per_scan }` exhaustively — adding any new field would break this destructure at compile time. Test name: run_summary_has_no_dry_run_emitted_field.
  </behavior>
  <action>
    Edit crates/miner-core/src/findings/mod.rs:

    1. Extend DataSlice (current lines 68-72). After the gap_manifest_ref field, add a new field `pub gap_manifest: Option<GapManifest>` annotated with `#[serde(default)]` (NOT skip_serializing_if). This ensures null serialises literally per PATTERNS line 572-580 — matches the dsr/fdr_q convention at lines 209-211 and 222-224.

    2. Add the DryRunFinding struct (new — place after GapAbortedFinding). Fields per CONTEXT D3-21: run_id (RunId), produced_at_utc (DateTime Utc), request (serde_json::Value), resolved_params (serde_json::Value), planned_data_slice (DataSlice), estimated_findings_count (u64). Derive Debug, Clone, PartialEq, Serialize, Deserialize, JsonSchema — same set as RunStart at lines 161-171. The struct is FRAMING-like (carries run_id + request + produced_at_utc), NOT a ResultFinding-like envelope — it does NOT carry schema_version, scan_id_at_version, param_hash, code_revision, dsr, fdr_q (those belong to the result family).

    3. Extend Finding enum (lines 293-301). Add a sixth variant DryRun(DryRunFinding) after RunEnd. The existing #[serde(tag = "kind", rename_all = "snake_case")] attribute already produces the dry_run discriminator — no extra serde attributes needed.

    4. RunSummary is NOT modified (Warning 9 pin). Do NOT add a `dry_run_emitted` field. The pin lives in the test `run_summary_has_no_dry_run_emitted_field` via an exhaustive destructure — adding any new field there would break the build, signalling the contract drift.

    5. Extend the existing all_variants_round_trip test (lines 514-548 area). Add a Finding::DryRun(...) entry to the array iterated by the test with a deterministic DryRunFinding constructor; assert round-trip via serde_json::to_string + from_str.

    6. Add a new unit test dry_run_does_not_increment_results_emitted (Pitfall 3 pin). Build a fresh RunSummary::default(); assert results_emitted == 0, scan_errors == 0, gap_aborted == 0; emit a Finding::DryRun(_) through a VecSink (existing test sink at sink.rs:188-216); confirm the sink accepts the variant without panic; assert RunSummary is still all-zero (the constructor doesn't touch counters — Plan 04 enforces the end-to-end counter discipline; Task 1 pins the type-level invariant).

    7. Add a new unit test dataslice_gap_manifest_serialises_as_null_when_absent: construct DataSlice with both gap_manifest fields None; serialise to a String; assert the string contains the literal substring `"gap_manifest":null` (NOT absent — PATTERNS line 572 invariant).

    8. Add a new compile-test `run_summary_has_no_dry_run_emitted_field` that exhaustively destructures `RunSummary { results_emitted: _, scan_errors: _, gap_aborted: _, per_scan: _ }` — if a future plan adds a field, this test fails at compile-time signalling the Warning 9 contract drift.

    NO unsafe code; NO println/eprintln/dbg (clippy gate). The BTreeMap discipline is inherited from serde_json::Value (no preserve_order feature anywhere — Pitfall 1).

    Remove any #[allow(dead_code, unused_variables)] this file received from Plan 01 — bodies are now real.
  </action>
  <verify>
    <automated>cargo test -p miner-core --lib findings -- all_variants_round_trip dry_run_does_not_increment_results_emitted dataslice_gap_manifest_serialises_as_null_when_absent run_summary_has_no_dry_run_emitted_field</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p miner-core --lib findings::tests::all_variants_round_trip passes AND iterates Finding::DryRun (verify by grep: `grep -c 'Finding::DryRun' crates/miner-core/src/findings/mod.rs` returns >= 3 — once in enum, once in test array, once in dry_run_does_not_increment test)
    - cargo test -p miner-core --lib findings::tests::dataslice_gap_manifest_serialises_as_null_when_absent passes
    - cargo test -p miner-core --lib findings::tests::dry_run_does_not_increment_results_emitted passes
    - cargo test -p miner-core --lib findings::tests::run_summary_has_no_dry_run_emitted_field passes (compile-time exhaustive destructure asserts RunSummary unchanged)
    - `grep -c 'pub gap_manifest: Option<GapManifest>' crates/miner-core/src/findings/mod.rs` returns 1
    - `grep -c 'DryRun(DryRunFinding)' crates/miner-core/src/findings/mod.rs` returns >= 1
    - `grep -c 'dry_run_emitted' crates/miner-core/src/findings/mod.rs` returns 0 (Warning 9 — field intentionally NOT added)
    - `grep -F skip_serializing_if crates/miner-core/src/findings/mod.rs | grep -cE 'gap_manifest|dry_run'` returns 0 (per PATTERNS line 572 — NO skip_serializing_if on the new field)
    - Per-task isolation (Warning 5): this task's `cargo test -p miner-core --lib findings -- ...` invocation runs ONLY findings unit tests; a failure in Task 2 (scan / registry) does not affect this task's verification result.
  </acceptance_criteria>
  <done>
    DataSlice carries the new optional gap_manifest field that serialises as null when absent; Finding has a sixth DryRun variant; DryRunFinding struct exists with the CONTEXT D3-21 fields; RunSummary unchanged (Warning 9 pinned via compile-time destructure); the existing round-trip test now covers DryRun; Pitfall 3 unit test pins the counter rule.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill Scan trait support-type bodies + working Registry::bootstrap() + FindingSink::write_raw_json + ScanRequest.dry_run + cfg-gated ScanCtx sleep hook + test-internal feature</name>
  <files>
    crates/miner-core/Cargo.toml,
    crates/miner-core/src/scan/mod.rs, crates/miner-core/src/scan/registry.rs,
    crates/miner-core/src/scan/shape.rs, crates/miner-core/src/findings/sink.rs
  </files>
  <read_first>
    - crates/miner-core/Cargo.toml (current [features] block — Plan 02 adds `test-internal` feature)
    - crates/miner-core/src/scan/mod.rs (Plan 01 scaffold — signatures present, bodies unimplemented)
    - crates/miner-core/src/scan/registry.rs (Plan 01 scaffold)
    - crates/miner-core/src/scan/ljung_box/mod.rs (Plan 01 scaffold — used only as `Box::new(LjungBoxScan)` import in bootstrap; LjungBoxScan::run stays unimplemented until Plan 04)
    - crates/miner-core/src/findings/sink.rs (current trait + StdoutSink/FileSink/VecSink impls — extend trait with new method + add three impl bodies)
    - crates/miner-core/src/reader.rs (lines 198-274 — Send + Sync trait template; object-safety test pattern)
    - crates/miner-core/src/aggregator.rs (lines 201-219 — AggregateError thiserror enum template for ScanError)
    - crates/miner-core/src/findings/mod.rs (lines 101-103 — BTreeMap doc-line discipline)
    - .planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md (D3-14, D3-15, D3-16, D3-17, D3-21 — trait + ctx + registry + naming + dry-run signal)
    - .planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md (lines 51-189 — scan/mod.rs, registry.rs, shape.rs analog walks)
    - .planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md (Pattern 2 lines 305-350 — Scan trait shape verified against object-safety rules; Pitfall 7 lines 599-611 — write_raw_json rationale; Pitfall 8 lines 612-622 — sleep hook rationale; Assumption A5 line 767 — sleep flag is acceptable)
  </read_first>
  <behavior>
    - Test 1: Registry::new() creates an empty registry; registry.scans.is_empty() is true.
    - Test 2: registry.register(Box::new(LjungBoxScan)) then registry.get("stats.autocorr.ljung_box", 1) returns Some(&dyn Scan); registry.get("nonexistent", 1) returns None; registry.get("stats.autocorr.ljung_box", 99) returns None.
    - Test 3: registry_uses_btreemap — compile-time assertion `let _: &BTreeMap<(String, u32), Box<dyn Scan>> = &registry.scans;` per PATTERNS line 178-180.
    - Test 4: Registry::bootstrap() returns a registry with exactly ONE entry: key ("stats.autocorr.ljung_box", 1) containing a LjungBoxScan.
    - Test 5: Registry::iter() over bootstrap() yields scans in lexicographic-key order (BTreeMap natural order). For Phase 3 with one scan this is trivially true; the test pins the contract for Phase 4 plans.
    - Test 6 (Scan trait): scan_trait_object_safe from Plan 01 still compiles (real method bodies don't break dyn-compat).
    - Test 7 (write_raw_json on VecSink): `let mut s = VecSink::default(); s.write_raw_json(&serde_json::json!({"a":1})).unwrap();` produces a single JSONL line in s.bytes() ending with newline.
    - Test 8 (write_raw_json on StdoutSink): the StdoutSink impl mirrors its existing write_envelope shape but uses the raw Value path (no Finding type-check).
    - Test 9 (ScanRequest.dry_run defaults to false on deserialise): `serde_json::from_str::<ScanRequest>(json_without_dry_run_field)` returns Ok with dry_run == false (per `#[serde(default)]`). Test name: scan_request_dry_run_defaults_false_when_absent.
    - Test 10 (ScanRequest.dry_run round-trips): a ScanRequest with dry_run=true serialises and deserialises back to dry_run=true. Test name: scan_request_dry_run_round_trips.
    - Test 11 (ScanCtx sleep field is cfg-gated): under `#[cfg(any(test, feature = "test-internal"))]` build (which `cargo test` activates), the field `sleep_after_first_finding_ms: Option<u64>` exists on ScanCtx. Verified structurally — the field is referenced by a `#[cfg(any(test, feature = "test-internal"))] #[test] fn scan_ctx_has_sleep_after_first_finding_ms_field()` that constructs a ScanCtx mentioning the field and asserts default None — compile-time evidence the field is available in test builds.
  </behavior>
  <action>
    1. **`crates/miner-core/Cargo.toml` — add `test-internal` feature** (Blocker 3 step 1):
       - In the `[features]` section (create the section if it doesn't exist), add: `test-internal = []` (empty feature list — the feature is purely a cfg-flag toggle, no extra deps).
       - Document via a leading comment: `# `test-internal` exposes test-only hooks (e.g., `ScanCtx.sleep_after_first_finding_ms`, `--sleep-after-first-finding-ms` CLI flag) without polluting production builds. NEVER enable in release profiles.`
       - Verify post-edit: `grep -c '^test-internal' crates/miner-core/Cargo.toml` returns 1.

    2. **`crates/miner-core/src/findings/sink.rs` — extend the FindingSink trait** with a new required method per RESEARCH Pitfall 7 option 3 + Open Question 8 resolution:
       - Add to the FindingSink trait declaration: `fn write_raw_json(&mut self, v: &serde_json::Value) -> std::io::Result<()>;` (NO default body — every impl provides its own writer reference). Document with the required doc-comment per Pitfall 7 line 608: `ONLY for non-Finding introspection lines (e.g. miner scans catalogue). Validate against a sibling schema such as schemas/scans-catalogue-v1.schema.json. Production scan output MUST use Self::write_envelope.`
       - Add three impls — one for StdoutSink, FileSink, VecSink — each mirroring its existing write_envelope shape but with serde_json::to_writer(writer, v) instead of serde_json::to_writer(writer, finding). After the write, append a newline byte and flush per the existing per-envelope flush discipline.
       - Add a unit test write_raw_json_to_vec_sink_emits_jsonl_line: build a VecSink, call write_raw_json with a simple json value, assert s.bytes() ends with the newline byte and parses back via serde_json::from_slice to the same value.

    3. **`crates/miner-core/src/scan/mod.rs` — fill the real bodies for the support types Plan 01 scaffolded**:
       - `pub trait Scan: Send + Sync { ... }` — keep the signatures from Plan 01 (Scan impls provide bodies; Plan 04 fills LjungBoxScan).
       - `pub struct ScanCtx<'a>` with these fields:
         - `pub bars: &'a BarFrame`
         - `pub gap_manifest: Option<&'a GapManifest>`
         - `pub run_id: RunId`
         - `pub code_revision: &'a str`
         - `pub cancel: std::sync::Arc<std::sync::atomic::AtomicBool>`
         - **Cfg-gated (Blocker 3 step 2):** `#[cfg(any(test, feature = "test-internal"))] pub sleep_after_first_finding_ms: Option<u64>` — placed after the cancel field with a doc-comment: `Test-only hook (Pitfall 8): when Some(ms), `LjungBoxScan::run` pauses after the first Result emit for the requested duration (cancel-aware sleep loop). NEVER reachable in release production builds.`
         - Per CONTEXT D3-15 + RESEARCH Open Question 4 resolution: by-value BarFrame in facade, borrowed in ctx.
       - `pub struct ScanRequest` with fields:
         - `pub scan_id: String`
         - `pub version: u32`
         - `pub instrument: String`
         - `pub side: Side`
         - `pub timeframe: Timeframe`
         - `pub window: ClosedRangeUtc`  (request-level)
         - `pub sub_range: TimeRange`    (per-finding — Pitfall 4 pin)
         - `pub gap_policy: GapPolicyKind`
         - `pub resolved_params: serde_json::Value`
         - `pub param_hash: Blake3Hex`
         - **Blocker 2 fix:** `#[serde(default)] pub dry_run: bool` — the CANONICAL dry-run signal per D3-21. Documented as: `When true, Plan 04's engine::run_one emits one Finding::DryRun envelope (between RunStart and RunEnd) and skips the scan body. Echoed into RunStart.request so param_hash + data_slice.range provenance both encode whether this was a dry-run. There is NO RunOutcome::DryRun enum variant.`
         - **Cfg-gated (Blocker 3 step 2 continued):** `#[cfg(any(test, feature = "test-internal"))] #[serde(default)] pub sleep_after_first_finding_ms: Option<u64>` — forwarded into ScanCtx by Plan 04's run_one. Doc-comment: `Test-only hook for Pitfall 8 SIGINT race mitigation. CLI surfaces this via the cfg-gated --sleep-after-first-finding-ms flag in Plan 05.`
         - Derive Debug, Clone, Serialize, Deserialize as appropriate.
       - `pub enum ScanError` with variants Compute(String), Kernel(String), Cancelled — thiserror::Error derive per aggregator.rs:201-219 template. No Serialize derive (kernel errors are translated to Finding::ScanError via ScanErrorCode mapping at the engine boundary).
       - Re-confirm `pub mod registry; pub mod shape; pub mod ljung_box;` declarations.
       - Remove the `#[allow(dead_code, unused_variables)]` added in Plan 01.

    4. **`crates/miner-core/src/scan/shape.rs`** — replace the Plan 01 scaffold with the working type ScanFindingShape { effect_extra_keys: &'static [&'static str], raw_series_keys: &'static [&'static str] }. Derives: Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, JsonSchema per PATTERNS line 97-103. Shared between Scan::finding_fields and the miner scans JSON output line.

    5. **`crates/miner-core/src/scan/registry.rs` — fill real bodies**:
       - `pub struct Registry { pub scans: BTreeMap<(String, u32), Box<dyn Scan>> }` with doc-line `BTreeMap (NEVER HashMap) — OUT-03 determinism.`
       - impl Registry with `pub fn new()` returning Self with an empty BTreeMap; `pub fn register(&mut self, scan: Box<dyn Scan>)` inserting at key `(scan.id().to_string(), scan.version())`; `pub fn get(&self, id: &str, version: u32) -> Option<&dyn Scan>`; `pub fn iter(&self) -> impl Iterator<Item = &dyn Scan> + '_` via `self.scans.values().map(|b| b.as_ref())`.
       - `pub fn bootstrap() -> Registry` constructing a fresh `Registry::new()` and calling `r.register(Box::new(crate::scan::ljung_box::LjungBoxScan))` before returning r.
       - Add tests 1-5 from `<behavior>` plus the compile-time BTreeMap type assertion per PATTERNS line 178-180.

    6. **Tests for the new ScanRequest.dry_run field + ScanCtx sleep field** live in `crates/miner-core/src/scan/mod.rs` under `#[cfg(test)] mod tests`:
       - `scan_request_dry_run_defaults_false_when_absent` — deserialise a JSON object that omits `dry_run` and assert `req.dry_run == false`.
       - `scan_request_dry_run_round_trips` — serialise then deserialise; field round-trips.
       - `#[cfg(any(test, feature = "test-internal"))] #[test] fn scan_ctx_has_sleep_after_first_finding_ms_field()` — constructs a ScanCtx using struct-literal syntax explicitly mentioning the cfg-gated field; asserts the default constructor leaves it None. Compile-time evidence the field is available under test builds.

    The LjungBoxScan referenced by bootstrap() is still the Plan 01 scaffold (id "stats.autocorr.ljung_box", version 1, run() body unimplemented). bootstrap() compiles because it only needs the type to exist with id() and version() returning the right values Plan 01 stubbed. Plan 04 fills the rest of LjungBoxScan, including the cfg-gated sleep loop that reads `ctx.sleep_after_first_finding_ms`.
  </action>
  <verify>
    <automated>cargo test -p miner-core --lib scan -- registry_starts_empty registry_register_and_get bootstrap_registers_ljung_box_scan registry_iter_lex_order registry_uses_btreemap scan_trait_object_safe scan_request_dry_run_defaults_false_when_absent scan_request_dry_run_round_trips scan_ctx_has_sleep_after_first_finding_ms_field && cargo test -p miner-core --lib findings::sink::tests::write_raw_json_to_vec_sink_emits_jsonl_line</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p miner-core --lib scan::registry::tests passes all 5 registry tests
    - cargo test -p miner-core --lib scan::tests::scan_trait_object_safe passes (Plan 01 invariant held)
    - cargo test -p miner-core --lib findings::sink::tests::write_raw_json_to_vec_sink_emits_jsonl_line passes
    - cargo test -p miner-core --lib scan::tests::scan_request_dry_run_defaults_false_when_absent passes (Blocker 2 acceptance)
    - cargo test -p miner-core --lib scan::tests::scan_request_dry_run_round_trips passes (Blocker 2 acceptance)
    - cargo test -p miner-core --lib scan::tests::scan_ctx_has_sleep_after_first_finding_ms_field passes under default `cargo test` (Blocker 3 acceptance — test cfg automatically activates the cfg-gated field)
    - `grep -c 'BTreeMap<(String, u32), Box<dyn Scan>>' crates/miner-core/src/scan/registry.rs` returns >= 2 (struct field + compile-time test)
    - `grep -c 'fn write_raw_json' crates/miner-core/src/findings/sink.rs` returns >= 4 (trait method + 3 impls)
    - `grep -c 'dry_run: bool' crates/miner-core/src/scan/mod.rs` returns >= 1 (Blocker 2 acceptance — `ScanRequest.dry_run` field present)
    - `grep -F 'sleep_after_first_finding_ms' crates/miner-core/src/scan/mod.rs | head -1` matches the cfg-gated declaration on ScanCtx AND on ScanRequest (Blocker 3 acceptance)
    - `grep -c 'feature = "test-internal"' crates/miner-core/src/scan/mod.rs` returns >= 2 (cfg-gates on ScanCtx and ScanRequest sleep fields)
    - `grep -c '^test-internal' crates/miner-core/Cargo.toml` returns 1 (Cargo feature declared)
    - cargo clippy -p miner-core -- -D warnings is clean (no lingering #[allow(dead_code)] from Plan 01)
    - cargo build -p miner-core (default features — no test-internal) compiles cleanly; the cfg-gated field is absent from the release surface but `cargo test` (which activates `cfg(test)`) makes it available — confirms Blocker 3 doesn't leak into release.
    - Per-task isolation (Warning 5): this task's `cargo test -p miner-core --lib scan -- ...` invocation runs only the scan + sink tests it owns; a failure in Task 1 (findings) does not affect this task's verification result (and vice-versa).
  </acceptance_criteria>
  <done>
    `test-internal` feature declared in miner-core Cargo.toml; FindingSink trait has write_raw_json with 3 working impls; Scan trait support types are fully typed; ScanRequest carries the canonical `dry_run: bool` field; ScanCtx + ScanRequest both expose the cfg-gated `sleep_after_first_finding_ms: Option<u64>` Pitfall 8 hook (test/feature-internal only); Registry has BTreeMap-keyed storage with register/get/iter/bootstrap; bootstrap() returns a registry containing LjungBoxScan; every behavior test passes; release build does NOT expose the sleep hook.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regenerate findings schema additively + create sibling scans-catalogue schema via xtask</name>
  <files>schemas/findings-v1.schema.json, schemas/scans-catalogue-v1.schema.json, xtask/src/main.rs</files>
  <read_first>
    - xtask/src/main.rs (current gen-schema subcommand — extend to emit two schemas)
    - schemas/findings-v1.schema.json (current locked schema — Plan 02 regenerates additively)
    - crates/miner-core/src/findings/mod.rs (post-Task-1 state — DryRun + gap_manifest present)
    - crates/miner-core/src/scan/shape.rs (post-Task-2 state — ScanFindingShape derives JsonSchema; the sibling-schema root type)
    - crates/miner-core/tests/schema_roundtrip.rs (existing round-trip discipline; Phase 3 must not break this)
    - .planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md (line 168-176 — two additive changes; both schema-additive without schema_version bump)
    - .planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md (Pattern 1 lines 252-303 — schemars 1.x tagged-enum additivity verified; Open Question 8 — sibling schema for catalogue lines)
    - .planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md (lines 1178-1184 — Schema-additivity gate)
  </read_first>
  <action>
    1. Extend xtask/src/main.rs to emit two schemas in the gen-schema subcommand:
       - Existing emit of schemas/findings-v1.schema.json from `schemars::schema_for!(miner_core::findings::Finding)` (or whichever root type the current xtask uses — read xtask/src/main.rs to confirm). DO NOT change the root type; the schema auto-extends from Task 1's type changes.
       - NEW emit: define a small shim type ScansCatalogueEntry with fields scan_id (String), version (u32), params (serde_json::Value), finding_fields (ScanFindingShape) — either inside xtask or as a `pub use` from miner_core::scan. Per CONTEXT D3-20 + RESEARCH Open Question 8, this is the line shape miner scans emits via FindingSink::write_raw_json — the sibling schema documents it. Emit schemas/scans-catalogue-v1.schema.json from `schemars::schema_for!(ScansCatalogueEntry)`.
       - The xtask emit function should pretty-print with stable key ordering (existing pattern — confirm via the current gen-schema body).

    2. Run `cargo run -p xtask -- gen-schema` to regenerate both files.

    3. Inspect the diff on schemas/findings-v1.schema.json. The diff MUST be additive only:
       - DataSlice.properties gains a gap_manifest entry typed via schemars 1.x as anyOf/null or oneOf with the GapManifest $ref.
       - Finding oneOf array gains an extra arm with properties.kind.const = "dry_run".
       - NO existing property removed, renamed, or retyped.
       - NO schema_version field bumped.
       If the diff includes a removal or rename, STOP and revert — that's a non-additive change. Plan-checker enforces this.

    4. Verify schemas/scans-catalogue-v1.schema.json exists with the four-property shape (scan_id, version, params, finding_fields).

    5. Confirm crates/miner-core/tests/schema_roundtrip.rs still passes — the existing round-trip test loads the schema + validates a canonical fixture; the new optional field + new variant must not break this.

    The CI gate `cargo run -p xtask -- gen-schema && git diff --exit-code schemas/findings-v1.schema.json` must succeed AFTER both schemas are committed; if it does not, the type changes drifted between writes. Commit BOTH schemas in the same PR as the Task 1 type changes.

    NO manual hand-edit of schemas/findings-v1.schema.json — xtask gen-schema is the only source of truth (RESEARCH §Don't Hand-Roll line 514).
  </action>
  <verify>
    <automated>cargo run -p xtask -- gen-schema && git diff --exit-code schemas/findings-v1.schema.json schemas/scans-catalogue-v1.schema.json && grep -c '"dry_run"' schemas/findings-v1.schema.json && grep -c 'gap_manifest' schemas/findings-v1.schema.json && cargo test -p miner-core --test schema_roundtrip</automated>
  </verify>
  <acceptance_criteria>
    - cargo run -p xtask -- gen-schema exits 0
    - git diff --exit-code schemas/findings-v1.schema.json schemas/scans-catalogue-v1.schema.json exits 0 (both schemas regenerated then committed; further regen is idempotent)
    - `grep -c '"const": "dry_run"' schemas/findings-v1.schema.json` returns >= 1 (the new Finding oneOf arm)
    - `grep -c 'gap_manifest' schemas/findings-v1.schema.json` returns >= 2 (DataSlice.properties.gap_manifest + a $ref in the gap_manifest property entry)
    - schemas/scans-catalogue-v1.schema.json exists and contains the literal strings "scan_id", "version", "params", "finding_fields"
    - cargo test -p miner-core --test schema_roundtrip exits 0 (existing round-trip discipline preserved)
    - git diff --stat schemas/findings-v1.schema.json shows additions only — no deletions (verify with `git diff --numstat schemas/findings-v1.schema.json` showing the deletions column as 0 or near-zero — minor whitespace shuffle is acceptable; structural removals are NOT)
    - Per-task isolation (Warning 5): this task's `cargo test -p miner-core --test schema_roundtrip` invocation runs only the schema round-trip integration test; a failure in Task 1 or Task 2 (lib tests) does not affect this task's verification result.
  </acceptance_criteria>
  <done>
    xtask gen-schema emits two schemas; findings-v1 schema gained an additive dry_run variant arm and a gap_manifest property; sibling scans-catalogue schema describes the miner scans line shape; existing schema_roundtrip test passes; the CI gate (gen-schema then diff exit 0) is idempotent.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust types → JSON Schema artifact | xtask gen-schema is the only sanctioned writer; manual edits forbidden |
| FindingSink writers → stdout bytes | New write_raw_json bypasses the typed Finding path — only safe behind doc-comment discipline |
| Test-only API surface → release builds | `test-internal` feature + `cfg(test)` gates ensure the sleep hook + sleep CLI flag never ship in release |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | schemas/findings-v1.schema.json | mitigate | xtask gen-schema is the only writer; `cargo run -p xtask -- gen-schema && git diff --exit-code schemas/` CI gate fails the build if hand-edits sneak in. Task 3 commits the regen alongside the type changes. |
| T-03-02-02 | Information Disclosure | FindingSink::write_raw_json | mitigate | Trait method has a mandatory doc-comment saying "ONLY for non-Finding introspection lines. Validate against a sibling schema." Plan 05's miner scans subcommand is the only production call site; Plan 06 wires the integration test to validate every catalogue line against schemas/scans-catalogue-v1.schema.json (NOT findings-v1). |
| T-03-02-03 | Tampering | Cargo.lock / serde_json features | mitigate | Pitfall 1 gate held: `grep preserve_order Cargo.lock` returns 0; the BTreeMap discipline depends on this. Tests in Task 1 indirectly exercise the gate (byte-equal round-trip impossible if preserve_order is enabled). |
| T-03-02-04 | Repudiation | DryRun envelope counted as a result | mitigate | dry_run_does_not_increment_results_emitted unit test pins the constructor invariant; Plan 04's engine test pins the end-to-end invariant. Pitfall 3 reference in test name lets the auditor trace the rule. |
| T-03-02-05 | Information Disclosure | test-only sleep hook leaking into release | mitigate | The hook field on `ScanCtx` + `ScanRequest` is `#[cfg(any(test, feature = "test-internal"))]`. Default `cargo build --release` does NOT activate `cfg(test)` and the `test-internal` feature is not enabled by any release profile. Plan 06's final gate inspects `miner --help` of the release binary and confirms the `--sleep-after-first-finding-ms` flag is absent. |
| T-03-02-06 | Repudiation | RunSummary silently extended with dry_run_emitted | mitigate | Task 1 adds a compile-time exhaustive destructure test `run_summary_has_no_dry_run_emitted_field` that fails to compile if any new field is added to RunSummary. The Warning 9 invariant is pinned at the type level. |
</threat_model>

<verification>
- cargo test -p miner-core --lib (all unit tests pass including the 8+ new ones added across the three tasks)
- cargo test -p miner-core --test schema_roundtrip (existing round-trip discipline preserved)
- cargo run -p xtask -- gen-schema then git diff --exit-code schemas/ (idempotent regen)
- cargo clippy -p miner-core -- -D warnings (no lingering #[allow] from Plan 01; new code clean)
- grep -c '"const": "dry_run"' schemas/findings-v1.schema.json >= 1
- grep -c 'gap_manifest' schemas/findings-v1.schema.json >= 2
- grep -c '^test-internal' crates/miner-core/Cargo.toml == 1
- grep -c 'dry_run: bool' crates/miner-core/src/scan/mod.rs >= 1
- grep -c 'sleep_after_first_finding_ms' crates/miner-core/src/scan/mod.rs >= 2
- cargo build --workspace (everything still compiles; Plan 03 picks up from here)
- cargo build --workspace --release (release build does NOT activate the cfg-gated sleep hook; sanity check the field is genuinely absent in release artifacts)
</verification>

<success_criteria>
1. DataSlice gained an optional gap_manifest field that serialises as JSON null when absent (Pitfall 3-class rule preserved)
2. Finding enum gained a sixth DryRun variant carrying DryRunFinding; the existing all_variants_round_trip test now covers it
3. dry_run_does_not_increment_results_emitted unit test passes (Pitfall 3 pinned at the type layer)
4. ScanRequest has a `dry_run: bool` field with `#[serde(default)]`; round-trips correctly (Blocker 2 fix)
5. ScanCtx + ScanRequest both expose `sleep_after_first_finding_ms: Option<u64>` cfg-gated under `#[cfg(any(test, feature = "test-internal"))]` (Blocker 3 fix); `test-internal` feature declared in `crates/miner-core/Cargo.toml`
6. RunSummary is NOT extended with `dry_run_emitted`; compile-time exhaustive destructure test pins the invariant (Warning 9 fix)
7. Registry has working BTreeMap-keyed storage; bootstrap() returns a one-scan registry; iter() / get() / register() all behave per behavior tests
8. FindingSink::write_raw_json exists as a trait method with three working impls (StdoutSink, FileSink, VecSink)
9. schemas/findings-v1.schema.json regenerated additively (no removed properties; no schema_version bump) AND schemas/scans-catalogue-v1.schema.json created
10. cargo run -p xtask -- gen-schema is idempotent (run twice = no diff)
11. Per-task verification independence: each task's `cargo test` invocation targets only its own new artifacts (Warning 5 fix)
</success_criteria>

<output>
Create `.planning/phases/03-scan-engine-facade-cli/03-02-SUMMARY.md` when done. Record: number of LOC added per file, the test names that passed (with `cargo test -p miner-core --lib -- --include-ignored 2>&1 | grep PASS` style evidence), the full output of `git diff --numstat schemas/` proving the additive nature, the new schema file contents (head -40 of schemas/scans-catalogue-v1.schema.json), the `cargo build --release` output proving the cfg-gated sleep hook is genuinely absent from release artifacts.
</output>
