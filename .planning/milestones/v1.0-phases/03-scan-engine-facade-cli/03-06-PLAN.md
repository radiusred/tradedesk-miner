---
phase: 03-scan-engine-facade-cli
plan: 06
type: execute
wave: 6
depends_on:
  - 03-05
files_modified:
  - crates/miner-cli/tests/fixtures/mod.rs
  - crates/miner-cli/tests/fixtures/ar1_seed.rs
  - crates/miner-cli/tests/fixtures/statsmodels_golden.rs
  - crates/miner-core/tests/fixtures/generate_golden.py
  - crates/miner-core/tests/fixtures/ljung_box_golden.json
  - crates/miner-core/tests/scan_ljung_box.rs
  - crates/miner-core/tests/scan_facade_determinism.rs
  - crates/miner-core/tests/shuffled_future_regression.rs
  - crates/miner-core/tests/gap_policy.rs
  - crates/miner-core/tests/dry_run.rs
  - crates/miner-cli/tests/scan_subcommand_smoke.rs
  - crates/miner-cli/tests/scans_catalogue.rs
  - crates/miner-cli/tests/sigint_preserves_stream.rs
  - crates/miner-core/tests/snapshots/scan_ljung_box__ljung_box_matches_statsmodels_golden.snap
  - README.md
autonomous: true
requirements: [OP-01, OP-05, OP-06, OP-07, OP-08, OUT-04]

must_haves:
  truths:
    - "`cargo test -p miner-core --test scan_ljung_box -- ljung_box_matches_statsmodels_golden` passes with byte-identical JSONL (volatile fields masked) matching the statsmodels 0.14.6 reference"
    - "`crates/miner-core/tests/fixtures/ljung_box_golden.json` is committed with a `provenance` block citing `statsmodels==0.14.6`, the Python script path `tests/fixtures/generate_golden.py`, the date generated, and the SHA of the input AR(1) seed-0 256-bar series (per D3-05 — Blocker 4 fix)"
    - "`crates/miner-core/tests/fixtures/generate_golden.py` exists as a checked-in ~30-LOC Python script that calls `statsmodels.stats.diagnostic.acorr_ljungbox` on the seed-0 256-bar AR(1) series and emits the expected JSON (q_stats, p_values, acf + provenance block)"
    - "The Rust kernel test in `scan_ljung_box.rs` compares against `ljung_box_golden.json` — the golden direction is statsmodels-to-Rust, NEVER Rust-to-itself (Blocker 4 — D3-05 invariant)"
    - "`cargo test -p miner-core --test scan_facade_determinism -- twice_run_byte_identical_when_volatile_fields_masked` passes — running the same scan twice in-process produces byte-equal JSONL"
    - "`cargo test -p miner-core --test shuffled_future_regression -- look_ahead_safe_under_post_t_shuffle_proptest` passes — stats up to T unchanged when bars > T shuffled; the proptest doc-comment phrasing is the Warning 10 wording (full D3-09 enforcement for Ljung-Box; Phase 4 ADDS cancellation_tests-style proptests per new rolling/causal scan — does NOT extend this proptest)"
    - "`cargo test -p miner-core --test gap_policy` passes all 5 named tests covering OUT-04 SC-3a..SC-3e"
    - "`cargo test -p miner-core --test dry_run -- dry_run_emits_dry_run_finding_only` passes with RunSummary.results_emitted == 0"
    - "`cargo test -p miner-cli --test scan_subcommand_smoke` passes all 5 tests covering OP-01 / OP-08 / OP-05 / D3-24 exit-code routing"
    - "`cargo test -p miner-cli --test scans_catalogue -- scans_emits_one_line_per_registered_scan` passes against schemas/scans-catalogue-v1.schema.json"
    - "`#[cfg(unix)] cargo test -p miner-cli --test sigint_preserves_stream` passes — SIGINT preserves already-streamed findings, exit 130 — using the Plan 02 cfg-gated ScanCtx/ScanRequest sleep_after_first_finding_ms field + Plan 05 CLI flag + Plan 04 LjungBoxScan cancel-aware sleep loop (NO Plan 02/04 retroactive edits in Plan 06 — Blocker 3 step 4 fix)"
    - "README.md updated with `miner scan` + `miner scans` Quickstart commands"
  artifacts:
    - path: "crates/miner-core/tests/fixtures/generate_golden.py"
      provides: "Checked-in Python script (~30 LOC) that generates ljung_box_golden.json from statsmodels==0.14.6 acorr_ljungbox on seed-0 256-bar AR(1) input — Blocker 4 fix per D3-05"
      contains: "acorr_ljungbox"
    - path: "crates/miner-core/tests/fixtures/ljung_box_golden.json"
      provides: "JSON fixture committed alongside generate_golden.py — contains q_stats, p_values, acf + provenance block (statsmodels version, script path, date, input SHA)"
      contains: "provenance"
    - path: "crates/miner-core/tests/scan_ljung_box.rs"
      provides: "Insta-snapshot golden test against the precomputed statsmodels 0.14.6 ljung_box_golden.json"
      contains: "insta::assert_json_snapshot"
    - path: "crates/miner-core/tests/scan_facade_determinism.rs"
      provides: "Twice-run byte-equality test with volatile fields masked"
      contains: "mask_volatile_fields"
    - path: "crates/miner-core/tests/shuffled_future_regression.rs"
      provides: "Proptest pinning look-ahead-safety invariant — D3-09; doc-comment matches Warning 10 wording"
      contains: "look_ahead_safe_under_post_t_shuffle"
    - path: "crates/miner-core/tests/gap_policy.rs"
      provides: "Five gap-policy behaviour tests per OUT-04 SC-3a..SC-3e"
      contains: "strict_with_gaps_emits_single_gap_aborted"
    - path: "crates/miner-cli/tests/sigint_preserves_stream.rs"
      provides: "#[cfg(unix)] integration test using nix::sys::signal::kill — relies on existing Plan 02 sleep_after_first_finding_ms field + Plan 04 cancel-aware sleep loop + Plan 05 --sleep-after-first-finding-ms CLI flag (Blocker 3 step 4 — NO retroactive edits)"
      contains: "nix::sys::signal::kill"
    - path: "crates/miner-cli/tests/fixtures/mod.rs"
      provides: "SyntheticCache builder + ar1_seed BarFrame helper + statsmodels_golden reference data (loaded from ljung_box_golden.json)"
      contains: "SyntheticCache"
    - path: "crates/miner-core/tests/snapshots/scan_ljung_box__ljung_box_matches_statsmodels_golden.snap"
      provides: "Insta-snapshot of the masked Ljung-Box golden envelope"
      contains: "ljung_box_q"
  key_links:
    - from: "crates/miner-cli/tests/scan_subcommand_smoke.rs"
      to: "crates/miner-cli (compiled binary via CARGO_BIN_EXE_miner)"
      via: "assert_cmd subprocess invocation"
      pattern: "Command::cargo_bin\\(\"miner\"\\)"
    - from: "crates/miner-core/tests/scan_ljung_box.rs"
      to: "crates/miner-core/tests/fixtures/ljung_box_golden.json"
      via: "load precomputed statsmodels reference; compare Rust output against it (NEVER the inverse)"
      pattern: "ljung_box_golden\\.json"
    - from: "crates/miner-core/tests/fixtures/generate_golden.py"
      to: "crates/miner-core/tests/fixtures/ljung_box_golden.json"
      via: "Python script produces JSON; run once per statsmodels bump on a developer machine with statsmodels==0.14.6 installed"
      pattern: "acorr_ljungbox"
    - from: "crates/miner-cli/tests/sigint_preserves_stream.rs"
      to: "Plan 02 ScanCtx/ScanRequest cfg-gated `sleep_after_first_finding_ms` field + Plan 04 LjungBoxScan cancel-aware sleep loop + Plan 05 ScanArgs `--sleep-after-first-finding-ms` CLI flag"
      via: "all three exist by Plan 06 execution time; Plan 06 only authors the test that uses them — Blocker 3 step 4 (NO retroactive edits in Plan 06)"
      pattern: "sleep-after-first-finding-ms"
---

<objective>
Close Phase 3 by filling every integration test scaffold from Plan 01 with real bodies, committing the statsmodels-derived golden fixture + matching Python provenance script (per D3-05 — Blocker 4 fix), generating the insta snapshot from the Rust output that matches the statsmodels reference, regenerating schemas one final time to confirm idempotency, and updating the README Quickstart with `miner scan` + `miner scans` examples. This plan converts the Phase 3 codebase from "compiles + unit-tests pass" to "the six ROADMAP success criteria are observable from real integration tests."

Purpose: Plans 01-05 built the engine + CLI; Plan 06 PROVES it works end-to-end against the contracts in CONTEXT.md and VALIDATION.md. Every test in the VALIDATION.md Per-Task Verification Map gets a real body, the proptest invariants pin determinism + look-ahead safety, and the README documents the user-facing surface. After Plan 06 the verifier can mark all 6 ROADMAP success criteria green.

The SIGINT integration test (Task 3) RELIES ON pre-existing artifacts landed by Plans 02/04/05 — it does NOT introduce any retroactive edits to those plans. Specifically (Blocker 3 step 4 fix):
- Plan 02 Task 2 already declared `ScanCtx.sleep_after_first_finding_ms` and `ScanRequest.sleep_after_first_finding_ms` as cfg-gated `Option<u64>` fields, plus declared the `test-internal` feature in `crates/miner-core/Cargo.toml`.
- Plan 04 Task 2 already wired the cancel-aware sleep loop in `LjungBoxScan::run`.
- Plan 05 Task 1 already added the cfg-gated `--sleep-after-first-finding-ms <ms>` CLI flag.
- Plan 06 only authors the assert_cmd + nix::kill integration test that exercises this end-to-end path.

The statsmodels golden direction is statsmodels-to-Rust, NEVER Rust-to-itself (Blocker 4 fix per D3-05):
- `tests/fixtures/generate_golden.py` is the canonical source — Python + statsmodels==0.14.6 generates the expected Q-stats / p-values / ACF.
- `tests/fixtures/ljung_box_golden.json` is the committed output of that script + a provenance block (statsmodels version, script path, date, input SHA).
- The Rust integration test LOADS `ljung_box_golden.json` and asserts the Rust kernel output matches.
- If precompute is genuinely blocked at execution time, the correct outcome is a phase-split or escalation — NOT inverting the golden direction by committing Rust output as the golden.

Output: ~9 integration test files filled out (Plan 01 created scaffolds with #[ignore] markers — Plan 06 strips them and adds real bodies); one insta golden snapshot file committed; one Python provenance script + one JSON golden fixture committed; README extended with two new Quickstart commands; cargo test --workspace --all-targets all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md
@.planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md
@.planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md
@.planning/phases/03-scan-engine-facade-cli/03-VALIDATION.md
@CLAUDE.md
@README.md
@crates/miner-cli/tests/cli_streams.rs
@crates/miner-core/tests/cache_smoke.rs
@crates/miner-core/tests/full_determinism.rs
@crates/miner-core/tests/gap_manifest_snapshot.rs
@crates/miner-reader-dukascopy/src/reader.rs

<interfaces>
<!-- Existing helpers Plan 06 reuses without modification. -->

From crates/miner-cli/tests/cli_streams.rs:
- mask_volatile_fields(v: &mut Value) — recursive masking; mask_volatile_fields_in_jsonl — full-output wrapper
- The assert_cmd::Command::cargo_bin("miner") pattern + #[serial_test::serial] discipline
- env_clear + MINER_CACHE_ROOT/MINER_BAR_CACHE_ROOT/MINER_OUTPUT env injection

From crates/miner-core/tests/full_determinism.rs:
- SyntheticCache builder pattern using public reader API (no internal poking)
- Two-runs byte-equality pattern

From crates/miner-core/tests/gap_manifest_snapshot.rs:
- insta::assert_json_snapshot!(typed_value) pattern

From RESEARCH §Code Examples lines 695-742:
- VERBATIM nix::sys::signal::kill + assert_cmd subprocess SIGINT-test pattern

From Plan 02 (already landed by Plan 06 execution time — Blocker 3 step 4 pin):
- `ScanCtx.sleep_after_first_finding_ms: Option<u64>` cfg-gated under `#[cfg(any(test, feature = "test-internal"))]` (Plan 02 Task 2)
- `ScanRequest.sleep_after_first_finding_ms: Option<u64>` cfg-gated identically (Plan 02 Task 2)
- `test-internal` feature in crates/miner-core/Cargo.toml (Plan 02 Task 2)

From Plan 04 (already landed by Plan 06 execution time — Blocker 3 step 4 pin):
- LjungBoxScan::run cancel-aware sleep loop polling ctx.cancel every ~10ms (Plan 04 Task 2)
- engine::cancellation_tests sub-module with cancel_at_entry / cancel_before_subrange / cancel_inside_scan_kernel (Plan 04 Task 3)

From Plan 05 (already landed by Plan 06 execution time — Blocker 3 step 4 pin):
- ScanArgs `#[arg(long, hide = true)] #[cfg(any(test, feature = "test-internal"))] pub sleep_after_first_finding_ms: Option<u64>` CLI flag (Plan 05 Task 1)
- main.rs forwards the flag into the constructed ScanRequest (Plan 05 Task 2)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build synthetic-cache fixtures + Ljung-Box statsmodels golden (Blocker 4 fix) + first three miner-core integration tests</name>
  <files>
    crates/miner-cli/tests/fixtures/mod.rs, crates/miner-cli/tests/fixtures/ar1_seed.rs,
    crates/miner-cli/tests/fixtures/statsmodels_golden.rs,
    crates/miner-core/tests/fixtures/generate_golden.py,
    crates/miner-core/tests/fixtures/ljung_box_golden.json,
    crates/miner-core/tests/scan_ljung_box.rs, crates/miner-core/tests/scan_facade_determinism.rs,
    crates/miner-core/tests/dry_run.rs,
    crates/miner-core/tests/snapshots/scan_ljung_box__ljung_box_matches_statsmodels_golden.snap
  </files>
  <read_first>
    - crates/miner-cli/tests/fixtures/mod.rs (Plan 01 scaffold — SyntheticCache + ar1_bar_frame helpers as unimplemented stubs)
    - crates/miner-core/tests/scan_ljung_box.rs (Plan 01 scaffold — `#[ignore]` test stub)
    - crates/miner-core/tests/scan_facade_determinism.rs (Plan 01 scaffold)
    - crates/miner-core/tests/dry_run.rs (Plan 01 scaffold)
    - crates/miner-core/tests/full_determinism.rs (whole file — synthetic-cache builder template)
    - crates/miner-core/tests/gap_manifest_snapshot.rs (whole file — insta snapshot template)
    - crates/miner-cli/tests/cli_streams.rs (lines 323-359 — mask_volatile_fields function to reuse / mirror)
    - .planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md (D3-01, D3-04, D3-05 — golden fixture spec — golden direction is statsmodels-to-Rust)
    - .planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md (Open Question 2 line 781 — redacted-JSONL byte equality approach; Code Examples lines 641-687 — statsmodels reference algorithm)
    - .planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md (lines 819-947 — every test analog)
  </read_first>
  <action>
    **PRECONDITION (Blocker 4 fix — D3-05 invariant — must be true BEFORE Task 1 is considered complete):**
    `crates/miner-core/tests/fixtures/ljung_box_golden.json` exists with the four top-level keys:
    1. `q_stats`: 10-element array of f64 produced by statsmodels.stats.diagnostic.acorr_ljungbox(returns, lags=10) on the seed-0 256-bar AR(1) series.
    2. `p_values`: 10-element array of f64 from the same call.
    3. `acf`: 11-element array (lag-0 to lag-10) of f64 from statsmodels.tsa.stattools.acf with adjusted=False.
    4. `provenance`: an object with the keys: `statsmodels_version` (string == "0.14.6"), `script_path` (string == "crates/miner-core/tests/fixtures/generate_golden.py"), `generated_at_utc` (ISO 8601 UTC date or datetime), `input_sha256` (hex SHA-256 of the seed-0 256-bar AR(1) close-price array bytes — LE-f64 packed).

    If `ljung_box_golden.json` cannot be precomputed at execution time (e.g., the developer machine lacks `python3 -m pip install 'statsmodels==0.14.6'`), the CORRECT outcome is to escalate via phase-split or operator action — NEVER invert the direction by committing Rust output as the golden (per D3-05 / Blocker 4). Document the escalation by returning `## PHASE SPLIT RECOMMENDED` to the orchestrator with the missing precondition; do not skip the golden.

    1. **`crates/miner-core/tests/fixtures/generate_golden.py`** (NEW — committed Python script, ~30 LOC — Blocker 4 fix):
       - Shebang: `#!/usr/bin/env python3`
       - Imports: `import json`, `import hashlib`, `import struct`, `from datetime import datetime, timezone`, `import numpy as np`, `from statsmodels.stats.diagnostic import acorr_ljungbox`, `from statsmodels.tsa.stattools import acf`.
       - Constants at the top: `STATSMODELS_REQUIRED = "0.14.6"`, `N = 256`, `LAGS = 10`, `AR_PHI = 0.4` (phi for AR(1)), `SEED = 0`.
       - Generate the deterministic AR(1) close-price series:
         - Set numpy RNG via `rng = np.random.default_rng(SEED)`.
         - Generate N standard-normal innovations.
         - x[0] = 0; for t in 1..N: x[t] = AR_PHI * x[t-1] + eps[t].
         - close[t] = 100.0 * np.exp(x[t] / 100.0) (positive, around 100).
       - Compute returns = log_returns(close).
       - Compute acf_values = acf(returns, nlags=LAGS, adjusted=False, fft=False) (statsmodels 0.14.6 signature).
       - Compute lb = acorr_ljungbox(returns, lags=LAGS, return_df=False) (returns Q-stats + p-values).
       - Compute input_sha256 = hashlib.sha256(struct.pack(f"<{N}d", *close.tolist())).hexdigest().
       - Assemble the JSON object: { "q_stats": lb["lb_stat"].tolist(), "p_values": lb["lb_pvalue"].tolist(), "acf": acf_values.tolist(), "provenance": { "statsmodels_version": STATSMODELS_REQUIRED, "script_path": "crates/miner-core/tests/fixtures/generate_golden.py", "generated_at_utc": datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ"), "input_sha256": input_sha256 } }.
       - Write to `crates/miner-core/tests/fixtures/ljung_box_golden.json` (pretty-printed; sorted keys).
       - Leading docstring documents the precondition: requires `pip install 'statsmodels==0.14.6'`; run from repo root.
       - The script is the canonical source-of-truth for the golden — bumping statsmodels means re-running this and committing the diff.

    2. **`crates/miner-core/tests/fixtures/ljung_box_golden.json`** (NEW — committed output of generate_golden.py — Blocker 4 fix). Generated by running `python3 crates/miner-core/tests/fixtures/generate_golden.py`. The file is committed verbatim. CI does NOT regenerate it (no Python in the Rust build); regeneration is a developer-machine task gated by statsmodels availability. The Rust test consumes this file; it is NOT regenerated by `cargo test`.

    3. **crates/miner-cli/tests/fixtures/mod.rs** (and submodules):
       - Replace Plan 01 stubs with working implementations.
       - `pub mod ar1_seed; pub mod statsmodels_golden;`
       - `pub struct SyntheticCache { tempdir: tempfile::TempDir, cache_root: PathBuf }` — builder pattern per full_determinism.rs analog:
         - `pub fn new() -> Self` creates a tempdir and an empty cache root.
         - `pub fn with_one_day(self, symbol: &str, side: Side, date: NaiveDate, bars: &[RawBar]) -> Self` writes a synthetic `.csv.zst` file at the Dukascopy path layout (use the existing `miner_reader_dukascopy::path_layout` helpers; DO NOT poke internals).
         - `pub fn root(&self) -> &Path` returns the cache root.
         - The builder writes complete daily CSV files compatible with DukascopyReader so engine::run_one reads them via the production code path.
       - `pub fn ar1_bar_frame_seeded(n: usize, seed: u64, start_ts: DateTime<Utc>, tf_minutes: u32) -> BarFrame` from `ar1_seed.rs`:
         - Deterministic implementation MATCHING the Python script's AR(1) formula: phi=0.4 + AR(1) with seeded innovations + close[t] = 100 * exp(x[t] / 100.0). The Rust PRNG MUST produce the same draws as numpy.random.default_rng(seed) — use the `rand` crate's `StdRng` with a seeded `from_seed([0u8; 32])` form OR (recommended) generate the close array once via the Python script and embed it as a constant in the Rust fixture so both sides use identical bytes. **Recommended path:** the script also dumps the close array as a `close: Vec<f64>` field in `ljung_box_golden.json` (extend the script accordingly); the Rust fixture loads that array verbatim. This eliminates Rust-vs-numpy PRNG drift entirely.
         - Pack into BarFrame with ts_open_utc spaced by tf_minutes; open=high=low=close (don't need true OHLC for Ljung-Box — only close used).
       - `pub fn load_statsmodels_golden() -> serde_json::Value` in `statsmodels_golden.rs`: loads `crates/miner-core/tests/fixtures/ljung_box_golden.json` via `include_str!` + `serde_json::from_str`; documents the provenance contract.
       - Add `pub fn mask_volatile_fields(v: &mut serde_json::Value)` if not already shared with miner-cli tests — DRY against cli_streams.rs:323-359. (Test files in different crates can't share a `mod` block easily; duplicate the function — it is 15 lines.)

    4. **crates/miner-core/tests/scan_ljung_box.rs** — fill the body per PATTERNS line 825-850 + Blocker 4 (statsmodels-to-Rust direction):
       - Use the fixture from above to build a deterministic BarFrame.
       - Construct ScanCtx + ScanRequest + VecSink IN-PROCESS (not via subprocess).
       - Call `LjungBoxScan.run(&ctx, &req, &mut sink)` directly OR `engine::run_one` (richer — also exercises framing). Choose engine::run_one because it gives the full envelope shape.
       - Parse sink.bytes() into Vec<Finding> via newline-split + serde_json::from_str.
       - Find the Finding::Result line; extract its envelope; mask volatile fields (run_id, produced_at_utc, started_at_utc, ended_at_utc, wall_clock_ms).
       - `insta::assert_json_snapshot!(masked_finding)` — creates the .snap file under tests/snapshots/.
       - Add a SECOND assertion (statsmodels-to-Rust direction — Blocker 4):
         - Load the golden via `let golden: serde_json::Value = serde_json::from_str(include_str!("fixtures/ljung_box_golden.json")).expect("golden present");`.
         - Verify the provenance block — assert `golden["provenance"]["statsmodels_version"] == "0.14.6"`. If absent or mismatched, panic with a clear message pointing at `generate_golden.py`.
         - Extract the expected Q-stats / p-values / acf from `golden`.
         - Decode the Base64 RawArrays from the Result envelope (q_stats, p_values, acf).
         - Compare element-by-element with tolerance 1e-12 (RESEARCH Open Question 2 fall-back path).
       - Remove the `#[ignore]` attribute from Plan 01.

    5. **crates/miner-core/tests/scan_facade_determinism.rs** — fill the body per PATTERNS line 863-878 + RESEARCH Open Question 2 redacted-JSONL byte-equality:
       - Run `engine::run_one` twice in-process against identical inputs.
       - mask_volatile_fields on both outputs.
       - assert_eq!(masked1, masked2) on the JSONL strings.
       - Use a SyntheticCache fixture + DukascopyReader so the full data-loading path is exercised.
       - Remove the `#[ignore]` attribute.

    6. **crates/miner-core/tests/dry_run.rs** — fill the body:
       - Build a ScanRequest with dry_run=true (the canonical Plan 02 field).
       - Call engine::run_one with the synthetic cache.
       - Parse sink.bytes() into envelopes; assert: exactly 3 envelopes [run_start, dry_run, run_end]; the dry_run envelope's resolved_params matches the input; `results_emitted == 0` in RunEnd.summary (Pitfall 3 invariant).
       - ADDITIONAL Warning 9 negative assertion: the raw JSONL output for the run does NOT contain the literal substring `"dry_run_emitted"` — confirms RunSummary was not silently extended.
       - Remove the `#[ignore]` attribute.

    7. **Create the .snap file** by running `cargo test -p miner-core --test scan_ljung_box -- ljung_box_matches_statsmodels_golden` (insta creates the .snap file on first run). Inspect the .snap content; commit it. Subsequent runs validate against the committed snapshot.

    NO unsafe; NO println in production paths (println in tests is fine — clippy.toml lint only applies outside the sanctioned macros for non-test code; verify whether the workspace clippy.toml exempts tests).
  </action>
  <verify>
    <automated>test -f crates/miner-core/tests/fixtures/generate_golden.py && test -f crates/miner-core/tests/fixtures/ljung_box_golden.json && grep -c '"statsmodels_version"' crates/miner-core/tests/fixtures/ljung_box_golden.json && grep -c '"0.14.6"' crates/miner-core/tests/fixtures/ljung_box_golden.json && cargo test -p miner-core --test scan_ljung_box && cargo test -p miner-core --test scan_facade_determinism && cargo test -p miner-core --test dry_run</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p miner-core --test scan_ljung_box -- ljung_box_matches_statsmodels_golden passes (Blocker 4 — golden direction is statsmodels-to-Rust)
    - cargo test -p miner-core --test scan_facade_determinism -- twice_run_byte_identical_when_volatile_fields_masked passes
    - cargo test -p miner-core --test dry_run -- dry_run_emits_dry_run_finding_only passes
    - `test -f crates/miner-core/tests/fixtures/generate_golden.py` (Blocker 4 — Python provenance script committed)
    - `test -f crates/miner-core/tests/fixtures/ljung_box_golden.json` (Blocker 4 — JSON golden committed)
    - `grep -c '"statsmodels_version"' crates/miner-core/tests/fixtures/ljung_box_golden.json` returns 1 (provenance block present)
    - `grep -c '"0.14.6"' crates/miner-core/tests/fixtures/ljung_box_golden.json` returns 1 (statsmodels version pinned per D3-05)
    - `grep -c 'acorr_ljungbox' crates/miner-core/tests/fixtures/generate_golden.py` returns >= 1 (script calls the canonical statsmodels API)
    - `grep -c 'input_sha256\|script_path\|generated_at_utc' crates/miner-core/tests/fixtures/ljung_box_golden.json` returns >= 3 (provenance block carries the four required keys)
    - `grep -c 'include_str!("fixtures/ljung_box_golden.json")\|ljung_box_golden\.json' crates/miner-core/tests/scan_ljung_box.rs` returns >= 1 (the Rust test loads the precomputed golden — Blocker 4 direction held)
    - `grep -c 'dry_run_emitted' crates/miner-core/tests/dry_run.rs` returns >= 1 (negative assertion present — Warning 9 pin)
    - crates/miner-core/tests/snapshots/scan_ljung_box__ljung_box_matches_statsmodels_golden.snap exists with non-empty content
    - `grep -c '#\[ignore' crates/miner-core/tests/scan_ljung_box.rs crates/miner-core/tests/scan_facade_determinism.rs crates/miner-core/tests/dry_run.rs` returns 0 (Plan 01's `#[ignore]` markers all stripped)
    - `grep -c 'fn ar1_bar_frame_seeded' crates/miner-cli/tests/fixtures/ar1_seed.rs` returns >= 1
    - `grep -c 'load_statsmodels_golden\|STATSMODELS' crates/miner-cli/tests/fixtures/statsmodels_golden.rs` returns >= 1
  </acceptance_criteria>
  <done>
    Three miner-core integration tests filled and green; insta snapshot committed; fixture builders work against the public reader/cache API; statsmodels-derived golden JSON committed with provenance block (Blocker 4 fix — D3-05 invariant: golden direction is statsmodels-to-Rust); Python provenance script committed; Warning 9 negative assertion present in dry_run.rs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gap-policy + shuffled-future regression proptest (Warning 10 doc-comment wording) + miner-cli scan_subcommand_smoke + scans_catalogue</name>
  <files>
    crates/miner-core/tests/gap_policy.rs, crates/miner-core/tests/shuffled_future_regression.rs,
    crates/miner-cli/tests/scan_subcommand_smoke.rs, crates/miner-cli/tests/scans_catalogue.rs
  </files>
  <read_first>
    - crates/miner-core/tests/gap_policy.rs (Plan 01 scaffold — 5 ignored tests)
    - crates/miner-core/tests/shuffled_future_regression.rs (Plan 01 scaffold — proptest gated by cfg)
    - crates/miner-cli/tests/scan_subcommand_smoke.rs (Plan 01 scaffold — 5 ignored tests)
    - crates/miner-cli/tests/scans_catalogue.rs (Plan 01 scaffold)
    - crates/miner-core/tests/cache_smoke.rs (lines 38-46 — proptest harness pattern)
    - crates/miner-cli/tests/cli_streams.rs (whole file — assert_cmd + env_clear + #[serial_test::serial] pattern)
    - schemas/scans-catalogue-v1.schema.json (Plan 02 — sibling schema scans_catalogue test validates against)
    - .planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md (D3-09 — shuffled-future regression spec; D3-22 — every gap-policy emission rule)
    - .planning/phases/03-scan-engine-facade-cli/03-VALIDATION.md (the 5 gap-policy test names + the OP-01/OP-08 named tests in the Per-Task Verification Map)
    - .planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md (lines 883-1024 — every test analog)
  </read_first>
  <action>
    1. **crates/miner-core/tests/gap_policy.rs** — fill the 5 named tests per OUT-04 SC-3a..SC-3e + the proptest invariant:
       - `strict_with_gaps_emits_single_gap_aborted`: SyntheticCache with one missing day (creating a real CACHE_HOLE per GapDetector) + Strict policy → assert envelopes [run_start, gap_aborted, run_end]; the gap_aborted carries the full manifest; RunSummary.gap_aborted == 1, results_emitted == 0.
       - `continuous_only_partitions_and_inlines_manifest`: SyntheticCache with one gap splitting requested range → assert 2 Result findings (one per sub-range), each carrying `data_slice.gap_manifest = Some(full_manifest)` inline per D3-10.
       - `strict_zero_gaps_emits_result_with_none_manifest`: SyntheticCache with all days present + Strict → assert one Result; `data_slice.gap_manifest == None` per D3-12.
       - `continuous_only_zero_gaps_emits_empty_manifest`: SyntheticCache with all days present + ContinuousOnly → assert one Result; `data_slice.gap_manifest == Some(GapManifest { gaps: vec![] })` (empty manifest present per D3-12 fast path).
       - `never_silently_emits_on_hole_proptest`: proptest invariant — for a random gap configuration + random gap_policy, the engine NEVER produces a Result whose data_slice.range overlaps any GapSpan. Use proptest::strategy to generate gaps + policy + window; run engine::run_one in-process; assert the invariant against every Result finding's data_slice.range.
       - All five tests use SyntheticCache from Task 1's fixtures; remove `#[ignore]` markers.

    2. **crates/miner-core/tests/shuffled_future_regression.rs** — fill the proptest body per CONTEXT D3-09 + PATTERNS line 890-906:
       - Strip the `#[cfg(disabled_in_scaffold)]` from Plan 01 (or whatever scaffold gate was used).
       - **Doc-comment phrasing (Warning 10 fix — exact wording):**
         ```
         /// This is the full D3-09 enforcement for Ljung-Box (a single-shot, non-rolling
         /// scan). Phase 4 will ADD additional cancellation_tests-style proptests for each
         /// new rolling/causal scan it introduces — it does NOT extend this proptest.
         ```
       - proptest! { #[test] fn look_ahead_safe_under_post_t_shuffle(seed in 0u64..1000) { ... } }:
         - Build an N=256 BarFrame via ar1_bar_frame_seeded(N, seed, ts0, 15).
         - Compute Ljung-Box up to cutpoint T=128: build a BarFrame slice of bars[..T+1] (T+1 closes → T returns), run LjungBoxScan.run, collect the result_q_stats.
         - Build a SHUFFLED-FUTURE BarFrame: bars[..T+1] unchanged + bars[T+1..N] permuted via a deterministic seed-derived rng. Run LjungBoxScan with the same lags AGAIN, but ONLY emit the result up to cutpoint T (the scan reads ctx.bars[..T+1] only — for the demo scan this is structural because the scan reads ctx.bars verbatim, and the test passes a sliced BarFrame).
         - Assert byte-equal Q-stats between the pre-shuffle and post-shuffle slices.

    3. **crates/miner-cli/tests/scan_subcommand_smoke.rs** — fill the 5 assert_cmd tests per VALIDATION.md Per-Task Verification Map:
       - `scan_emits_run_start_result_run_end`: invoke `miner scan stats.autocorr.ljung_box@1 --instrument EURUSD --side bid --timeframe 15m --window 2024-01-01:2024-01-02 ...` against a SyntheticCache + temp dirs; assert exit 0; parse stdout JSONL; assert envelopes are exactly [run_start, result, run_end].
       - `unknown_scan_emits_wireerror_exit_1`: invoke `miner scan nope@1 ...`; assert exit 1, stdout EMPTY, stderr contains a WireError with code "unknown_scan".
       - `invalid_params_emits_wireerror_exit_1`: invoke `miner scan stats.autocorr.ljung_box@1 ... --params malformed`; assert exit 1, code "invalid_parameter".
       - `dry_run_emits_dry_run_finding_only`: invoke `... --dry-run`; assert exit 0, envelopes [run_start, dry_run, run_end].
       - `exit_code_routing_zero_one_two`: this is harder to test in one binary invocation; build it as three small sub-invocations (happy path → 0; unknown scan → 1; mid-run error injected via a synthetic param that triggers ScanError::Compute → 2). For the "2" case, choose a SyntheticCache with bars but force lags out of range to hit the ScanError path. Verify exit codes match D3-24.
       - All five tests use #[serial_test::serial] + env_clear per cli_streams.rs discipline; remove `#[ignore]` markers.

    4. **crates/miner-cli/tests/scans_catalogue.rs** — fill the body:
       - Invoke `miner scans` via assert_cmd; assert exit 0.
       - Parse stdout JSONL: assert exactly ONE line for Phase 3.
       - Validate that line against schemas/scans-catalogue-v1.schema.json (load via std::fs::read_to_string; compile with jsonschema; validate the parsed Value). Per Open Question 8 resolution — this is the canonical sibling-schema test.
       - Validate that line does NOT pass schemas/findings-v1.schema.json (negative assertion — confirms the catalogue lines are STRUCTURALLY different from Findings).
       - Assert the line has scan_id == "stats.autocorr.ljung_box", version == 1, params is a JSON-Schema-shaped Value, finding_fields has the four extra_keys and two raw_series_keys per D3-04.
       - Remove `#[ignore]`.

    NO unsafe; integration tests in /tests/ may use println in test helpers (clippy lint applies to library code, not test code — but verify against the workspace clippy.toml first). #[serial_test::serial] on all assert_cmd tests per cli_streams.rs discipline (existing pattern).
  </action>
  <verify>
    <automated>cargo test -p miner-core --test gap_policy && cargo test -p miner-core --test shuffled_future_regression && cargo test -p miner-cli --test scan_subcommand_smoke && cargo test -p miner-cli --test scans_catalogue</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p miner-core --test gap_policy passes all 5 tests (>=4 named + the proptest)
    - cargo test -p miner-core --test shuffled_future_regression -- look_ahead_safe_under_post_t_shuffle passes (the proptest test name uses look_ahead_safe_under_post_t_shuffle per Plan 01's scaffolded list — `_proptest` suffix variants may also pass per VALIDATION.md command form)
    - cargo test -p miner-cli --test scan_subcommand_smoke passes all 5 tests
    - cargo test -p miner-cli --test scans_catalogue passes
    - `grep -c '#\[ignore' crates/miner-core/tests/gap_policy.rs crates/miner-cli/tests/scan_subcommand_smoke.rs crates/miner-cli/tests/scans_catalogue.rs` returns 0
    - `grep -c '#\[cfg(disabled_in_scaffold)\]' crates/miner-core/tests/shuffled_future_regression.rs` returns 0 (gate removed)
    - `grep -c 'scans-catalogue-v1.schema.json' crates/miner-cli/tests/scans_catalogue.rs` returns >= 1 (sibling-schema validation per Open Question 8)
    - `grep -F 'full D3-09 enforcement for Ljung-Box (a single-shot, non-rolling scan)' crates/miner-core/tests/shuffled_future_regression.rs` returns >= 1 (Warning 10 — exact doc-comment phrasing required)
    - `grep -F 'Phase 4 will ADD additional cancellation_tests-style proptests' crates/miner-core/tests/shuffled_future_regression.rs` returns >= 1 (Warning 10 — second clause of the required phrasing)
    - `grep -F 'does NOT extend this proptest' crates/miner-core/tests/shuffled_future_regression.rs` returns >= 1 (Warning 10 — third clause)
    - cargo test --workspace --all-targets (full suite passes — Phase 3 verifier-ready)
  </acceptance_criteria>
  <done>
    11 integration test functions across 4 test files filled and passing; shuffled-future invariant pinned with the Warning 10 exact doc-comment phrasing; gap-policy emission rules end-to-end verified against the production engine; scans catalogue validated against the sibling schema; assert_cmd subprocess tests confirm the four-tier exit code routing.
  </done>
</task>

<task type="auto">
  <name>Task 3: SIGINT integration test (consumes Plan 02/04/05 artifacts — Blocker 3 step 4 — NO retroactive edits) + README Quickstart + final schema-regen idempotency check + Phase 3 sign-off</name>
  <files>crates/miner-cli/tests/sigint_preserves_stream.rs, README.md</files>
  <read_first>
    - crates/miner-cli/tests/sigint_preserves_stream.rs (Plan 01 scaffold — #[cfg(unix)] gated)
    - crates/miner-cli/src/scan_args.rs (Plan 05 — ScanArgs struct ALREADY has the cfg-gated `--sleep-after-first-finding-ms` flag from Plan 05 Task 1; Plan 06 only consumes it)
    - crates/miner-core/src/scan/mod.rs (Plan 02 — ScanCtx + ScanRequest ALREADY have the cfg-gated `sleep_after_first_finding_ms: Option<u64>` field from Plan 02 Task 2; Plan 06 only consumes it)
    - crates/miner-core/src/scan/ljung_box/mod.rs (Plan 04 — LjungBoxScan::run ALREADY has the cancel-aware sleep loop from Plan 04 Task 2; Plan 06 only exercises it)
    - crates/miner-core/Cargo.toml (Plan 02 — `test-internal` feature ALREADY declared; Plan 06 only enables it for the test build if needed)
    - README.md (existing Phase 1 Quickstart section)
    - .planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md (Code Examples lines 695-742 — verbatim sigint test pattern; Pitfall 8 lines 612-622 — race-avoidance via the test-only sleep flag; Assumption A5 line 767 — sleep flag is acceptable per Plan-phase decision)
    - .planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md (D3-22, D3-24 — SIGINT semantics)
    - .planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md (lines 1025-1075 — sigint test analog)
    - .planning/phases/03-scan-engine-facade-cli/03-VALIDATION.md (Sign-Off ritual)
  </read_first>
  <action>
    **PRE-CONDITIONS (Blocker 3 step 4 — assertions, not work items for Plan 06):**
    By the time Plan 06 executes, the following ALREADY exist via earlier plans. Plan 06 must NOT add them retroactively:
    - Plan 02 Task 2: `ScanCtx.sleep_after_first_finding_ms: Option<u64>` cfg-gated; `ScanRequest.sleep_after_first_finding_ms: Option<u64>` cfg-gated; `test-internal` feature in `crates/miner-core/Cargo.toml`. Plan 06 verifies via:
      - `grep -c 'feature = "test-internal"' crates/miner-core/src/scan/mod.rs` returns >= 2
      - `grep -c '^test-internal' crates/miner-core/Cargo.toml` returns 1
    - Plan 04 Task 2: LjungBoxScan::run cancel-aware sleep loop. Plan 06 verifies via:
      - `grep -c 'while.*cancel\.load' crates/miner-core/src/scan/ljung_box/mod.rs` returns >= 1
    - Plan 05 Task 1: ScanArgs `--sleep-after-first-finding-ms <ms>` flag cfg-gated identically. Plan 06 verifies via:
      - `grep -c '#\[cfg(any(test, feature = "test-internal"))\]' crates/miner-cli/src/scan_args.rs` returns >= 1
      - `grep -c 'sleep-after-first-finding-ms\|sleep_after_first_finding_ms' crates/miner-cli/src/scan_args.rs` returns >= 1
    If ANY of the above grep checks fail at execution time, STOP and surface a precondition violation — earlier plans drifted from the contract; do not paper over with retroactive edits in Plan 06.

    1. **Fill crates/miner-cli/tests/sigint_preserves_stream.rs** per RESEARCH Code Examples lines 695-742:
       - Module attribute `#![cfg(unix)]` already from Plan 01.
       - Test `sigint_preserves_already_streamed_findings_and_exits_130`:
         - Spawn `miner scan stats.autocorr.ljung_box@1 ... --sleep-after-first-finding-ms 5000` via std::process::Command (not assert_cmd — need raw stdio access). The binary must be built with the `test-internal` feature enabled OR via `cargo test` (which activates `cfg(test)` and makes the flag available — see Plan 05's ScanArgs cfg gate).
         - The test sets `CARGO_FEATURE_TEST_INTERNAL=1` env or invokes `cargo build -p miner-cli --features test-internal --bin miner` ahead of time IF the `CARGO_BIN_EXE_miner` resolved by `env!("CARGO_BIN_EXE_miner")` lacks the cfg-gated flag. Document the build prerequisite in a leading doc-comment.
         - BufRead the child's stdout; read until seeing the first JSONL line containing `"kind":"run_start"`; read the next line containing `"kind":"result"` (the sleep blocks behind this).
         - Send SIGINT via `nix::sys::signal::kill(nix::unistd::Pid::from_raw(child.id() as i32), nix::sys::signal::Signal::SIGINT)`.
         - `child.wait()`; assert `status.code() == Some(130)`.
         - Read any remaining stdout — assert the run_start + result are still present + parseable + the run_end line is present (clean RunEnd despite the SIGINT — per CONTEXT D3-22 the engine still emits RunEnd before exiting).
         - `#[serial_test::serial]` per cli_streams discipline.
         - Use a SyntheticCache fixture via `MINER_CACHE_ROOT` env var; build the binary path via `env!("CARGO_BIN_EXE_miner")`.
       - Remove `#[ignore]`.

    2. **Update README.md** with the Phase 3 Quickstart additions:
       - After the existing Phase 1 emit-fixture example, add a section "## Running a Scan":
         - One example of `miner scan stats.autocorr.ljung_box@1 --instrument EURUSD --side bid --timeframe 15m --window 2024-01-01:2024-01-02` against a fixture cache.
         - One example of `miner scans | jq .` showing the catalogue introspection.
         - One example of `miner scan ... --dry-run` showing the dry-run output.
         - Brief note: SIGINT preserves streamed findings; exit code 130.
         - Brief note: --gap-policy strict vs continuous_only behaviour.
       - The README MUST not break the existing Phase 1 Quickstart (which is what `cli_streams::run_emit_fixture_happy` exercises).

    3. **Run schema regen + assert idempotency**:
       - `cargo run -p xtask -- gen-schema` followed by `git diff --exit-code schemas/`. Both schemas should be unchanged from Plan 02's commit.
       - If the diff is non-empty, the Rust types drifted between Plan 02 and Plan 06; flag and revert the type change.

    4. **Phase 3 sign-off ritual** — run the full validation suite per VALIDATION.md "Validation Sign-Off":
       - `cargo test --workspace --all-targets` — green
       - `cargo clippy --workspace --all-targets -- -D warnings` — clean
       - `cargo fmt --all --check` — clean
       - `cargo tree -p miner-core | grep -iE 'tokio|async-std'` — empty (FOUND-04 gate)
       - `cargo run -p xtask -- gen-schema && git diff --exit-code schemas/findings-v1.schema.json schemas/scans-catalogue-v1.schema.json` — clean (Plan 02 schemas held)
       - `grep -c preserve_order Cargo.lock` — 0 (Pitfall 1 gate)
       - Update `.planning/phases/03-scan-engine-facade-cli/03-VALIDATION.md` frontmatter from `nyquist_compliant: false` to `nyquist_compliant: true` and `wave_0_complete: false` to `wave_0_complete: true` and `status: draft` to `status: ready` (the Plan-checker / verifier may further escalate). Also update the SC-5b row's Plan/Wave columns to `03-04 / 4` and the Automated Command column to reference `engine::cancellation_tests::cancel_at_*` test names matching Plan 04's sub-module — see VALIDATION.md update list in `<output>` below.
  </action>
  <verify>
    <automated>cargo test --workspace --all-targets && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --all --check && cargo run -p xtask -- gen-schema && git diff --exit-code schemas/findings-v1.schema.json schemas/scans-catalogue-v1.schema.json && cargo tree -p miner-core | grep -iE 'tokio|async-std' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p miner-cli --test sigint_preserves_stream -- sigint_preserves_already_streamed_findings_and_exits_130 passes (Unix only; on non-Unix the file's #![cfg(unix)] makes the test absent — acceptable)
    - cargo test --workspace --all-targets passes (full suite green; Phase 3 ready for verifier)
    - cargo clippy --workspace --all-targets -- -D warnings is clean
    - cargo fmt --all --check is clean
    - cargo run -p xtask -- gen-schema && git diff --exit-code schemas/findings-v1.schema.json schemas/scans-catalogue-v1.schema.json exits 0 (schemas idempotent vs Plan 02)
    - `cargo tree -p miner-core | grep -iE 'tokio|async-std' | wc -l` returns 0 (FOUND-04 held)
    - `grep -c preserve_order Cargo.lock` returns 0 (Pitfall 1 held)
    - README.md contains the literal substring "miner scan stats.autocorr.ljung_box@1" (Phase 3 Quickstart documented)
    - .planning/phases/03-scan-engine-facade-cli/03-VALIDATION.md frontmatter shows nyquist_compliant: true, wave_0_complete: true, status: ready
    - .planning/phases/03-scan-engine-facade-cli/03-VALIDATION.md SC-5b row updated: Plan column = `03-04`, Wave column = `4`, Automated Command unchanged (it already cites `engine::cancellation_tests::cancel_at_*` per the original VALIDATION.md template)
    - `grep -c '#\[ignore' crates/miner-cli/tests/sigint_preserves_stream.rs` returns 0
    - **Precondition checks (Blocker 3 step 4 — verify earlier plans landed the hooks; do NOT add them here):**
      - `grep -c 'feature = "test-internal"' crates/miner-core/src/scan/mod.rs` returns >= 2 (Plan 02 Task 2 fields)
      - `grep -c '^test-internal' crates/miner-core/Cargo.toml` returns 1 (Plan 02 Task 2 feature)
      - `grep -c 'while.*cancel\.load' crates/miner-core/src/scan/ljung_box/mod.rs` returns >= 1 (Plan 04 Task 2 cancel-aware loop)
      - `grep -c 'sleep_after_first_finding_ms' crates/miner-cli/src/scan_args.rs` returns >= 1 (Plan 05 Task 1 CLI flag)
  </acceptance_criteria>
  <done>
    SIGINT integration test passes using the Plan 02/04/05 cfg-gated artifacts that ALREADY exist (Blocker 3 step 4 — no retroactive edits to earlier plans); README documents the Phase 3 user-facing surface; schemas are idempotent vs Plan 02 (no type-drift); full workspace passes clippy + fmt + test; VALIDATION.md frontmatter promoted and SC-5b row Plan/Wave columns populated; Phase 3 is verifier-ready.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic cache fixtures → DukascopyReader | SyntheticCache writes real .csv.zst files; the production reader processes them unchanged |
| Subprocess → parent test process | assert_cmd / std::process spawn the real miner binary; SIGINT delivered via nix::kill |
| Plan 02/04/05 cfg-gated artifacts → Plan 06 integration test | All cfg-gated work landed in the wave that owns the type; Plan 06 only consumes (Blocker 3 step 4 enforced via precondition grep checks) |
| statsmodels Python → Rust golden fixture | The Python script + JSON output is the single source of truth (Blocker 4 — statsmodels-to-Rust direction; never the inverse) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | golden statsmodels constants drift | mitigate | The `ljung_box_golden.json` file embeds a `provenance` block naming the statsmodels version (0.14.6), the Python script path, the generation date, and the SHA of the input AR(1) close-price array. Future Phase 4/7 regenerations follow the same provenance discipline. Re-running `generate_golden.py` is the canonical bump path. (Blocker 4 — D3-05 invariant: golden direction is statsmodels-to-Rust.) |
| T-03-06-02 | Denial of Service | SIGINT integration test flake | mitigate | Pitfall 8 mitigation: the `--sleep-after-first-finding-ms` flag (Plan 05) drives the cfg-gated `ScanCtx.sleep_after_first_finding_ms` field (Plan 02), which LjungBoxScan::run polls in a cancel-aware loop (Plan 04). The flag pauses the scan kernel between emitting the first result and returning; the cancel token is polled inside the sleep loop so SIGINT lands deterministically. CI flake risk reduced from O(microseconds) to O(seconds). Plan 06 verifies the precondition grep checks before running the integration test. |
| T-03-06-03 | Information Disclosure | test-only flag accidentally enabled in production | mitigate | The flag is cfg-gated by `#[cfg(any(test, feature = "test-internal"))]`. The default `cargo build --release` does not build `test`; the `test-internal` feature is not in any release profile. CI release-build gate (FOUND-01 closure) confirms the flag is absent in the release binary via `--help` inspection. |
| T-03-06-04 | Repudiation | scan catalogue lines validated against wrong schema | mitigate | scans_catalogue.rs explicitly validates against schemas/scans-catalogue-v1.schema.json (positive) AND against schemas/findings-v1.schema.json (NEGATIVE — must fail). Pitfall 7 fully covered. |
| T-03-06-05 | Tampering | look-ahead safety violated by Phase 4 scans | accept | Phase 3 proptest covers LjungBoxScan only (the Phase 3 demo scan). Phase 4 plans MUST ADD additional cancellation_tests-style proptests per new rolling/causal scan — they do NOT extend this proptest. The Warning 10 exact doc-comment phrasing in `shuffled_future_regression.rs` documents this scope rule. |
| T-03-06-06 | Tampering | Plan 06 silently adds retroactive edits to earlier plans | mitigate | Blocker 3 step 4 fix: Plan 06's `<read_first>` and acceptance_criteria explicitly check that Plan 02/04/05 already landed the cfg-gated artifacts. If the grep checks fail, Plan 06 surfaces the precondition violation rather than papering over with retroactive edits. |
</threat_model>

<verification>
- cargo test --workspace --all-targets — full suite green (the entire Per-Task Verification Map from VALIDATION.md is now executable)
- cargo clippy --workspace --all-targets -- -D warnings — clean
- cargo fmt --all --check — clean
- cargo run -p xtask -- gen-schema then git diff --exit-code schemas/ — clean (idempotent vs Plan 02)
- cargo tree -p miner-core | grep -iE 'tokio|async-std' — empty
- grep preserve_order Cargo.lock — empty
- The 19 named test commands from VALIDATION.md Per-Task Verification Map all pass
- README documents `miner scan` + `miner scans` + `--dry-run` + SIGINT behaviour + gap-policy choice
- VALIDATION.md frontmatter promoted to nyquist_compliant: true / wave_0_complete: true / status: ready
- VALIDATION.md SC-5b row Plan/Wave columns populated to `03-04 / 4`
- Precondition checks (Blocker 3 step 4) succeed: Plan 02/04/05 cfg-gated artifacts present without Plan 06 retroactive edits
- ljung_box_golden.json provenance block carries statsmodels_version="0.14.6" (Blocker 4)
</verification>

<success_criteria>
1. SC-1 (OP-01): miner scan ... NDJSON on stdout for one fully-implemented scan with resolved params echoed — scan_emits_run_start_result_run_end passes
2. SC-2 (OP-07 + OP-08): miner scans introspects; unknown scan + invalid param rejected at the boundary — scans_catalogue + unknown_scan + invalid_params tests pass
3. SC-3 (OUT-04): strict aborts with one error record; continuous_only partitions + inlines manifest; never silently emits on a hole — 5 gap_policy tests pass + never_silently_emits_on_hole proptest passes
4. SC-4 (OP-05): --dry-run shows resolved job + data_slice + estimated_findings_count — dry_run_emits_dry_run_finding_only passes
5. SC-5 (OP-06): SIGINT preserves every already-streamed finding; rayon worker pool shuts down cleanly; exit 130 — sigint_preserves_already_streamed_findings_and_exits_130 passes via Plan 02/04/05 cfg-gated artifacts (Blocker 3 step 4 — no retroactive edits)
6. SC-6 (OUT-03): byte-identical JSONL across runs; shuffled-future regression passes — twice_run_byte_identical_when_volatile_fields_masked + look_ahead_safe_under_post_t_shuffle pass; Warning 10 exact doc-comment phrasing present in shuffled_future_regression.rs
7. D3-05 (Blocker 4 fix): ljung_box_golden.json carries a provenance block citing statsmodels 0.14.6 + script path + generated date + input SHA; the Rust test loads this JSON (statsmodels-to-Rust direction); generate_golden.py committed as the canonical source-of-truth.
8. README updated; schemas idempotent; full workspace passes clippy + fmt + test; Phase 3 verifier-ready
</success_criteria>

<output>
Create `.planning/phases/03-scan-engine-facade-cli/03-06-SUMMARY.md` when done. Record:
- The 19 named cargo test commands from VALIDATION.md Per-Task Verification Map with their PASS status.
- The full `cargo test --workspace --all-targets 2>&1 | tail -20` output showing the final test counts (target: ~120-150 total tests).
- The README diff (a few lines of additions).
- The VALIDATION.md frontmatter promotion AND the SC-5b row Plan/Wave update (`03-04 / 4`).
- The contents of the newly-committed scan_ljung_box golden snapshot (a few lines so the reviewer can sanity-check the masked envelope shape).
- The output of `cat crates/miner-core/tests/fixtures/ljung_box_golden.json | head -20` showing the provenance block.
- The output of the precondition grep checks (Blocker 3 step 4) confirming Plan 02/04/05 cfg-gated artifacts are present.
</output>
