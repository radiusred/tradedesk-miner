---
phase: 07-hardening-benchmarks-reproducibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-fixture-cache.sh
  - crates/miner-bench/src/bin/gen-fixtures.rs
  - crates/miner-bench/Cargo.toml
  - tests/fixtures/cache/SHA256SUMS
  - tests/fixtures/cache/.gitkeep
  - README.md
  - .gitignore
autonomous: true
requirements:
  - CACHE-04
  - OUT-03

must_haves:
  truths:
    - "User can clone the repo, run `bash scripts/generate-fixture-cache.sh`, and produce a working tests/fixtures/cache directory whose bytes match the checked-in tests/fixtures/cache/SHA256SUMS."
    - "User can run the README quickstart command against the regenerated fixture cache and receive at least one Finding::Result envelope on stdout."
    - "User can verify no Dukascopy-licensed bytes are committed to the repo — the fixture cache is fully synthetic, deterministic-from-seed, and zstd-level-3 single-threaded byte-identical across machines."
  artifacts:
    - path: "scripts/generate-fixture-cache.sh"
      provides: "Shell wrapper invoking the Rust gen-fixtures binary"
      contains: "cargo run"
    - path: "crates/miner-bench/src/bin/gen-fixtures.rs"
      provides: "Rust generator producing synthetic Dukascopy-shape CSV.zst files for EURUSD + GBPUSD over 2024-01"
      contains: "fn main"
    - path: "tests/fixtures/cache/SHA256SUMS"
      provides: "Pinned hashes for every byte in tests/fixtures/cache/"
      contains: "EURUSD/2024/00/"
    - path: "README.md"
      provides: "Updated ## Example block using ./tests/fixtures/cache"
      contains: "./tests/fixtures/cache"
  key_links:
    - from: "scripts/generate-fixture-cache.sh"
      to: "crates/miner-bench/src/bin/gen-fixtures.rs"
      via: "cargo run --release -p miner-bench --bin gen-fixtures"
      pattern: "cargo run.*gen-fixtures"
    - from: "crates/miner-bench/src/bin/gen-fixtures.rs"
      to: "tests/fixtures/cache/"
      via: "zstd::stream::write::Encoder single-threaded at level 3"
      pattern: "Encoder::new.*3"
    - from: "tests/fixtures/cache/SHA256SUMS"
      to: "tests/fixtures/cache/EURUSD/2024/00/*.csv.zst"
      via: "sha256sum -c"
      pattern: "EURUSD/2024/00/"
---

<objective>
Deliver the clone-and-run quickstart artifact per D7-01 + ROADMAP success criterion #4. After this plan lands, a fresh `git clone` of the repo (with no external download, no real Dukascopy access) supports the README quickstart producing at least one `Finding::Result` envelope.

Three things ship:

1. A synthetic-stub fixture cache at `tests/fixtures/cache/` containing EURUSD + GBPUSD bid-side bytes for 2024-01 (one month each), in the Dukascopy `<SYMBOL>/<YYYY>/<MM 00-indexed>/<DD>_<side>.csv.zst` layout, ≤ 5 MB total compressed (D7-01 budget).
2. A deterministic Rust generator (`gen-fixtures` binary on `miner-bench`) plus a thin shell wrapper, both reproducible byte-for-byte across machines (zstd level 3 single-threaded per RESEARCH Pitfall 4, deterministic LCG closes per PATTERNS Pattern C).
3. A `tests/fixtures/cache/SHA256SUMS` checked-in alongside the bytes so `sha256sum -c` proves byte-identity on every clone.

The existing `## Example` block in README.md (lines 50-58) gets its `MINER_CACHE_ROOT=/path/to/cache` swapped for `MINER_CACHE_ROOT=./tests/fixtures/cache` per D7-01 quickstart, and the scan invocation changes to the documented `seas.bucket.hour_of_day@1` single-instrument quickstart.

Purpose: Forms the data foundation for the clone-and-run success criterion. Plan 07-05 (noise-replay) does NOT depend on this fixture (it uses in-memory synthetic series); Plan 07-08 (miner-bench) does NOT depend on this fixture (it runs against production data via `MINER_CACHE_ROOT` env var); Plan 07-07 (data_sources.md) references the synthetic-stub policy documented here. So this plan is correctly in Wave 1 with no in-phase dependents on its files except the README block (which 07-07 and 07-08 amend further in later waves).

Output: One new shell script, one new Rust binary, one updated crate manifest, one new fixture directory + SHA256SUMS, an updated README, and an extended .gitignore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-CONTEXT.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md
@crates/miner-core/tests/common/synthetic_cache.rs
@crates/miner-reader-dukascopy/src/path_layout.rs
@crates/miner-bench/Cargo.toml
@crates/miner-bench/src/main.rs
@scripts/install-git-hooks.sh
@README.md
@.gitignore

<interfaces>
<!-- Existing API the generator reuses (Pattern in PATTERNS §"tests/fixtures/cache/"): -->
<!-- crates/miner-reader-dukascopy/src/path_layout.rs:103 -->
pub fn day_csv_zst(cache_root: &Path, symbol: &str, date: NaiveDate, side: Side) -> PathBuf
<!-- Returns <root>/<SYMBOL>/<YYYY>/<MM 00-indexed>/<DD>_<bid|ask>.csv.zst -->

<!-- Existing in-memory pattern to mirror (crates/miner-core/tests/common/synthetic_cache.rs:86-107): -->
<!-- CSV header: "timestamp,open,high,low,close,volume\n" -->
<!-- Per-minute row: "{ts %Y-%m-%d %H:%M:%S%:z},{open},{high},{low},{close},{volume}\n" -->
<!-- Volume per row: (i + 1) as f64; high = close + 0.00005; low = close - 0.00005; open = close. -->

<!-- zstd determinism contract (RESEARCH Pitfall 4): -->
<!-- Single-threaded zstd::stream::write::Encoder::new(File::create(path)?, 3)?; -->
<!-- NEVER call .multithread(N). Must match tradedesk-dukascopy/export.py:442 producer settings. -->

<!-- Existing LCG (PATTERNS Pattern C, byte_identical_rerun.rs:74-83): -->
<!-- Numerical Recipes constants 1_664_525 + 1_013_904_223; cross-platform deterministic. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement gen-fixtures Rust binary + add it to miner-bench manifest</name>
  <files>crates/miner-bench/src/bin/gen-fixtures.rs, crates/miner-bench/Cargo.toml</files>
  <read_first>
    - crates/miner-core/tests/common/synthetic_cache.rs (full file — the existing in-memory synthetic CSV generator; this binary mirrors its CSV-row formatting + LCG close generation, but writes to a real filesystem path under `tests/fixtures/cache/`)
    - crates/miner-reader-dukascopy/src/path_layout.rs (lines around the `day_csv_zst` function — exact path-construction logic the generator MUST use; do NOT re-derive)
    - crates/miner-core/tests/byte_identical_rerun.rs (lines 74-83 for the canonical `lcg_closes` function — PATTERNS Pattern C; constants 1_664_525 and 1_013_904_223 are non-negotiable)
    - crates/miner-bench/Cargo.toml (current state — see PATTERNS §"Phase 7 replacement target" lines 712-740 for the eventual shape; THIS plan adds ONLY what's needed for gen-fixtures and leaves the rest for Plan 07-08)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md §"Pitfall 4" (zstd single-threaded level 3 determinism contract)
    - /home/darren/projects/radiusred/tradedesk-dukascopy/tradedesk_dukascopy/export.py (lines 310-320 for the volume `.sum()` semantic and around line 442 for `zstd.ZstdCompressor(level=3)` parity — confirms our single-threaded level-3 choice)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §"tests/fixtures/cache/" (full section)
  </read_first>
  <action>
    Create `crates/miner-bench/src/bin/gen-fixtures.rs`. SPDX header on lines 1-2 per PATTERNS Pattern B. Module-doc preamble explaining: this binary generates the synthetic Dukascopy-shape fixture cache for `tests/fixtures/cache/`; output is deterministic from seed; single-threaded zstd level 3 matches `tradedesk-dukascopy/export.py:442` for byte parity with the production producer.

    Generator behaviour:
    - Compute repo-root via `std::env::var("CARGO_MANIFEST_DIR")` then `..` twice (since the binary lives under `crates/miner-bench/src/bin/`). Output dir resolves to `<repo_root>/tests/fixtures/cache`.
    - Two symbols: `EURUSD` (seed `0xEUR0_USD0` — pick any literal `u64`; document the choice inline), `GBPUSD` (seed `0xGBP0_USD0` equivalent). Both bid side only.
    - Date range: 2024-01-01 .. 2024-01-31 inclusive. Skip non-trading days (Saturday `chrono::Weekday::Sat` and Sunday `chrono::Weekday::Sun`) — matches the realistic Dukascopy-cache shape where weekend files do not exist on disk. The aggregator + gap-manifest pipeline must observe this exact pattern (D7-01: "Weekend and exchange-holiday gaps are intentional, not missing data").
    - For each `(symbol, date)` trading day, generate 1440 one-minute bars: open=high=low=close = a deterministic float in `[1.0, 2.0]` per the `lcg_closes(1440, seed_per_day)` shape from PATTERNS Pattern C (per-day seed derived as `blake3::hash(format!("{symbol}-{date}").as_bytes())` truncated to `u64` so re-runs are byte-identical across machines — DO NOT use `rand::SmallRng` per RESEARCH Anti-Patterns). Volume per row = `(i + 1) as f64` (matches `synthetic_cache.rs:103`).
    - CSV header line MUST be `timestamp,open,high,low,close,volume\n` byte-identical to `synthetic_cache.rs:88`. Per-row formatting: `"{ts %Y-%m-%d %H:%M:%S%:z},{open},{high},{low},{close},{volume}\n"` using `chrono::DateTime<Utc>` and the `%:z` offset specifier — copy the exact format string from `synthetic_cache.rs:97-105`.
    - Path constructor: invoke `miner_reader_dukascopy::path_layout::day_csv_zst(&fixture_root, symbol, date, Side::Bid)`. Do NOT hand-roll the `<MM 00-indexed>` math — the existing function encapsulates the quirk and is boundary-tested per CACHE-04 ground truth.
    - Compression: `let mut enc = zstd::stream::write::Encoder::new(File::create(path)?, 3)?;` followed by `std::io::copy(&mut csv_bytes.as_slice(), &mut enc)?; enc.finish()?;`. NEVER call `.multithread(N)`. Per RESEARCH Pitfall 4 this is the canonical single-threaded level-3 producer.
    - Create intermediate directories via `std::fs::create_dir_all(path.parent().unwrap())?` before opening the file.
    - After writing ALL files, walk `tests/fixtures/cache/` in sorted order (per `walkdir::WalkDir::new(...).sort_by_file_name()`), compute SHA-256 per file (use `sha2::Sha256` — already a transitive workspace dep via blake3-adjacent crates, or add `sha2` as a `gen-fixtures`-only dep on miner-bench if not present), and emit `tests/fixtures/cache/SHA256SUMS` in `sha256sum`-compatible format (`<64-hex>  <path-relative-to-cache-root>\n` per line) sorted by path.
    - Use stderr for tracing logs (`tracing_subscriber::fmt().with_writer(std::io::stderr).init();` matches `crates/miner-bench/src/main.rs:9-13`).
    - NO `println!` (workspace `clippy::disallowed_macros` lint gate per PATTERNS Pattern I).
    - Emit a single summary JSON line to stdout via `serde_json::to_writer(std::io::stdout().lock(), &summary)?; writeln!(std::io::stdout().lock())?;` where summary is `{ "files_written": N, "total_bytes": M, "sha256sums_path": "tests/fixtures/cache/SHA256SUMS" }`.

    Modify `crates/miner-bench/Cargo.toml` (MINIMAL change in this plan — Plan 07-08 makes the full replacement). Add only the deps `gen-fixtures` needs that aren't already present:
    - Register the new binary: append `[[bin]]\nname = "gen-fixtures"\npath = "src/bin/gen-fixtures.rs"` after the existing `[[bin]] name = "miner-bench"` block.
    - In `[dependencies]`, add (only those missing): `miner-core.workspace = true`, `miner-reader-dukascopy.workspace = true`, `chrono.workspace = true`, `zstd.workspace = true`, `walkdir.workspace = true`, `sha2 = "0.10"` (NEW workspace consideration — add to root `[workspace.dependencies]` if not already there; the workspace already pulls `blake3` but sha2 is the canonical sha256sum-compatible crate. If blake3 already provides sha256 via a feature, use that — verify by reading `crates/miner-bench/Cargo.toml` after edit), `serde_json.workspace = true`, `anyhow.workspace = true`, `blake3.workspace = true` (for the per-day seed derivation).

    Verify by running `cargo build -p miner-bench --bin gen-fixtures` — must compile without warnings (workspace `-D warnings` lint will catch).
  </action>
  <acceptance_criteria>
    - `crates/miner-bench/src/bin/gen-fixtures.rs` exists.
    - File contains `zstd::stream::write::Encoder::new` with literal `3` as compression level: `grep -E 'Encoder::new\([^,]+,\s*3' crates/miner-bench/src/bin/gen-fixtures.rs` returns at least one match.
    - File does NOT contain `.multithread(`: `grep -c '\.multithread(' crates/miner-bench/src/bin/gen-fixtures.rs` returns 0 (RESEARCH Pitfall 4).
    - File invokes the canonical path constructor: `grep -c 'day_csv_zst' crates/miner-bench/src/bin/gen-fixtures.rs` returns at least 1.
    - File uses the canonical LCG constants: `grep -c '1_664_525' crates/miner-bench/src/bin/gen-fixtures.rs` returns at least 1, OR the file calls a function imported from miner-core that wraps those constants (PATTERNS Pattern C).
    - File does NOT contain `println!`: `grep -c 'println!' crates/miner-bench/src/bin/gen-fixtures.rs` returns 0.
    - `crates/miner-bench/Cargo.toml` contains `name = "gen-fixtures"`: `grep -c 'name = "gen-fixtures"' crates/miner-bench/Cargo.toml` returns 1.
    - `cargo build -p miner-bench --bin gen-fixtures` exits 0 with no warnings.
    - `cargo clippy -p miner-bench --bin gen-fixtures -- -D warnings` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; test -f crates/miner-bench/src/bin/gen-fixtures.rs; grep -qE "Encoder::new\([^,]+,\s*3" crates/miner-bench/src/bin/gen-fixtures.rs; [ "$(grep -c "\.multithread(" crates/miner-bench/src/bin/gen-fixtures.rs)" = "0" ]; grep -q "day_csv_zst" crates/miner-bench/src/bin/gen-fixtures.rs; [ "$(grep -c "println!" crates/miner-bench/src/bin/gen-fixtures.rs)" = "0" ]; cargo build -p miner-bench --bin gen-fixtures && cargo clippy -p miner-bench --bin gen-fixtures -- -D warnings'</automated>
  </verify>
  <done>gen-fixtures.rs compiles cleanly under workspace lint gates; uses single-threaded zstd level 3 + canonical path constructor + canonical LCG constants; emits no stdout other than the structured summary line.</done>
</task>

<task type="auto">
  <name>Task 2: Write scripts/generate-fixture-cache.sh + run it + commit the resulting fixture bytes + SHA256SUMS + .gitignore</name>
  <files>scripts/generate-fixture-cache.sh, tests/fixtures/cache/SHA256SUMS, tests/fixtures/cache/.gitkeep, .gitignore</files>
  <read_first>
    - scripts/install-git-hooks.sh (canonical shell-script template per PATTERNS Pattern F)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §"scripts/generate-fixture-cache.sh" + §"Pattern B" (SPDX) + §"Pattern F" (shell template)
    - .gitignore (existing rules — ensure no overlap)
    - The output of Task 1: `crates/miner-bench/src/bin/gen-fixtures.rs`
  </read_first>
  <action>
    Create `scripts/generate-fixture-cache.sh` (mode 0755). Line 1: `#!/usr/bin/env bash`. Lines 2-3: SPDX header per Pattern B. Body block: header comment explaining purpose ("Regenerate the synthetic Dukascopy-shape fixture cache at tests/fixtures/cache/ from a deterministic seed; output is byte-identical across machines per RESEARCH Pitfall 4."). Then:
    - `set -euo pipefail`
    - `REPO_ROOT="$(git rev-parse --show-toplevel)"; cd "$REPO_ROOT"`
    - `rm -rf tests/fixtures/cache/EURUSD tests/fixtures/cache/GBPUSD` (clean state — preserves `.gitkeep` and `SHA256SUMS` files; the next steps overwrite SHA256SUMS)
    - `cargo run --release -p miner-bench --bin gen-fixtures` — the binary writes the bytes AND the SHA256SUMS file itself per Task 1.
    - `( cd tests/fixtures/cache && sha256sum -c SHA256SUMS )` — verify the bytes the generator just wrote match the SHA256SUMS it wrote alongside them. This is the round-trip determinism check.
    - Final echo: `echo "[generate-fixture-cache] OK — synthetic cache regenerated and SHA256SUMS verified."`

    Run the script. After it completes:
    - `tests/fixtures/cache/EURUSD/2024/00/01_bid.csv.zst` through `31_bid.csv.zst` exist (minus weekend days — Jan 6+7, 13+14, 20+21, 27+28 in 2024 are weekends so are absent; that's 31 - 8 = 23 weekday files per symbol).
    - `tests/fixtures/cache/GBPUSD/2024/00/` has the same 23 files.
    - `tests/fixtures/cache/SHA256SUMS` is a 46-line file (23 EURUSD + 23 GBPUSD).
    - Total compressed size: `du -sb tests/fixtures/cache | awk '{print $1}'` returns a value ≤ 5,242,880 bytes (5 MiB — D7-01 budget).
    - Add an empty `tests/fixtures/cache/.gitkeep` so the empty cache dir is checked in even before the generator runs (so a fresh clone can also run `scripts/generate-fixture-cache.sh` from an empty fixture dir).

    Edit `.gitignore`: ensure `tests/fixtures/cache/` itself is NOT gitignored (the bytes are committed). However, add an explicit rule to KEEP `tests/fixtures/cache/SHA256SUMS` and `tests/fixtures/cache/**/*.csv.zst` committed and only ignore any scratch artifacts (e.g., `*.tmp`, `*.bak` inside that dir if any). If `.gitignore` already lacks any rule that would mask `tests/fixtures/cache/`, no edit is needed beyond verification — only add an explicit note comment if a future-developer might mistakenly add `tests/` to `.gitignore`.
  </action>
  <acceptance_criteria>
    - `scripts/generate-fixture-cache.sh` exists, is executable.
    - First three lines contain `#!/usr/bin/env bash`, SPDX header, and `set -euo pipefail` somewhere in the first 12 lines.
    - `grep -c 'cargo run --release -p miner-bench --bin gen-fixtures' scripts/generate-fixture-cache.sh` returns 1.
    - `grep -c 'sha256sum -c SHA256SUMS' scripts/generate-fixture-cache.sh` returns 1.
    - `bash scripts/generate-fixture-cache.sh` exits 0.
    - `find tests/fixtures/cache/EURUSD/2024/00 -name '*_bid.csv.zst' | wc -l` returns 23 (Jan 2024 weekdays).
    - `find tests/fixtures/cache/GBPUSD/2024/00 -name '*_bid.csv.zst' | wc -l` returns 23.
    - `test -f tests/fixtures/cache/SHA256SUMS` and `wc -l < tests/fixtures/cache/SHA256SUMS` returns 46.
    - `( cd tests/fixtures/cache && sha256sum -c SHA256SUMS )` exits 0 (round-trip determinism).
    - `du -sb tests/fixtures/cache | awk '{print $1}'` returns a value ≤ 5242880.
    - Running `bash scripts/generate-fixture-cache.sh` a SECOND time produces no changes to git status: `bash scripts/generate-fixture-cache.sh && git status --porcelain tests/fixtures/cache/` returns empty output (idempotency).
    - `tests/fixtures/cache/.gitkeep` exists.
    - No `.csv.zst` file under `tests/fixtures/cache/` is gitignored: `git check-ignore tests/fixtures/cache/EURUSD/2024/00/01_bid.csv.zst` exits with a non-zero status.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; test -x scripts/generate-fixture-cache.sh; grep -q "cargo run --release -p miner-bench --bin gen-fixtures" scripts/generate-fixture-cache.sh; grep -q "sha256sum -c SHA256SUMS" scripts/generate-fixture-cache.sh; bash scripts/generate-fixture-cache.sh && [ "$(find tests/fixtures/cache/EURUSD/2024/00 -name "*_bid.csv.zst" | wc -l)" = "23" ] && [ "$(find tests/fixtures/cache/GBPUSD/2024/00 -name "*_bid.csv.zst" | wc -l)" = "23" ] && [ "$(wc -l < tests/fixtures/cache/SHA256SUMS)" = "46" ] && ( cd tests/fixtures/cache && sha256sum -c SHA256SUMS ) >/dev/null && [ "$(du -sb tests/fixtures/cache | awk "{print \$1}")" -le 5242880 ] && bash scripts/generate-fixture-cache.sh >/dev/null && [ -z "$(git status --porcelain tests/fixtures/cache/)" ]'</automated>
  </verify>
  <done>scripts/generate-fixture-cache.sh produces a 46-file fixture cache under 5 MiB; SHA256SUMS verifies; second run produces zero git diffs.</done>
</task>

<task type="auto">
  <name>Task 3: Swap README ## Example block + add "If you cloned the repo" line + verify quickstart produces ≥1 Finding::Result envelope</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 50-58 for the current `## Example` block — exact text per PATTERNS §"README.md")
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-CONTEXT.md §D7-01 (chosen quickstart command + the "If you cloned the repo, this works as-is" sentence wording)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §"README.md" (the two edits to apply)
  </read_first>
  <action>
    Locate the `## Example` block in README.md. The current code block contains:
    ```
    MINER_CACHE_ROOT=/path/to/cache \
    MINER_BAR_CACHE_ROOT=/tmp/bar \
    MINER_OUTPUT=stdout \
    cargo run -p miner-cli -- scan stats.autocorr.ljung_box@1 \
        --instrument EURUSD:bid --timeframe 15m \
        --window 2024-01-01:2024-12-31
    ```
    Replace it with the D7-01-locked quickstart:
    ```
    MINER_CACHE_ROOT=./tests/fixtures/cache \
    MINER_BAR_CACHE_ROOT=/tmp/bar \
    MINER_OUTPUT=stdout \
    cargo run -p miner-cli -- scan seas.bucket.hour_of_day@1 \
        --instrument EURUSD:bid --timeframe 15m \
        --window 2024-01-01:2024-01-31
    ```
    Immediately below the code block (and before any pre-existing trailing text in the `## Example` section), insert exactly the sentence from D7-01: `If you cloned the repo, this works as-is — no external download needed.`

    Do NOT add the `## Data source caveats` section here — Plan 07-07 owns that addition (it places the section in a wave that runs after this plan). Do NOT add the `## Performance` pointer — Plan 07-08 owns that.

    Run the quickstart end-to-end as the verification step:
    ```
    bash scripts/generate-fixture-cache.sh   # ensure fixture cache is fresh
    MINER_CACHE_ROOT=./tests/fixtures/cache MINER_BAR_CACHE_ROOT=/tmp/bar MINER_OUTPUT=stdout \
      cargo run --release -p miner-cli -- scan seas.bucket.hour_of_day@1 \
        --instrument EURUSD:bid --timeframe 15m \
        --window 2024-01-01:2024-01-31 \
        2>/tmp/miner-stderr.log | tee /tmp/miner-stdout.log
    ```
    Assert that `/tmp/miner-stdout.log` contains at least one line with the substring `"kind":"Finding::Result"` (or equivalent — check the canonical envelope `kind` discriminator string by reading `crates/miner-core/src/types.rs` or any existing test that asserts on the Result variant; the existing emit-fixture test at `crates/miner-cli/tests/cli_streams.rs` is a reliable source).
  </action>
  <acceptance_criteria>
    - `grep -c 'MINER_CACHE_ROOT=./tests/fixtures/cache' README.md` returns 1.
    - `grep -c 'seas.bucket.hour_of_day@1' README.md` returns 1.
    - `grep -c '2024-01-01:2024-01-31' README.md` returns 1.
    - `grep -c 'MINER_CACHE_ROOT=/path/to/cache' README.md` returns 0 (old path removed).
    - `grep -c 'stats.autocorr.ljung_box@1' README.md` returns 0 (old scan removed from Example block; if it appears elsewhere in README that's fine — re-grep against just the Example block lines).
    - `grep -c 'If you cloned the repo' README.md` returns 1.
    - End-to-end quickstart produces a Result finding: `MINER_CACHE_ROOT=./tests/fixtures/cache MINER_BAR_CACHE_ROOT=/tmp/bar MINER_OUTPUT=stdout cargo run --release -p miner-cli -- scan seas.bucket.hour_of_day@1 --instrument EURUSD:bid --timeframe 15m --window 2024-01-01:2024-01-31 2>/dev/null | grep -c 'Finding::Result\|"kind":"Result"'` returns at least 1.
    - `cargo run --release -p miner-cli -- scan seas.bucket.hour_of_day@1 ...` exits with status 0 (no scan errors).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; grep -q "MINER_CACHE_ROOT=./tests/fixtures/cache" README.md; grep -q "seas.bucket.hour_of_day@1" README.md; grep -q "2024-01-01:2024-01-31" README.md; ! grep -q "MINER_CACHE_ROOT=/path/to/cache" README.md; grep -q "If you cloned the repo" README.md; OUT=$(MINER_CACHE_ROOT=./tests/fixtures/cache MINER_BAR_CACHE_ROOT=/tmp/bar MINER_OUTPUT=stdout cargo run --release -p miner-cli -- scan seas.bucket.hour_of_day@1 --instrument EURUSD:bid --timeframe 15m --window 2024-01-01:2024-01-31 2>/dev/null); echo "$OUT" | grep -qE "Finding::Result|\"kind\":\"Result\"" || { echo "NO Result finding in stdout"; exit 1; }'</automated>
  </verify>
  <done>README ## Example block now references ./tests/fixtures/cache + seas.bucket.hour_of_day@1; clone-and-run quickstart produces ≥1 Finding::Result envelope against the synthetic fixture cache.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → public-facing fixture cache | The committed fixture bytes are world-readable; they MUST contain zero Dukascopy-licensed bytes. |
| generator binary → repo filesystem | Determinism is the trust contract — non-deterministic output breaks the SHA256SUMS gate downstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Compliance + Reputation | Dukascopy-licensed bytes committed to public repo | mitigate | gen-fixtures uses deterministic LCG-derived synthetic OHLCV; no network access; no real-Dukascopy reader paths. PATTERNS §"tests/fixtures/cache/" enforces synthetic-stub posture. |
| T-07-02-02 | Tampering (drift) | Synthetic-stub fixture-cache generator non-deterministic across machines | mitigate | `tests/fixtures/cache/SHA256SUMS` checked in; generator's invocation reproducible from seed + pinned zstd level 3 single-threaded (RESEARCH Pitfall 4); Task 2 verifies via second invocation producing zero git diffs. |
| T-07-02-03 | Tampering | Generator output drift on Rust toolchain version bump | accept | LCG constants (Numerical Recipes) are byte-stable across rustc versions; zstd 0.13 byte-format is locked. If a future toolchain changes float formatting we'd see a SHA256SUMS mismatch in CI and regenerate. |
</threat_model>

<verification>
- `cargo test --workspace --no-fail-fast` does not regress.
- `cargo clippy --workspace --all-targets -- -D warnings` passes.
- `( cd tests/fixtures/cache && sha256sum -c SHA256SUMS )` exits 0.
- `bash scripts/generate-fixture-cache.sh && git status --porcelain tests/fixtures/cache/` returns empty (idempotency).
- Clone-and-run quickstart from README produces ≥1 Finding::Result envelope.
- `du -sb tests/fixtures/cache` reports ≤ 5 MiB.
</verification>

<success_criteria>
1. `tests/fixtures/cache/` contains a deterministic, byte-identical synthetic cache mirroring the Dukascopy layout.
2. `scripts/generate-fixture-cache.sh` regenerates the cache byte-for-byte from seed (idempotent).
3. SHA256SUMS gates the byte-identity of the cache.
4. README quickstart produces a real finding against the cache (proof of CACHE-04 + OUT-03 closure).
5. No Dukascopy-licensed bytes are committed.
</success_criteria>

<output>
Create `.planning/phases/07-hardening-benchmarks-reproducibility/07-02-SUMMARY.md` when done.
</output>
