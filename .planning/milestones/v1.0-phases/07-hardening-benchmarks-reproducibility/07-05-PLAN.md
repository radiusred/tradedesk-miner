---
phase: 07-hardening-benchmarks-reproducibility
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/miner-core/Cargo.toml
  - crates/miner-core/src/scan/hygiene/null.rs
  - crates/miner-core/src/scan/ljung_box/mod.rs
  - crates/miner-core/src/scan/anom/ljung_box_sq/mod.rs
  - crates/miner-core/src/scan/anom/variance_ratio/mod.rs
  - crates/miner-core/src/scan/cross/lead_lag/mod.rs
  - crates/miner-core/src/scan/cross/engle_granger/mod.rs
  - crates/miner-core/tests/noise_replay_regression.rs
autonomous: true
requirements:
  - FOUND-04
  - HYG-02
  - HYG-05

must_haves:
  truths:
    - "User can request `null = \"iaaft\"` on the five scans named in the Plan 05-02 matrix and receive a real phase-scrambled p-value (no `PreflightCode::HygieneNotSupported` error)."
    - "User can run the noise-replay regression test and observe ≤30 false-positive findings at α=0.05 across 300 synthetic-null jobs, proving BH-FDR controls multiple-testing."
    - "User can re-run the noise-replay test twice with the same seed and produce byte-identical SweepSummary output (HYG-05 reproducibility)."
    - "User can verify miner-core stays tokio-free even after adding realfft via `cargo tree -p miner-core --edges normal,build | grep -E 'tokio|async'` returning empty."
  artifacts:
    - path: "crates/miner-core/src/scan/hygiene/null.rs"
      provides: "iaaft_phase_scramble_null_p sibling to circular_shift_null_p"
      contains: "pub fn iaaft_phase_scramble_null_p"
    - path: "crates/miner-core/tests/noise_replay_regression.rs"
      provides: "300-job synthetic-null sweep regression test"
      contains: "fn noise_replay"
    - path: "Cargo.toml"
      provides: "realfft = \"3.5\" in workspace dependencies"
      contains: "realfft"
    - path: "crates/miner-core/Cargo.toml"
      provides: "realfft.workspace = true in [dependencies]"
      contains: "realfft.workspace = true"
  key_links:
    - from: "crates/miner-core/src/scan/hygiene/null.rs"
      to: "realfft::RealFftPlanner"
      via: "FFT for phase randomisation"
      pattern: "RealFftPlanner"
    - from: "crates/miner-core/tests/noise_replay_regression.rs"
      to: "crates/miner-core/src/sweep/executor.rs"
      via: "run_sweep with 300 jobs"
      pattern: "run_sweep"
    - from: "5 scan mod.rs files"
      to: "crates/miner-core/src/scan/hygiene/null.rs"
      via: "supports_null_method(PhaseScramble) returns true"
      pattern: "PhaseScramble.*true"
---

<objective>
Close the largest non-doc verification-debt item in Phase 7: the IAAFT phase-scramble kernel that Plan 05-02-SUMMARY explicitly deferred to Phase 7 (line 167 of that SUMMARY pins `realfft = "3"` + max-iter 10 + rank-distance convergence). After this plan lands:

1. `iaaft_phase_scramble_null_p` is a sibling function to `circular_shift_null_p` in `crates/miner-core/src/scan/hygiene/null.rs`, mirroring its positional signature contract for byte-identical-rerun parity.
2. The five scans documented in 07-RESEARCH.md Pattern 4 ("Scans that should enable PhaseScramble after IAAFT lands") flip `Scan::supports_null_method(NullMethod::PhaseScramble)` from `false` to `true`.
3. The noise-replay regression test (D7-04 — 300 jobs across 100 GBM-seeded synthetic instruments × 3 scans at α=0.05) lands at `crates/miner-core/tests/noise_replay_regression.rs` and asserts `false_positive_count <= 30` (Wilson 99% upper bound on binomial(300, 0.05) is ~28; safety margin 30 per D7-04) AND byte-identical SweepSummary across two runs with the same seed.

The miner-core tokio-free invariant (FOUND-04) is asserted in the acceptance criteria — `realfft` + `rustfft` are sync-only per RESEARCH §"Standard Stack" verification, but the plan re-checks `cargo tree -p miner-core --edges normal,build` in situ as a hard gate.

Purpose: Closes HYG-02 (BH-FDR proven on a synthetic null) + HYG-05 (bit-for-bit reproducibility proven on a sweep with seed-echoing). Adds the genuinely-new code (IAAFT kernel + noise-replay test driver) that the other Phase 7 plans are verification glue around.

Output: One new test file; one extended kernel file; two workspace manifest edits; five scan-trait `mod.rs` edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-CONTEXT.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md
@.planning/phases/05-statistical-hygiene-sweep-runner/05-02-SUMMARY.md
@crates/miner-core/src/scan/hygiene/null.rs
@crates/miner-core/src/scan/ljung_box/mod.rs
@crates/miner-core/src/scan/anom/ljung_box_sq/mod.rs
@crates/miner-core/src/scan/anom/variance_ratio/mod.rs
@crates/miner-core/src/scan/cross/lead_lag/mod.rs
@crates/miner-core/src/scan/cross/engle_granger/mod.rs
@crates/miner-core/src/sweep/manifest.rs
@crates/miner-core/src/sweep/executor.rs
@crates/miner-core/tests/sweep_byte_identical_rerun.rs
@crates/miner-core/tests/byte_identical_rerun.rs
@crates/miner-core/tests/common/mod.rs
@crates/miner-core/tests/common/synthetic_cache.rs

<interfaces>
<!-- Sibling pattern (PATTERNS Pattern 4 + the canonical signature from null.rs:89-138): -->
pub fn circular_shift_null_p<F>(
    values: &[f64],
    observed_stat: f64,
    stat: F,
    n_resamples: u32,
    seed: u64,
    tail: Tail,
    cancel: &AtomicBool,
) -> f64
where F: Fn(&[f64]) -> f64

<!-- IAAFT shape (PATTERNS §"hygiene/null.rs" + RESEARCH §"Pattern 4"): -->
pub fn iaaft_phase_scramble_null_p<F>(
    values: &[f64],
    observed_stat: f64,
    stat: F,
    n_resamples: u32,
    seed: u64,
    tail: Tail,
    cancel: &AtomicBool,
    max_iter: u32,           // default callsite passes 10
    convergence_tol: f64,    // rank-distance tolerance, ≤ 1 for IEEE-754 inputs
) -> f64
where F: Fn(&[f64]) -> f64

<!-- Constants from circular_shift_null_p that IAAFT MUST mirror: -->
const HYGIENE_RESAMPLE_CEILING: u32 = /* from crate::engine */;  // clamp n_resamples
const BOOTSTRAP_CANCEL_POLL_CADENCE: u32 = /* from crate::engine */;  // cancel-poll interval
// P-value floor: (1.0 + more_extreme as f64) / (1.0 + n_resamples as f64) per Davison & Hinkley 1997 §4.2

<!-- realfft 3.5 API (RESEARCH §"Standard Stack" + §"Pitfall 7"): -->
use realfft::RealFftPlanner;
let mut planner = RealFftPlanner::<f64>::new();
let r2c = planner.plan_fft_forward(n_padded);  // padded to next 5-smooth length
let c2r = planner.plan_fft_inverse(n_padded);
// r2c.process(&mut input, &mut spectrum)?;
// c2r.process(&mut spectrum, &mut output)?;
// output values are NOT normalised — divide by n_padded after inverse.

<!-- Five sites flipping supports_null_method (PATTERNS §"hygiene/null.rs" + RESEARCH Pattern 4): -->
//  crates/miner-core/src/scan/ljung_box/mod.rs:118-122
//  crates/miner-core/src/scan/anom/ljung_box_sq/mod.rs:111-115
//  crates/miner-core/src/scan/anom/variance_ratio/mod.rs:109-113
//  crates/miner-core/src/scan/cross/lead_lag/mod.rs (planner verifies the line number)
//  crates/miner-core/src/scan/cross/engle_granger/mod.rs (planner verifies)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add realfft workspace dep + implement iaaft_phase_scramble_null_p kernel + behaviour tests</name>
  <files>Cargo.toml, crates/miner-core/Cargo.toml, crates/miner-core/src/scan/hygiene/null.rs</files>
  <read_first>
    - crates/miner-core/src/scan/hygiene/null.rs (full file — the existing `circular_shift_null_p` is the sibling pattern; the module-level header doc at lines 9-19 names IAAFT as DEFERRED to Phase 7 and MUST be edited to delete the deferral note)
    - .planning/phases/05-statistical-hygiene-sweep-runner/05-02-SUMMARY.md (lines 157-181 + 235 — IAAFT design pin: realfft = "3", max-iter 10, rank-distance convergence)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md §"Pattern 4" (algorithm sketch + critical pitfalls) + §"Pitfall 7" (FFT-length padding regression — 5-smooth padding helper)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §"crates/miner-core/src/scan/hygiene/null.rs" (signature contract)
    - https://docs.rs/realfft/3.5.0/realfft/ (real-input FFT API surface)
    - Cargo.toml (workspace root — see PATTERNS §"Cargo.toml workspace (MODIFY)" for the comment-then-declare convention with Phase markers at lines 64-71)
    - crates/miner-core/Cargo.toml (current state — Phase 4/5 additions blocks at lines 53-66)
  </read_first>
  <behavior>
    - Test 1 (`iaaft_returns_nan_for_short_input`): n < 4 returns NaN (mirrors circular_shift_null_p's n < 2 floor; IAAFT needs at least 4 samples for a meaningful FFT). Use `assert!(iaaft_phase_scramble_null_p(&[1.0, 2.0, 3.0], 0.0, |x| x.len() as f64, 100, 42, Tail::Right, &AtomicBool::new(false), 10, 1.0).is_nan());`
    - Test 2 (`iaaft_returns_nan_for_zero_resamples`): n_resamples == 0 returns NaN.
    - Test 3 (`iaaft_floor_is_one_over_resamples_plus_one`): For an observed-stat so extreme no resample exceeds it (e.g., observed_stat = f64::INFINITY, stat = |x| x.iter().sum()), the returned p-value equals `(0.0 + 1.0) / (n_resamples as f64 + 1.0)` exactly (the Davison & Hinkley floor; same as circular_shift_null_p line 137).
    - Test 4 (`iaaft_byte_identical_across_runs_with_same_seed`): Two calls with identical inputs (including the same seed) produce identical f64 outputs (`to_bits()` equality, not approximate). This is the HYG-05 contract.
    - Test 5 (`iaaft_marginal_preserved`): Surrogate series (generated INSIDE the kernel during randomisation, before stat() is called) preserves the marginal distribution of the input. The test inserts a stat function that captures the surrogates via interior mutability, then asserts `sort(surrogate) == sort(input)` for at least one surrogate. (This is the defining IAAFT property — preserve marginal AND power spectrum.)
    - Test 6 (`iaaft_uses_stable_rank_shuffle`): Insert input with tied values (e.g., `[1.0, 1.0, 2.0, 2.0, 3.0, 3.0]`) and assert that two runs with the same seed produce identical surrogates (the stable-rank-shuffle anti-pattern check from RESEARCH §"Anti-Patterns": `sort_unstable` would produce non-deterministic output on ties).
    - Test 7 (`iaaft_cancel_aborts_resampling`): Set the cancel `AtomicBool` to `true` BEFORE invocation. The kernel returns NaN (or a sentinel — match what `circular_shift_null_p` does on early cancel; read its code).
    - Test 8 (`iaaft_padding_uses_5_smooth_length`): A direct unit test against the helper `next_5_smooth` (added to the same module): `next_5_smooth(100_000) == 100_000` (already 5-smooth: 2^5 * 5^5 * ... — wait, verify; if the assertion fails, document the correct expected value and update the assert). `next_5_smooth(1009)` returns 1024 (next 5-smooth above the prime 1009).
  </behavior>
  <action>
    PART A — workspace manifest.

    Edit `Cargo.toml` (workspace root). After the existing Phase 5 additions block (around lines 64-71 per RESEARCH §"Pattern excerpt (existing convention)"), append a new Phase 7 additions block following the same comment-then-declare convention. Comment lines explain: criterion/dhat additions are owned by Plan 07-06; THIS plan adds `realfft = "3.5"` ONLY. The block:

    ```toml
    # Phase 7 (Plan 07-05) additions per 07-RESEARCH.md §"Standard Stack":
    # - `realfft` 3.5 — real-input FFT for IAAFT phase-scramble (Plan 05-02 deferred).
    #   Transitively pulls rustfft 6.x; both verified sync-only via crates.io README inspection
    #   and `cargo tree -p miner-core --edges normal,build` post-add.
    realfft = "3.5"
    ```

    Do NOT add `criterion`, `dhat`, or `[profile.release] debug = 1` here — Plan 07-06 owns those (this avoids file conflicts within Wave 2: Plan 07-05 and Plan 07-06 both edit Cargo.toml, so the sub-edits MUST be additive and clearly separated).

    PART B — miner-core manifest.

    Edit `crates/miner-core/Cargo.toml`. In the `[dependencies]` table, add `realfft.workspace = true` next to the existing Phase 5 additions. Update the inline comment to remove any "DEFERRED to Phase 7" note that called out the intentional exclusion of realfft.

    PART C — IAAFT kernel.

    Edit `crates/miner-core/src/scan/hygiene/null.rs`:
    1. Delete the module-level deferral note at lines 9-19 (RESEARCH §"Pattern 4 / Implementation site"). Replace it with a paragraph saying IAAFT now lives in this module as a sibling to `circular_shift_null_p`.
    2. Add a `next_5_smooth(n: usize) -> usize` helper (RESEARCH §"Pitfall 7" — exact algorithm; place it as a non-public function or `pub(super)` so other hygiene modules can reuse).
    3. Add the IAAFT kernel `pub fn iaaft_phase_scramble_null_p<F>(...) -> f64` with the signature from `<interfaces>` above. Implementation contract:
       - Edge cases: n < 4 -> NaN, n_resamples == 0 -> NaN. (Mirror circular_shift_null_p, but raise n floor from 2 to 4.)
       - RNG: `Xoshiro256PlusPlus::seed_from_u64(seed)` (matches circular_shift_null_p line 108).
       - Clamp `n_resamples = n_resamples.min(crate::engine::HYGIENE_RESAMPLE_CEILING)`.
       - Pre-loop setup (computed once, outside the resample loop):
         - `n_padded = next_5_smooth(n)`.
         - Build `realfft::RealFftPlanner::<f64>::new()` once.
         - FFT-forward the (zero-padded to n_padded) input series to get target amplitudes |X(f)|.
         - Compute the sorted rank order of the input series with a stable sort (RESEARCH §"Anti-Patterns" — `sort_by` with `(idx, val)` tuple + index tiebreaker; NEVER `sort_unstable`).
       - Per-resample loop (n_resamples iterations):
         - Generate uniform-random phases φ(f) ~ U(0, 2π) via the seeded RNG.
         - Construct spectrum X(f) = |X_target(f)| * exp(i*φ(f)); ensure Hermitian symmetry for real-input inverse FFT (realfft handles this automatically for the real-output API).
         - Iterative correction loop (max `max_iter` iterations):
           - Inverse FFT to time domain, divide by n_padded (realfft does not normalise).
           - Rank-shuffle: replace each surrogate value with the input value of matching rank (preserves marginal).
           - Forward FFT, replace amplitudes with target amplitudes (preserves spectrum), keep new phases.
           - Convergence test: rank-distance from previous iteration < `convergence_tol` (use ≤ 1 for IEEE-754 tolerance per RESEARCH §"Pattern 4" + §"Anti-Patterns").
         - Truncate surrogate to original length `n` (drop padding).
         - Compute `stat(&surrogate)`; if compares more-extreme than `observed_stat` per `tail`, increment `more_extreme` counter.
         - Every `BOOTSTRAP_CANCEL_POLL_CADENCE` resamples: check `cancel.load(Ordering::Relaxed)`; if true, return NaN immediately (match circular_shift_null_p's cancel behaviour exactly — read it from the source).
       - Return `(more_extreme as f64 + 1.0) / (n_resamples as f64 + 1.0)` (Davison & Hinkley floor).
    4. Add the 8 unit tests from `<behavior>` above as a `#[cfg(test)] mod tests` block at the bottom of `null.rs` (or extend the existing one if present).

    Run `cargo build -p miner-core` — must compile with zero warnings (`-D warnings` lint). Run `cargo test -p miner-core scan::hygiene::null` — all 8 tests pass.

    HARD GATE — FOUND-04: After adding realfft, run the same check the CI gate runs (RESEARCH §"Existing CI workflow tokio-free check" + .github/workflows/ci.yml:60-72):
    ```
    PROHIBITED='^(tokio|tokio-[^ ]+|async-std|async-std-[^ ]+|smol|smol-[^ ]+|async-trait|async-io|async-channel|async-executor|async-task)$'
    LEAKED=$(cargo tree -p miner-core --edges normal,build --prefix none | awk '{print $1}' | sort -u | grep -E "$PROHIBITED" || true)
    [ -z "$LEAKED" ]
    ```
    Must exit 0 (no leaked async-runtime crates). If realfft transitively pulls anything matching the prohibited regex, STOP — the plan's assumption A10 (RESEARCH §"Assumptions Log") is invalidated and the issue must be surfaced in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `Cargo.toml` workspace contains `realfft = "3.5"`: `grep -cE '^\s*realfft\s*=\s*"3\.5"' Cargo.toml` returns 1.
    - `crates/miner-core/Cargo.toml` contains `realfft.workspace = true`: `grep -c 'realfft\.workspace\s*=\s*true' crates/miner-core/Cargo.toml` returns 1.
    - `crates/miner-core/src/scan/hygiene/null.rs` contains `pub fn iaaft_phase_scramble_null_p`: `grep -c 'pub fn iaaft_phase_scramble_null_p' crates/miner-core/src/scan/hygiene/null.rs` returns 1.
    - The deferral note at the module head is removed: `grep -c 'DEFERRED to Phase 7' crates/miner-core/src/scan/hygiene/null.rs` returns 0.
    - The kernel uses stable sort: `grep -c 'sort_unstable' crates/miner-core/src/scan/hygiene/null.rs` returns 0 (anti-pattern check).
    - `next_5_smooth` helper exists: `grep -c 'fn next_5_smooth' crates/miner-core/src/scan/hygiene/null.rs` returns 1.
    - 8 unit tests are present: `grep -c '#\[test\]' crates/miner-core/src/scan/hygiene/null.rs` returns at least 8 plus whatever already existed (record the pre-edit baseline; post-edit count must be baseline + 8).
    - Build clean: `cargo build -p miner-core` exits 0 with no warnings.
    - Unit tests pass: `cargo test -p miner-core scan::hygiene::null` exits 0.
    - FOUND-04 holds: `bash -c 'PROHIBITED='\''^(tokio|tokio-[^ ]+|async-std|async-std-[^ ]+|smol|smol-[^ ]+|async-trait|async-io|async-channel|async-executor|async-task)$'\''; LEAKED=$(cargo tree -p miner-core --edges normal,build --prefix none | awk "{print \$1}" | sort -u | grep -E "$PROHIBITED" || true); [ -z "$LEAKED" ]'` exits 0.
    - Clippy clean: `cargo clippy -p miner-core --lib -- -D warnings` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; grep -qE "^\s*realfft\s*=\s*\"3\.5\"" Cargo.toml; grep -q "realfft.workspace = true" crates/miner-core/Cargo.toml; grep -q "pub fn iaaft_phase_scramble_null_p" crates/miner-core/src/scan/hygiene/null.rs; [ "$(grep -c "DEFERRED to Phase 7" crates/miner-core/src/scan/hygiene/null.rs)" = "0" ]; [ "$(grep -c "sort_unstable" crates/miner-core/src/scan/hygiene/null.rs)" = "0" ]; grep -q "fn next_5_smooth" crates/miner-core/src/scan/hygiene/null.rs; cargo build -p miner-core; cargo test -p miner-core scan::hygiene::null; PROHIBITED="^(tokio|tokio-[^ ]+|async-std|async-std-[^ ]+|smol|smol-[^ ]+|async-trait|async-io|async-channel|async-executor|async-task)$"; LEAKED=$(cargo tree -p miner-core --edges normal,build --prefix none | awk "{print \$1}" | sort -u | grep -E "$PROHIBITED" || true); [ -z "$LEAKED" ] || { echo "FOUND-04 BROKEN: $LEAKED"; exit 1; }; cargo clippy -p miner-core --lib -- -D warnings'</automated>
  </verify>
  <done>realfft added to workspace + miner-core; iaaft_phase_scramble_null_p kernel implemented with 8 behaviour tests passing; module-level deferral note removed; FOUND-04 tokio-free invariant preserved; clippy/build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Flip supports_null_method(PhaseScramble) -> true on the five scans + add scan-trait wiring tests</name>
  <files>crates/miner-core/src/scan/ljung_box/mod.rs, crates/miner-core/src/scan/anom/ljung_box_sq/mod.rs, crates/miner-core/src/scan/anom/variance_ratio/mod.rs, crates/miner-core/src/scan/cross/lead_lag/mod.rs, crates/miner-core/src/scan/cross/engle_granger/mod.rs</files>
  <read_first>
    - crates/miner-core/src/scan/ljung_box/mod.rs (lines 115-125 — current `supports_null_method` impl declares PhaseScramble | CircularShift but body returns false for PhaseScramble per the Plan 05-02 deferral)
    - crates/miner-core/src/scan/anom/ljung_box_sq/mod.rs (lines 108-118 — same pattern)
    - crates/miner-core/src/scan/anom/variance_ratio/mod.rs (lines 106-116 — same pattern)
    - crates/miner-core/src/scan/cross/lead_lag/mod.rs (planner finds the supports_null_method impl by grepping for `supports_null_method` in that file)
    - crates/miner-core/src/scan/cross/engle_granger/mod.rs (same — grep for the impl)
    - crates/miner-core/src/scan/hygiene/null.rs (the kernel from Task 1 — its signature is what the scan-trait method now declares as supported)
    - crates/miner-core/src/types/scan.rs OR crates/miner-core/src/types.rs (wherever `NullMethod` enum is defined; locate via `grep -rn 'enum NullMethod' crates/miner-core/src/`)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §"crates/miner-core/src/scan/hygiene/null.rs" — the bullet list of "Scans that should enable PhaseScramble after IAAFT lands"
    - .planning/phases/05-statistical-hygiene-sweep-runner/05-02-SUMMARY.md (the per-scan support matrix that pinned which scans should eventually support PhaseScramble)
  </read_first>
  <action>
    For each of the FIVE scan `mod.rs` files, locate the `fn supports_null_method(&self, method: NullMethod) -> bool` impl. The current body matches `PhaseScramble | CircularShift` but returns `false` for the `PhaseScramble` arm (per the Plan 05-02 deferral). EDIT each impl to return `true` for both arms.

    The exact pattern to find and replace will be one of these shapes (varies per file — read each first):

    Pattern A (match-arm with comment):
    ```rust
    fn supports_null_method(&self, method: NullMethod) -> bool {
        match method {
            NullMethod::CircularShift => true,
            NullMethod::PhaseScramble => false, // DEFERRED to Phase 7 — IAAFT not implemented
        }
    }
    ```
    → Replace `false` with `true` and DELETE the `// DEFERRED to Phase 7` comment.

    Pattern B (single-line):
    ```rust
    fn supports_null_method(&self, method: NullMethod) -> bool {
        matches!(method, NullMethod::CircularShift)
    }
    ```
    → Replace with `matches!(method, NullMethod::CircularShift | NullMethod::PhaseScramble)`.

    Pattern C (explicit-false body):
    ```rust
    fn supports_null_method(&self, _method: NullMethod) -> bool { false }
    ```
    → Replace with `matches!(_method, NullMethod::CircularShift | NullMethod::PhaseScramble)`. (Rename `_method` to `method` since it's now used.)

    For each of the five files, verify post-edit that the resulting impl returns `true` for `NullMethod::PhaseScramble`.

    Add OR EXTEND a per-scan unit test in each file's `#[cfg(test)] mod tests` block. Test name: `supports_null_phase_scramble` (or extend an existing `supports_null_method_matrix` test if one already exists). Assertion: `assert!(<ScanType>.supports_null_method(NullMethod::PhaseScramble));` AND `assert!(<ScanType>.supports_null_method(NullMethod::CircularShift));`.

    Run `cargo test -p miner-core` — full miner-core test suite must pass. This exercises any per-scan tests that already verified `supports_null_method(PhaseScramble) == false` — they will now fail, which is the intended behaviour change. Update those tests in place to assert `== true` instead. Find them via `grep -rn 'PhaseScramble' crates/miner-core/src/scan/ crates/miner-core/tests/` and update each false→true assertion that's now invalidated.

    If the engine has a `PreflightCode::HygieneNotSupported` emission site that was specifically gated on `NullMethod::PhaseScramble`, it should NO LONGER fire for these five scans — but it MUST still fire for any scan that doesn't list PhaseScramble in its `supports_null_method` (e.g., scans that haven't been added to the matrix). Do not touch that preflight code; the trait-level flip is sufficient.
  </action>
  <acceptance_criteria>
    - All five mod.rs files now return `true` for `NullMethod::PhaseScramble` in their `supports_null_method` impl. Verify per-file by grep+context:
      - `grep -B2 -A8 'fn supports_null_method' crates/miner-core/src/scan/ljung_box/mod.rs | grep -q 'PhaseScramble' && grep -B2 -A8 'fn supports_null_method' crates/miner-core/src/scan/ljung_box/mod.rs | grep -v 'false' | grep -q 'PhaseScramble'`
      - (Same shape for the other four files. Easier sanity: each file's impl no longer contains the literal `// DEFERRED to Phase 7` comment.)
    - `grep -rn 'DEFERRED to Phase 7' crates/miner-core/src/scan/ljung_box/mod.rs crates/miner-core/src/scan/anom/ljung_box_sq/mod.rs crates/miner-core/src/scan/anom/variance_ratio/mod.rs crates/miner-core/src/scan/cross/lead_lag/mod.rs crates/miner-core/src/scan/cross/engle_granger/mod.rs` returns no matches (all five deferral notes removed).
    - All five files contain a test asserting PhaseScramble support: `for f in ljung_box/mod.rs anom/ljung_box_sq/mod.rs anom/variance_ratio/mod.rs cross/lead_lag/mod.rs cross/engle_granger/mod.rs; do grep -q 'supports_null_method.*PhaseScramble' "crates/miner-core/src/scan/$f" || { echo "missing test in $f"; exit 1; }; done`
    - `cargo test -p miner-core --lib` exits 0.
    - `cargo clippy -p miner-core --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; for f in scan/ljung_box/mod.rs scan/anom/ljung_box_sq/mod.rs scan/anom/variance_ratio/mod.rs scan/cross/lead_lag/mod.rs scan/cross/engle_granger/mod.rs; do grep -q "DEFERRED to Phase 7" "crates/miner-core/src/$f" && { echo "deferral note still in $f"; exit 1; } || true; grep -q "supports_null_method" "crates/miner-core/src/$f" || { echo "missing supports_null_method in $f"; exit 1; }; done; cargo test -p miner-core --lib && cargo clippy -p miner-core --all-targets -- -D warnings'</automated>
  </verify>
  <done>Five scan mod.rs files flip supports_null_method(PhaseScramble) -> true; deferral comments removed; per-scan unit tests assert the new behaviour; full miner-core test suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Author noise_replay_regression integration test + assert ≤30 FPs at α=0.05 + byte-identical SweepSummary</name>
  <files>crates/miner-core/tests/noise_replay_regression.rs</files>
  <read_first>
    - crates/miner-core/tests/sweep_byte_identical_rerun.rs (full file — exact pattern for in-memory TOML manifest → parse_manifest_str → run_sweep → BufferSink → mask → assert; PATTERNS §"crates/miner-core/tests/noise_replay_regression.rs" Pattern excerpt)
    - crates/miner-core/tests/byte_identical_rerun.rs (lines 74-83 — canonical `lcg_closes` function the test reuses)
    - crates/miner-core/tests/common/mod.rs (lines 72-95 — `mask_volatile_fields` 5-field variant including `produced_at_utc` per PATTERNS Pattern D)
    - crates/miner-core/tests/common/synthetic_cache.rs (full file — builds `SyntheticCache` with N instruments per the test's needs; lines 73-127 cover the multi-instrument pattern)
    - crates/miner-core/src/sweep/manifest.rs (lines 30-100 — `SweepManifest`, `JobBlock`, `FdrConfig` types; `parse_manifest_str` entry point)
    - crates/miner-core/src/sweep/executor.rs (line ~154 — `run_sweep` signature)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-CONTEXT.md §D7-04 (the exact 300-job sweep manifest TOML; assertion bounds: false_positive_count <= 30; seeds 0xC0FFEE_C0FFEE for GBM + 0xCAFEBABE for sweep)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §"crates/miner-core/tests/noise_replay_regression.rs" (full pattern assignment)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md §"Open Question 2" — default `#[ignore]`d (30-60s runtime; CI runs via `cargo test -- --ignored noise_replay`)
  </read_first>
  <action>
    Create `crates/miner-core/tests/noise_replay_regression.rs`. SPDX header on lines 1-2 per PATTERNS Pattern B. Top-of-file doc comment explaining D7-04 scope, the 300-job sweep shape, and the `<= 30` Wilson 99% bound rationale.

    `mod common;` at the top (so this integration test can reuse `common::mask_volatile_fields` per PATTERNS Pattern D).

    Per RESEARCH Open Question 2: the test is `#[ignore]`d by default — annotate with `#[ignore = "noise_replay: 30-60s runtime; run via `cargo test -- --ignored noise_replay` in CI per D7-04"]`. The CI workflow added by Plan 07-03 includes the existing `cargo test --workspace --no-fail-fast` step; Plan 07-09 will add a second explicit `cargo test --workspace -- --ignored noise_replay` step in the CI sign-off (or the executor can add it here — coordinate via the SUMMARY).

    Test function `#[test] fn noise_replay_300_jobs_at_alpha_005_caps_false_positives_at_30()`:

    1. Build 100 synthetic NULL instruments:
       - Deterministic seeding: `let gbm_seed: u64 = 0xC0FFEE_C0FFEE;` (literal from D7-04).
       - Per-instrument seed derivation: `let inst_seed = blake3::hash(format!("NULL_{i:02}", i = i).as_bytes()).as_bytes()[..8].try_into().map(u64::from_le_bytes).unwrap() ^ gbm_seed;` (or similar — use the existing `derive_job_seed` from `crates/miner-core/src/scan/hygiene/seed.rs` if it's `pub`; otherwise inline a small helper).
       - GBM log-returns: `μ=0, σ=1e-4` for N=100,000 bars (≈ 70 trading days at 15-minute resolution would be ~6720 bars; D7-04 uses 100,000 bars of 1-min synthetic-source = ~70 trading days. After aggregator-to-15m the consuming scan sees ~6700 15m bars per instrument. Verify the count matches what `seas.bucket.hour_of_day@1` needs to emit a non-empty result.).
       - Use `lcg_closes` from byte_identical_rerun.rs PATTERN C as the underlying RNG, or `rand_xoshiro::Xoshiro256PlusPlus::seed_from_u64(inst_seed)` with `rand_distr::Normal::new(0.0, 1e-4)` (already a workspace dep). Pick one — pick `Xoshiro256PlusPlus + Normal` since IAAFT contract already uses Xoshiro and that's the HYG-05 seed primitive.
       - IAAFT-randomise each series IN-MEMORY (call `crate::scan::hygiene::null::iaaft_phase_scramble_null_p` would be wrong — that returns a p-value; we want surrogate series). Instead, refactor or expose a `pub(crate)` `iaaft_surrogate(values: &[f64], seed: u64, max_iter: u32) -> Vec<f64>` helper that produces ONE surrogate. If the kernel from Task 1 doesn't expose this, ADD it as a helper that the kernel internally uses, then make it `pub(crate)` so this test can call it. Reference: Task 1's IAAFT kernel must call this helper internally for each resample, so it already exists structurally — just promote its visibility.
    2. Materialise the 100 surrogate series as in-memory 1m bars via the existing `common::synthetic_cache::SyntheticCache` builder, then let the aggregator convert to 15m at scan time (the SweepManifest specifies 15m timeframes; the aggregator handles the conversion).
    3. Build the SweepManifest in-memory (NOT via TOML file — use `parse_manifest_str(&toml_string)` where `toml_string` is built inline; PATTERNS §"crates/miner-core/tests/noise_replay_regression.rs" shows the structure):
       - `[sweep] seed = 0xCAFEBABE`
       - `[fdr] family = "scan_id"` + `alpha = 0.05`
       - Three `[[jobs]]` blocks per D7-04 (`stats.autocorr.ljung_box@1` with 100 instruments + `cross.cointegration.engle_granger@1` with 50 instrument pairs + `seas.bucket.hour_of_day@1` with 100 instruments; total 250 single-instrument jobs + 50 pair-arity jobs = 300 jobs).
    4. Invoke `run_sweep(manifest, SweepOptions::default(), &cfg, &reader, &bar_cache, &mut sink, Arc::new(AtomicBool::new(false)))` per PATTERNS pattern excerpt (sweep wiring).
    5. Parse `sink` output: locate the `Finding::SweepSummary` line (last line of stdout per executor.rs contract); deserialize via `serde_json::from_str::<serde_json::Value>(line)`; iterate `summary["fdr_families"]` (or whatever the actual key is — read manifest.rs `FdrFamilySummary` struct); count entries with `q_value <= 0.05`. Assertions:
       - `assert!(false_positive_count <= 30, "expected <= 30 false positives, got {false_positive_count}");`
       - `assert!(false_positive_count > 0 || n_runs == 1, "literal zero across multiple seeds suggests overly conservative FDR");` (per D7-04; for a single test run this assertion is effectively skipped because `n_runs == 1`).
       - `assert_eq!(summary["jobs_planned"], 300);` and `assert_eq!(summary["jobs_completed"], 300);` and `assert_eq!(summary["scan_errors"], 0);` and `assert_eq!(summary["gap_aborted"], 0);` (D7-04 §"What the test asserts").
    6. HYG-05 contract — run the sweep a SECOND time (same manifest, same seeds) and assert byte-identical SweepSummary after masking volatile fields via `common::mask_volatile_fields` (the 5-field variant). Use the helper from `sweep_byte_identical_rerun.rs:42-66` (`run_sweep_once`) as a model — extract a local `run_sweep_once` helper inside this test file or reuse the existing one if it's exposed via `mod common;`.

    HARD RULES:
    - No `println!` or `eprintln!`. Use `tracing::info!` if needed.
    - Use `BufferSink` (find via `grep -rn 'pub.* BufferSink' crates/miner-core/`) to capture stdout instead of touching real stdout.
    - Test must be self-contained — no external network, no external files, no real-Dukascopy reader. Synthetic-cache-only.
    - The `#[ignore]` attribute is non-negotiable per RESEARCH Open Question 2 — the test runs only under `--ignored`.

    Verify by running `cargo test -p miner-core --test noise_replay_regression -- --ignored` and confirming:
    - Test passes (exits 0).
    - Wall-clock < 120s (D7-04 estimate: 30-60s; budget at 2x).
    - Two invocations produce identical SweepSummary bytes after masking.
  </action>
  <acceptance_criteria>
    - `crates/miner-core/tests/noise_replay_regression.rs` exists.
    - File starts with SPDX header: `head -3 crates/miner-core/tests/noise_replay_regression.rs | grep -c 'SPDX-License-Identifier'` returns 1.
    - File contains `#[ignore` attribute (default-ignored per Open Question 2): `grep -c '#\[ignore' crates/miner-core/tests/noise_replay_regression.rs` returns at least 1.
    - File references D7-04 seed literals: `grep -c '0xC0FFEE_C0FFEE\|0xCAFEBABE' crates/miner-core/tests/noise_replay_regression.rs` returns at least 2.
    - File invokes `run_sweep`: `grep -c 'run_sweep' crates/miner-core/tests/noise_replay_regression.rs` returns at least 1.
    - File contains the FP-bound assertion: `grep -cE 'false_positive_count\s*<=\s*30' crates/miner-core/tests/noise_replay_regression.rs` returns at least 1.
    - File uses `mod common;` reuse pattern: `grep -c '^mod common' crates/miner-core/tests/noise_replay_regression.rs` returns 1.
    - File does not contain `println!` or `eprintln!`: `grep -cE '(println|eprintln)!' crates/miner-core/tests/noise_replay_regression.rs` returns 0.
    - Test compiles: `cargo build -p miner-core --tests` exits 0.
    - Test passes under --ignored: `cargo test -p miner-core --test noise_replay_regression -- --ignored` exits 0 within 120s wall-clock.
    - Standard test run (without --ignored) skips it cleanly: `cargo test -p miner-core --test noise_replay_regression` exits 0 with "0 passed; 1 ignored" or similar.
    - Clippy clean: `cargo clippy -p miner-core --test noise_replay_regression -- -D warnings` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; test -f crates/miner-core/tests/noise_replay_regression.rs; head -3 crates/miner-core/tests/noise_replay_regression.rs | grep -q SPDX-License-Identifier; grep -q "#\[ignore" crates/miner-core/tests/noise_replay_regression.rs; grep -qE "0xC0FFEE_C0FFEE|0xCAFEBABE" crates/miner-core/tests/noise_replay_regression.rs; grep -q "run_sweep" crates/miner-core/tests/noise_replay_regression.rs; grep -qE "false_positive_count\s*<=\s*30" crates/miner-core/tests/noise_replay_regression.rs; grep -q "^mod common" crates/miner-core/tests/noise_replay_regression.rs; [ "$(grep -cE "(println|eprintln)!" crates/miner-core/tests/noise_replay_regression.rs)" = "0" ]; cargo build -p miner-core --tests; timeout 150 cargo test -p miner-core --test noise_replay_regression -- --ignored && cargo clippy -p miner-core --test noise_replay_regression -- -D warnings'</automated>
  </verify>
  <done>noise_replay_regression.rs lands, default `#[ignore]`d, runs 300-job sweep under 120s, asserts ≤30 false positives + byte-identical SweepSummary across two runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed input → IAAFT kernel | Deterministic-from-seed is the HYG-05 contract; any non-determinism here breaks bit-for-bit reproducibility. |
| miner-core (sync) → realfft transitive deps | realfft → rustfft. Both verified sync-only per RESEARCH; this plan re-verifies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | IAAFT rank-shuffle non-determinism via `sort_unstable` | mitigate | Task 1 acceptance criteria forbids `sort_unstable`; uses `sort_by` with `(idx, val)` tuple + explicit index tiebreaker per RESEARCH Anti-Patterns. Pinned by Test 6 (`iaaft_uses_stable_rank_shuffle`). |
| T-07-05-02 | Tampering | FFT-length padding regression (non-5-smooth lengths cause ~10x perf hit + non-determinism risk for some primes) | mitigate | `next_5_smooth` helper + Test 8; pads to next 5-smooth before FFT per RESEARCH Pitfall 7. |
| T-07-05-03 | Tampering | realfft + rustfft transitive deps leak async runtime into miner-core | mitigate | Task 1 acceptance criteria runs the FOUND-04 gate (`cargo tree -p miner-core --edges normal,build | grep -E '<async-regex>'`). Verified empty per RESEARCH Assumption A10. |
| T-07-05-04 | Information Disclosure | dhat allocator surfaces hidden allocation patterns in IAAFT | accept | dhat is Plan 07-08's concern; Plan 07-05's kernel does no per-resample heap allocation in the inner loop (preallocated buffers reused across resamples — a CONTRIBUTING.md performance-aware pattern). |
</threat_model>

<verification>
- `cargo build -p miner-core` exits 0 with no warnings.
- `cargo test -p miner-core --lib` exits 0.
- `cargo test -p miner-core --test noise_replay_regression -- --ignored` exits 0 within 120s.
- `cargo clippy -p miner-core --all-targets -- -D warnings` exits 0.
- FOUND-04 gate: `cargo tree -p miner-core --edges normal,build` produces no tokio/async-* matches.
- `grep -rn 'DEFERRED to Phase 7' crates/miner-core/src/scan/` returns no matches (five deferral notes removed; the null.rs module-doc note removed).
</verification>

<success_criteria>
1. IAAFT kernel `iaaft_phase_scramble_null_p` lands as a sibling to `circular_shift_null_p` with 8 unit tests passing.
2. Five scan trait impls flip `supports_null_method(PhaseScramble) -> true`.
3. Noise-replay regression test asserts BH-FDR controls multiple testing (`false_positive_count <= 30`).
4. Test asserts byte-identical SweepSummary across two seeded runs (HYG-05).
5. `realfft` added to workspace + miner-core; FOUND-04 tokio-free invariant preserved.
6. Per-resample inner loop avoids heap allocation (RESEARCH §"Anti-Patterns" performance note).
</success_criteria>

<output>
Create `.planning/phases/07-hardening-benchmarks-reproducibility/07-05-SUMMARY.md` when done.
</output>
