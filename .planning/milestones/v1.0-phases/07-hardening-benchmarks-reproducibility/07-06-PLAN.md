---
phase: 07-hardening-benchmarks-reproducibility
plan: 06
type: execute
wave: 2
depends_on:
  - 07-02
  - 07-05
files_modified:
  - Cargo.toml
  - crates/miner-core/Cargo.toml
  - crates/miner-core/benches/bench_zstd_decompress_1day.rs
  - crates/miner-core/benches/bench_csv_parse_1day.rs
  - crates/miner-core/benches/bench_aggregate_1m_to_15m.rs
  - crates/miner-core/benches/bench_rolling_corr.rs
  - crates/miner-core/benches/bench_ljung_box.rs
  - crates/miner-core/benches/bench_ols_fit_4d.rs
autonomous: true
requirements:
  - FOUND-04

must_haves:
  truths:
    - "User can run `cargo bench -p miner-core` and observe six criterion microbenches execute without errors, producing target/criterion/<bench>/index.html reports for each."
    - "User can re-run any individual bench via `cargo bench -p miner-core --bench bench_aggregate_1m_to_15m` and observe the same kernel coverage."
    - "User can verify the [profile.release] debug = 1 setting is present in workspace Cargo.toml (required for dhat symbol attribution in Plan 07-08)."
    - "User can verify miner-core is still tokio-free after the criterion + realfft adds: `cargo tree -p miner-core --edges normal,build` shows no async crate."
  artifacts:
    - path: "crates/miner-core/benches/"
      provides: "Criterion microbench directory (six bench files)"
      contains: "bench_zstd_decompress_1day.rs"
    - path: "Cargo.toml"
      provides: "criterion 0.8 + dhat 0.3 in workspace deps; [profile.release] debug = 1"
      contains: "criterion"
    - path: "crates/miner-core/Cargo.toml"
      provides: "criterion as dev-dep + six [[bench]] entries with harness = false"
      contains: "harness = false"
  key_links:
    - from: "crates/miner-core/benches/bench_aggregate_1m_to_15m.rs"
      to: "miner_core::aggregator::aggregate"
      via: "direct function call inside criterion bench"
      pattern: "aggregator::aggregate"
    - from: "crates/miner-core/Cargo.toml"
      to: "crates/miner-core/benches/"
      via: "[[bench]] entries with harness = false"
      pattern: "harness = false"
---

<objective>
Land D7-03 Layer 1 (criterion microbenches) at `crates/miner-core/benches/` — a new directory containing six bench files exercising the hot kernels (zstd decompress, csv parse, aggregator, rolling corr, Ljung-Box, OLS-4D). Add `criterion = 0.8` and `dhat = 0.3` to `[workspace.dependencies]` (criterion is needed now; dhat is wired in by Plan 07-08 but added here so workspace lockfile updates land in a single coherent step). Add `[profile.release] debug = 1` to workspace `Cargo.toml` (required for dhat symbol attribution per RESEARCH Pitfall 1; line-tables-only — NOT `debug = true` which 5x's binary size).

Depends on Plan 07-05 (sequential) because:
1. Both edit workspace `Cargo.toml` — sequencing avoids merge conflicts on `[workspace.dependencies]`.
2. Both edit `crates/miner-core/Cargo.toml` — same reason.

Purpose: Layer 1 of the bench harness is the canonical-Rust micro-bench framework. CI does NOT run criterion benches (per D7-03 — criterion's variance on shared runners is too high for a useful gate), but local `cargo bench -p miner-core` runs them all, and `--save-baseline` lets contributors regression-test their own changes.

Output: One new bench directory with six files; two manifest edits (workspace + miner-core).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-CONTEXT.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md
@Cargo.toml
@crates/miner-core/Cargo.toml
@crates/miner-core/src/aggregator.rs
@crates/miner-core/src/scan/ljung_box/kernel.rs
@crates/miner-core/src/scan/cross/corr_rolling/kernel.rs
@crates/miner-core/src/scan/cross/engle_granger/kernel.rs
@crates/miner-core/tests/byte_identical_rerun.rs
@crates/miner-reader-dukascopy/src/parser.rs

<interfaces>
<!-- Canonical criterion 0.8 pattern (RESEARCH §"Standard Stack / criterion" + PATTERNS §"crates/miner-core/benches/bench_*.rs"): -->
use criterion::{black_box, criterion_group, criterion_main, Criterion};

fn bench_<name>(c: &mut Criterion) {
    // Build inputs OUTSIDE the timed loop.
    let input = build_input();
    c.bench_function("<bench-name>", |b| {
        b.iter(|| {
            let result = function_under_test(black_box(&input));
            black_box(result);
        });
    });
}

criterion_group!(benches, bench_<name>);
criterion_main!(benches);

<!-- REQUIRED Cargo.toml registration (RESEARCH Pitfall 2): -->
[[bench]]
name = "bench_<name>"
harness = false

<!-- Per-bench kernel targets (PATTERNS §"Per-bench kernel targets"): -->
//  bench_zstd_decompress_1day      → zstd::stream::read::Decoder over tests/fixtures/cache/EURUSD/2024/00/01_bid.csv.zst
//  bench_csv_parse_1day            → csv::ReaderBuilder against decompressed bytes
//  bench_aggregate_1m_to_15m       → miner_core::aggregator::aggregate (callsite at src/aggregator.rs:304)
//  bench_rolling_corr              → rolling Pearson + rolling Spearman (src/scan/cross/corr_rolling/kernel.rs)
//  bench_ljung_box                 → src/scan/ljung_box/kernel.rs::ljung_box_q_and_p over lag grid [5,10,20,50]
//  bench_ols_fit_4d                → 4-dim nalgebra OLS (callsite at src/scan/cross/engle_granger/kernel.rs)

<!-- LCG closes (PATTERNS Pattern C) is the canonical synthetic-OHLCV source for benches that need closes: -->
fn lcg_closes(n: usize, seed: u64) -> Vec<f64> {
    // Numerical Recipes constants 1_664_525 + 1_013_904_223 — cross-platform deterministic.
    // See crates/miner-core/tests/byte_identical_rerun.rs:74-83.
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add criterion + dhat to workspace + [profile.release] debug = 1 + register six [[bench]] entries on miner-core</name>
  <files>Cargo.toml, crates/miner-core/Cargo.toml</files>
  <read_first>
    - Cargo.toml (workspace root — after Plan 07-05's `realfft` addition, the Phase 7 additions block is at the end of `[workspace.dependencies]`; extend it with criterion + dhat per the comment-then-declare convention)
    - crates/miner-core/Cargo.toml (current state after Plan 07-05's realfft add; check for existing `[dev-dependencies]` and `[[bench]]` sections — the file has neither today)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md §"Standard Stack / Core" + §"Pitfall 1" (debug = 1 line-tables-only) + §"Pitfall 2" (harness = false)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §"Cargo.toml workspace (MODIFY)" + §"crates/miner-core/Cargo.toml (MODIFY)"
  </read_first>
  <action>
    PART A — workspace Cargo.toml.

    Locate the Phase 7 additions block that Plan 07-05 created (the one containing `realfft = "3.5"`). Extend that block with two more lines following the same comment-then-declare convention. Update the leading comment to acknowledge that Plan 07-05 added realfft and Plan 07-06 adds criterion + dhat. The block target shape:

    ```toml
    # Phase 7 (Plans 07-05, 07-06) additions per 07-RESEARCH.md §"Standard Stack":
    # - `realfft` 3.5 — real-input FFT for IAAFT phase-scramble (Plan 05-02 deferred, Plan 07-05 landed).
    # - `criterion` 0.8 — microbench harness; html_reports feature enables target/criterion/.../index.html.
    # - `dhat` 0.3 — heap profiler; used as #[global_allocator] BEHIND `dhat` feature on miner-bench only
    #   (FOUND-04 invariant: miner-core stays dhat-free; the workspace-dep declaration is reuse-only).
    realfft   = "3.5"   # added by Plan 07-05
    criterion = { version = "0.8", features = ["html_reports"] }
    dhat      = "0.3"
    ```

    ALSO add a new `[profile.release]` section at the END of the workspace `Cargo.toml` (after `[workspace.lints]` or wherever the file currently ends — search for any existing `[profile.*]` blocks first; if a `[profile.release]` already exists, just add `debug = 1` to it; do NOT duplicate the section):

    ```toml
    [profile.release]
    # `debug = 1` (line-tables-only) is required for dhat-rs symbol attribution per RESEARCH §"Pitfall 1".
    # MUST NOT be `debug = true` (full DWARF, 5x binary size bloat). One-time `cargo clean` may be required
    # after this change because cached release artifacts won't have the debug-info build flag set.
    debug = 1
    ```

    PART B — miner-core Cargo.toml.

    In `crates/miner-core/Cargo.toml`:
    1. Add `[dev-dependencies]` section (if not already present) and include `criterion.workspace = true`. The realfft addition from Plan 07-05 belongs in `[dependencies]` (production code path); criterion belongs in `[dev-dependencies]` (bench-only).
    2. At the bottom of the file, after `[lints]`, add six `[[bench]]` entries — each with `harness = false` REQUIRED per RESEARCH Pitfall 2 (omitting it causes "no main function" errors):

    ```toml
    [[bench]]
    name = "bench_zstd_decompress_1day"
    harness = false

    [[bench]]
    name = "bench_csv_parse_1day"
    harness = false

    [[bench]]
    name = "bench_aggregate_1m_to_15m"
    harness = false

    [[bench]]
    name = "bench_rolling_corr"
    harness = false

    [[bench]]
    name = "bench_ljung_box"
    harness = false

    [[bench]]
    name = "bench_ols_fit_4d"
    harness = false
    ```

    Do NOT add `dhat` as a dep on `crates/miner-core/Cargo.toml` — RESEARCH §"Anti-Patterns" forbids it. dhat is gated to `miner-bench` only via Plan 07-08.

    HARD GATE — re-run the FOUND-04 check from Plan 07-05 acceptance criteria. The criterion + dhat workspace additions should NOT leak into `miner-core` (criterion is `[dev-dependencies]` so it's not in `--edges normal,build`; dhat is not added to miner-core at all). Run:
    ```
    PROHIBITED='^(tokio|tokio-[^ ]+|async-std|async-std-[^ ]+|smol|smol-[^ ]+|async-trait|async-io|async-channel|async-executor|async-task)$'
    cargo tree -p miner-core --edges normal,build --prefix none | awk '{print $1}' | sort -u | grep -E "$PROHIBITED" || true
    ```
    Must produce empty output.

    NOTE: a `cargo clean` may be required for the `debug = 1` change to take effect for downstream builds. Document this in Plan 07-06 SUMMARY's note for Plan 07-08 (which uses dhat profiling and depends on this debug setting).
  </action>
  <acceptance_criteria>
    - Workspace Cargo.toml contains criterion: `grep -cE '^\s*criterion\s*=' Cargo.toml` returns 1.
    - Workspace Cargo.toml contains dhat: `grep -cE '^\s*dhat\s*=' Cargo.toml` returns 1.
    - Workspace Cargo.toml retains realfft from Plan 07-05: `grep -cE '^\s*realfft\s*=' Cargo.toml` returns 1.
    - Workspace Cargo.toml has `[profile.release]` with `debug = 1` (NOT `debug = true`): `grep -A3 '^\[profile\.release\]' Cargo.toml | grep -c 'debug\s*=\s*1' ` returns at least 1, AND `grep -A3 '^\[profile\.release\]' Cargo.toml | grep -c 'debug\s*=\s*true' ` returns 0.
    - miner-core/Cargo.toml has `criterion.workspace = true`: `grep -c 'criterion\.workspace\s*=\s*true' crates/miner-core/Cargo.toml` returns 1.
    - miner-core/Cargo.toml does NOT have dhat dep: `grep -cE '^\s*dhat' crates/miner-core/Cargo.toml` returns 0.
    - Six `[[bench]]` entries present: `grep -c '^\[\[bench\]\]' crates/miner-core/Cargo.toml` returns 6.
    - Every `[[bench]]` entry has `harness = false`: the count of `harness = false` lines equals the count of `[[bench]]` headers — `[ "$(grep -c '^\[\[bench\]\]' crates/miner-core/Cargo.toml)" = "$(grep -c 'harness = false' crates/miner-core/Cargo.toml)" ]`.
    - Manifests parse: `cargo metadata --no-deps --format-version 1 > /dev/null` exits 0.
    - FOUND-04 holds: `cargo tree -p miner-core --edges normal,build --prefix none | awk '{print $1}' | sort -u | grep -E '^(tokio|async-std|smol|async-trait|async-io|async-channel|async-executor|async-task)$' || true` produces empty output (negated assertion).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; grep -qE "^\s*criterion\s*=" Cargo.toml; grep -qE "^\s*dhat\s*=" Cargo.toml; grep -qE "^\s*realfft\s*=" Cargo.toml; grep -A3 "^\[profile\.release\]" Cargo.toml | grep -q "debug\s*=\s*1"; [ "$(grep -A3 "^\[profile\.release\]" Cargo.toml | grep -c "debug\s*=\s*true")" = "0" ]; grep -q "criterion\.workspace\s*=\s*true" crates/miner-core/Cargo.toml; [ "$(grep -cE "^\s*dhat" crates/miner-core/Cargo.toml)" = "0" ]; [ "$(grep -c "^\[\[bench\]\]" crates/miner-core/Cargo.toml)" = "6" ]; [ "$(grep -c "^\[\[bench\]\]" crates/miner-core/Cargo.toml)" = "$(grep -c "harness = false" crates/miner-core/Cargo.toml)" ]; cargo metadata --no-deps --format-version 1 > /dev/null; LEAKED=$(cargo tree -p miner-core --edges normal,build --prefix none | awk "{print \$1}" | sort -u | grep -E "^(tokio|async-std|smol|async-trait|async-io|async-channel|async-executor|async-task)$" || true); [ -z "$LEAKED" ]'</automated>
  </verify>
  <done>Workspace adds criterion + dhat + [profile.release] debug = 1; miner-core gains criterion dev-dep + six [[bench]] entries with harness = false; FOUND-04 preserved; manifests parse.</done>
</task>

<task type="auto">
  <name>Task 2: Author six criterion microbench files under crates/miner-core/benches/</name>
  <files>crates/miner-core/benches/bench_zstd_decompress_1day.rs, crates/miner-core/benches/bench_csv_parse_1day.rs, crates/miner-core/benches/bench_aggregate_1m_to_15m.rs, crates/miner-core/benches/bench_rolling_corr.rs, crates/miner-core/benches/bench_ljung_box.rs, crates/miner-core/benches/bench_ols_fit_4d.rs</files>
  <read_first>
    - crates/miner-core/src/aggregator.rs (around line 304 — `aggregate` function signature + the `AggParams` shape the bench inputs need to build)
    - crates/miner-core/src/scan/ljung_box/kernel.rs (line 96 — `ljung_box_q_and_p` signature; what lag grid + sample size the bench should sweep)
    - crates/miner-core/src/scan/cross/corr_rolling/kernel.rs (full file — Pearson + Spearman entry points)
    - crates/miner-core/src/scan/cross/engle_granger/kernel.rs (the 4-dim OLS callsite — likely a private function the bench needs to either wrap or call via the kernel's public path)
    - crates/miner-reader-dukascopy/src/parser.rs (the canonical CSV parsing callsite — bench mirrors this code path)
    - crates/miner-core/tests/byte_identical_rerun.rs (lines 74-83 — canonical `lcg_closes` to copy into bench files as a `mod common;` helper or inline)
    - tests/fixtures/cache/EURUSD/2024/00/01_bid.csv.zst (the fixture file the zstd + csv benches operate on — this exists if Plan 07-02 has run)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §"crates/miner-core/benches/bench_*.rs" (canonical pattern excerpt + per-bench kernel targets table)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md §"Standard Stack" criterion notes
  </read_first>
  <action>
    Each of the six bench files MUST follow this canonical structure (per PATTERNS §"crates/miner-core/benches/bench_*.rs"):

    1. SPDX header (lines 1-2 per PATTERNS Pattern B).
    2. Module-doc comment describing what kernel is benched, what the input shape is, and what `cargo bench -p miner-core --bench <name>` reports.
    3. `use criterion::{black_box, criterion_group, criterion_main, Criterion};`
    4. Any `use miner_core::...` for the kernel under test.
    5. Per-bench helper(s) — typically `lcg_closes` from PATTERNS Pattern C inline (DO NOT cross-reference from `tests/` — benches and tests live in separate compilation units; copy the function or extract into a `bench-utils` module under `benches/common/mod.rs` if reused across multiple benches).
    6. The bench function(s) — input construction OUTSIDE `b.iter()`, kernel call INSIDE wrapped in `black_box`.
    7. `criterion_group!(benches, bench_<name>);` + `criterion_main!(benches);`

    NO `println!`, `eprintln!`, or `dbg!` (workspace clippy::disallowed_macros lint).

    Per-bench specifics:

    **bench_zstd_decompress_1day.rs:**
    - Reads `tests/fixtures/cache/EURUSD/2024/00/01_bid.csv.zst` (one day, generated by Plan 07-02; file path resolved via `CARGO_MANIFEST_DIR`-relative since benches run with CWD == crate dir).
    - Kernel: `zstd::stream::read::Decoder::new(File::open(path)?)?` + `std::io::copy(&mut decoder, &mut Vec::new())?` (decompress to in-memory buffer).
    - Input setup: read the file bytes ONCE outside `b.iter()` and pass `Cursor` wrapping the bytes into Decoder each iteration (avoid re-reading from disk on every iteration).
    - Bench name string: `"zstd_decompress_1day_eurusd"`.

    **bench_csv_parse_1day.rs:**
    - Decompresses the same fixture day file ONCE outside `b.iter()`, holding the CSV bytes in memory.
    - Kernel: `csv::ReaderBuilder::new().has_headers(true).from_reader(&csv_bytes[..])` + iterate `.records()` collecting into `Vec<csv::StringRecord>`.
    - Bench name string: `"csv_parse_1day_eurusd"`.

    **bench_aggregate_1m_to_15m.rs:**
    - Synthesises 1m bars for one year (365 days × 1440 minutes ≈ 525,600 bars; skip weekends to 260 days × 1440 ≈ 374,400 bars; pick exactly 1440 × 250 = 360,000 to keep the bench fast). Use `lcg_closes(360_000, 0xCAFE)` for the closes; manufacture `OHLCV` rows with `high = close + 0.00005, low = close - 0.00005, open = close, volume = (i+1) as f64` (matches the synthetic_cache.rs:86-107 pattern).
    - Convert the bar array to whatever `miner_core::aggregator::aggregate` accepts (read aggregator.rs to find the input type — likely `BarFrame` or a slice of `OhlcvBar`).
    - Kernel: `miner_core::aggregator::aggregate(input, AggParams { timeframe: Timeframe::Min15, ... })`.
    - Bench name string: `"aggregate_1m_to_15m_360000_bars"`.

    **bench_rolling_corr.rs:**
    - Two synthetic close arrays: `lcg_closes(10_000, 0xCAFE)` and `lcg_closes(10_000, 0xBEEF)`.
    - Two bench functions inside one `criterion_group`: `bench_pearson_rolling` and `bench_spearman_rolling`, each calling the corresponding kernel from `src/scan/cross/corr_rolling/kernel.rs` with a fixed window size (e.g., window = 100).
    - Bench name strings: `"rolling_corr_pearson_w100_n10000"` and `"rolling_corr_spearman_w100_n10000"`.

    **bench_ljung_box.rs:**
    - Synthetic returns array: `lcg_closes(10_000, 0xCAFE)` converted to log-returns via `(closes[i] / closes[i-1]).ln()` (1 fewer element).
    - Lag grid: 4 separate bench functions in one group — `bench_ljung_box_lag_5`, `bench_ljung_box_lag_10`, `bench_ljung_box_lag_20`, `bench_ljung_box_lag_50` — each calling `ljung_box_q_and_p(&returns, lag)`.
    - Bench name strings include the lag suffix: `"ljung_box_q_p_n10000_lag5"`, etc.

    **bench_ols_fit_4d.rs:**
    - 4-column design matrix (intercept + 3 regressors): 10,000 rows. Build via `lcg_closes` for each column with different seeds.
    - Y vector: another `lcg_closes`-derived array.
    - Kernel: the 4-dim OLS callsite from `src/scan/cross/engle_granger/kernel.rs`. If the OLS is `pub(crate)` only, this bench needs `pub(crate)` to `pub(in crate)` promotion — but DON'T leak it `pub`; instead, expose a `pub fn ols_fit_4d_for_benches(x: &DMatrix<f64>, y: &DVector<f64>) -> [f64; 4]` helper in the kernel module marked with a `#[cfg(any(test, feature = "bench-internals"))]` attribute. If adding a bench-internals feature is overkill, the simpler path is to have the bench call the public engle_granger scan path with the right inputs — that's higher-level but still benches the OLS hot path.
    - Bench name string: `"ols_fit_4d_n10000"`.

    Run `cargo bench -p miner-core --bench bench_zstd_decompress_1day` after each file is authored to catch wiring issues incrementally. If any of these takes longer than 60 seconds per bench iteration to converge, REDUCE the input size — bench iteration count is auto-tuned by criterion; the goal is NOT to benchmark large inputs but to provide a stable per-kernel timing baseline.

    Verify by running `cargo bench -p miner-core --no-run` (compiles all six without executing) — must succeed with zero warnings.

    Then run `cargo bench -p miner-core` end-to-end. Acceptance criteria below caps total runtime; if a single bench file takes >60s, halve its input size and re-run.
  </action>
  <acceptance_criteria>
    - All six bench files exist:
      - `test -f crates/miner-core/benches/bench_zstd_decompress_1day.rs`
      - `test -f crates/miner-core/benches/bench_csv_parse_1day.rs`
      - `test -f crates/miner-core/benches/bench_aggregate_1m_to_15m.rs`
      - `test -f crates/miner-core/benches/bench_rolling_corr.rs`
      - `test -f crates/miner-core/benches/bench_ljung_box.rs`
      - `test -f crates/miner-core/benches/bench_ols_fit_4d.rs`
    - Each file starts with SPDX header: `for f in crates/miner-core/benches/bench_*.rs; do head -3 "$f" | grep -q SPDX-License-Identifier || { echo "missing SPDX in $f"; exit 1; }; done`
    - Each file invokes `criterion_main!`: `for f in crates/miner-core/benches/bench_*.rs; do grep -q "criterion_main!" "$f" || { echo "missing criterion_main in $f"; exit 1; }; done`
    - No file contains `println!`: `[ "$(grep -c 'println!' crates/miner-core/benches/*.rs | awk -F: '{s+=$2} END {print s}')" = "0" ]`
    - `cargo bench -p miner-core --no-run` exits 0 (compiles all six without warnings).
    - `cargo bench -p miner-core` exits 0 (executes all six). Wall-clock budget: ≤ 600 seconds total across all six benches (criterion does its own iteration tuning; if any bench exceeds 120s alone, reduce its input).
    - `target/criterion/` is populated after the bench run: `test -d target/criterion`.
    - Clippy clean on benches: `cargo clippy -p miner-core --benches -- -D warnings` exits 0.
    - The fixture file used by bench_zstd_decompress_1day and bench_csv_parse_1day exists (provided by Plan 07-02): `test -f tests/fixtures/cache/EURUSD/2024/00/01_bid.csv.zst`. If Plan 07-02 has not run yet in this workspace, the executor MUST run `bash scripts/generate-fixture-cache.sh` first.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; for f in bench_zstd_decompress_1day bench_csv_parse_1day bench_aggregate_1m_to_15m bench_rolling_corr bench_ljung_box bench_ols_fit_4d; do test -f "crates/miner-core/benches/$f.rs" || { echo "missing $f"; exit 1; }; head -3 "crates/miner-core/benches/$f.rs" | grep -q SPDX-License-Identifier || { echo "SPDX missing in $f"; exit 1; }; grep -q "criterion_main!" "crates/miner-core/benches/$f.rs" || { echo "criterion_main missing in $f"; exit 1; }; done; [ "$(grep -c "println!" crates/miner-core/benches/*.rs | awk -F: "{s+=\$2} END {print s}")" = "0" ]; [ -f tests/fixtures/cache/EURUSD/2024/00/01_bid.csv.zst ] || bash scripts/generate-fixture-cache.sh; cargo bench -p miner-core --no-run; timeout 700 cargo bench -p miner-core && test -d target/criterion && cargo clippy -p miner-core --benches -- -D warnings'</automated>
  </verify>
  <done>Six criterion microbench files compile + execute under `cargo bench -p miner-core`; target/criterion/ populated with HTML reports; clippy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benches (dev-dep) → miner-core (production) | criterion is `[dev-dependencies]` only; must not leak into production binary distributions. |
| miner-core → realfft + rustfft transitive (still) | Plan 07-05 verified; Plan 07-06's adds don't re-introduce concerns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | criterion-leaks-into-release-binary | mitigate | criterion is `[dev-dependencies]`; `cargo build --release` does not include it; FOUND-04 gate (tokio-free check) doubles as a leak detector for dev-deps. |
| T-07-06-02 | Information Disclosure | `[profile.release] debug = 1` ships line-table debug info in distributed binaries | accept | RESEARCH §"Pitfall 1" notes this is line-tables-only (NOT full DWARF) — ~2x binary size, not 5x. Trade-off accepted for dhat symbol attribution. Binary distribution is not a concern (miner ships as source via Apache-2.0 license). |
</threat_model>

<verification>
- `cargo metadata --no-deps --format-version 1 > /dev/null` exits 0.
- `cargo bench -p miner-core --no-run` exits 0.
- `cargo bench -p miner-core` exits 0 within 700s wall-clock.
- `cargo tree -p miner-core --edges normal,build` shows no tokio/async-* deps.
- `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
- `target/criterion/` directory is populated.
</verification>

<success_criteria>
1. Six criterion microbench files compile and execute under `cargo bench -p miner-core`.
2. Each bench has `harness = false` registered in `crates/miner-core/Cargo.toml`.
3. `[profile.release] debug = 1` added to workspace Cargo.toml (precondition for Plan 07-08 dhat profiling).
4. FOUND-04 invariant preserved (miner-core tokio-free, dhat-free).
5. No `println!` in any bench file (workspace lint discipline).
</success_criteria>

<output>
Create `.planning/phases/07-hardening-benchmarks-reproducibility/07-06-SUMMARY.md` when done.
</output>
