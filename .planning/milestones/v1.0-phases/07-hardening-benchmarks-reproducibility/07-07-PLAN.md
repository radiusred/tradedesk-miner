---
phase: 07-hardening-benchmarks-reproducibility
plan: 07
type: execute
wave: 2
depends_on:
  - 07-02
files_modified:
  - docs/data_sources.md
  - README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can read docs/data_sources.md and find a deep reference covering Dukascopy cache layout, CSV schema, bid/ask independence, time zones + DST, gap policies, and data licensing posture."
    - "User can read README.md `## Data source caveats` and find a 6-line summary linking to the deep doc."
    - "User can verify the new doc ends with the Apache-2.0 footer byte-identical to docs/.license-footer.md."
  artifacts:
    - path: "docs/data_sources.md"
      provides: "Deep Dukascopy caveats reference"
      contains: "## Licensing posture"
    - path: "README.md"
      provides: "Updated landing page with Data source caveats section + link to docs/data_sources.md"
      contains: "## Data source caveats"
  key_links:
    - from: "README.md"
      to: "docs/data_sources.md"
      via: "markdown link"
      pattern: "\\[docs/data_sources\\.md\\]\\(docs/data_sources\\.md\\)"
    - from: "docs/data_sources.md"
      to: "docs/.license-footer.md"
      via: "byte-identical Apache-2.0 footer"
      pattern: "Apache License, Version 2.0"
---

<objective>
Land D7-02 in full: `docs/data_sources.md` (the deep reference for Dukascopy caveats) plus the README `## Data source caveats` summary section linking to it. Depends on Plan 07-02 because both edit `README.md` — sequencing ensures the Plan 07-02 `## Example` edit lands first, then this plan inserts the new section after it.

Per RESEARCH Open Question 6, the licensing-posture section pins a `Verified against tradedesk-dukascopy commit <sha> (<date>)` reference; the executor runs `git -C /home/darren/projects/radiusred/tradedesk-dukascopy log -1 --format='%h %ad' --date=short` at land time and substitutes the result.

Per PATTERNS §"docs/data_sources.md" open carry: `docs/.license-footer.md` DOES exist in the repo (verified: 222 bytes, 8 lines). The plan reads its body and pastes byte-identically into `docs/data_sources.md` per Pattern A.

Purpose: D7-02 closes the "users can't interpret findings without understanding the data semantics" gap. The README summary points users to the deep doc; the deep doc documents the licensing posture that makes the synthetic-stub fixture cache (Plan 07-02) a sound engineering choice.

Output: One new doc file; two README section additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-CONTEXT.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md
@docs/agent_integration.md
@docs/findings_envelope.md
@docs/.license-footer.md
@README.md
@crates/miner-reader-dukascopy/src/path_layout.rs
@crates/miner-reader-dukascopy/src/parser.rs
@crates/miner-core/tests/dst_spring_forward.rs
@crates/miner-core/tests/dst_fall_back.rs

<interfaces>
<!-- D7-02 required sections (PATTERNS §"docs/data_sources.md / Required sections"): -->
<!--   ## Cache layout — on-disk path convention, 00-indexed months -->
<!--   ## CSV schema — column-by-column with citations to crates/miner-reader-dukascopy/src/ -->
<!--   ## Bid vs ask independence — SYMBOL:side semantics -->
<!--   ## Time zones and DST — UTC throughout; cite crates/miner-core/tests/dst_*.rs -->
<!--   ## Gap policies — restate strict vs continuous_only for data-shape audience -->
<!--   ## Licensing posture — synthetic-stub policy; link to Dukascopy upstream terms -->
<!--   ## See Also + Apache-2.0 footer -->

<!-- README addition (D7-02 — the exact 6-line body): -->
<!--   ## Data source caveats -->
<!--   -->
<!--   `tradedesk-miner` reads the cache layout `tradedesk-dukascopy` produces. A few -->
<!--   non-obvious conventions matter for interpreting findings: -->
<!--   -->
<!--   - Months are **00-indexed** on disk (`2024/00/` = January, `2024/11/` = December). -->
<!--   - The `volume` column is a **tick count**, not lot volume. -->
<!--   - Bid and ask sides are processed independently; spread reconstruction is out of scope. -->
<!--   - Weekend and exchange-holiday gaps are intentional, not missing data; the -->
<!--     `gap_policy` flag controls how scans treat them. -->
<!--   -->
<!--   See [docs/data_sources.md](docs/data_sources.md) for the full reference, including -->
<!--   the data licensing posture. -->

<!-- Canonical Apache-2.0 footer (PATTERNS Pattern A — verified to exist at docs/.license-footer.md): -->
<!--   --- -->
<!--   ## License -->
<!--   Licensed under the Apache License, Version 2.0. -->
<!--   See: https://www.apache.org/licenses/LICENSE-2.0 -->
<!--   Copyright 2026 [Radius Red Ltd.](https://www.radiusred.uk) | [Contact](mailto:opensource@radiusred.uk) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/data_sources.md with all six required sections + Apache-2.0 footer</name>
  <files>docs/data_sources.md</files>
  <read_first>
    - docs/agent_integration.md (full file — the canonical "sibling doc" layout: top-level intro, sectional `##` headings, `## See Also` trailer, Apache-2.0 footer — copy the trailer + footer structure exactly)
    - docs/findings_envelope.md (full file — same sectional discipline; see how it cites source files in the body)
    - docs/.license-footer.md (the byte-identical Apache-2.0 footer — 8 lines, 222 bytes — paste verbatim at end)
    - crates/miner-reader-dukascopy/src/path_layout.rs (the `day_csv_zst` function + the 00-indexed-months math — cite the exact line numbers as the ground-truth for the `## Cache layout` section)
    - crates/miner-reader-dukascopy/src/parser.rs (the CSV parsing path — cite column-by-column with file:line for the `## CSV schema` section)
    - crates/miner-core/tests/dst_spring_forward.rs and crates/miner-core/tests/dst_fall_back.rs (the canonical DST tests cited in `## Time zones and DST`)
    - /home/darren/projects/radiusred/tradedesk/docs/data_sources_guide.md (sibling-repo doc — RESEARCH §"Code Examples / docs/data_sources.md analog"; mirror its sectional discipline + related-docs trailer style)
    - /home/darren/projects/radiusred/tradedesk-dukascopy/tradedesk_dukascopy/export.py (around line 310-320 for the volume `.sum()` semantic — cite this as the ground-truth tick-volume vs lot-volume claim; around line 442 for zstd level 3 cited as the producer-side compression contract)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-CONTEXT.md §D7-02 (the required-sections list + the licensing-posture wording)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §"docs/data_sources.md" (full pattern assignment)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md §"Open Question 6" (verified-against-commit-SHA recommendation)
  </read_first>
  <action>
    Create `docs/data_sources.md`. Length target: 180-250 lines including footer per D7-02 estimate. Structure (in order):

    1. H1 title: `# Data source caveats` (or `# Dukascopy data source caveats` — pick one and use it consistently across the file and the README link).

    2. One-paragraph intro: explain that miner reads the cache layout `tradedesk-dukascopy` produces (link to `https://github.com/radiusred/tradedesk-dukascopy` if a public URL exists; otherwise reference the sibling-repo by name only). State the doc's audience (agents + humans interpreting findings).

    3. `## Cache layout` — on-disk path convention. MUST include:
       - The exact path template: `<root>/<SYMBOL>/<YYYY>/<MM 00-indexed>/<DD>_<bid|ask>.csv.zst`.
       - Worked example: `EURUSD/2024/00/01_bid.csv.zst` = "EURUSD bid quotes for 2024-01-01".
       - Why 00-indexed: producer-side python convention from `tradedesk-dukascopy/export.py`; miner's reader encapsulates the quirk in `crates/miner-reader-dukascopy/src/path_layout.rs` (cite the exact `day_csv_zst` function name + line number); CACHE-05 boundary tests pin this.
       - Note that the synthetic fixture cache at `tests/fixtures/cache/` mirrors this layout EXACTLY (link to Plan 07-02's clone-and-run quickstart in the README).

    4. `## CSV schema` — column-by-column, cite `crates/miner-reader-dukascopy/src/parser.rs`. Columns:
       - `timestamp_utc` (UTC ms since epoch — verify the exact format and units against parser.rs; some Dukascopy variants use `%Y-%m-%d %H:%M:%S%:z` per synthetic_cache.rs:97 — document whichever miner ACTUALLY accepts).
       - `open` / `high` / `low` / `close` (mid-quote for the chosen side — clarify "mid-quote" if bid/ask CSVs are separate files containing already-side-specific quotes).
       - `volume` — **TICK COUNT** per bar, NOT contract/lot volume; cite `tradedesk-dukascopy/export.py` around line 310 where the `.sum()` semantic produces this value. miner surfaces it as `tick_volume: f64` per CACHE-05.

    5. `## Bid vs ask independence` — why miner treats sides as separate logical instruments. MUST explain:
       - The CLI syntax `--instrument SYMBOL:side` (e.g., `EURUSD:bid`).
       - Why miner does NOT reconstruct spreads: it's out of scope (this is the "discovery layer", not the "execution layer"); spread reconstruction belongs in `tradedesk`.
       - What an agent should do if it wants spread-aware findings: run the same scan on both sides (`EURUSD:bid` and `EURUSD:ask`), compare findings externally.

    6. `## Time zones and DST` — UTC throughout. MUST cite:
       - `crates/miner-core/tests/dst_spring_forward.rs` and `crates/miner-core/tests/dst_fall_back.rs` as the canonical aggregator DST contract.
       - State that the aggregator's UTC discipline means a 15m bar starting at `2024-03-31 01:45:00 UTC` and the next bar starting at `2024-03-31 02:00:00 UTC` are EXACTLY 15 minutes apart even though London local time may have shifted across the DST boundary — miner does NOT translate to local time anywhere on the analysis path.

    7. `## Gap policies` — restate `strict` vs `continuous_only` from the DATA perspective (the agent_integration.md version is consumer-shaped; this version is data-shape-shaped). MUST mention:
       - Weekend gaps (Fri 22:00 UTC → Sun 22:00 UTC) are NOT data anomalies — they are the FX market closing.
       - Exchange holidays (Christmas Day, New Year's Day) are gaps for the same reason.
       - `strict` aborts the scan with the gap manifest; `continuous_only` partitions the window and emits one finding per gap-free sub-range. Neither EVER silently produces findings on ranges containing holes — this is a load-bearing OUT-04 contract.

    8. `## Licensing posture` — the most important new section per D7-02. MUST include:
       - Dukascopy bid/ask cache is licensed to the end user; `tradedesk-dukascopy` downloads the bytes on the end user's machine.
       - miner reads bytes that already exist on disk and does NOT redistribute them.
       - The checked-in `tests/fixtures/cache/` directory contains **synthetic data** produced by `scripts/generate-fixture-cache.sh` (per Plan 07-02 / D7-01) — NO Dukascopy-licensed bytes are committed to the repo.
       - Anyone building against real data needs their own Dukascopy access. Link to the upstream terms: `https://www.dukascopy.com/swiss/english/marketwatch/historical/` (bare URL per D6-04 footer convention; do NOT use a markdown autolink).
       - Per RESEARCH Open Question 6: include a `Verified against tradedesk-dukascopy commit <sha> (<date>)` line. The executor runs `git -C /home/darren/projects/radiusred/tradedesk-dukascopy log -1 --format='%h %ad' --date=short` at land time and substitutes the real values. If `/home/darren/projects/radiusred/tradedesk-dukascopy/` is not a git repo on the executor's machine, the executor MUST document the omission in the SUMMARY rather than fabricating a SHA.

    9. `## See Also` — related-docs trailer per `docs/findings_envelope.md` tail pattern. Include at minimum:
       - `[agent_integration.md](agent_integration.md)` — the consumer-shaped guide to running miner from an agent subprocess.
       - `[findings_envelope.md](findings_envelope.md)` — what the agent receives back.
       - `[../ARCHITECTURE.md](../ARCHITECTURE.md)` — system map.
       - `[../README.md](../README.md)` — landing page.

    10. Apache-2.0 footer — paste the contents of `docs/.license-footer.md` BYTE-IDENTICALLY. Verify via `diff <(tail -8 docs/data_sources.md) <(cat docs/.license-footer.md)` exits 0.

    Style discipline:
    - Use the same `##` (H2) section depth as `docs/agent_integration.md` for top-level sections; `###` for sub-points (e.g., per-column subsections within `## CSV schema` if needed for readability).
    - No fenced code blocks for the path template — use inline backticks (`<root>/<SYMBOL>/...`).
    - Tables welcome for the CSV schema column list (markdown table with columns: Column, Type, Meaning, Source file:line).
    - No emojis. No SPDX header (docs use the footer instead — PATTERNS Pattern B applies to source/scripts/TOML, not markdown).
    - BTreeMap discipline does not apply (this is markdown, not JSON-serialised output).
  </action>
  <acceptance_criteria>
    - `docs/data_sources.md` exists.
    - Six required sections present (each at H2): `for s in "Cache layout" "CSV schema" "Bid vs ask independence" "Time zones and DST" "Gap policies" "Licensing posture"; do grep -q "^## $s" docs/data_sources.md || { echo "missing section: $s"; exit 1; }; done`
    - `## See Also` section present: `grep -c '^## See Also' docs/data_sources.md` returns 1.
    - Apache-2.0 footer byte-identical to canonical: `diff -q <(tail -8 docs/data_sources.md) <(cat docs/.license-footer.md)` exits 0. (If `docs/.license-footer.md` is 8 lines, the last 8 lines of `docs/data_sources.md` should match exactly.)
    - Cites 00-indexed months: `grep -c '00-indexed' docs/data_sources.md` returns at least 1.
    - Cites tick-count volume: `grep -ciE 'tick.count|tick.volume' docs/data_sources.md` returns at least 1.
    - Cites synthetic-stub posture: `grep -c 'synthetic' docs/data_sources.md` returns at least 1.
    - Links to upstream Dukascopy terms: `grep -c 'dukascopy.com/swiss/english/marketwatch/historical' docs/data_sources.md` returns 1.
    - Cites the canonical reader: `grep -c 'crates/miner-reader-dukascopy' docs/data_sources.md` returns at least 1.
    - Cites the DST tests: `grep -cE 'dst_(spring_forward|fall_back)' docs/data_sources.md` returns at least 1.
    - Either contains a real `Verified against tradedesk-dukascopy commit` line OR a documented omission note: `grep -c 'Verified against tradedesk-dukascopy commit\|verification SHA omitted' docs/data_sources.md` returns at least 1.
    - File length within budget: `wc -l docs/data_sources.md | awk '{print $1}'` returns a value between 130 and 320 (looser than D7-02's 180-250 estimate to allow editorial judgment).
    - No emojis: `grep -cP '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' docs/data_sources.md` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; test -f docs/data_sources.md; for s in "Cache layout" "CSV schema" "Bid vs ask independence" "Time zones and DST" "Gap policies" "Licensing posture"; do grep -q "^## $s" docs/data_sources.md || { echo "missing: $s"; exit 1; }; done; grep -q "^## See Also" docs/data_sources.md; diff -q <(tail -8 docs/data_sources.md) <(cat docs/.license-footer.md); grep -q "00-indexed" docs/data_sources.md; grep -qiE "tick.count|tick.volume" docs/data_sources.md; grep -q "synthetic" docs/data_sources.md; grep -q "dukascopy.com/swiss/english/marketwatch/historical" docs/data_sources.md; grep -q "crates/miner-reader-dukascopy" docs/data_sources.md; grep -qE "dst_(spring_forward|fall_back)" docs/data_sources.md; L=$(wc -l < docs/data_sources.md); [ "$L" -ge 130 ] && [ "$L" -le 320 ]'</automated>
  </verify>
  <done>docs/data_sources.md exists with six required sections + See Also + byte-identical Apache-2.0 footer; cites all four ground-truth sources (reader, parser, DST tests, upstream Dukascopy terms).</done>
</task>

<task type="auto">
  <name>Task 2: Insert ## Data source caveats section into README + verify rendering</name>
  <files>README.md</files>
  <read_first>
    - README.md (post-Plan-07-02 state — the `## Example` block now uses `./tests/fixtures/cache` and includes the "If you cloned the repo" sentence; identify the exact insertion point for the new section. Recommendation: INSERT immediately AFTER the entire `## Example` block ends, BEFORE whatever section currently follows it. Read the file to determine the exact transition point.)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-CONTEXT.md §D7-02 (the exact 6-line body)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §"README.md (MODIFY)" (edit instructions)
  </read_first>
  <action>
    Locate the end of the `## Example` block in README.md (post Plan 07-02). The block currently ends with the "If you cloned the repo, this works as-is — no external download needed." sentence.

    Insert immediately AFTER that block, BEFORE the next section header, the following body (exactly as documented in D7-02):

    ```markdown
    ## Data source caveats

    `tradedesk-miner` reads the cache layout `tradedesk-dukascopy` produces. A few
    non-obvious conventions matter for interpreting findings:

    - Months are **00-indexed** on disk (`2024/00/` = January, `2024/11/` = December).
    - The `volume` column is a **tick count**, not lot volume.
    - Bid and ask sides are processed independently; spread reconstruction is out of scope.
    - Weekend and exchange-holiday gaps are intentional, not missing data; the
      `gap_policy` flag controls how scans treat them.

    See [docs/data_sources.md](docs/data_sources.md) for the full reference, including
    the data licensing posture.
    ```

    Do NOT add the `## Performance` pointer in this plan — Plan 07-08 owns that addition (sequencing avoids file-conflict in Wave 3).

    Verify the insertion didn't break the markdown structure: every `## ` line in the file should remain a section header (not orphaned in a code block or list).
  </action>
  <acceptance_criteria>
    - README.md contains the new section: `grep -c '^## Data source caveats' README.md` returns 1.
    - The four caveats are listed: `grep -c '00-indexed' README.md` returns at least 1; `grep -ciE 'tick.count' README.md` returns at least 1; `grep -c 'bid and ask' README.md` returns at least 1 (case-insensitive check OK); `grep -c 'weekend' README.md` returns at least 1.
    - Link to docs/data_sources.md present: `grep -c '\[docs/data_sources\.md\](docs/data_sources\.md)' README.md` returns 1.
    - The section appears AFTER `## Example`: `awk '/^## Example/{found=1} /^## Data source caveats/{if(found) hit=1} END {exit hit?0:1}' README.md` exits 0.
    - The Plan 07-02 quickstart edits are preserved: `grep -c './tests/fixtures/cache' README.md` returns at least 1, `grep -c 'seas.bucket.hour_of_day@1' README.md` returns at least 1.
    - Cargo build still works (sanity — markdown edit shouldn't break it): `cargo build --workspace` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; grep -q "^## Data source caveats" README.md; grep -q "00-indexed" README.md; grep -qiE "tick.count" README.md; grep -qi "bid and ask" README.md; grep -qi "weekend" README.md; grep -qE "\[docs/data_sources\.md\]\(docs/data_sources\.md\)" README.md; awk "/^## Example/{found=1} /^## Data source caveats/{if(found) hit=1} END {exit hit?0:1}" README.md; grep -q "./tests/fixtures/cache" README.md; grep -q "seas.bucket.hour_of_day@1" README.md; cargo build --workspace'</automated>
  </verify>
  <done>README.md has new `## Data source caveats` section after `## Example`, with the canonical 6-line body and a link to docs/data_sources.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| publicly-readable repo → upstream Dukascopy licensing | The doc explicitly clarifies that no Dukascopy bytes are redistributed; documentation IS the mitigation here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Compliance + Reputation | Reader misinterprets fixture cache as real Dukascopy data | mitigate | `## Licensing posture` section in docs/data_sources.md explicitly documents the synthetic-stub policy; cross-references Plan 07-02's generator script. README ## Data source caveats summary surfaces this for casual readers. |
| T-07-07-02 | Documentation drift | docs/data_sources.md cites outdated source-file line numbers | accept | RESEARCH Open Question 6 recommendation includes the `Verified against tradedesk-dukascopy commit <sha>` line so future-readers can re-verify; doc/source drift is a v1.x concern, not a v1.0 blocker. |
</threat_model>

<verification>
- `cargo build --workspace` exits 0 (markdown edits should not break compilation; sanity check).
- `docs/data_sources.md` parses as valid markdown (no broken code blocks or unclosed sections).
- README structure preserved: section count grew by exactly 1 (new `## Data source caveats`).
- `diff -q <(tail -8 docs/data_sources.md) <(cat docs/.license-footer.md)` exits 0 (footer byte-identity).
</verification>

<success_criteria>
1. `docs/data_sources.md` exists, covers six required sections (Cache layout / CSV schema / Bid vs ask / Time zones / Gap policies / Licensing).
2. Apache-2.0 footer byte-identical to `docs/.license-footer.md`.
3. README `## Data source caveats` section exists, 6 lines summary, links to deep doc.
4. RESEARCH Open Question 6 honored: `Verified against tradedesk-dukascopy commit <sha>` line included (or documented omission).
</success_criteria>

<output>
Create `.planning/phases/07-hardening-benchmarks-reproducibility/07-07-SUMMARY.md` when done.
</output>
