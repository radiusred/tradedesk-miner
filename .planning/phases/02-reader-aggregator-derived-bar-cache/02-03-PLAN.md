---
phase: 02-reader-aggregator-derived-bar-cache
plan: 03
type: execute
wave: 1
depends_on:
  - 02-01
  - 02-02
files_modified:
  - crates/miner-core/tests/dst_spring_forward.rs
  - crates/miner-core/tests/dst_fall_back.rs
  - crates/miner-core/tests/aggregator_edge_cases.rs
autonomous: true
requirements:
  - CACHE-04
must_haves:
  truths:
    - "Aggregator works in UTC throughout; no localtime conversion anywhere. DST is INVISIBLE to the aggregator."
    - "DST spring-forward fixture: source 1m bars span the wall-clock hour-skip (e.g., London 2024-03-31 01:00 -> 02:00 BST). The aggregator's 15m / 1h / 1d output bars across that transition are evenly spaced in UTC — bar boundaries at 01:00 / 01:15 / 01:30 / 01:45 / 02:00 UTC etc."
    - "DST fall-back fixture: source 1m bars span the wall-clock hour-repeat (London 2024-10-27 02:00 -> 01:00 GMT). The aggregator's output bars across that transition remain evenly spaced in UTC."
    - "Weekend gap: source data missing Fri 22:00 UTC -> Sun 22:00 UTC produces NO bars and NO gap-manifest entries (closed time is not a gap; Plan 04 owns the manifest)."
    - "Holiday: source data missing Dec 25 / Jan 1 produces no bars and no gap entries (Calendar::is_open_at returns false)."
    - "Instrument first cache day: aggregator handles the case where the source cache starts on a date in the middle of the requested range — bars before first-available-day are simply not emitted; intra-range bars work normally."
    - "Instrument last cache day: aggregator handles the case where the source cache ends before range.end — bars after last-available-day are simply not emitted."
    - "Partial-bar session open: When trading-session re-opens with <50% of the bucket's open-minutes present, the aggregator emits the bar with the partial-OHLC values it has."
    - "unsafe_code = \"forbid\" stays. All tracing through tracing::* macros. BTreeMap only for serialised maps."
  artifacts:
    - path: "crates/miner-core/tests/dst_spring_forward.rs"
      provides: "spring-forward DST integration test"
      contains: "fn bars_evenly_spaced_across_spring_forward"
    - path: "crates/miner-core/tests/dst_fall_back.rs"
      provides: "fall-back DST integration test"
      contains: "fn bars_evenly_spaced_across_fall_back"
    - path: "crates/miner-core/tests/aggregator_edge_cases.rs"
      provides: "weekend / holiday / first-day / last-day / partial-session-open edge cases"
      contains: "fn weekend_gap_emits_no_bars"
  key_links:
    - from: "crates/miner-core/tests/dst_spring_forward.rs"
      to: "crates/miner-core/tests/aggregator_fixtures.rs"
      via: "mod aggregator_fixtures; — shared MockReader"
      pattern: "mod aggregator_fixtures"
    - from: "crates/miner-core/tests/dst_fall_back.rs"
      to: "crates/miner-core/tests/aggregator_fixtures.rs"
      via: "mod aggregator_fixtures; — shared MockReader"
      pattern: "mod aggregator_fixtures"
---

<objective>
Ship the DST + edge-case fixture tests for the aggregator. These integration tests live in `tests/` and consume the shared `aggregator_fixtures.rs` MockReader from Plan 02. They are NOT in the unit-test path; they're integration tests against the FROZEN `miner_core::*` re-export surface.

Purpose: CACHE-04 success criterion 5 explicitly enumerates these edge cases: DST spring-forward, DST fall-back, weekend gap, holiday, instrument first/last cache day, partial-bar session open. Each is a separate test file (or a section within one file) so test parallelism is maximised.

Output: 2 dedicated DST test files + 1 edge-case file containing 5 additional fixture tests. Together they close out CACHE-04's edge-case verification debt (the byte-identity gate + non-edge unit tests were Plan 02; the gap manifest tests are Plan 04).

**Wave 1 with intra-wave dependency edge on 02-02 (revision pass 1):** This plan compiles against `crates/miner-core/tests/aggregator_fixtures.rs` which is OWNED by Plan 02-02 Task 2. Adding `02-02` to `depends_on` makes that compile-time dependency explicit. `gsd-executor` honours `depends_on` regardless of wave, so execute-phase serialises 02-02 → 02-03 inside Wave 1 even though both are wave 1; 02-04 runs in true parallel with both (owns its own `crates/miner-core/src/gap.rs` and does not touch `tests/aggregator_fixtures.rs`). The planner rule `wave = max(deps) + 1` is intentionally NOT applied here because the execution engine handles the intra-wave edge correctly; bumping 02-03 to Wave 2 would needlessly serialise it after Plans that have no real conflict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-reader-aggregator-derived-bar-cache/02-CONTEXT.md
@.planning/phases/02-reader-aggregator-derived-bar-cache/02-RESEARCH.md
@.planning/phases/02-reader-aggregator-derived-bar-cache/02-PATTERNS.md
@.planning/phases/02-reader-aggregator-derived-bar-cache/02-VALIDATION.md
@.planning/phases/02-reader-aggregator-derived-bar-cache/02-01-PLAN.md
@.planning/phases/02-reader-aggregator-derived-bar-cache/02-02-PLAN.md
@crates/miner-core/src/lib.rs
@crates/miner-core/src/aggregator.rs
@crates/miner-core/src/calendar.rs
@crates/miner-core/src/reader.rs
@crates/miner-core/tests/aggregator_fixtures.rs
@crates/miner-core/tests/schema_roundtrip.rs

<interfaces>
<!-- From Plan 02 (Wave 1 earlier task): -->
From crates/miner-core/tests/aggregator_fixtures.rs:
```rust
pub struct MockReader {
    pub bars: BTreeMap<(String, Side, NaiveDate), Vec<RawBar>>,
    pub calendar: Calendar,
}
impl MockReader {
    pub fn new() -> Self;
    pub fn with_calendar(self, c: Calendar) -> Self;
    pub fn insert_day(&mut self, symbol: &str, side: Side, date: NaiveDate, bars: Vec<RawBar>);
}
impl Reader for MockReader { type Error = std::io::Error; /* ... */ }

pub fn build_24h_1m_bars(date: NaiveDate, open_at_zero: f64) -> Vec<RawBar>;
pub fn build_partial_day_1m_bars(start: DateTime<Utc>, count: usize, open: f64) -> Vec<RawBar>;
```

From miner_core (Plan 02):
```rust
pub use aggregator::{AGGREGATOR_VERSION, AggParams, BarFrame, Timeframe, aggregate, AggregateError};
pub use calendar::Calendar;
pub use reader::{Blake3Hex, ClosedRangeUtc, RawBar, Reader, Side};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DST spring-forward fixture test</name>
  <files>crates/miner-core/tests/dst_spring_forward.rs</files>
  <read_first>
    - crates/miner-core/tests/aggregator_fixtures.rs (Plan 02 — MockReader + bar helpers)
    - crates/miner-core/src/aggregator.rs (Plan 02 — aggregator kernel + Timeframe)
    - 02-CONTEXT.md D2-11 (Aggregator is fully UTC-agnostic about DST)
    - 02-RESEARCH.md §"Aggregator Semantics" §"Gap omission rule" lines 518-524
    - 02-PATTERNS.md "crates/miner-core/tests/dst_spring_forward.rs" lines 52-53 (analog: schema_roundtrip.rs)
    - VALIDATION.md per-task row "DST spring-forward fixture: bars are evenly spaced through the transition"
  </read_first>
  <action>
    Create `crates/miner-core/tests/dst_spring_forward.rs`:

    1. `mod aggregator_fixtures;` to pull in Plan 02's MockReader + helpers.

    2. `use miner_core::{aggregate, AggParams, Calendar, ClosedRangeUtc, RawBar, Reader, Side, Timeframe};`
    3. `use chrono::{NaiveDate, TimeZone, Utc};`

    4. `#[test] fn bars_evenly_spaced_across_spring_forward()`:
       - The 2024 London spring-forward transition: at 2024-03-31T01:00:00Z UTC, London wall clocks jump from 01:00 GMT to 02:00 BST. Dukascopy timestamps are UTC throughout (verified in 02-RESEARCH.md §"CSV Schema"); the wall-clock hour-skip is invisible in the UTC source data. The test must prove the aggregator handles this WITHOUT special-casing.
       - Build a MockReader with 1m bars for `EURUSD bid` on 2024-03-30, 2024-03-31, 2024-04-01 (3 days × 1440 bars each). Use `aggregator_fixtures::build_24h_1m_bars(date, 1.0)` to populate. **All 4320 bars present in UTC** — no missing minutes.
       - Aggregate at `Timeframe::Tf15m` for the range `[2024-03-30T00:00:00Z, 2024-04-02T00:00:00Z)`.
       - Assert `frame.len() == 3 * 96 == 288` 15m bars.
       - Assert bars are evenly spaced in UTC: `for i in 1..frame.len() { assert_eq!(frame.ts_open_utc[i] - frame.ts_open_utc[i-1], chrono::Duration::minutes(15)); }`.
       - Assert the bar boundaries at the transition are intact: find the bar with `ts_open_utc == 2024-03-31T01:00:00Z`. Assert it exists. Find the next bar — its `ts_open_utc` MUST be `2024-03-31T01:15:00Z` (15 minutes later in UTC), NOT `2024-03-31T02:15:00Z` (which would indicate a localtime-leak bug).

    5. `#[test] fn bars_evenly_spaced_across_spring_forward_1h()`:
       - Same input dataset, but aggregate at `Tf1h`. Assert `frame.len() == 72` (3 × 24).
       - Find the bar with `ts_open_utc == 2024-03-31T01:00:00Z`. Assert it exists.
       - Find the next bar — its `ts_open_utc` MUST be `2024-03-31T02:00:00Z` (1h later in UTC).

    6. `#[test] fn bars_evenly_spaced_across_spring_forward_1d()`:
       - Same input dataset, but aggregate at `Tf1d`. Assert `frame.len() == 3`.
       - Assert `frame.ts_open_utc == [2024-03-30T00:00:00Z, 2024-03-31T00:00:00Z, 2024-04-01T00:00:00Z]`.

    All three tests run in parallel by default. No `#[serial_test::serial]` (no env mutation).

    **Implementation note for the helper:** `aggregator_fixtures::build_24h_1m_bars` already produces 1440 UTC-aligned bars per day. The DST test just stitches 3 days together — no special-casing needed. The "DST" in the name is informational: it documents the human-time event the test covers, but the test fixture itself is plain UTC bars.
  </action>
  <verify>
    <automated>cargo test -p miner-core --test dst_spring_forward 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `crates/miner-core/tests/dst_spring_forward.rs` exists with 3 tests.
    - `cargo test -p miner-core --test dst_spring_forward bars_evenly_spaced_across_spring_forward` exits 0.
    - `cargo test -p miner-core --test dst_spring_forward bars_evenly_spaced_across_spring_forward_1h` exits 0.
    - `cargo test -p miner-core --test dst_spring_forward bars_evenly_spaced_across_spring_forward_1d` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: DST fall-back fixture test</name>
  <files>crates/miner-core/tests/dst_fall_back.rs</files>
  <read_first>
    - crates/miner-core/tests/aggregator_fixtures.rs (Plan 02 — MockReader + bar helpers)
    - crates/miner-core/tests/dst_spring_forward.rs (Plan 03 Task 1 — same shape, just different date)
    - 02-CONTEXT.md D2-11 (DST invisibility)
    - VALIDATION.md per-task row "DST fall-back fixture"
  </read_first>
  <action>
    Create `crates/miner-core/tests/dst_fall_back.rs`:

    Mirror Plan 03 Task 1 structure exactly, but for the 2024 London fall-back transition: at 2024-10-27T01:00:00Z UTC, London wall clocks jump from 02:00 BST back to 01:00 GMT. Like spring-forward, the wall-clock hour-repeat is invisible in the UTC source data.

    1. `mod aggregator_fixtures;` and the same imports.

    2. `#[test] fn bars_evenly_spaced_across_fall_back()`:
       - Build MockReader with 1m bars for `EURUSD bid` on 2024-10-26, 2024-10-27, 2024-10-28 (3 days × 1440 bars).
       - Aggregate at Tf15m for `[2024-10-26T00:00:00Z, 2024-10-29T00:00:00Z)`.
       - Assert `frame.len() == 288`.
       - Assert even spacing.
       - Assert the bar at `ts_open_utc == 2024-10-27T01:00:00Z` exists AND the next bar is `2024-10-27T01:15:00Z` (UTC-evenly-spaced).

    3. `#[test] fn bars_evenly_spaced_across_fall_back_1h()` — same shape, Tf1h, 72 bars.

    4. `#[test] fn bars_evenly_spaced_across_fall_back_1d()` — same shape, Tf1d, 3 bars.

    The implementation is structurally identical to Task 1's spring-forward test. The two files exist as separate units so the executor can run them in parallel without serial deps.

    **Why both?** Spring-forward and fall-back exercise different mental-model failures: spring-forward might trigger off-by-one in code that thought-incorrectly about "the missing hour", while fall-back might trigger duplicate-handling bugs around "the repeated hour". The aggregator is UTC-only so neither bug class applies — but the tests gate against future regressions where someone adds a localtime helper.
  </action>
  <verify>
    <automated>cargo test -p miner-core --test dst_fall_back 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `crates/miner-core/tests/dst_fall_back.rs` exists with 3 tests.
    - All 3 fall-back tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Aggregator edge cases (weekend / holiday / first day / last day / partial session open)</name>
  <files>crates/miner-core/tests/aggregator_edge_cases.rs</files>
  <read_first>
    - crates/miner-core/tests/aggregator_fixtures.rs (Plan 02 — MockReader + helpers)
    - crates/miner-core/src/aggregator.rs (Plan 02 — aggregator kernel)
    - crates/miner-core/src/calendar.rs (Plan 02 — Calendar::fx_major)
    - 02-CONTEXT.md D2-07 (FX-major: Fri 22:00 UTC -> Sun 22:00 UTC closed, Dec 25 + Jan 1 holidays)
    - 02-CONTEXT.md D2-19 (Partial-bar threshold 50%)
    - 02-RESEARCH.md §"Aggregator Semantics" §"Gap omission rule" lines 518-524 (closed-hours buckets emit no bar, no gap entry)
    - ROADMAP.md Phase 2 success criterion 5 (the edge cases enumerated: DST spring/fall, weekend, holiday, instrument first/last cache day, partial-bar session open)
    - VALIDATION.md — manifest-side closed-hours tests are Plan 04's; this plan tests the aggregator-side bar-emission behaviour
  </read_first>
  <action>
    Create `crates/miner-core/tests/aggregator_edge_cases.rs`:

    1. `mod aggregator_fixtures;`. Standard imports.

    2. `#[test] fn weekend_gap_emits_no_bars()`:
       - Build MockReader with 1m bars ONLY for `EURUSD bid` on Friday 2024-06-14 (full day) and the first half of Sunday 2024-06-16 (only 22:00-23:59 UTC, when markets re-open). NO bars for Saturday 2024-06-15. NO bars for Friday after 22:00 UTC.
       - Aggregate at Tf1h for `[2024-06-14T00:00:00Z, 2024-06-17T00:00:00Z)`.
       - Assert `frame.len() ==` (24 Friday hours - the 22:00 and 23:00 closed hours) + (Sunday 22:00 and 23:00 open hours) = 22 + 2 = 24 hours.
       - Assert NO bar exists with `ts_open_utc` in `[Fri 22:00 UTC, Sun 22:00 UTC)` — verify via `frame.ts_open_utc.iter().all(|ts| !(*ts >= friday_22 && *ts < sunday_22))`.
       - **Implementation reality check:** the aggregator emits a bar whenever there are source 1m bars in the bucket. The aggregator does NOT itself consult Calendar to omit closed-hour buckets — its omission rule is "no source bars → no output bar". Plan 04's gap detector consults Calendar to decide whether the absence is a gap or a closed-hours hole. So this test essentially verifies the data setup: closed hours have no source bars → aggregator emits no bars there. Document this in a test-level comment.

    3. `#[test] fn christmas_day_emits_no_bars()`:
       - Build MockReader with 1m bars for Dec 24 (full day) and Dec 26 (full day) in 2024, but NO bars for Dec 25 (Christmas — closed per Calendar::fx_major holidays).
       - Aggregate at Tf1d for `[2024-12-24T00:00:00Z, 2024-12-27T00:00:00Z)`.
       - Assert `frame.len() == 2`. The two bars are Dec 24 and Dec 26.
       - Assert NO bar with `ts_open_utc == 2024-12-25T00:00:00Z`.

    4. `#[test] fn instrument_first_cache_day()`:
       - Build MockReader with 1m bars ONLY for `EURUSD bid` on 2024-06-15 onwards (full day) — but the requested range starts on 2024-06-13.
       - Aggregate at Tf1d for `[2024-06-13T00:00:00Z, 2024-06-17T00:00:00Z)`.
       - Assert `frame.len() == 2` (Jun 15, Jun 16). No bar for Jun 13 / Jun 14 — instrument's history doesn't go back that far.
       - This proves the aggregator doesn't error or panic on a range partially preceding the data; it just omits the missing portion.

    5. `#[test] fn instrument_last_cache_day()`:
       - Build MockReader with 1m bars ONLY for `EURUSD bid` UP TO 2024-06-16 — the range extends to 2024-06-20.
       - Aggregate at Tf1d for `[2024-06-15T00:00:00Z, 2024-06-21T00:00:00Z)`.
       - Assert `frame.len() == 2` (Jun 15, Jun 16). No bar for Jun 17-20 — instrument's history ends.

    6. `#[test] fn partial_bar_session_open()`:
       - The Sunday market re-open is 22:00 UTC. If the data source has bars for 22:00, 22:01, ..., 22:14 (15 minutes; full first 15m bucket), the Tf15m bucket at 22:00 emits a full bar with 15 constituent 1m bars.
       - But if data starts at 22:08 (8 minutes into the first 15m bucket, so 7 of 15 minutes missing), per D2-19's "more than 50% of the 1m sub-buckets missing" rule, the bar IS emitted with whatever 1m bars are present (8 of 15 = 53% present, 47% missing) — **the bar IS emitted, and the gap manifest entry is Plan 04's responsibility**.
       - The aggregator's responsibility here: emit the bar with `open` = first present bar's open, `high/low/close` per the 8 present bars, `tick_volume` = sum of the 8 bars.
       - **Test setup:** MockReader has 1m bars for Sunday 2024-06-16 from 22:08 UTC onwards (count: 1440 - (22*60+8) = 8 bars at 22:08, ..., 22:15, then continuing — actually for the test, just provide 22:08 to 22:14 (7 bars) and verify the 15m bucket at 22:00 is emitted.
       - **Correction:** 22:08, 22:09, 22:10, 22:11, 22:12, 22:13, 22:14 = 7 bars. The 15m bucket is `[22:00, 22:15)` — 7 of 15 minutes have data, 8 missing → 8/15 = 53% missing → just over the 50% threshold. But D2-19 says ">50% missing during open hours → emitted AND flagged in gap manifest". 8/15 = 53.3% > 50% → emit + flag.
       - Aggregate at Tf15m for the range `[2024-06-16T22:00:00Z, 2024-06-16T22:30:00Z)`.
       - Assert `frame.len() == 1` (the 22:00 bucket is emitted; the 22:15 bucket has 0 bars in this range so omitted).
       - Assert `frame.ts_open_utc[0] == 2024-06-16T22:00:00Z`.
       - Assert `frame.open[0] == bar at 22:08's open` (per the OHLC reduction rule — `open = r[0].open`).
       - Assert `frame.tick_volume[0]` is the sequential sum of all 7 bars' tick_volumes.
       - **Gap-manifest emission is NOT tested here — that's Plan 04. This test is only about aggregator bar-emission semantics.**

    Total: 5 test functions. Each uses `aggregator_fixtures::MockReader` + the bar-helper functions.
  </action>
  <verify>
    <automated>cargo test -p miner-core --test aggregator_edge_cases 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    - `crates/miner-core/tests/aggregator_edge_cases.rs` exists with 5 tests.
    - All 5 edge-case tests pass.
    - Tests use only the FROZEN `miner_core::*` re-export surface; no direct module access.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → aggregator | MockReader from `aggregator_fixtures.rs` is in-test trusted code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | aggregator with localtime leak | mitigate | DST spring + fall tests assert UTC-evenly-spaced bars; a localtime leak would fail the `frame.ts_open_utc[i] - frame.ts_open_utc[i-1] == Duration::minutes(15)` assertion. |
| T-02-10 | Tampering | aggregator silently emitting/dropping bars near range boundaries | mitigate | first_cache_day + last_cache_day tests gate boundary behaviour. |
</threat_model>

<verification>
Run `cargo test --workspace --test dst_spring_forward --test dst_fall_back --test aggregator_edge_cases` and confirm all green.

Per success criterion: CACHE-04 success criterion 5 (DST spring/fall, weekend, holiday, instrument first/last cache day, partial-bar session open) — 5 of 6 covered here (DST counted as 2 separate fixtures = 2 of 6; weekend, holiday, first day, last day, partial-session-open = 5 more; total 7 tests across DST + edge files). Plan 04 covers the gap-manifest-emission side of weekend/holiday/intra-day-gap.
</verification>

<success_criteria>
1. 3 DST spring-forward tests + 3 DST fall-back tests pass.
2. 5 edge-case tests (weekend, christmas, first day, last day, partial session open) pass.
3. All tests run via the public `miner_core::*` re-export surface.
4. All tests use the shared `aggregator_fixtures::MockReader` from Plan 02.
5. `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
</success_criteria>

<output>
Create `.planning/phases/02-reader-aggregator-derived-bar-cache/02-03-SUMMARY.md` when done.
</output>
