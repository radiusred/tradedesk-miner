---
phase: 02-reader-aggregator-derived-bar-cache
plan: 06
type: execute
wave: 3
depends_on:
  - 02-05
  - 02-04
  - 02-03
files_modified:
  - crates/miner-core/tests/full_determinism.rs
  - crates/miner-core/src/lib.rs
  - crates/miner-reader-dukascopy/tests/reader_trait_object_safety.rs
  - .planning/phases/02-reader-aggregator-derived-bar-cache/02-VALIDATION.md
autonomous: true
requirements:
  - CACHE-04
  - CACHE-06
must_haves:
  truths:
    - "User can run the full Phase 2 pipeline (reader → aggregator → cache) twice on the same synthetic source and observe byte-identical Arrow IPC + sidecar JSON bytes."
    - "User can import every Phase 2 public type via `use miner_core::*;` — the FROZEN re-export block exposes Reader, RawBar, Side, ClosedRangeUtc, Blake3Hex, Calendar, Timeframe, AggParams, BarFrame, AggregateError, aggregate, AGGREGATOR_VERSION, BarCache, CacheError, ARROW_SCHEMA_VERSION, FingerprintSidecar, GapDetector, GapManifest, GapSpan, GapReason — every name reachable without naming an internal module."
    - "Reader trait object-safety regression test lives in miner-reader-dukascopy (in addition to the inline one in miner-core)."
    - "Workspace dev-deps audit: every new dev-dep (proptest, insta, tempfile, serial_test) declared exactly once at the correct level."
    - "VALIDATION.md `## Per-Task Verification Map` has every TBD|TBD row replaced with the real {plan-id}-{task-id} identifier."
    - "unsafe_code = \"forbid\" stays. All tracing through tracing::* macros. BTreeMap only for serialised maps. No HashMap anywhere in the Phase 2 deterministic write paths."
    - "Phase 2 success criteria 1-5 in ROADMAP.md are all measurable + closed."
  artifacts:
    - path: "crates/miner-core/tests/full_determinism.rs"
      provides: "End-to-end pipeline byte-identity test (reader -> aggregator -> cache write twice)"
      contains: "fn two_runs_byte_identical"
    - path: "crates/miner-reader-dukascopy/tests/reader_trait_object_safety.rs"
      provides: "Standalone dyn-compat test for DukascopyReader"
      contains: "fn reader_trait_object_safety"
  key_links:
    - from: "crates/miner-core/tests/full_determinism.rs"
      to: "miner_reader_dukascopy::DukascopyReader"
      via: "real-source-format pipeline test"
      pattern: "DukascopyReader::new"
    - from: "crates/miner-core/src/lib.rs"
      to: "ALL Phase 2 public types"
      via: "FROZEN re-export block"
      pattern: "pub use"
---

<objective>
Close out Phase 2: the end-to-end determinism gate that proves the full pipeline (DukascopyReader → aggregate → BarCache write) is byte-identical across two runs on the same synthetic source. Audit the `miner_core::*` re-export surface so every Phase 2 type is reachable through the frozen public-surface block. Add a separate `reader_trait_object_safety` integration test in `miner-reader-dukascopy` matching the inline one in `miner-core`. Update VALIDATION.md's per-task verification map with the actual plan-task IDs.

Purpose: This is the goal-backward closer. Plans 01-05 produced the building blocks; Plan 06 wires the gate that proves they integrate correctly. Without `full_determinism.rs`, CACHE-04's success-criterion #5 ("byte-identical re-run determinism") and CACHE-06's "deterministic cache writes" are unverified at the integration level — unit + cache-smoke tests prove their parts but not the seam.

Output: full_determinism.rs + a separate dyn-compat test crate + lib.rs FROZEN-surface audit + VALIDATION.md update. Phase 2 COMPLETE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-reader-aggregator-derived-bar-cache/02-CONTEXT.md
@.planning/phases/02-reader-aggregator-derived-bar-cache/02-RESEARCH.md
@.planning/phases/02-reader-aggregator-derived-bar-cache/02-PATTERNS.md
@.planning/phases/02-reader-aggregator-derived-bar-cache/02-VALIDATION.md
@.planning/phases/02-reader-aggregator-derived-bar-cache/02-01-PLAN.md
@.planning/phases/02-reader-aggregator-derived-bar-cache/02-02-PLAN.md
@.planning/phases/02-reader-aggregator-derived-bar-cache/02-03-PLAN.md
@.planning/phases/02-reader-aggregator-derived-bar-cache/02-04-PLAN.md
@.planning/phases/02-reader-aggregator-derived-bar-cache/02-05-PLAN.md
@crates/miner-core/src/lib.rs
@crates/miner-cli/tests/cli_streams.rs
@crates/miner-reader-dukascopy/tests/fixtures/mod.rs
@crates/miner-reader-dukascopy/tests/reader_smoke.rs

<interfaces>
<!-- Everything Plans 01-05 produced. The Phase 2 FROZEN public surface should be: -->
```rust
pub use reader::{Blake3Hex, ClosedRangeUtc, RawBar, Reader, Side};
pub use calendar::Calendar;
pub use aggregate::{AGGREGATOR_VERSION, AggParams, AggregateError, BarFrame, Timeframe, aggregate};
pub use cache::{ARROW_SCHEMA_VERSION, BarCache, CacheError, build_arrow_schema};
pub use cache::fingerprints::FingerprintSidecar;
pub use gap::{GapDetector, GapManifest, GapReason, GapSpan};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: full_determinism.rs — end-to-end pipeline byte-identity test</name>
  <files>crates/miner-core/tests/full_determinism.rs</files>
  <read_first>
    - crates/miner-reader-dukascopy/tests/fixtures/mod.rs (Plan 01 — SyntheticCache helper)
    - crates/miner-core/src/cache.rs (Plan 05 — BarCache + get_or_build)
    - crates/miner-cli/tests/cli_streams.rs (lines 320-450 — twice-run byte-identity Test 6 pattern)
    - 02-RESEARCH.md §"Determinism contract" lines 526-534 (5 safeguards)
    - 02-PATTERNS.md §"crates/miner-core/tests/full_determinism.rs" lines 794-823 (the full pattern)
    - VALIDATION.md row for "Two full runs of aggregator + cache produce byte-identical .arrow + .fingerprints.json"
  </read_first>
  <action>
    Create `crates/miner-core/tests/full_determinism.rs`:

    1. **Dependency on the SyntheticCache fixture** — this test uses `miner_reader_dukascopy::DukascopyReader` driven against a synthetic cache, NOT the MockReader. The whole point is to prove the REAL reader pipeline is deterministic, including the zstd-CSV parse + the path layout + the BLAKE3 fingerprints + the Arrow write. Add `miner-reader-dukascopy = { path = "../miner-reader-dukascopy" }` to `[dev-dependencies]` of `miner-core/Cargo.toml` if not already present — this creates a dev-only cycle (miner-core dev-depends on miner-reader-dukascopy which in turn depends on miner-core). This is fine: dev-deps don't participate in the main dependency graph and cargo handles it.

       Pull the SyntheticCache helper. **Option A:** copy/inline its essential bits into a new `mod synthetic_cache;` inside the test (avoids cross-crate dev-dep coupling). **Option B:** add `miner-reader-dukascopy` as a dev-dep and import via `#[path = "../../miner-reader-dukascopy/tests/fixtures/mod.rs"] mod fixtures;` — this is fragile (path-based include). **Pick Option A: inline a minimal `SyntheticCacheBuilder` helper inside the test file.** It's ~30 lines of zstd-encoder + path_layout glue; well worth the inline copy to keep the dependency story clean.

       However, `path_layout::day_csv_zst` IS part of the public surface of `miner-reader-dukascopy` (Plan 01 re-exported it). So the test does need `miner-reader-dukascopy` as a dev-dep to construct paths the reader will recognise. **Use Option B with cleanup:** add `miner-reader-dukascopy = { path = "../miner-reader-dukascopy" }` to miner-core's `[dev-dependencies]`. Use `miner_reader_dukascopy::{DukascopyReader, day_csv_zst}` in the test. **Verify with cargo: cyclic dev-deps are allowed.**

    2. `use chrono::{NaiveDate, Utc};`
       `use miner_core::{aggregate, AggParams, BarCache, ClosedRangeUtc, Reader, Side, Timeframe};`
       `use miner_reader_dukascopy::{DukascopyReader, day_csv_zst};`
       `use tempfile::TempDir;`

    3. Helper function `fn build_synthetic_dukascopy_cache(root: &std::path::Path) -> ()`:
       - Create 3 days of synthetic Dukascopy CSV files: 2024-06-15 (Sat — closed, but fine for test data, will produce a gap or no bars per Calendar — actually pick 2024-06-12/13/14 which is Wed/Thu/Fri to stay in open hours).
       - For each (date, side) ∈ {(2024-06-12, Bid), (2024-06-12, Ask), (2024-06-13, Bid), (2024-06-13, Ask), (2024-06-14, Bid), (2024-06-14, Ask)}, compose a 1m CSV with header `timestamp,open,high,low,close,volume` and 1440 rows. Synthetic values: `open = 1.0 + i * 0.0001`, `high = open + 0.00005`, `low = open - 0.00005`, `close = open + 0.00003`, `volume = (i % 100) as f64 + 1.0`. Timestamps `2024-06-12 00:00:00+00:00`, `2024-06-12 00:01:00+00:00`, etc.
       - Use `path_layout::day_csv_zst(root, "EURUSD", date, side)` to build the path; mkdir-p parent; zstd-encode the CSV bytes with level 3; write to file.

    4. Helper function `fn run_full_pipeline(source_root: &Path, cache_root: &Path) -> (Vec<u8>, Vec<u8>)`:
       - `let reader = DukascopyReader::new(source_root);`
       - `let cache = BarCache::new(cache_root);`
       - `let range = ClosedRangeUtc { start: Utc.with_ymd_and_hms(2024, 6, 12, 0, 0, 0).unwrap(), end: Utc.with_ymd_and_hms(2024, 6, 15, 0, 0, 0).unwrap() };`
       - `let _frame = cache.get_or_build(&reader, AggParams { symbol: "EURUSD", side: Side::Bid, tf: Timeframe::Tf15m, range }).unwrap();`
       - Read back the Arrow file bytes + sidecar bytes:
         - `let arrow_path = cache_root.join("dukascopy/EURUSD/15m_bid.arrow");`
         - `let sidecar_path = cache_root.join("dukascopy/EURUSD/15m_bid.fingerprints.json");`
         - Return `(fs::read(&arrow_path)?, fs::read(&sidecar_path)?)`.

    5. `#[test] fn two_runs_byte_identical()`:
       - `let tmp = TempDir::new()?;`
       - `let source = tmp.path().join("source"); std::fs::create_dir_all(&source)?; build_synthetic_dukascopy_cache(&source);`
       - `let run1_cache = tmp.path().join("run1"); let (run1_arrow, run1_sidecar) = run_full_pipeline(&source, &run1_cache);`
       - `let run2_cache = tmp.path().join("run2"); let (run2_arrow, run2_sidecar) = run_full_pipeline(&source, &run2_cache);`
       - `assert_eq!(run1_arrow, run2_arrow, "Arrow IPC bytes must be byte-identical across runs");`
       - `assert_eq!(run1_sidecar, run2_sidecar, "sidecar JSON must be byte-identical across runs");`
       - This is the highest-confidence test in Phase 2: the cache-write path, the sidecar JSON, the aggregator sums, the blake3 hashes — ALL must be deterministic. Any HashMap, any clock-read, any parallel f64 sum, any walkdir without sort_by_file_name, any Arrow metadata constructed from a HashMap with non-stable iteration order — any of these break this test.

    6. `#[test] fn two_runs_byte_identical_three_timeframes()`:
       - Same source, but iterate over `[Tf15m, Tf1h, Tf1d]` × `[Side::Bid, Side::Ask]` (6 combinations). Each combination: run twice, byte-compare. Asserts byte-identity holds at every timeframe.

    Document in a header comment: "If this test fails, candidates for the cause are listed in 02-RESEARCH §"Determinism contract" lines 526-534: HashMap in a Serialize path, par_iter inside a reduction, clock reads, walkdir without sort_by_file_name, Arrow Schema from a HashMap iterated in non-stable order."
  </action>
  <verify>
    <automated>cargo test -p miner-core --test full_determinism 2>&amp;1 | tail -15 ; grep -c 'miner-reader-dukascopy' crates/miner-core/Cargo.toml</automated>
  </verify>
  <done>
    - `crates/miner-core/Cargo.toml` `[dev-dependencies]` includes `miner-reader-dukascopy = { path = "../miner-reader-dukascopy" }`.
    - `crates/miner-core/tests/full_determinism.rs` exists with `build_synthetic_dukascopy_cache`, `run_full_pipeline`, 2 tests.
    - `cargo test -p miner-core --test full_determinism two_runs_byte_identical` exits 0.
    - `cargo test -p miner-core --test full_determinism two_runs_byte_identical_three_timeframes` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: DukascopyReader dyn-compat regression test + FROZEN-surface audit</name>
  <files>crates/miner-reader-dukascopy/tests/reader_trait_object_safety.rs, crates/miner-core/src/lib.rs</files>
  <read_first>
    - crates/miner-core/src/lib.rs (the FROZEN re-export block after all of Plans 01-05's appends)
    - crates/miner-core/src/findings/sink.rs (lines 399-409 — the canonical dyn-compat test in Phase 1)
    - crates/miner-core/src/reader.rs (Plan 01 — Reader trait + inline dyn-compat test in #[cfg(test)] mod tests)
    - VALIDATION.md row for "Reader trait is dyn-compatible and DukascopyReader: Reader"
  </read_first>
  <action>
    Create `crates/miner-reader-dukascopy/tests/reader_trait_object_safety.rs`:

    1. `use miner_core::Reader; use miner_reader_dukascopy::{DukascopyError, DukascopyReader};`
    2. `#[test] fn dukascopy_reader_is_dyn_compatible()`:
       - `fn _accept(_r: &dyn miner_core::Reader<Error = miner_reader_dukascopy::DukascopyError>) {}`
       - `let cache_root = std::path::PathBuf::from("/tmp/nonexistent-test-cache-root");`
       - `let reader = DukascopyReader::new(&cache_root);`
       - `_accept(&reader);` — compile-time proves DukascopyReader can be coerced to &dyn Reader<Error = DukascopyError>.
       - Add `let _boxed: Box<dyn Reader<Error = DukascopyError>> = Box::new(reader);` — also proves Box-coercion works.
    3. **No filesystem touches at runtime.** This is a compile-time gate.

    `lib.rs` audit:
    1. Re-read `crates/miner-core/src/lib.rs` after Plans 01-05 have all appended their re-exports.
    2. Verify the FROZEN block now contains (in any order — the comment at lib.rs:20-23 explicitly permits reordering for readability):
       - `pub use reader::{Blake3Hex, ClosedRangeUtc, RawBar, Reader, Side};`
       - `pub use calendar::Calendar;`
       - `pub use aggregate::{AGGREGATOR_VERSION, AggParams, AggregateError, BarFrame, Timeframe, aggregate};`
       - `pub use cache::{ARROW_SCHEMA_VERSION, BarCache, CacheError, build_arrow_schema};`
       - `pub use cache::fingerprints::FingerprintSidecar;`
       - `pub use gap::{GapDetector, GapManifest, GapReason, GapSpan};`
    3. If any name is missing, add it. Group Phase 2 entries under a `// Phase 2 (Reader, Aggregator, Cache, Gap)` comment header, preserving Phase 1's existing block above. This makes Phase 3 / Phase 4 future appends clean.
    4. Add a sanity test in `crates/miner-core/tests/schema_roundtrip.rs` (extend, not replace — Phase 1 test) OR create `crates/miner-core/tests/public_surface_audit.rs`:

       `#[test] fn phase_2_public_surface_present()`:
       - Imports every name above via `use miner_core::{...};` literally.
       - If a name is missing from the FROZEN block, the test fails to compile.
       - Body: just call typecheck-equivalent constructors / use the names in basic-but-compiling expressions:
         - `let _: miner_core::Side = miner_core::Side::Bid;`
         - `let _: miner_core::Timeframe = miner_core::Timeframe::Tf15m;`
         - `let _ = miner_core::AGGREGATOR_VERSION;`
         - `let _ = miner_core::ARROW_SCHEMA_VERSION;`
         - `let _ = miner_core::Calendar::fx_major();`
         - `let _: &dyn Fn(&miner_core::aggregate::AggParams) -> () = &|_p| ();` (forces the type to be reachable)
         - Build a GapManifest via the public types.

       Create as `crates/miner-core/tests/public_surface_audit.rs` rather than extend schema_roundtrip.rs (separates Phase 2 surface from Phase 1).
  </action>
  <verify>
    <automated>cargo test -p miner-reader-dukascopy --test reader_trait_object_safety 2>&amp;1 | tail -5 ; cargo test -p miner-core --test public_surface_audit 2>&amp;1 | tail -10 ; for name in Reader RawBar Side ClosedRangeUtc Blake3Hex Calendar Timeframe AggParams BarFrame AggregateError aggregate AGGREGATOR_VERSION BarCache CacheError ARROW_SCHEMA_VERSION FingerprintSidecar GapDetector GapManifest GapSpan GapReason; do echo "$name: $(grep -c $name crates/miner-core/src/lib.rs)"; done</automated>
  </verify>
  <done>
    - `cargo test -p miner-reader-dukascopy --test reader_trait_object_safety dukascopy_reader_is_dyn_compatible` exits 0.
    - `cargo test -p miner-core --test public_surface_audit phase_2_public_surface_present` exits 0.
    - Each of the 20 Phase-2 public names appears in `grep` of `crates/miner-core/src/lib.rs` `pub use` lines.
  </done>
</task>

<task type="auto">
  <name>Task 3: VALIDATION.md Per-Task Verification Map update + nyquist_compliant flag</name>
  <files>.planning/phases/02-reader-aggregator-derived-bar-cache/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-reader-aggregator-derived-bar-cache/02-VALIDATION.md (current state — every Task ID + Plan column is `TBD | TBD`)
    - All five Plan files just produced (01-05): the task names per plan are the source of truth for the {plan-id}-{task-id} mapping.
  </read_first>
  <action>
    Edit `.planning/phases/02-reader-aggregator-derived-bar-cache/02-VALIDATION.md`:

    1. Replace each `TBD | TBD` row's first two columns with the actual `{plan-id} | {wave-num}` mapping based on the test name. Map:
       - **Plan 01 (Wave 0)** owns: `reads_one_day_in_order`, `zero_byte_file`, `reader_trait_object_safety` (both inline in miner-core AND the standalone in miner-reader-dukascopy from Plan 06), `tick_volume_from_csv_volume`, `path_layout::jan_maps_to_00`, `path_layout::dec_maps_to_11`, `path_layout::out_of_range_panics`, `path_layout::path_round_trip_proptest`. So rows: CACHE-01 (reads_one_day_in_order, zero_byte_file) → Plan `02-01`. CACHE-02 (reader_trait_object_safety) → `02-01` (and `02-06` for the standalone integration test). CACHE-05 (tick_volume_from_csv_volume + the 4 path_layout tests) → `02-01`.
       - **Plan 02 (Wave 1)** owns: `aggregator::tests::three_timeframes`, `ohlc_monotonicity_proptest`, `omits_gaps_never_interpolates`, `bid_ask_independent`, and `aggregator_determinism::byte_identical_two_runs`. CACHE-03 + CACHE-04 (4 aggregator rows + the byte_identical row) → Plan `02-02`.
       - **Plan 03 (Wave 1)** owns: `dst_spring_forward` + `dst_fall_back` integration tests. CACHE-04 (2 DST rows) → Plan `02-03`.
       - **Plan 04 (Wave 1)** owns: all `gap::tests` rows + `gap_manifest_snapshot`. CACHE-07 (6 rows) + CACHE-08 (gap_manifest_schemars_roundtrip) → Plan `02-04`.
       - **Plan 05 (Wave 2)** owns: all `cache_smoke` rows + `arrow_schema_snapshot`. CACHE-06 (4 cache_smoke rows + arrow_schema row) → Plan `02-05`.
       - **Plan 06 (Wave 3)** owns: `full_determinism::two_runs_byte_identical`. ALL (determinism) → Plan `02-06`.

       Map each row to `{plan-id} | {wave-num}` in the table.

    2. Wave column: replace the current literal `0`/`1`/`2` placeholders with the actual wave each test runs in:
       - Plan 01 tests are in Wave 0 (test infrastructure wave per VALIDATION's own Wave 0 Requirements section).
       - Plan 02/03/04 tests run in Wave 1.
       - Plan 05 tests run in Wave 2.
       - Plan 06 tests run in Wave 3.
       The existing Wave column values are MOSTLY correct (0 for CACHE-01/02/05 path/reader tests, 1 for CACHE-03/04 aggregator + gap, 2 for CACHE-06 cache). Verify each row.

    3. Update the frontmatter:
       - `nyquist_compliant: true` (was `false`).
       - `wave_0_complete: true` (was `false`).

    4. Update the "Wave 0 Requirements" checklist:
       - Mark each item as `[x]` since Plans 01-05 have collectively produced all the artifacts listed (synthetic-fixture helper, reader_smoke.rs, path_layout.rs, aggregator_fixtures.rs, dst_*.rs, cache_smoke.rs, gap_manifest_snapshot.rs, arrow_schema_snapshot.rs, full_determinism.rs, snapshots/ directory, workspace dev-deps proptest/insta/tempfile, unsafe_code = forbid kept).

    5. Update the "Validation Sign-Off" checklist:
       - Each item gets `[x]` (assertions hold against the just-completed plan set).
       - `Approval: pending` → `Approval: approved by Plan 02-06`.

    6. Add a "Coverage Map" footer (new section at end of file) summarising which CACHE-NN ID is closed by which plan:

       ```
       ## CACHE-NN Coverage Map

       | Requirement | Plans | Status |
       |-------------|-------|--------|
       | CACHE-01 | 02-01 | CLOSED |
       | CACHE-02 | 02-01, 02-06 | CLOSED |
       | CACHE-03 | 02-02 | CLOSED |
       | CACHE-04 | 02-02 (kernel + byte-identity), 02-03 (DST + edge cases), 02-06 (end-to-end) | CLOSED |
       | CACHE-05 | 02-01 | CLOSED |
       | CACHE-06 | 02-05 (cache), 02-06 (end-to-end determinism) | CLOSED |
       | CACHE-07 | 02-04 | CLOSED |
       | CACHE-08 | 02-04 (data shape ships; Phase 3 owns enforcement) | CLOSED for Phase 2 scope |
       ```

    This footer makes the orchestrator's coverage check trivial: every CACHE-NN appears in the table; no orphans.
  </action>
  <verify>
    <automated>grep -c 'TBD | TBD' .planning/phases/02-reader-aggregator-derived-bar-cache/02-VALIDATION.md ; grep -c 'nyquist_compliant: true' .planning/phases/02-reader-aggregator-derived-bar-cache/02-VALIDATION.md ; grep -c 'CACHE-NN Coverage Map' .planning/phases/02-reader-aggregator-derived-bar-cache/02-VALIDATION.md</automated>
  </verify>
  <done>
    - `grep -c 'TBD | TBD' .planning/phases/02-reader-aggregator-derived-bar-cache/02-VALIDATION.md` returns 0 (all TBD rows replaced).
    - Frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`.
    - CACHE-NN Coverage Map section present, showing every CACHE-NN ID → plan(s) mapping.
    - Wave 0 Requirements all checked.
    - Validation Sign-Off section all checked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| All Phase 2 modules | Audited as a unit by this plan's end-to-end determinism gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-19 | Tampering | end-to-end pipeline non-determinism | mitigate | `two_runs_byte_identical` test is the catch-all gate for HashMap leaks, par_iter leaks, clock leaks, walkdir non-sort, Arrow metadata non-stable order. |
| T-02-20 | Repudiation | public surface drift (a downstream consumer reaches into internal module) | mitigate | `phase_2_public_surface_present` test gates every Phase 2 name via the FROZEN re-export block; if a future refactor renames an internal module, downstream consumers using `miner_core::*` keep working as long as the FROZEN re-export keeps the name. |
| T-02-21 | Information Disclosure | inline cycle in dev-deps causes test crate cycles | accept | cargo permits dev-dep cycles; the cycle `miner-core (dev-deps) → miner-reader-dukascopy → miner-core` is harmless because dev-deps are not part of the published API graph. Documented in 02-06 Task 1's commit message. |
</threat_model>

<verification>
Run the full Phase 2 test suite:
```
cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --all --check && cargo tree -p miner-core | grep -E '(tokio|async-std|async-trait)' | head -5
```

Expected outcome: zero failures, zero warnings, zero async deps in miner-core, fmt clean.

Per-requirement closure check (CACHE-NN Coverage Map in VALIDATION.md is the source of truth):
- CACHE-01..CACHE-08: all CLOSED per the table.
- ROADMAP.md Phase 2 success criteria #1-5: all observably TRUE through `cargo test` + `cargo run -p miner-cli -- emit-fixture …`.
</verification>

<success_criteria>
1. `cargo test --workspace` green across all 5 prior plans + this plan.
2. `crates/miner-core/tests/full_determinism.rs` proves byte-identical Arrow + sidecar across two pipeline runs (real DukascopyReader; not MockReader).
3. `crates/miner-reader-dukascopy/tests/reader_trait_object_safety.rs` exists; cargo test green.
4. `crates/miner-core/tests/public_surface_audit.rs` exists; gates the 20-name FROZEN re-export block.
5. VALIDATION.md per-task verification map has no `TBD | TBD` rows; `nyquist_compliant: true`.
6. `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
7. `cargo tree -p miner-core` shows zero tokio/async-std/async-trait — FOUND-04 gate intact.
8. CACHE-NN Coverage Map section in VALIDATION.md shows all 8 requirements CLOSED.
9. `grep -c tick_count crates/miner-core crates/miner-reader-dukascopy --include='*.rs' -r` returns 0 (entire Phase 2 codebase A1-rename clean).
</success_criteria>

<output>
Create `.planning/phases/02-reader-aggregator-derived-bar-cache/02-06-SUMMARY.md` when done. The summary marks Phase 2 COMPLETE.
</output>
