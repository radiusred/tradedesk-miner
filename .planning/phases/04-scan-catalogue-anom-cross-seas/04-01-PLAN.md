---
phase: 04-scan-catalogue-anom-cross-seas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/miner-core/Cargo.toml
  - Cargo.toml
  - crates/miner-core/src/scan/mod.rs
  - crates/miner-core/src/findings/mod.rs
  - crates/miner-core/src/error/codes.rs
  - crates/miner-core/src/reader.rs
  - schemas/findings-v1.schema.json
  - schemas/scans-catalogue-v1.schema.json
  - .planning/phases/04-scan-catalogue-anom-cross-seas/04-01-SCHEMA-DIFF.md
autonomous: true
requirements:
  - ANOM-01
  - CROSS-01
tags:
  - rust
  - facade
  - schema

must_haves:
  truths:
    - "User can compile miner-core after ScanRequest.instruments: Vec<InstrumentSpec> field replaces instrument+side (D4-01)"
    - "User can call Scan::arity() on every dyn Scan; ScanArity has Single + Pair variants serialising as snake_case (D4-02)"
    - "User can construct DataSlice with sources: Vec<Source> (D4-03) OR fallback to D4-03-ALT (source + peer_sources: Vec<Source>) per recorded schema-diff decision"
    - "User can pattern-match PreflightCode::WrongInstrumentArity with as_str() => \"wrong_instrument_arity\" (D4-02)"
    - "User can read .planning/phases/04-scan-catalogue-anom-cross-seas/04-01-SCHEMA-DIFF.md recording the D4-03 vs D4-03-ALT decision (Pitfall 7 / Pitfall 8)"
    - "User can run cargo run -p xtask -- gen-schema twice and observe no diff (idempotent)"
    - "User can run cargo tree -p miner-core | grep -E 'tokio|async-std' returning nothing (FOUND-04 sync-only invariant preserved)"
  artifacts:
    - path: "crates/miner-core/Cargo.toml"
      provides: "ndarray, ndarray-stats, nalgebra dependencies"
      contains: "ndarray"
    - path: "crates/miner-core/src/scan/mod.rs"
      provides: "InstrumentSpec struct, ScanArity enum, Scan::arity() method, ScanRequest.instruments Vec field"
      contains: "pub enum ScanArity"
    - path: "crates/miner-core/src/findings/mod.rs"
      provides: "DataSlice with Vec<Source> sources (or peer_sources fallback)"
      contains: "DataSlice"
    - path: "crates/miner-core/src/error/codes.rs"
      provides: "PreflightCode::WrongInstrumentArity variant"
      contains: "WrongInstrumentArity"
    - path: ".planning/phases/04-scan-catalogue-anom-cross-seas/04-01-SCHEMA-DIFF.md"
      provides: "Recorded schema-diff and D4-03 vs D4-03-ALT decision"
      min_lines: 20
  key_links:
    - from: "crates/miner-core/src/scan/mod.rs"
      to: "ScanArity enum + Scan::arity() trait method"
      via: "trait body extension"
      pattern: "fn arity\\(&self\\) -> ScanArity"
    - from: "crates/miner-core/src/scan/ljung_box/mod.rs"
      to: "Scan trait impl with arity() returning ScanArity::Single"
      via: "trait impl extension required by trait change"
      pattern: "fn arity\\(&self\\) -> ScanArity"
---

<objective>
Extend the Phase 3 facade surface with Phase 4's four locked decisions: D4-01 (ScanRequest.instruments: Vec<InstrumentSpec>), D4-02 (Scan::arity() + ScanArity enum + PreflightCode::WrongInstrumentArity), D4-03 (DataSlice.sources: Vec<Source> OR fallback D4-03-ALT), plus the workspace dependency additions (ndarray, ndarray-stats, nalgebra). Run the schemars regen spike (Pitfall 7 / Pitfall 8 mitigation) BEFORE any callsite code is changed — record the diff and the D4-03/D4-03-ALT decision in a per-phase memo.

Purpose: every Phase 4 scan plan depends on the trait extension + envelope shape extension being in place; the schema-diff verification must happen first to choose between D4-03 and D4-03-ALT.
Output: extended trait + types + error variant + workspace deps + recorded schema-diff decision. NO scan registrations (those land in Plan 04-02 onward).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-CONTEXT.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-VALIDATION.md

<interfaces>
<!-- Key existing surfaces. Executor reads the files themselves; this block names the load-bearing identifiers. -->

From crates/miner-core/src/scan/mod.rs (Phase 3, unchanged shape):
- pub trait Scan { fn id(&self) -> &'static str; fn version(&self) -> u32; fn param_schema(&self) -> serde_json::Value; fn finding_fields(&self) -> ScanFindingShape; fn run(&self, ctx: &ScanCtx<'_>, req: &ScanRequest, sink: &mut dyn FindingSink) -> Result<(), ScanError>; }
- pub struct ScanRequest { pub instrument: String, pub side: Side, pub timeframe: Timeframe, pub window: ClosedRangeUtc, pub resolved_params: BTreeMap<String, serde_json::Value>, pub param_hash: ParamHash, pub gap_policy: GapPolicyKind, pub dry_run: bool, ... }
- pub struct ScanCtx<'a> { pub bars: &'a BarFrame, pub gap_manifest: Option<&'a GapManifest>, pub run_id: &'a RunId, pub code_revision: &'a str, pub cancel: Arc<AtomicBool>, ... }

From crates/miner-core/src/findings/mod.rs (Phase 3):
- pub struct DataSlice { pub source: Source, pub time_range: TimeRange, pub gap_manifest: Option<GapManifest>, ... }
- pub struct Source { pub source_id: String, pub symbol: String, pub side: Side }

From crates/miner-core/src/error/codes.rs (Phase 3):
- pub enum PreflightCode { InvalidParameter, UnknownScan, UnknownInstrument, MissingRequiredConfig, InvalidConfig, SweepTooLarge, InternalError }

From crates/miner-core/src/reader.rs (Phase 2):
- pub enum Side { Bid, Ask } with FromStr impl
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schemars regen spike — apply D4-01/D4-03 type changes on scratch, record diff and D4-03 vs D4-03-ALT decision</name>
  <files>crates/miner-core/Cargo.toml, Cargo.toml, .planning/phases/04-scan-catalogue-anom-cross-seas/04-01-SCHEMA-DIFF.md</files>
  <read_first>
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md (§1.1 Schemars schema-additive verification, §1.10 ScanArity variants, §Section 7 Schemars Regen Recipe, Pitfall 7, Pitfall 8)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md (Pattern F ScanArity, Pattern G PreflightCode)
    - crates/miner-core/Cargo.toml (current dependency list — lines 26-48 already-present block)
    - Cargo.toml workspace [workspace.dependencies] block (mirror the new entries)
    - schemas/findings-v1.schema.json (current committed artifact — baseline for diff)
    - schemas/scans-catalogue-v1.schema.json (baseline)
    - xtask/src/main.rs (the gen-schema implementation — lines 58-134 per research §Section 7)
    - crates/miner-core/src/findings/mod.rs (current DataSlice struct lines 80-90; Source struct lines 96-102)
    - crates/miner-core/src/scan/mod.rs (current ScanRequest struct ~lines 185-232; ScanCtx struct ~lines 128-155)
  </read_first>
  <action>
    Add three workspace dependencies (per RESEARCH.md §Section 4 Dependency-Add Audit): ndarray = "0.16", ndarray-stats = "0.6", nalgebra = { version = "0.33", default-features = false, features = ["std"] } to root Cargo.toml [workspace.dependencies], then mirror into crates/miner-core/Cargo.toml [dependencies] using workspace = true syntax matching existing entries.

    Run cargo build -p miner-core to confirm clean compile with new deps. Then run cargo tree -p miner-core | grep -E 'tokio|async-std|smol' and confirm zero matches (FOUND-04 sync-only invariant).

    Apply a SCRATCH type-change to ScanRequest (replace `pub instrument: String, pub side: Side` with `pub instruments: Vec<InstrumentSpec>` where InstrumentSpec is a struct { symbol: String, side: Side } with full derives Debug+Clone+Serialize+Deserialize+JsonSchema) AND DataSlice (replace `pub source: Source` with `pub sources: Vec<Source>`). Add JsonSchema derive on InstrumentSpec. Do NOT yet update callsites — just the type-shape change locally. The goal is to run `cargo run -p xtask -- gen-schema /tmp/schemas-scratch` and obtain a diff against the committed schemas/.

    Record findings in .planning/phases/04-scan-catalogue-anom-cross-seas/04-01-SCHEMA-DIFF.md with: (a) the unified diff for both schema files; (b) classification per RESEARCH.md §Section 7 "What additive means" — additive (no schema_version bump), non-additive but accepted (type generalisation, no schema_version bump per CONTEXT.md "consumers haven't shipped yet, Phase 6 wrappers don't exist"), or schema_version bump required (none apply); (c) the recommended path — D4-03 (commit the Vec<Source> generalisation as deliberate facade-shape change, schema_version stays 1) or D4-03-ALT (keep source: Source for primary leg + add peer_sources: Vec<Source> sibling); (d) ditto for D4-01 instruments Vec.

    Per CONTEXT.md and RESEARCH.md §1.1: the EXPECTED outcome is "non-additive but accepted; commit as deliberate facade update; keep schema_version = 1." The fallback path D4-03-ALT only triggers if a downstream constraint we missed surfaces (none expected — Phase 6 hasn't started).

    Revert the scratch type-change at the end of this task (git checkout the source files); the dependency additions and the SCHEMA-DIFF.md memo persist. The next tasks will apply the chosen D4-03 path to the real source.
  </action>
  <acceptance_criteria>
    - crates/miner-core/Cargo.toml `[dependencies]` block contains `ndarray.workspace = true`, `ndarray-stats.workspace = true`, `nalgebra.workspace = true`
    - root Cargo.toml `[workspace.dependencies]` table contains `ndarray = "0.16"`, `ndarray-stats = "0.6"`, and `nalgebra = { version = "0.33", default-features = false, features = ["std"] }`
    - cargo build -p miner-core exits 0 with the three new deps resolved
    - cargo tree -p miner-core | grep -cE 'tokio|async-std|smol' returns 0 (FOUND-04 preserved)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-01-SCHEMA-DIFF.md exists, is ≥ 20 lines, and contains: (a) labelled "## Findings v1 Schema Diff" section with the unified diff between current schemas/findings-v1.schema.json and the regenerated /tmp/schemas-scratch/findings-v1.schema.json; (b) labelled "## Catalogue v1 Schema Diff" section; (c) labelled "## Classification" section using one of the three RESEARCH §Section 7 categories; (d) labelled "## Decision" section explicitly recording "D4-03" or "D4-03-ALT" with a one-sentence rationale; (e) labelled "## Decision: D4-01 instruments Vec" with the same recording.
    - The scratch type-changes have been reverted from crates/miner-core/src/scan/mod.rs and crates/miner-core/src/findings/mod.rs (git diff shows only Cargo.toml + workspace Cargo.toml + new SCHEMA-DIFF.md changes — NOT type changes).
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p miner-core && cargo tree -p miner-core 2>&1 | grep -cE 'tokio|async-std|smol' | grep -q '^0$' && test -f .planning/phases/04-scan-catalogue-anom-cross-seas/04-01-SCHEMA-DIFF.md && grep -q 'D4-03' .planning/phases/04-scan-catalogue-anom-cross-seas/04-01-SCHEMA-DIFF.md && grep -q 'D4-01' .planning/phases/04-scan-catalogue-anom-cross-seas/04-01-SCHEMA-DIFF.md</automated>
  </verify>
  <done>The three new workspace deps build cleanly, sync-only invariant holds, SCHEMA-DIFF.md records the diff and locks the D4-01/D4-03 path for downstream tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend Scan trait (arity), add ScanArity enum, InstrumentSpec struct, generalise ScanRequest.instruments + DataSlice.sources per recorded decision</name>
  <files>crates/miner-core/src/scan/mod.rs, crates/miner-core/src/findings/mod.rs, crates/miner-core/src/reader.rs, crates/miner-core/src/scan/ljung_box/mod.rs</files>
  <read_first>
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-01-SCHEMA-DIFF.md (the decision recorded by Task 1 — D4-03 vs D4-03-ALT)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md (Pattern F ScanArity enum, full code block with derives + serde + JsonSchema; Pattern H validate_arity helper; Pattern A imports block showing `use crate::scan::{Scan, ScanArity, ...}`)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md (§1.5 BarFrameView, §1.10 ScanArity, §Section 8 mod.rs skeleton)
    - crates/miner-core/src/scan/mod.rs (current Scan trait body lines 68-104; ScanRequest struct lines 185-232; ScanCtx struct lines 128-155; existing `scan_trait_object_safe` compile gate test ~line 352-356)
    - crates/miner-core/src/findings/mod.rs (current DataSlice struct lines 80-90; Source struct lines 96-102)
    - crates/miner-core/src/reader.rs (Side enum definition + FromStr impl)
    - crates/miner-core/src/scan/ljung_box/mod.rs (Scan impl that consumes Scan trait — needs `fn arity()` added)
  </read_first>
  <behavior>
    - Test 1 (`scan_arity_serialises_snake_case` in scan/mod.rs tests): ScanArity::Single serialises as "single"; ScanArity::Pair as "pair"; round-trip deser equality holds for both
    - Test 2 (`instrument_spec_parse_round_trip` in reader.rs or scan/mod.rs tests): InstrumentSpec { symbol: "EURUSD".into(), side: Side::Bid } serialises to `{"symbol":"EURUSD","side":"bid"}` and round-trips deser equally
    - Test 3 (`scan_request_instruments_len_one_serialises` in scan/mod.rs tests): ScanRequest with `instruments = vec![InstrumentSpec { symbol: "EURUSD".into(), side: Side::Bid }]` serialises to JSON where `instruments` is a JSON array of length 1
    - Test 4 (`data_slice_sources_vec_round_trip` in findings/mod.rs tests; under D4-03 path) OR (`data_slice_peer_sources_vec_round_trip` under D4-03-ALT path): the chosen Vec field round-trips JSON with length matching the inserted Vec; single-leg findings carry a length-1 Vec
    - Test 5 (compile-only `scan_trait_object_safe` regression — already exists at scan/mod.rs:352-356): MUST continue to compile (Box<dyn Scan> still constructible) after the `fn arity(&self) -> ScanArity` method is added
    - Test 6 (`ljung_box_scan_reports_single_arity`): LjungBoxScan.arity() == ScanArity::Single
  </behavior>
  <action>
    Add `InstrumentSpec` struct to crates/miner-core/src/scan/mod.rs (or crates/miner-core/src/reader.rs — analog: existing `Side` enum location; PATTERNS.md Pattern K recommends scan/mod.rs OR reader.rs; place it next to `Side` in reader.rs for cohesion). Derives: Debug + Clone + PartialEq + Eq + Hash + Serialize + Deserialize + JsonSchema. Fields: pub symbol: String, pub side: Side. Implement Display + FromStr (`"EURUSD:bid".parse() -> InstrumentSpec`) returning a parse error type used by CLI value-parser in Plan 04-02.

    Add `ScanArity` enum to scan/mod.rs (verbatim from PATTERNS.md Pattern F): `#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, JsonSchema)] #[serde(rename_all = "snake_case")] pub enum ScanArity { Single, Pair }`. Add an `as_str(&self) -> &'static str` method mirroring `GapPolicyKind::as_str` (gap_policy.rs analog). Document v2 extension path in a doc comment: "// v2: Many(min, max) — basket scans (Johansen etc.)".

    Add `fn arity(&self) -> ScanArity` to the `Scan` trait (position: between `version` and `param_schema` per RESEARCH.md §1.10 ordering). No default body — every impl must explicitly declare. Update the existing `scan_trait_object_safe` test (mod.rs:352-356) to confirm the new method does not break object-safety (it must not — `&self -> ScanArity` is dyn-safe).

    Generalise ScanRequest per recorded D4-01 path: replace `pub instrument: String` + `pub side: Side` with `pub instruments: Vec<InstrumentSpec>`. Update Debug + Serialize + Deserialize + JsonSchema derives unchanged.

    Generalise DataSlice per recorded D4-03 path. If SCHEMA-DIFF.md records D4-03: replace `pub source: Source` with `pub sources: Vec<Source>`. If SCHEMA-DIFF.md records D4-03-ALT: keep `pub source: Source` (primary leg) and add `pub peer_sources: Vec<Source>` (defaults to empty Vec via `#[serde(default, skip_serializing_if = "Vec::is_empty")]`). The decision is fixed in 04-01-SCHEMA-DIFF.md; this task must NOT re-decide.

    Add `fn arity(&self) -> ScanArity { ScanArity::Single }` to the existing LjungBoxScan impl (crates/miner-core/src/scan/ljung_box/mod.rs) so the codebase compiles with the new trait method. Update any places that construct a ScanRequest in Phase 3 callsites (preflight tests, engine tests) to use the new `instruments: vec![InstrumentSpec { symbol, side }]` shape — these are mechanical replacements; per PATTERNS.md Pattern K the analog mapping is `instrument: s -> instruments: vec![InstrumentSpec { symbol: s, side }]`. Likewise DataSlice constructions: `source: Source { ... } -> sources: vec![Source { ... }]` (or `source + peer_sources: vec![]` under D4-03-ALT).

    Add the six unit tests listed in <behavior> alongside the existing tests block in each file. Each test follows the Phase 3 `preflight_code_serialises_snake_case` shape (codes.rs:140-161 analog).
  </action>
  <acceptance_criteria>
    - `cargo build -p miner-core --all-targets` exits 0 (the trait change cascades through every Scan impl, including LjungBoxScan, plus tests)
    - `grep -n 'pub enum ScanArity' crates/miner-core/src/scan/mod.rs` finds exactly one match with `#[serde(rename_all = "snake_case")]` on the preceding lines
    - `grep -n 'fn arity(&self) -> ScanArity' crates/miner-core/src/scan/mod.rs` returns ≥ 1 match (the trait method declaration)
    - `grep -n 'fn arity(&self) -> ScanArity' crates/miner-core/src/scan/ljung_box/mod.rs` returns 1 match (LjungBoxScan impl)
    - `grep -n 'pub struct InstrumentSpec' crates/miner-core/src/` (recursive) finds exactly one struct definition with `pub symbol: String` and `pub side: Side` fields
    - `grep -n 'pub instruments: Vec<InstrumentSpec>' crates/miner-core/src/scan/mod.rs` finds exactly one occurrence (ScanRequest field)
    - Per SCHEMA-DIFF.md D4-03 path: `grep -n 'pub sources: Vec<Source>' crates/miner-core/src/findings/mod.rs` finds one occurrence AND `grep -nE 'pub source: Source' crates/miner-core/src/findings/mod.rs | grep -v 'sources'` finds zero (the singleton field is removed). OR D4-03-ALT path: both `pub source: Source` and `pub peer_sources: Vec<Source>` exist.
    - cargo nextest run -p miner-core --no-fail-fast (or `cargo test -p miner-core`) produces 0 failing tests including the six new behavior tests (Tests 1-6 above)
    - The existing LjungBox golden test `cargo test -p miner-core --test scan_ljung_box` continues to pass byte-identically (D3-23 invariant — the type change only adds an arity() method and rewires the request/dataslice envelopes, not the LjungBox math)
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p miner-core --all-targets && cargo test -p miner-core --lib scan::tests::scan_arity_serialises_snake_case scan::tests::instrument_spec_parse_round_trip scan::tests::scan_request_instruments_len_one_serialises findings::tests::data_slice_sources_vec_round_trip scan::tests::ljung_box_scan_reports_single_arity 2>&1 | grep -v '^#' | grep -c 'test result: ok' | grep -q '^[1-9]'</automated>
  </verify>
  <done>ScanArity + InstrumentSpec exist with serde+JsonSchema derives; Scan::arity() is a trait method that every impl (incl. LjungBoxScan) declares; ScanRequest.instruments is a Vec; DataSlice carries Vec per D4-03 (or D4-03-ALT); the workspace builds and all six new behavior tests + existing Phase 3 LjungBox golden pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add PreflightCode::WrongInstrumentArity variant + regen schemas + verify schema-sync gate</name>
  <files>crates/miner-core/src/error/codes.rs, schemas/findings-v1.schema.json, schemas/scans-catalogue-v1.schema.json</files>
  <read_first>
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md (Pattern G PreflightCode addition — full enum + as_str arm + existing test row to extend)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md (§Section 7 Schemars Regen Recipe — the gen-schema invocation)
    - crates/miner-core/src/error/codes.rs (current enum lines 20-53; existing as_str impl; existing `preflight_code_serialises_snake_case` test lines 140-161 — the cases array)
    - xtask/src/main.rs (gen-schema subcommand body — for invocation idempotency reasoning)
    - schemas/findings-v1.schema.json (current — diff target)
    - schemas/scans-catalogue-v1.schema.json (current — diff target)
  </read_first>
  <behavior>
    - Test 1 (`preflight_code_serialises_snake_case`, extending the existing cases array at codes.rs:140-161): the array gains one row `(PreflightCode::WrongInstrumentArity, "wrong_instrument_arity")` and the test passes for all 8 variants
    - Test 2 (`preflight_code_as_str_wrong_instrument_arity`): `PreflightCode::WrongInstrumentArity.as_str() == "wrong_instrument_arity"`
    - Test 3 (schema regen idempotent — Bash verifier): two successive `cargo run -p xtask -- gen-schema` invocations produce zero diff in schemas/
  </behavior>
  <action>
    Add `WrongInstrumentArity` as a new variant in PreflightCode enum (codes.rs:20-53) — alphabetical position is AFTER `UnknownScan` (the enum is grouped semantically, not strictly alphabetical; insert in the position recommended by Pattern G — "8th variant after UnknownScan"). Add the matching `as_str` arm returning `"wrong_instrument_arity"`. Extend the existing test cases array at codes.rs:140-161 with the new `(PreflightCode::WrongInstrumentArity, "wrong_instrument_arity")` row.

    Add Test 2 above as a sibling unit test in the same `mod tests` block in codes.rs.

    Run `cargo run -p xtask -- gen-schema` to regenerate schemas/findings-v1.schema.json and schemas/scans-catalogue-v1.schema.json. The expected diff is the one recorded in 04-01-SCHEMA-DIFF.md (D4-01 instruments Vec + D4-03 sources Vec + new PreflightCode variant + new InstrumentSpec definition + new ScanArity definition).

    Run gen-schema TWICE in succession and `git diff --exit-code schemas/` after the second run — MUST exit 0 (idempotent regeneration; the schema artifact equals itself after a second regen).

    Commit the regenerated schemas as-is. Per CONTEXT.md and RESEARCH.md §Section 7 + 04-01-SCHEMA-DIFF.md decision: schema_version stays = 1 because Phase 6 wrappers don't exist yet (no downstream consumers).
  </action>
  <acceptance_criteria>
    - `grep -n 'WrongInstrumentArity' crates/miner-core/src/error/codes.rs` finds ≥ 3 matches (enum variant declaration + as_str arm + the test cases array row)
    - `grep -n 'wrong_instrument_arity' crates/miner-core/src/error/codes.rs` finds ≥ 2 matches (the as_str return value + the test cases array)
    - `cargo test -p miner-core --lib error::codes::tests::preflight_code_serialises_snake_case` passes with the 8-variant cases array
    - `cargo test -p miner-core --lib error::codes::tests::preflight_code_as_str_wrong_instrument_arity` passes
    - `grep -q '"wrong_instrument_arity"' schemas/findings-v1.schema.json` succeeds (variant appears in regenerated enum schema)
    - First `cargo run -p xtask -- gen-schema` invocation completes successfully (exit 0)
    - Second consecutive `cargo run -p xtask -- gen-schema` produces no diff: `git diff --exit-code schemas/` exits 0 after the second invocation
    - The regenerated schema diff matches what 04-01-SCHEMA-DIFF.md predicted (manual cross-check: the diff in `git diff schemas/` against the pre-Task-2 baseline shows the instruments Vec, sources Vec, ScanArity enum, InstrumentSpec definition, and WrongInstrumentArity preflight variant — and nothing else unexpected)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p miner-core --lib error::codes::tests && cargo run -p xtask -- gen-schema >/dev/null 2>&1 && cargo run -p xtask -- gen-schema >/dev/null 2>&1 && git diff --exit-code schemas/ && grep -q '"wrong_instrument_arity"' schemas/findings-v1.schema.json</automated>
  </verify>
  <done>WrongInstrumentArity is a registered PreflightCode variant; schemas/ is regenerated and idempotent under repeated `cargo run -p xtask -- gen-schema`; the diff matches the 04-01-SCHEMA-DIFF.md prediction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI parse → `ScanRequest` | Untrusted symbol/side strings from `--instrument SYMBOL:side` cross here; integer overflow / DoS via huge instrument count |
| Workspace dep install | `cargo add` of ndarray / ndarray-stats / nalgebra brings transitive code into the build |
| Schema regen | `gen-schema` writes to `schemas/` — must be idempotent to avoid CI flakiness from non-determinism |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | `cargo add` of ndarray/ndarray-stats/nalgebra | mitigate | Package Legitimacy Audit in RESEARCH.md §Package Legitimacy Audit verified all three as authoritative ([CITED: CLAUDE.md TL;DR + docs.rs]); cargo deny licenses gate accepts MIT/Apache-2.0. Task 1 acceptance criterion confirms `cargo tree -p miner-core` shows no tokio/async-std transitive add. |
| T-04-01-02 | Denial of Service | ScanRequest.instruments Vec | mitigate | `Scan::arity()` validated at preflight (Plan 04-02 implements `validate_arity`); a request with 1000 instruments is rejected before any allocation work happens. Vec size capped implicitly by `ScanArity` arms (Single = 1, Pair = 2). |
| T-04-01-03 | Information Disclosure | Schema regen non-determinism | mitigate | Task 3 acceptance verifies idempotent gen-schema via `git diff --exit-code schemas/` after second invocation. RESEARCH.md §Section 7 documents the determinism contract. |
| T-04-01-04 | Tampering | LjungBox golden divergence on type change | mitigate | Task 2 acceptance criterion requires `cargo test -p miner-core --test scan_ljung_box` to pass byte-identically; the type-shape change only rewires envelope fields, not the kernel arithmetic. Pitfall 9 (Phase 3 LjungBox golden divergence) is explicitly the gating concern. |
| T-04-01-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH.md §Package Legitimacy Audit shows three packages all `[APPROVED]`; no `[ASSUMED]` or `[SUS]` packages — no blocking-human checkpoint required for Phase 4. |
</threat_model>

<verification>
- cargo build -p miner-core --all-targets passes (full trait + type cascade compiles)
- cargo nextest run -p miner-core or cargo test -p miner-core passes including all six new behavior tests
- cargo run -p xtask -- gen-schema is idempotent: `git diff --exit-code schemas/` exits 0 after a second consecutive run
- The Phase 3 LjungBox golden test (cargo test -p miner-core --test scan_ljung_box) passes byte-identically after the trait extension
- The Phase 3 facade tests (gap_policy.rs, dry_run.rs, scan_facade_determinism.rs) all pass after callsite updates
- cargo tree -p miner-core | grep -E 'tokio|async-std' returns no matches (FOUND-04 sync-only preserved)
- .planning/phases/04-scan-catalogue-anom-cross-seas/04-01-SCHEMA-DIFF.md records the D4-03 / D4-01 path decision in writing
</verification>

<success_criteria>
- Trait `Scan` declares `fn arity(&self) -> ScanArity` and every existing impl (LjungBoxScan) provides one
- Type `InstrumentSpec` exists with full derives and a FromStr that parses "SYMBOL:side"
- Type `ScanArity` exists with Single + Pair variants, snake_case serde renaming, and as_str()
- `ScanRequest.instruments: Vec<InstrumentSpec>` replaces the singleton `instrument: String + side: Side`
- `DataSlice` carries either `sources: Vec<Source>` (D4-03) or `source + peer_sources: Vec<Source>` (D4-03-ALT) per the recorded SCHEMA-DIFF.md decision
- `PreflightCode::WrongInstrumentArity` exists and serialises to "wrong_instrument_arity"
- ndarray, ndarray-stats, nalgebra deps are wired and the workspace stays sync-only
- schemas/findings-v1.schema.json and schemas/scans-catalogue-v1.schema.json are regenerated, idempotent, and committed
</success_criteria>

<output>
Create `.planning/phases/04-scan-catalogue-anom-cross-seas/04-01-SUMMARY.md` documenting: the chosen D4-03 / D4-01 path, dependency versions resolved, exact list of new types + trait methods, and any deviations from Pattern F/G/K (none expected).
</output>
