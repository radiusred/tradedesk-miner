---
phase: 04-scan-catalogue-anom-cross-seas
plan: 11
type: execute
wave: 7
depends_on:
  - 04-06
  - 04-08
  - 04-10
files_modified:
  - crates/miner-core/tests/goldens/python-requirements.lock
  - crates/miner-core/tests/goldens/generate_summary_welford.py
  - crates/miner-core/tests/goldens/generate_engle_granger.py
  - crates/miner-core/tests/goldens/generate_hour_of_day.py
  - crates/miner-core/tests/goldens/stats.summary.welford.jsonl
  - crates/miner-core/tests/goldens/cross.cointegration.engle_granger.jsonl
  - crates/miner-core/tests/goldens/seas.bucket.hour_of_day.jsonl
  - crates/miner-core/tests/scan_summary_welford.rs
  - crates/miner-core/tests/scan_engle_granger.rs
  - crates/miner-core/tests/scan_seas_hour_of_day.rs
  - crates/miner-core/tests/byte_identical_rerun.rs
  - schemas/findings-v1.schema.json
  - schemas/scans-catalogue-v1.schema.json
  - README.md
autonomous: false
requirements: []
success_criteria:
  - "ROADMAP Phase 4 SC#4: consistent Finding envelope shape across all 22 scans, single discriminant by scan_id, scan-specific extras in documented effect.extra object — pinned by crates/miner-core/tests/byte_identical_rerun.rs which exercises one ANOM + one CROSS + one SEAS scan and asserts identical envelope structure modulo run_id + clock-read fields"
  - "ROADMAP Phase 4 SC#5: byte-identical golden fixtures for one ANOM (stats.summary.welford), one CROSS (cross.cointegration.engle_granger), and one SEAS (seas.bucket.hour_of_day) scan within RESEARCH.md §Section 2 tolerances (1e-10 ANOM / 1e-8 CROSS coint / 1e-12 SEAS) — pinned by the three updated integration tests"
tags:
  - rust
  - goldens
  - phase-verification

must_haves:
  truths:
    - "User finds at least one ANOM (stats.summary.welford), one CROSS (cross.cointegration.engle_granger), and one SEAS (seas.bucket.hour_of_day) golden under crates/miner-core/tests/goldens/ with byte-identical re-run cargo-test comparison (Success Criterion #5)"
    - "User runs `cargo run -p xtask -- gen-schema` and `git diff --exit-code schemas/` exits 0 (schemas regenerate idempotently after all 22+1 scan registrations)"
    - "User reads the README Quickstart and finds three new scan-invocation examples — one ANOM, one CROSS, one SEAS — with expected JSONL fragment for each"
    - "User runs `cargo nextest run --workspace --no-fail-fast` (or `cargo test --workspace`) and observes zero failures across all Phase 1-4 tests"
    - "User runs any scan twice with the same input and observes byte-identical JSONL output modulo run_id + clock-read fields (D3-23 invariant — pinned for representative ANOM/CROSS/SEAS scans in this plan; SC#4 evidence)"
    - "User reads `.planning/phases/04-scan-catalogue-anom-cross-seas/04-11-PHASE-VERIFICATION.md` documenting the Phase 4 sign-off ritual"
  artifacts:
    - path: "crates/miner-core/tests/goldens/stats.summary.welford.jsonl"
      provides: "Pinned scipy.stats.describe golden for SEAS-02 (ANOM-02) within 1e-10 tolerance — SC#5 evidence (ANOM)"
      min_lines: 1
    - path: "crates/miner-core/tests/goldens/cross.cointegration.engle_granger.jsonl"
      provides: "Pinned statsmodels.coint + hand-derived OU golden for CROSS-05 within 1e-8 tolerance (β within 1e-10) — SC#5 evidence (CROSS)"
      min_lines: 1
    - path: "crates/miner-core/tests/goldens/seas.bucket.hour_of_day.jsonl"
      provides: "Pinned pandas.groupby golden for SEAS-01 within 1e-12 tolerance — SC#5 evidence (SEAS)"
      min_lines: 1
    - path: "crates/miner-core/tests/byte_identical_rerun.rs"
      provides: "D3-23 byte-identical-rerun integration test for representative ANOM/CROSS/SEAS scans — SC#4 evidence (consistent envelope shape across families)"
      min_lines: 80
    - path: ".planning/phases/04-scan-catalogue-anom-cross-seas/04-11-PHASE-VERIFICATION.md"
      provides: "Phase 4 sign-off ritual + 22-requirement traceability checklist"
      min_lines: 40
  key_links:
    - from: "crates/miner-core/tests/scan_summary_welford.rs"
      to: "crates/miner-core/tests/goldens/stats.summary.welford.jsonl"
      via: "include_str! macro loads the golden JSONL, runs the scan on the same input, asserts envelope match within 1e-10 (Pattern J Step 7) — SC#5 (ANOM) evidence"
      pattern: "include_str!.*goldens/stats.summary.welford.jsonl"
    - from: "schemas/findings-v1.schema.json"
      to: "23 scan_id constants across crates/miner-core/src/scan/{anom,cross,seas}/"
      via: "cargo run -p xtask -- gen-schema regenerates idempotently with all 23 scans in the catalogue (Phase 4's facade-shape changes from Plans 04-01 + the 22 new scans + ANOM-04 squared variant) — SC#4 (consistent envelope) evidence"
      pattern: "scans-catalogue-v1.schema.json"
---

<objective>
Finalise Phase 4: author three statsmodels/scipy/pandas goldens (representative ANOM + CROSS + SEAS per Success Criterion #5); pin tolerances per RESEARCH.md §Section 2; regen + commit schemas; extend the existing three integration tests (scan_summary_welford, scan_engle_granger, scan_seas_hour_of_day) with golden cross-checks; add a byte-identical-rerun regression test; update README Quickstart with three Phase 4 invocation examples; produce a Phase 4 sign-off memo with the 22-requirement traceability table.

This plan has empty `requirements: []` because it does NOT introduce new scan-level requirements — every Phase 4 REQ-ID is shipped by Plans 04-01..04-10. Instead, this plan's value is the verification + cross-family invariants captured by ROADMAP Phase 4 Success Criteria #4 (consistent Finding envelope shape) and #5 (byte-identical golden fixtures), which are listed in the `success_criteria` frontmatter field for traceability.

Contains a CHECKPOINT for human verification of the Quickstart README example outputs and the golden tolerance acceptance.

Purpose: closes the Phase 4 v1 contract. After this plan, all 22 REQ-IDs are shipped AND validated against authoritative Python references AND the schema-sync CI gate is locked AND the byte-identical-rerun invariant (D3-23) extends to all 23 scans (3 pinned by integration test, the other 20 inherit via the same code path).
Output: 3 golden files + 3 golden-generator Python scripts + 3 updated integration tests + 1 new byte-identical-rerun test + regenerated schemas + README Quickstart + Phase 4 sign-off memo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-CONTEXT.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-VALIDATION.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-01-SUMMARY.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-02-SUMMARY.md
@.planning/phases/04-scan-catalogue-anom-cross-seas/04-10-SUMMARY.md
@crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md
@crates/miner-core/tests/fixtures/generate_golden.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author three goldens (ANOM-02 summary, CROSS-05 engle_granger, SEAS-01 hour_of_day) + Python generators + update integration tests to cross-check</name>
  <files>crates/miner-core/tests/goldens/python-requirements.lock, crates/miner-core/tests/goldens/generate_summary_welford.py, crates/miner-core/tests/goldens/generate_engle_granger.py, crates/miner-core/tests/goldens/generate_hour_of_day.py, crates/miner-core/tests/goldens/stats.summary.welford.jsonl, crates/miner-core/tests/goldens/cross.cointegration.engle_granger.jsonl, crates/miner-core/tests/goldens/seas.bucket.hour_of_day.jsonl, crates/miner-core/tests/scan_summary_welford.rs, crates/miner-core/tests/scan_engle_granger.rs, crates/miner-core/tests/scan_seas_hour_of_day.rs</files>
  <read_first>
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-PATTERNS.md (Pattern J 8-step integration test; "Fixtures & goldens" table)
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md (§Section 2 tolerance pins per row; §1.2 reference pinning policy)
    - crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md (Plan 04-02 created — statsmodels==0.14.6, scipy==1.14.1, arch==7.2.0)
    - crates/miner-core/tests/fixtures/generate_golden.py (Phase 3 LjungBox generator — verbatim analog for the three new generators)
    - crates/miner-core/tests/fixtures/ljung_box_golden.json (Phase 3 golden — analog for the JSONL shape)
    - The three integration tests (scan_summary_welford.rs, scan_engle_granger.rs, scan_seas_hour_of_day.rs) created in Plans 04-03 / 04-08 / 04-09
  </read_first>
  <action>
    Finalise the goldens/ directory. Three goldens must be authored + checked in: one for ANOM (stats.summary.welford), one for CROSS (cross.cointegration.engle_granger), one for SEAS (seas.bucket.hour_of_day). Per RESEARCH.md §1.2 pinning policy: all three use the SAME version pin (`statsmodels==0.14.6`, `scipy==1.14.1`, `arch==7.2.0`) — the path of least resistance.

    Author `crates/miner-core/tests/goldens/python-requirements.lock` with the exact hash-pinned freeze (run pip in a venv if available; otherwise commit a documented placeholder + the regeneration recipe baked into REFERENCE-VERSIONS.md from Plan 04-02). Acceptable form: the file contains the canonical `package==version` lines at minimum (statsmodels, scipy, arch, numpy, pandas, plus their transitive deps as pinned by `pip freeze`). If you cannot run pip in-environment, commit a stub that explicitly states "RUN `python -m venv /tmp/venv && /tmp/venv/bin/pip install statsmodels==0.14.6 scipy==1.14.1 arch==7.2.0 && /tmp/venv/bin/pip freeze > crates/miner-core/tests/goldens/python-requirements.lock` to populate."

    Author three Python generators in `crates/miner-core/tests/goldens/`:

    `generate_summary_welford.py`:
    - Uses scipy.stats.describe + scipy.stats.iqr against a deterministic input (the same LCG-seeded series Rust uses, encoded as a Python list literal or generated via numpy with a fixed seed)
    - Emits a JSON object with keys: `provenance` ({scipy_version, statsmodels_version}), `input` (the close array), `expected` ({mean, std, skew, excess_kurtosis, iqr, min, max, n})
    - Writes to stats.summary.welford.jsonl as a single JSON object on one line

    `generate_engle_granger.py`:
    - Uses statsmodels.tsa.stattools.coint(y0=leg_a, y1=leg_b) per D4-09; computes residuals via the underlying OLS regression manually (coint doesn't return residuals; replicate the two-step internally); fits AR(1) on residuals to get OU half-life
    - Emits keys: provenance, input ({close_a, close_b, timestamps_ms}), expected ({hedge_ratio_beta, hedge_ratio_alpha, adf_stat, adf_p_value, ou_half_life, residual_std, residuals[]})

    `generate_hour_of_day.py`:
    - Uses pandas.DataFrame.groupby(pd.to_datetime(ts).hour).agg([mean, std, count, lambda x: scipy.stats.iqr(x)]) over a deterministic 24-hour-distributed input
    - Emits keys: provenance, input, expected ({buckets[24], means[24], stds[24], counts[24], t_stats[24], iqrs[24]})

    All three generators are runnable as `python crates/miner-core/tests/goldens/generate_<name>.py > crates/miner-core/tests/goldens/<scan_id>.jsonl` — they must be deterministic across re-runs.

    Run each generator against the pinned versions (or commit pre-generated outputs if pip-install is not available in this task's environment; document the regen recipe so a developer can refresh).

    Extend the three integration tests created in earlier plans to cross-check against the goldens (Pattern J Step 1-7):
    - `scan_summary_welford.rs`: add a test `summary_welford_matches_scipy_describe_golden` that loads stats.summary.welford.jsonl, parses the input array, builds a BarFrame from it (where close = input), runs the scan, decodes effect.value + effect.extra.* against the golden's expected.* within tolerance 1e-10 per RESEARCH.md §Section 2
    - `scan_engle_granger.rs`: analog test `engle_granger_matches_statsmodels_coint_golden` within tolerance 1e-8 for ADF-stat / p-value and 1e-10 for β / α / residuals
    - `scan_seas_hour_of_day.rs`: analog test `hour_of_day_matches_pandas_groupby_golden` within tolerance 1e-12 (pure aggregation)

    Each golden cross-check follows Pattern J Step 1's provenance gate: `assert_eq!(golden["provenance"]["statsmodels_version"].as_str(), Some("0.14.6"), "regenerate after pinning statsmodels==0.14.6")`. This prevents accidental golden drift.

    If the IF the executor environment does not have a Python venv available, the generator scripts and JSONL goldens may be committed as documented stubs with the regen recipe in their leading comment AND the Rust integration test marked `#[ignore]` with a comment "Re-enable after running scripts/gen-goldens.sh against pinned Python lock". This is a fallback ONLY if pip is unavailable; the executor SHOULD attempt to run the generators if possible.
  </action>
  <acceptance_criteria>
    - `test -f crates/miner-core/tests/goldens/stats.summary.welford.jsonl`
    - `test -f crates/miner-core/tests/goldens/cross.cointegration.engle_granger.jsonl`
    - `test -f crates/miner-core/tests/goldens/seas.bucket.hour_of_day.jsonl`
    - `test -f crates/miner-core/tests/goldens/generate_summary_welford.py`
    - `test -f crates/miner-core/tests/goldens/generate_engle_granger.py`
    - `test -f crates/miner-core/tests/goldens/generate_hour_of_day.py`
    - Each golden JSONL contains a `provenance` object with at least `scipy_version` OR `statsmodels_version` matching the pinned `0.14.6` / `1.14.1` per REFERENCE-VERSIONS.md
    - `cargo test -p miner-core --test scan_summary_welford summary_welford_matches_scipy_describe_golden` passes within 1e-10 tolerance (OR is `#[ignore]`d with the documented regen recipe if pip is unavailable)
    - `cargo test -p miner-core --test scan_engle_granger engle_granger_matches_statsmodels_coint_golden` passes (or `#[ignore]`d with rationale)
    - `cargo test -p miner-core --test scan_seas_hour_of_day hour_of_day_matches_pandas_groupby_golden` passes within 1e-12 (or `#[ignore]`d)
    - All other tests including Phase 3 LjungBox golden continue to pass byte-identically
  </acceptance_criteria>
  <verify>
    <automated>test -f crates/miner-core/tests/goldens/stats.summary.welford.jsonl && test -f crates/miner-core/tests/goldens/cross.cointegration.engle_granger.jsonl && test -f crates/miner-core/tests/goldens/seas.bucket.hour_of_day.jsonl && grep -q '0.14.6\|1.14.1' crates/miner-core/tests/goldens/stats.summary.welford.jsonl && cargo test -p miner-core --test scan_ljung_box --test scan_summary_welford --test scan_engle_granger --test scan_seas_hour_of_day</automated>
  </verify>
  <done>Three goldens committed for representative ANOM + CROSS + SEAS scans (Success Criterion #5); golden cross-check integration tests pass within RESEARCH.md §Section 2 tolerances OR are deliberately `#[ignore]`d with documented regen recipe pending pip availability.</done>
</task>

<task type="auto">
  <name>Task 2: Schema regen + byte-identical-rerun integration test + README Quickstart extension + Phase 4 sign-off memo</name>
  <files>schemas/findings-v1.schema.json, schemas/scans-catalogue-v1.schema.json, crates/miner-core/tests/byte_identical_rerun.rs, README.md, .planning/phases/04-scan-catalogue-anom-cross-seas/04-11-PHASE-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-scan-catalogue-anom-cross-seas/04-RESEARCH.md (§Section 7 Schemars Regen Recipe — the idempotent gen-schema invocation; the diff is expected NON-empty due to 22 new scan_id constants in scans-catalogue-v1.schema.json plus any envelope ramifications from Plan 04-01)
    - .planning/ROADMAP.md (Phase 4 success criteria §4: "consistent Finding envelope shape across all 22 scans, single discriminant by scan_id, scan-specific extras in documented effect.extra object" — this task's byte-identical-rerun test verifies the envelope-shape invariant; SC#5: "byte-identical golden fixtures for ANOM + CROSS + SEAS")
    - README.md (current state — for the location to extend Quickstart)
    - The existing scan_facade_determinism.rs (analog for byte-identical-rerun integration test pattern)
    - All prior 04-*-SUMMARY.md memos (for the Phase 4 sign-off traceability matrix content)
  </read_first>
  <action>
    Run `cargo run -p xtask -- gen-schema` to regenerate schemas/findings-v1.schema.json + schemas/scans-catalogue-v1.schema.json. The expected diff is non-empty (22 new scan_ids in catalogue + any envelope changes from Plan 04-01 / Plan 04-02). Run gen-schema TWICE and `git diff --exit-code schemas/` after the second run — MUST exit 0 (idempotent regeneration after the schemas are committed).

    Commit the regenerated schemas. Per CONTEXT.md and RESEARCH.md §Section 7: schema_version stays = 1 (Phase 6 wrappers don't exist).

    Create `crates/miner-core/tests/byte_identical_rerun.rs` — a new integration test pinning the D3-23 byte-identical-rerun invariant for THREE representative scans (one ANOM, one CROSS, one SEAS). Test pattern:
    1. Build deterministic LCG-seeded BarFrame
    2. Run scan A (e.g., stats.summary.welford@1) twice via engine::run_one_with_registry; capture both sinks
    3. Mask volatile fields (run_id, started_at_utc, produced_at_utc, ended_at_utc) via common::mask_volatile_fields
    4. Assert the two masked JSONL outputs are byte-identical (==)
    5. Repeat for scan B (cross.cointegration.engle_granger@1 — but as Pair-arity, needs TWO BarFrames)
    6. Repeat for scan C (seas.bucket.hour_of_day@1)
    7. ALSO run scan A with a DIFFERENT run_id and assert the JSONL differs only in the run_id + timestamp fields — pinning that masking is the only structural difference

    Extend `README.md` Quickstart section with three new invocation examples (one ANOM, one CROSS, one SEAS) per ROADMAP success criterion. Each example shows:
    - The exact `miner scan ...` command (using a fixture path)
    - The expected first JSONL line (or fragment — first 80 chars) of stdout
    - A note pointing to the per-scan param schema via `miner scans | jq '.[] | select(.id == "...")'`

    Format the README extension under the existing Quickstart subheading; do NOT introduce a separate section unless the Phase 1-3 README uses one.

    Create `.planning/phases/04-scan-catalogue-anom-cross-seas/04-11-PHASE-VERIFICATION.md` — Phase 4 sign-off memo with:
    - **Requirement Traceability Matrix:** 22 rows, one per requirement ID (ANOM-01..11, CROSS-01..05, SEAS-01..06), each row recording the registered scan_id + the plan number that landed it + the integration test file name
    - **Success Criteria Checklist:** the 5 ROADMAP §Phase 4 criteria (single-leg ANOM scans match scipy/statsmodels; CROSS scans carry both-legs provenance + joint data_slice; SEAS scans emit per-bucket arrays; SC#4 = consistent envelope shape across all 22 scans pinned by byte_identical_rerun.rs; SC#5 = golden fixtures for ≥1 per family within tolerances) — each marked ✓ with evidence (test file or commit hash). SC#4 and SC#5 are explicitly cross-referenced to the `success_criteria` frontmatter field of this plan.
    - **D4-XX Decision Audit:** D4-01 (instruments Vec), D4-02 (arity trait + WrongInstrumentArity), D4-03 (sources Vec OR D4-03-ALT), D4-04 (intersection gap policy), D4-05 (ANOM-01 single scan with variant param), D4-06 (LjungBox refactor), D4-07 (mixed-side legs permitted), D4-08 (effect.value canonical picks per §Section 2 row), D4-09 (Engle-Granger sign convention) — each with the file path or test that pins the decision
    - **Open Items:** any deferred items not closed in Phase 4 (e.g., golden cross-checks marked `#[ignore]` pending pip; HYG-* hygiene layer landing in Phase 5; MCP/HTTP wrappers in Phase 6)
    - **Sign-off:** A "Ready for `/gsd-verify-work`" statement when everything above is checked, OR a list of remaining blockers
  </action>
  <acceptance_criteria>
    - First `cargo run -p xtask -- gen-schema` exits 0
    - Second consecutive gen-schema: `git diff --exit-code schemas/` exits 0 (idempotent)
    - `grep -c '"id":' schemas/scans-catalogue-v1.schema.json` reflects 23 registered scans (the schema either contains 23 scan-id entries OR has the schema-level enum mentioning them; the precise form depends on how `miner scans` materialises the catalogue schema)
    - `test -f crates/miner-core/tests/byte_identical_rerun.rs && wc -l < crates/miner-core/tests/byte_identical_rerun.rs | awk '$1 >= 80 { exit 0 } { exit 1 }'`
    - `cargo test -p miner-core --test byte_identical_rerun` passes (all 3 representative scans produce byte-identical JSONL across two runs modulo run_id + timestamps) — SC#4 evidence
    - `grep -c 'miner scan' README.md | awk '$1 >= 4 { exit 0 } { exit 1 }'` (README has at least 4 `miner scan` invocations: the Phase 3 example + the 3 Phase 4 ANOM/CROSS/SEAS examples)
    - `test -f .planning/phases/04-scan-catalogue-anom-cross-seas/04-11-PHASE-VERIFICATION.md && wc -l < .planning/phases/04-scan-catalogue-anom-cross-seas/04-11-PHASE-VERIFICATION.md | awk '$1 >= 40 { exit 0 } { exit 1 }'`
    - 04-11-PHASE-VERIFICATION.md contains a Requirement Traceability Matrix with 22 entries (one per Phase 4 REQ-ID): `grep -cE 'ANOM-|CROSS-|SEAS-' .planning/phases/04-scan-catalogue-anom-cross-seas/04-11-PHASE-VERIFICATION.md | awk '$1 >= 22 { exit 0 } { exit 1 }'`
    - 04-11-PHASE-VERIFICATION.md mentions all D4-XX decision IDs: D4-01 through D4-09 (9 decisions per CONTEXT.md): `for d in D4-01 D4-02 D4-03 D4-04 D4-05 D4-06 D4-07 D4-08 D4-09; do grep -q "$d" .planning/phases/04-scan-catalogue-anom-cross-seas/04-11-PHASE-VERIFICATION.md || exit 1; done`
    - 04-11-PHASE-VERIFICATION.md cross-references ROADMAP SC#4 and SC#5 explicitly (matching the `success_criteria` frontmatter field of this plan): `grep -cE 'SC#4|SC#5|SC-4|SC-5|Success Criterion #4|Success Criterion #5' .planning/phases/04-scan-catalogue-anom-cross-seas/04-11-PHASE-VERIFICATION.md | awk '$1 >= 2 { exit 0 } { exit 1 }'`
    - `cargo nextest run --workspace --no-fail-fast` (or `cargo test --workspace`) exits 0 — full workspace test suite passes
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0 — no lint warnings across the workspace
  </acceptance_criteria>
  <verify>
    <automated>cargo run -p xtask -- gen-schema && cargo run -p xtask -- gen-schema && git diff --exit-code schemas/ && cargo test -p miner-core --test byte_identical_rerun && cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && test -f .planning/phases/04-scan-catalogue-anom-cross-seas/04-11-PHASE-VERIFICATION.md</automated>
  </verify>
  <done>Schemas regenerate idempotently and reflect all 23 registered scans; byte-identical-rerun test pins D3-23 for representative ANOM/CROSS/SEAS scans (ROADMAP SC#4); README Quickstart shows three new Phase 4 invocations; Phase 4 sign-off memo records 22-requirement traceability + 9 D4-XX decision audit + SC#4/SC#5 cross-reference + signed-off status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — Quickstart README examples + golden tolerances + 22-requirement sign-off</name>
  <files>README.md, .planning/phases/04-scan-catalogue-anom-cross-seas/04-11-PHASE-VERIFICATION.md, crates/miner-core/tests/goldens/*.jsonl</files>
  <what-built>
    Plans 04-01 through 04-10 land 22 Phase 4 scan implementations + facade extensions + CLI + integration tests. Plan 04-11 Task 1 authors three reference goldens (ANOM-02 / CROSS-05 / SEAS-01); Plan 04-11 Task 2 regenerates schemas, adds the byte-identical-rerun regression, extends README Quickstart, and produces the Phase 4 sign-off memo.

    Net deliverables across Plans 04-01 through 04-11:
    - 22 v1 scan IDs registered (ANOM-01..11 + CROSS-02..05 + SEAS-01..06) plus 1 ANOM-04 squared variant + CROSS-01 kernel primitive (not registered)
    - 3 statsmodels/scipy goldens checked in with pinned versions per RESEARCH.md §1.2
    - Schemas regenerated; `git diff --exit-code schemas/` clean after a second gen-schema
    - byte_identical_rerun.rs pins D3-23 for one scan from each family (ROADMAP SC#4 evidence)
    - README Quickstart shows three new Phase 4 invocations
    - 04-11-PHASE-VERIFICATION.md records the full traceability + decision audit + ROADMAP SC#4/SC#5 cross-reference
  </what-built>
  <how-to-verify>
    1. Run the README Quickstart example for one ANOM scan, one CROSS scan, one SEAS scan against the checked-in fixture cache. For each: paste the exact `miner scan ...` command from the README, observe the first JSONL line of stdout, and confirm it parses as valid JSON with a `scan_id` field matching the requested scan. Expected: each scan emits at least one Finding::Result envelope.
    2. Open `.planning/phases/04-scan-catalogue-anom-cross-seas/04-11-PHASE-VERIFICATION.md`. Walk the Requirement Traceability Matrix — confirm all 22 rows are present with a scan_id + plan number + test file. Confirm the D4-01..D4-09 decision audit is complete. Confirm ROADMAP SC#4 and SC#5 are explicitly cross-referenced and tied to the `success_criteria` frontmatter field of this plan.
    3. Inspect the three goldens under `crates/miner-core/tests/goldens/`. Each must contain a `provenance` block with the version pin (`statsmodels==0.14.6`, `scipy==1.14.1`). If any golden is currently a stub (because pip was unavailable in the executor environment), confirm the regen recipe is documented and the Rust integration test is properly `#[ignore]`d with a TODO comment.
    4. Run `cargo test --workspace` and `cargo clippy --workspace --all-targets -- -D warnings` — confirm zero failures and zero warnings.
    5. Run `cargo run -p miner-cli -- scans 2>/dev/null | wc -l` — confirm ≥ 23 catalogue lines.
    6. Run `cargo run -p xtask -- gen-schema && cargo run -p xtask -- gen-schema && git diff --exit-code schemas/` — confirm exit 0 (schemas idempotent).
    7. Eyeball one golden's expected tolerance against the Rust-emitted output by running `cargo test -p miner-core --test scan_summary_welford summary_welford_matches_scipy_describe_golden -- --show-output` (or whichever golden test name was used) — confirm the assertion message reports an actual tolerance ≤ 1e-10.
  </how-to-verify>
  <action>
    Wait for human verification of the items in <how-to-verify>. NO automated work is performed in this task — the human walks the README Quickstart examples, inspects the Phase 4 sign-off memo, runs the workspace test suite, and confirms the 22-requirement traceability matrix is complete. Resume only after the human signals approval via the resume-signal line.
  </action>
  <verify>
    <human-check>Human types "approved" or describes specific issues (per the resume-signal contract below). The /gsd-execute-phase orchestrator pauses here and awaits human input before declaring Phase 4 complete.</human-check>
  </verify>
  <done>Human signs off with "approved — Phase 4 ready for /gsd-verify-work" OR records remaining blockers for follow-up.</done>
  <resume-signal>Type "approved — Phase 4 ready for /gsd-verify-work" or describe any issues found (e.g., "golden tolerance fails", "README example breaks", "missing requirement traceability row").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python venv → Rust integration test | Goldens cross the boundary as committed JSONL artefacts; statsmodels version pin in `provenance` is the contract |
| `cargo run -p xtask -- gen-schema` → committed schemas/ | Determinism gate — must be idempotent across consecutive runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11-01 | Tampering | Golden drift | mitigate | Each integration test's Pattern J Step 1 has `assert_eq!(provenance.statsmodels_version, Some("0.14.6"))` — drift surfaces as a TEST FAILURE with a clear regenerate-on-pin instruction. |
| T-04-11-02 | Information Disclosure | Schema non-idempotency | mitigate | Task 2 acceptance verifies `cargo run -p xtask -- gen-schema && cargo run -p xtask -- gen-schema && git diff --exit-code schemas/` exits 0. Phase 3 already established this CI gate. |
| T-04-11-03 | Tampering | Python supply chain in goldens | mitigate | python-requirements.lock pins hashes (when generated by pip freeze --require-hashes); REFERENCE-VERSIONS.md documents the regen recipe. Generators run in a developer venv, not in CI directly — generated JSONLs are reviewed in PR. |
| T-04-11-SC | Tampering | npm/pip/cargo installs | mitigate | Python deps are golden-generation-only (not runtime); RESEARCH.md §Package Legitimacy Audit notes "All three are top-100 PyPI packages with decade-plus histories." No Rust packages added in this plan beyond Plan 04-01's audited deps. |
</threat_model>

<verification>
- cargo nextest run --workspace --no-fail-fast (or cargo test --workspace) exits 0
- cargo clippy --workspace --all-targets -- -D warnings exits 0
- cargo run -p xtask -- gen-schema is idempotent across two runs
- All 22 Phase 4 requirements traced in 04-11-PHASE-VERIFICATION.md
- All 9 D4-XX decisions audited in 04-11-PHASE-VERIFICATION.md
- ROADMAP Phase 4 SC#4 + SC#5 cross-referenced in 04-11-PHASE-VERIFICATION.md (matching this plan's `success_criteria` frontmatter)
- README Quickstart shows one Phase 4 invocation per family
- The byte-identical-rerun test pins D3-23 for three representative scans (SC#4 evidence)
- Three goldens with pinned provenance are committed (SC#5 evidence)
- Human checkpoint passes (README examples produce valid JSONL; tolerance ≤ documented thresholds)
</verification>

<success_criteria>
- All 22 Phase 4 REQ-IDs satisfied + traced in sign-off memo
- All 9 D4-XX decisions audited in sign-off memo
- ROADMAP Phase 4 SC#4 (consistent Finding envelope shape across 22 scans) pinned by byte_identical_rerun.rs
- ROADMAP Phase 4 SC#5 (byte-identical golden fixtures for ANOM + CROSS + SEAS) pinned by the three goldens + integration tests within RESEARCH.md §Section 2 tolerances
- Schemas regenerate idempotently; schema_version stays = 1
- README Quickstart extended with Phase 4 ANOM + CROSS + SEAS invocation examples
- cargo nextest run --workspace passes; clippy clean
- Human checkpoint approves Phase 4 as ready for `/gsd-verify-work`
</success_criteria>

<output>
Create `.planning/phases/04-scan-catalogue-anom-cross-seas/04-11-SUMMARY.md` documenting: the three committed goldens with their tolerance bounds, the byte-identical-rerun coverage list, the README Quickstart additions, the explicit cross-reference between this plan's `success_criteria` frontmatter (SC#4 + SC#5) and the ROADMAP Phase 4 success criteria, and the Phase 4 sign-off pointer to 04-11-PHASE-VERIFICATION.md.
</output>
