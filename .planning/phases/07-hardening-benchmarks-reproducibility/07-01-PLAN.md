---
phase: 07-hardening-benchmarks-reproducibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/regen-goldens.sh
  - crates/miner-core/tests/goldens/stats.summary.welford.jsonl
  - crates/miner-core/tests/goldens/cross.cointegration.engle_granger.jsonl
  - crates/miner-core/tests/goldens/seas.bucket.hour_of_day.jsonl
  - crates/miner-core/tests/scan_summary_welford.rs
  - crates/miner-core/tests/scan_engle_granger.rs
  - crates/miner-core/tests/scan_seas_hour_of_day.rs
  - CONTRIBUTING.md
autonomous: true
requirements:
  - FOUND-03
  - OUT-03

must_haves:
  truths:
    - "User can run scripts/regen-goldens.sh on a machine with uv installed and produce byte-identical real goldens (no STUB placeholders) for the three family golden files."
    - "User can run `cargo test -p miner-core --test scan_summary_welford` and the previously #[ignore]d golden-parity test executes and passes."
    - "User can read CONTRIBUTING.md `## Regenerating goldens` and reproduce the regen flow without reading source."
  artifacts:
    - path: "scripts/regen-goldens.sh"
      provides: "Pinned-Python-3.11 uv-driven regen recipe for the three family goldens"
      contains: "uv venv --python 3.11"
    - path: "crates/miner-core/tests/goldens/stats.summary.welford.jsonl"
      provides: "Real (not stub) ANOM-02 golden against scipy 1.14.1"
      contains: '"scipy_version"'
    - path: "crates/miner-core/tests/goldens/cross.cointegration.engle_granger.jsonl"
      provides: "Real CROSS-05 golden against statsmodels"
      contains: '"statsmodels_version"'
    - path: "crates/miner-core/tests/goldens/seas.bucket.hour_of_day.jsonl"
      provides: "Real SEAS-01 golden against pandas"
      contains: '"pandas_version"'
    - path: "CONTRIBUTING.md"
      provides: "Regenerating goldens subsection"
      contains: "## Regenerating goldens"
  key_links:
    - from: "scripts/regen-goldens.sh"
      to: "crates/miner-core/tests/goldens/python-requirements.lock"
      via: "uv pip install --no-deps -r"
      pattern: "python-requirements\\.lock"
    - from: "crates/miner-core/tests/scan_summary_welford.rs"
      to: "crates/miner-core/tests/goldens/stats.summary.welford.jsonl"
      via: "include_str! + provenance gate"
      pattern: "include_str!.*stats\\.summary\\.welford\\.jsonl"
---

<objective>
Phase 7's verification-debt closure cannot land until the three family goldens are REAL (not STUB). All three currently carry `"_stub_note": "STUB GOLDEN — placeholder until regenerated against pinned Python 3.11..."` and their integration tests are `#[ignore]`d (Plan 04-11 Pattern J Step 1 deferral). Phase 7 Plan 01 closes that debt:

1. Land a reproducible regen recipe via uv (developer machine has Python 3.14 only — uv handles 3.11 install without polluting system Python) per RESEARCH Open Question 1.
2. Regenerate the three family goldens against pinned `python-requirements.lock` in a `uv venv --python 3.11` venv.
3. Un-`#[ignore]` the three matching golden-parity tests so `cargo test --workspace` exercises them.
4. Document the workflow in CONTRIBUTING.md `## Regenerating goldens` per D7-06.

Purpose: This plan is a HARD prerequisite for Plan 07-09 (envelope-snapshot test) — the envelope snapshot must run against real, non-stub family-golden infrastructure to be a meaningful verification gate.

Output: Three regenerated `.jsonl` goldens; one new shell script; one new CONTRIBUTING subsection; three test files with `#[ignore]` removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-CONTEXT.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md
@crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md
@crates/miner-core/tests/goldens/python-requirements.lock
@scripts/install-git-hooks.sh
@CONTRIBUTING.md

<interfaces>
<!-- Pinned reference versions per crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md -->
<!-- Provenance-gate identifiers expected by the three golden tests: -->
<!--   stats.summary.welford.jsonl   → provenance.scipy_version == "1.14.1" -->
<!--   cross.cointegration.engle_granger.jsonl → provenance.statsmodels_version (per generate_engle_granger.py output) -->
<!--   seas.bucket.hour_of_day.jsonl → provenance.pandas_version (per generate_hour_of_day.py output) -->
<!-- Existing #[ignore] markers to remove: -->
<!--   crates/miner-core/tests/scan_summary_welford.rs:163  -->
<!--   crates/miner-core/tests/scan_engle_granger.rs:333    -->
<!--   crates/miner-core/tests/scan_seas_hour_of_day.rs:135 -->
<!-- The doc comments at lines 160 / 330 / 132 are also stale and must be updated to reflect that the golden is now real. -->
<!-- Python regen scripts the recipe drives: -->
<!--   crates/miner-core/tests/goldens/generate_summary_welford.py  -->
<!--   crates/miner-core/tests/goldens/generate_engle_granger.py    -->
<!--   crates/miner-core/tests/goldens/generate_hour_of_day.py      -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/regen-goldens.sh + CONTRIBUTING.md ## Regenerating goldens subsection</name>
  <files>scripts/regen-goldens.sh, CONTRIBUTING.md</files>
  <read_first>
    - scripts/install-git-hooks.sh (shell-script template per PATTERNS.md Pattern F: `#!/usr/bin/env bash` + SPDX header + `set -euo pipefail` + `REPO_ROOT="$(git rev-parse --show-toplevel)"`)
    - crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md (pinned scipy 1.14.1 / statsmodels / pandas versions)
    - crates/miner-core/tests/goldens/python-requirements.lock (the lockfile uv installs into the 3.11 venv)
    - CONTRIBUTING.md (existing "Quality gates" + "License headers" subsections; new "## Regenerating goldens" inserts after Quality gates per D7-06)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §"scripts/regen-goldens.sh" + §"Pattern F" + §"Pattern B" (SPDX header convention)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md §"Open Question 1" (uv-based recipe rationale) + §"Pitfall 3" (regen recipe)
  </read_first>
  <action>
    Create `scripts/regen-goldens.sh` (executable, mode 0755). First two lines: SPDX header per PATTERNS.md Pattern B — `# SPDX-License-Identifier: Apache-2.0` then `# Copyright 2026 Radius Red Ltd.` Body: `#!/usr/bin/env bash` shebang on line 1 (so SPDX moves to lines 2-3 with a header comment block describing purpose between them — match `scripts/install-git-hooks.sh` shape exactly: shebang line 1, blank line 2, comment block lines 3-N, blank line, `set -euo pipefail`, blank line, `REPO_ROOT="$(git rev-parse --show-toplevel)"`, `cd "$REPO_ROOT"`).

    Body steps (after `cd "$REPO_ROOT"`):
    1. `command -v uv >/dev/null 2>&1 || { echo "uv not installed; install via https://docs.astral.sh/uv/" >&2; exit 2; }`
    2. `uv venv --python 3.11 .venv-goldens` (creates pinned 3.11 venv at `.venv-goldens/` — must be gitignored; add `.venv-goldens/` to `.gitignore` if not already present, but only if `git check-ignore .venv-goldens` returns non-zero).
    3. `uv pip install --no-deps -r crates/miner-core/tests/goldens/python-requirements.lock --python .venv-goldens/bin/python` — `--no-deps` because the lockfile pins exact transitive versions per the REFERENCE-VERSIONS.md guarantee.
    4. Three regen invocations, output redirected to the matching golden path:
       - `.venv-goldens/bin/python crates/miner-core/tests/goldens/generate_summary_welford.py > crates/miner-core/tests/goldens/stats.summary.welford.jsonl`
       - `.venv-goldens/bin/python crates/miner-core/tests/goldens/generate_engle_granger.py > crates/miner-core/tests/goldens/cross.cointegration.engle_granger.jsonl`
       - `.venv-goldens/bin/python crates/miner-core/tests/goldens/generate_hour_of_day.py > crates/miner-core/tests/goldens/seas.bucket.hour_of_day.jsonl`
    5. Final echo line: `echo "[regen-goldens] OK — three goldens regenerated; review diff and commit as a single \"chore(07): regenerate family goldens\" commit per CONTRIBUTING.md ## Regenerating goldens."`

    Run the script. Verify each regenerated `.jsonl` no longer contains the `_stub_note` key (use `grep -c '_stub_note' <file>` per file — count must be 0). Verify each file contains the matching `provenance.<library>_version` key with a non-"STUB" value.

    Then edit CONTRIBUTING.md: insert a new `## Regenerating goldens` subsection AFTER the existing `## Quality gates` table (which ends around line 50). The new subsection MUST contain:
    - One-paragraph framing: the three family goldens (`stats.summary.welford.jsonl`, `cross.cointegration.engle_granger.jsonl`, `seas.bucket.hour_of_day.jsonl`) are bit-for-bit pinned against the Python reference versions in `crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md`. Regen is required only when REFERENCE-VERSIONS.md is bumped or when the regen scripts themselves change.
    - Code block showing `./scripts/regen-goldens.sh` as the one-line invocation.
    - Commit-discipline note (per D7-06): the resulting diff must land as a single `chore: regen goldens after <reason>` commit; never mix with behavioural changes.
    - Cross-reference link to `crates/miner-core/tests/goldens/REFERENCE-VERSIONS.md` for the pinned version table.

    Do NOT touch the existing Quality gates table rows (Plan 07-03 will extend that table). Do NOT add a "samply" subsection here (Plan 07-08 owns that).
  </action>
  <acceptance_criteria>
    - `scripts/regen-goldens.sh` exists and is executable: `test -x scripts/regen-goldens.sh` exits 0.
    - First three lines of `scripts/regen-goldens.sh` are (in order): `#!/usr/bin/env bash`, blank or comment, `# SPDX-License-Identifier: Apache-2.0` somewhere in lines 2-5.
    - `grep -c 'uv venv --python 3.11' scripts/regen-goldens.sh` returns 1.
    - `grep -c 'set -euo pipefail' scripts/regen-goldens.sh` returns 1.
    - `grep -c 'generate_summary_welford\.py' scripts/regen-goldens.sh` returns 1.
    - `grep -c 'generate_engle_granger\.py' scripts/regen-goldens.sh` returns 1.
    - `grep -c 'generate_hour_of_day\.py' scripts/regen-goldens.sh` returns 1.
    - `bash scripts/regen-goldens.sh` exits 0 (within ~60s on a machine with uv installed).
    - After running the script: `grep -c '_stub_note' crates/miner-core/tests/goldens/stats.summary.welford.jsonl` returns 0.
    - After running the script: `grep -c '_stub_note' crates/miner-core/tests/goldens/cross.cointegration.engle_granger.jsonl` returns 0.
    - After running the script: `grep -c '_stub_note' crates/miner-core/tests/goldens/seas.bucket.hour_of_day.jsonl` returns 0.
    - After running the script: `grep -oE '"scipy_version"\s*:\s*"[^"]+"' crates/miner-core/tests/goldens/stats.summary.welford.jsonl` shows `"scipy_version": "1.14.1"` (matches REFERENCE-VERSIONS.md pin).
    - `grep -c '^## Regenerating goldens' CONTRIBUTING.md` returns 1.
    - `CONTRIBUTING.md` contains the string `scripts/regen-goldens.sh` and the string `chore: regen goldens` (commit-discipline note).
    - `.venv-goldens/` is gitignored: `git check-ignore -q .venv-goldens` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; test -x scripts/regen-goldens.sh; grep -q "uv venv --python 3.11" scripts/regen-goldens.sh; grep -q "set -euo pipefail" scripts/regen-goldens.sh; grep -q "^## Regenerating goldens" CONTRIBUTING.md; grep -q "scripts/regen-goldens.sh" CONTRIBUTING.md; for f in stats.summary.welford.jsonl cross.cointegration.engle_granger.jsonl seas.bucket.hour_of_day.jsonl; do [ "$(grep -c "_stub_note" crates/miner-core/tests/goldens/$f)" = "0" ] || { echo "STUB still present in $f"; exit 1; }; done'</automated>
  </verify>
  <done>scripts/regen-goldens.sh exists, executable, runs to completion against uv-managed Python 3.11; three goldens regenerated with no `_stub_note` and real provenance versions; CONTRIBUTING.md has new `## Regenerating goldens` subsection.</done>
</task>

<task type="auto">
  <name>Task 2: Un-ignore the three golden-parity tests + refresh their stale doc comments + verify they pass</name>
  <files>crates/miner-core/tests/scan_summary_welford.rs, crates/miner-core/tests/scan_engle_granger.rs, crates/miner-core/tests/scan_seas_hour_of_day.rs</files>
  <read_first>
    - crates/miner-core/tests/scan_summary_welford.rs (lines 155-180 for the doc-comment + `#[ignore]` attribute + provenance gate code)
    - crates/miner-core/tests/scan_engle_granger.rs (lines 310-355 for the same shape)
    - crates/miner-core/tests/scan_seas_hour_of_day.rs (lines 125-155 for the same shape)
    - crates/miner-core/tests/goldens/stats.summary.welford.jsonl (now-regenerated golden — first line should show real provenance)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §"crates/miner-core/tests/findings_envelope_snapshot.rs" (Pattern J Step 1 provenance gate excerpt — same shape these three tests already use)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md §"Pitfall 3"
  </read_first>
  <action>
    For each of the three files, locate the `#[ignore = "Phase 4 Plan 04-11: golden is a STUB until pinned Python 3.11 ..."]` attribute that precedes the golden-parity test function (specifically: `scan_summary_welford.rs:163`, `scan_engle_granger.rs:333`, `scan_seas_hour_of_day.rs:135`). DELETE that single attribute line entirely.

    Above each `#[ignore]` deletion site, the doc comment (`/// this test is #[ignore]d until a developer runs the regen recipe...`) is now stale. UPDATE the doc comment to reflect the new reality: the test is active; regen is documented in CONTRIBUTING.md `## Regenerating goldens`. Replace the "until a developer runs..." sentence with: "Regen is documented in CONTRIBUTING.md `## Regenerating goldens`; the provenance gate below pins the expected version against REFERENCE-VERSIONS.md."

    Do NOT modify the provenance-gate `assert_eq!(prov, Some("1.14.1"), ...)` body in any of the three tests — Plan 07-09 may extend the masking pattern, but this plan leaves the body alone.

    Run `cargo test -p miner-core --test scan_summary_welford -- summary_welford_matches_scipy_describe_golden` and confirm it passes. Repeat for the engle_granger and hour_of_day equivalents (use `cargo test -p miner-core --test scan_engle_granger -- engle_granger_matches_statsmodels_golden` and `cargo test -p miner-core --test scan_seas_hour_of_day -- hour_of_day_matches_pandas_groupby_golden`; if the exact function names differ, derive them from the `#[test]` immediately following the deleted `#[ignore]`).

    If any test fails after un-ignoring, the regen output is mismatched against current miner-core behaviour — STOP, do not paper over with `#[allow]` or `#[ignore]`. The failure indicates a real verification-debt finding the plan must surface.
  </action>
  <acceptance_criteria>
    - `grep -c '#\[ignore' crates/miner-core/tests/scan_summary_welford.rs` returns 0.
    - `grep -c '#\[ignore' crates/miner-core/tests/scan_engle_granger.rs` returns 0.
    - `grep -c '#\[ignore' crates/miner-core/tests/scan_seas_hour_of_day.rs` returns 0.
    - The three files MUST still contain `Pattern J Step 1` or `provenance` gate assertions — `grep -c 'provenance' crates/miner-core/tests/scan_summary_welford.rs` returns at least 1.
    - `cargo test -p miner-core --test scan_summary_welford` exits 0.
    - `cargo test -p miner-core --test scan_engle_granger` exits 0.
    - `cargo test -p miner-core --test scan_seas_hour_of_day` exits 0.
    - Stale doc-comment phrase "#[ignore]d until a developer" no longer appears in any of the three files: `grep -c "ignore.*until a developer" crates/miner-core/tests/scan_summary_welford.rs crates/miner-core/tests/scan_engle_granger.rs crates/miner-core/tests/scan_seas_hour_of_day.rs | awk -F: '{s+=$2} END {exit s}'` returns exit 0 (sum is 0).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; for f in crates/miner-core/tests/scan_summary_welford.rs crates/miner-core/tests/scan_engle_granger.rs crates/miner-core/tests/scan_seas_hour_of_day.rs; do [ "$(grep -c "#\[ignore" $f)" = "0" ] || { echo "$f still has #[ignore]"; exit 1; }; done && cargo test -p miner-core --test scan_summary_welford && cargo test -p miner-core --test scan_engle_granger && cargo test -p miner-core --test scan_seas_hour_of_day'</automated>
  </verify>
  <done>All three `#[ignore]` attributes removed; stale doc comments refreshed; `cargo test --workspace` now exercises the three golden-parity tests and they pass against the regenerated goldens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → uv-managed venv | uv downloads Python 3.11 + lockfile-pinned wheels from PyPI; this crosses an untrusted-source boundary. |
| regen scripts → committed goldens | Python regen output becomes test-suite ground truth; tampering here would invalidate every downstream golden test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | Goldens drift silently as upstream Python libs change | mitigate | Pattern J Step 1 provenance gate (already in place in all three tests) refuses to run unless `provenance.<library>_version` matches the pinned REFERENCE-VERSIONS.md value. Plan 07-01 preserves this — the assert_eq! gates stay byte-identical. |
| T-07-01-02 | Tampering | uv pulls compromised wheel from PyPI | accept | `--no-deps` against `python-requirements.lock` pins exact wheel hashes (pip-tools lockfile format); registry-level compromise is mitigated by PyPI's own signing posture. This is venv-local and never crosses into the Rust build. |
| T-07-01-03 | Information Disclosure | `.venv-goldens/` accidentally committed to repo | mitigate | Add `.venv-goldens/` to `.gitignore` in Task 1; script verifies via `git check-ignore`. |
</threat_model>

<verification>
- `cargo test -p miner-core --test scan_summary_welford` passes.
- `cargo test -p miner-core --test scan_engle_granger` passes.
- `cargo test -p miner-core --test scan_seas_hour_of_day` passes.
- `cargo test --workspace --no-fail-fast` does not regress (full suite green).
- `bash scripts/regen-goldens.sh` re-runs and the three goldens remain byte-identical on a second invocation (idempotency check).
- `grep -rn '#\[ignore' crates/miner-core/tests/scan_summary_welford.rs crates/miner-core/tests/scan_engle_granger.rs crates/miner-core/tests/scan_seas_hour_of_day.rs` returns no lines.
</verification>

<success_criteria>
1. The three family goldens are REAL (no `_stub_note`, real `provenance.*_version` strings matching `REFERENCE-VERSIONS.md`).
2. The three golden-parity integration tests run and pass under `cargo test --workspace`.
3. `scripts/regen-goldens.sh` is the canonical, reproducible recipe; CONTRIBUTING.md documents the workflow.
4. Plan 07-09 (envelope snapshot) can land downstream against real golden infrastructure.
</success_criteria>

<output>
Create `.planning/phases/07-hardening-benchmarks-reproducibility/07-01-SUMMARY.md` when done.
</output>
