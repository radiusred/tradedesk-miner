---
phase: 07-hardening-benchmarks-reproducibility
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - deny.toml
  - .github/workflows/ci.yml
  - CONTRIBUTING.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can run `cargo deny check` locally (after `cargo install cargo-deny`) against the workspace and see zero violations against the locked allowlist."
    - "User can open a PR and observe two new CI jobs (cargo audit + cargo deny check) running after the existing schema sync step; both pass clean on main."
    - "User can read CONTRIBUTING.md `## Quality gates` and find two new numbered rows describing the cargo audit + cargo deny gates."
  artifacts:
    - path: "deny.toml"
      provides: "cargo-deny 0.19.6 v2-schema config with locked license allowlist"
      contains: "[advisories]"
    - path: ".github/workflows/ci.yml"
      provides: "Two new CI steps (cargo audit + cargo deny) after schema sync"
      contains: "EmbarkStudios/cargo-deny-action@v2"
    - path: "CONTRIBUTING.md"
      provides: "Two new Quality-gates rows (rows 7-8)"
      contains: "cargo audit"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "deny.toml"
      via: "EmbarkStudios/cargo-deny-action@v2 reads deny.toml from repo root"
      pattern: "EmbarkStudios/cargo-deny-action@v2"
    - from: ".github/workflows/ci.yml"
      to: "rustsec advisory database"
      via: "rustsec/audit-check@v2.0.0"
      pattern: "rustsec/audit-check@v2.0.0"
---

<objective>
Land the two supply-chain CI gates locked by D7-05: `cargo audit` (RustSec advisory DB on every push and PR) and `cargo deny check` (license allowlist + bans + sources + advisories). Both run as canonical GH Actions per RESEARCH Open Question 5 + RESEARCH §"Canonical cargo audit + cargo deny CI steps" — `rustsec/audit-check@v2.0.0` and `EmbarkStudios/cargo-deny-action@v2`. The cargo-deny config lives at `deny.toml` in the repo root using the **CORRECTED v2 schema** from RESEARCH Pitfall 6 (the CONTEXT.md D7-05 keys `[advisories] vulnerability = "deny"` and `unsound = "deny"` are INVALID on cargo-deny 0.19.6+ and MUST NOT appear).

This plan depends on Plan 07-01 ONLY because both touch CONTRIBUTING.md — 07-01 adds the `## Regenerating goldens` subsection, this plan extends the `## Quality gates` table. Sequencing prevents merge conflicts.

Purpose: Closes the supply-chain leg of Phase 7 verification debt — supply-chain hygiene is the ONE security-relevant new surface in Phase 7 (RESEARCH §"Security Domain"). The threat model is non-trivial; see `<threat_model>` below.

Output: One new root-level config file; two new CI workflow steps; two new CONTRIBUTING.md table rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-CONTEXT.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md
@.github/workflows/ci.yml
@CONTRIBUTING.md
@Cargo.lock

<interfaces>
<!-- Canonical deny.toml v2 schema per RESEARCH Pitfall 6: -->
<!-- [advisories] -->
<!--   yanked = "deny" -->
<!--   unmaintained = "all"  (or "workspace" — see Pitfall 6) -->
<!--   ignore = [] -->
<!--   NO `vulnerability`, NO `unsound`, NO `notice`, NO `severity-threshold` — REMOVED in 0.14+ -->

<!-- License allowlist (D7-05 locked): -->
<!-- ["Apache-2.0", "MIT", "BSD-2-Clause", "BSD-3-Clause", "ISC", -->
<!--  "Unicode-DFS-2016", "Unicode-3.0", "Zlib", "MPL-2.0"] -->

<!-- bans: multiple-versions = "warn" (not deny); wildcards = "deny" -->
<!-- sources: unknown-registry = "deny"; unknown-git = "deny" -->

<!-- Canonical CI steps to APPEND after the existing `schema sync` step (around line 88 of ci.yml): -->
<!-- - name: cargo audit -->
<!--     uses: rustsec/audit-check@v2.0.0 -->
<!--     with: -->
<!--       token: ${{ secrets.GITHUB_TOKEN }} -->
<!-- - name: cargo deny check -->
<!--     uses: EmbarkStudios/cargo-deny-action@v2 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author deny.toml with corrected v2 schema + verify it against current Cargo.lock</name>
  <files>deny.toml</files>
  <read_first>
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md §"Pitfall 6" (the canonical v2 keyset — lines 567-593) AND §"Standard Stack alternatives" (cargo-deny 0.19.6 verified version)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §"deny.toml (NEW repo root)" (the file:line excerpt of the target schema)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-CONTEXT.md §D7-05 (the license allowlist; note that the `vulnerability = "deny"` and `unsound = "deny"` keys from CONTEXT.md MUST NOT be ported verbatim — research finding overrides CONTEXT.md per the planner instructions)
    - https://embarkstudios.github.io/cargo-deny/checks/advisories/cfg.html (authoritative source for the current valid keyset — confirm `yanked`, `unmaintained`, `ignore`, `db-path`, `db-urls`, `maximum-db-staleness` are valid; confirm `vulnerability`, `unsound`, `notice`, `severity-threshold` are REMOVED)
    - Cargo.lock (verify the current dependency tree against the proposed allowlist — RESEARCH Open Question 5)
  </read_first>
  <action>
    Create `deny.toml` at the repo root with the following EXACT v2-schema body. Author every section explicitly; cite RESEARCH §"Pitfall 6" inline as a comment at the top of the file so a future reader doesn't reintroduce the removed keys:

    Sections required (in this order):

    1. Top-of-file comment block: SPDX header (`# SPDX-License-Identifier: Apache-2.0` + `# Copyright 2026 Radius Red Ltd.`) + one-paragraph commentary noting that this file uses the cargo-deny 0.19.6+ schema where `[advisories]` keys `vulnerability`, `unsound`, `notice`, and `severity-threshold` have been REMOVED (all advisories now error by default). Reference RESEARCH §"Pitfall 6" and the upstream docs URL `https://embarkstudios.github.io/cargo-deny/checks/advisories/cfg.html`.

    2. `[advisories]` — keys ONLY from the current valid set:
       - `yanked = "deny"`
       - `unmaintained = "all"` (per RESEARCH Pitfall 6 — `"workspace"` is the narrower alternative if `"all"` produces too many false positives against transitive deps; pick `"all"` for the initial baseline since the workspace is small)
       - `ignore = []` (empty allowlist-by-exception; populated only when a contributor needs a temporary advisory ignore with an inline justification per D7-05 policy)
       - Do NOT include any of: `vulnerability`, `unsound`, `notice`, `severity-threshold`. If included, cargo-deny 0.19.6 will fail with "unknown key" before reading Cargo.lock.

    3. `[licenses]`:
       - `confidence-threshold = 0.93`
       - `allow = [ "Apache-2.0", "MIT", "BSD-2-Clause", "BSD-3-Clause", "ISC", "Unicode-DFS-2016", "Unicode-3.0", "Zlib", "MPL-2.0" ]` (exact list from D7-05; preserve case + ordering).

    4. `[bans]`:
       - `multiple-versions = "warn"`
       - `wildcards = "deny"`
       - `# (we leave `deny = []` / `allow = []` / `skip = []` at their defaults; can be tightened in v1.x)`

    5. `[sources]`:
       - `unknown-registry = "deny"`
       - `unknown-git = "deny"`

    After writing the file, the developer must verify the baseline against the current Cargo.lock. The verification step is to install cargo-deny locally and run the four checks:
    - `cargo install --locked cargo-deny@0.19.6` (skip if already installed)
    - `cargo deny --manifest-path Cargo.toml check licenses` — must exit 0
    - `cargo deny --manifest-path Cargo.toml check advisories` — must exit 0
    - `cargo deny --manifest-path Cargo.toml check bans` — must exit 0 (multi-version WARNINGS are OK; only fail on deny rules)
    - `cargo deny --manifest-path Cargo.toml check sources` — must exit 0
    - `cargo deny --manifest-path Cargo.toml check` — must exit 0 (runs all four)

    If `cargo deny check licenses` surfaces a license NOT in the allowlist, the executor STOPS and surfaces the offending crate + license in the PLAN's `08-03-SUMMARY.md` summary; the planner did not pre-verify against Cargo.lock so this is a legitimate plan-finding moment per D7-05 policy. The remediation is to either replace the dep (preferred) or extend the allowlist with an inline `# allowed-for: <crate>@<version> — <license> — <reason>` comment (per D7-05 allowlist-by-exception policy).

    Do NOT add a `[graph]` section unless cargo-deny complains (defaults are fine). Do NOT add a `[output]` section. Do NOT add a `version = 2` field — cargo-deny 0.19.6 detects v2 implicitly per RESEARCH Pitfall 6 ("version field is no longer required").
  </action>
  <acceptance_criteria>
    - `deny.toml` exists at repo root: `test -f deny.toml`.
    - Contains corrected `[advisories]` keys: `grep -E '^\s*(yanked|unmaintained|ignore)\s*=' deny.toml | wc -l` returns at least 3.
    - Does NOT contain the removed keys: `grep -cE '^\s*(vulnerability|unsound|notice|severity-threshold)\s*=' deny.toml` returns 0.
    - License allowlist matches D7-05 exactly: `grep -c '"Apache-2.0"' deny.toml` returns at least 1, AND `grep -c '"MPL-2.0"' deny.toml` returns at least 1.
    - Bans posture matches D7-05: `grep -E '^\s*multiple-versions\s*=\s*"warn"' deny.toml` returns 1 match, `grep -E '^\s*wildcards\s*=\s*"deny"' deny.toml` returns 1 match.
    - Sources posture matches D7-05: `grep -cE '^\s*unknown-(registry|git)\s*=\s*"deny"' deny.toml` returns 2.
    - SPDX header present: `head -3 deny.toml | grep -c 'SPDX-License-Identifier' ` returns 1.
    - `cargo deny --manifest-path Cargo.toml check` exits 0 (run after `cargo install --locked cargo-deny@0.19.6` if not on PATH).
    - If the executor cannot install cargo-deny on this machine, the alternative is to trust the GH Action that runs in Task 2 (the action installs cargo-deny itself); the executor MUST run the action manually via `act` if available, OR document in the SUMMARY that local verification was skipped and CI is the gate.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; test -f deny.toml; [ "$(grep -cE "^\s*(vulnerability|unsound|notice|severity-threshold)\s*=" deny.toml)" = "0" ]; grep -qE "^\s*yanked\s*=\s*\"deny\"" deny.toml; grep -qE "^\s*unmaintained\s*=" deny.toml; grep -q "Apache-2.0" deny.toml; grep -q "MPL-2.0" deny.toml; grep -qE "^\s*multiple-versions\s*=\s*\"warn\"" deny.toml; grep -qE "^\s*wildcards\s*=\s*\"deny\"" deny.toml; head -5 deny.toml | grep -q "SPDX-License-Identifier"; command -v cargo-deny >/dev/null 2>&1 && cargo deny --manifest-path Cargo.toml check || echo "cargo-deny not installed locally; CI is the gate"'</automated>
  </verify>
  <done>deny.toml lives at repo root with v2 schema; current Cargo.lock satisfies the allowlist (or executor surfaces violations to be triaged); the four removed advisory keys are absent.</done>
</task>

<task type="auto">
  <name>Task 2: Append cargo audit + cargo deny CI steps after schema sync + verify workflow lint</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (full file — six existing steps: build, clippy, fmt, test, tokio-free, schema sync. The new steps insert AFTER the `name: schema sync` step around line 88. Match the existing YAML indentation EXACTLY — use the same indentation as the `tokio-free miner-core` and `schema sync` steps; preserve trailing-newline policy)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md §"Canonical cargo audit + cargo deny CI steps" (the exact YAML snippet) + §"Standard Stack alternatives" (`@v2.0.0` and `@v2` are the canonical versions)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §".github/workflows/ci.yml (MODIFY — append two steps)"
  </read_first>
  <action>
    Edit `.github/workflows/ci.yml`. Locate the `- name: schema sync` step. AFTER that step's closing line (typically a `run:` block ending), APPEND two new steps. The indentation MUST match the existing steps exactly (the file uses 6-space indent for step names if the steps live under a `steps:` list nested under a job; verify by reading the file before writing).

    The two new steps (literal YAML, with the indentation pattern your read of the file establishes):
    ```yaml
          - name: cargo audit
            uses: rustsec/audit-check@v2.0.0
            with:
              token: ${{ secrets.GITHUB_TOKEN }}

          - name: cargo deny check
            uses: EmbarkStudios/cargo-deny-action@v2
    ```

    Verify by:
    1. Running `python3 -c 'import yaml,sys; yaml.safe_load(open(".github/workflows/ci.yml"))'` (or `yq` if available) — the file must parse as valid YAML.
    2. `grep -c 'rustsec/audit-check@v2.0.0' .github/workflows/ci.yml` returns exactly 1.
    3. `grep -c 'EmbarkStudios/cargo-deny-action@v2' .github/workflows/ci.yml` returns exactly 1.
    4. Optional but recommended: run `act -j <jobname>` (if `act` is installed) to dry-run the workflow locally. If `act` is not installed, the executor accepts CI as the gate.

    Do NOT pin to `@v2.0.1` or `@v3` — Plan 07-03 explicitly chooses the published-canonical major-version refs per RESEARCH (`@v2.0.0` for rustsec is the canonical released version; `@v2` for cargo-deny is the canonical major-rolling alias). Do NOT add a separate nightly schedule for v1 (D7-05 explicitly: same pattern as existing fmt/clippy/test gates — every push, every PR).
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` parses as valid YAML: `python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/ci.yml"))'` exits 0.
    - `grep -c 'rustsec/audit-check@v2.0.0' .github/workflows/ci.yml` returns 1.
    - `grep -c 'EmbarkStudios/cargo-deny-action@v2' .github/workflows/ci.yml` returns 1.
    - The new steps appear AFTER `schema sync`: `awk '/name: schema sync/{found=1} /name: cargo audit/{if(found) hit=1} END {exit hit?0:1}' .github/workflows/ci.yml` exits 0.
    - The two new steps have `name:` keys (not just `uses:` without a name).
    - Existing six steps remain unchanged (no regressions in step count): the file's step count should grow by exactly 2.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; python3 -c "import yaml; yaml.safe_load(open(\".github/workflows/ci.yml\"))" ; [ "$(grep -c "rustsec/audit-check@v2.0.0" .github/workflows/ci.yml)" = "1" ]; [ "$(grep -c "EmbarkStudios/cargo-deny-action@v2" .github/workflows/ci.yml)" = "1" ]; awk "/name: schema sync/{found=1} /name: cargo audit/{if(found) hit=1} END {exit hit?0:1}" .github/workflows/ci.yml'</automated>
  </verify>
  <done>CI workflow has two new steps (cargo audit, cargo deny check) after schema sync; file remains valid YAML; canonical action versions used.</done>
</task>

<task type="auto">
  <name>Task 3: Extend CONTRIBUTING.md ## Quality gates with rows 7 + 8 (cargo audit + cargo deny)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (full file — the existing `## Quality gates` table around lines 31-50 lists 6 numbered gates. Plan 07-01 added a new `## Regenerating goldens` subsection AFTER the table — preserve that section; this plan extends the table itself)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §"CONTRIBUTING.md (MODIFY...)" (the two extensions described)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-CONTEXT.md §D7-05 (allowlist-by-exception policy — the failure-mode + temporary-ignore language for the new rows)
  </read_first>
  <action>
    Edit `CONTRIBUTING.md`. Locate the existing numbered list under `## Quality gates`. Currently rows 1-6 cover: cargo fmt, cargo clippy -D warnings, cargo test, cargo build, Tokio-free miner-core, Schema sync. APPEND two new numbered rows (7 and 8) immediately AFTER row 6 (and BEFORE the `## Regenerating goldens` subsection that Plan 07-01 added).

    Row 7 (cargo audit) — must include:
    - The gate name: `**cargo audit.**`
    - The command running in CI: `rustsec/audit-check@v2.0.0` against the RustSec advisory database.
    - The failure mode (per D7-05): "Fails the build on any advisory hit. Zero days tolerance."
    - The temporary-ignore process (per D7-05): "If a CVE genuinely needs a temporary ignore (upstream hasn't released a fix), document it in `deny.toml`'s `[advisories] ignore = []` list with an inline `# RUSTSEC-YYYY-NNNN — <one-line reason> — review by YYYY-MM-DD` comment."

    Row 8 (cargo deny check) — must include:
    - The gate name: `**cargo deny check.**`
    - The command running in CI: `EmbarkStudios/cargo-deny-action@v2` against `deny.toml` at the repo root.
    - The four sub-checks covered: licenses (allowlist), bans (wildcards/duplicates), advisories (mirrors cargo audit), sources (unknown-registry/unknown-git denied).
    - The license allowlist's allowlist-by-exception policy (per D7-05): "New deps must satisfy the allowlist out of the box. If a contributor needs a license outside the allowlist, the PR explains why, the addition lands as a separate commit with an inline `# allowed-for: <reason>` comment in `deny.toml`."

    Do NOT add a samply paragraph here — Plan 07-08 owns that. Do NOT touch the `## Regenerating goldens` subsection — Plan 07-01 owns that. Do NOT renumber existing rows 1-6.
  </action>
  <acceptance_criteria>
    - `grep -c '^[0-9]\+\.\s*\*\*cargo audit\.\*\*' CONTRIBUTING.md` returns 1 (row 7).
    - `grep -c '^[0-9]\+\.\s*\*\*cargo deny check\.\*\*' CONTRIBUTING.md` returns 1 (row 8).
    - `grep -c 'rustsec/audit-check@v2.0.0' CONTRIBUTING.md` returns 1.
    - `grep -c 'EmbarkStudios/cargo-deny-action@v2' CONTRIBUTING.md` returns 1.
    - `grep -c 'allowlist-by-exception' CONTRIBUTING.md` returns at least 1 (the policy term — exact wording may vary, but the concept must appear).
    - The existing six gates remain present: `grep -cE '^[0-9]+\.\s+\*\*(cargo fmt|cargo clippy|cargo test|cargo build|Tokio-free|Schema sync)' CONTRIBUTING.md | awk '{exit ($1 < 6 ? 1 : 0)}'` exits 0 (at least 6 of the original gate names appear as numbered list rows).
    - The `## Regenerating goldens` subsection added by Plan 07-01 is still present: `grep -c '^## Regenerating goldens' CONTRIBUTING.md` returns 1.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; grep -qE "^[0-9]+\. *\*\*cargo audit" CONTRIBUTING.md; grep -qE "^[0-9]+\. *\*\*cargo deny check" CONTRIBUTING.md; grep -q "rustsec/audit-check@v2.0.0" CONTRIBUTING.md; grep -q "EmbarkStudios/cargo-deny-action@v2" CONTRIBUTING.md; grep -q "allowlist-by-exception" CONTRIBUTING.md; grep -q "^## Regenerating goldens" CONTRIBUTING.md'</automated>
  </verify>
  <done>CONTRIBUTING.md ## Quality gates table extended with rows 7 + 8; allowlist-by-exception policy documented; Plan 07-01's Regenerating goldens subsection preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo.lock → public CI | Every dep transitively contributes; supply-chain compromise here ships to every downstream consumer. |
| crates.io registry → developer machine | Slopsquats, namespace confusion, compromised package updates. |
| Third-party GH Actions → CI runner | `rustsec/audit-check@v2.0.0` and `EmbarkStudios/cargo-deny-action@v2` execute arbitrary code on every push; pin to known-good major versions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | Slopsquatted dependency on `cargo add` | mitigate | `cargo deny check sources` denies unknown registries (D7-05 + RESEARCH Security Domain). Researcher already ran slopcheck on Phase 7 deps — all `[OK]`. |
| T-07-03-02 | Tampering | Compromised crates.io package update | mitigate | `cargo audit` against RustSec advisory DB on every push (rustsec/audit-check@v2.0.0). |
| T-07-03-03 | Compliance | GPL-contaminated transitive dep | mitigate | `cargo deny check licenses` against the explicit allowlist of 9 permissive licenses; out-of-allowlist deps require explicit `# allowed-for` comment per D7-05. |
| T-07-03-04 | Tampering | Compromised third-party GH Action | mitigate | Pin to known-good major versions (`@v2.0.0` for audit; `@v2` for deny). For higher assurance v1.x can pin to commit SHAs (GitHub's recommended posture); v1 accepts major-tag refs per industry convention. |
| T-07-03-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH §"Package Legitimacy Audit" verified all 4 new deps via slopcheck v0.6.1; no [SLOP] or [SUS] packages introduced. |
</threat_model>

<verification>
- `cargo deny --manifest-path Cargo.toml check` exits 0 locally (after `cargo install --locked cargo-deny@0.19.6`).
- `python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/ci.yml"))'` exits 0.
- `deny.toml` does NOT contain `vulnerability`, `unsound`, `notice`, or `severity-threshold`.
- `deny.toml` license allowlist matches D7-05 exactly (9 entries).
- CONTRIBUTING.md `## Quality gates` table has rows 7-8 with the canonical action versions.
- On the next CI run after this lands: both `cargo audit` and `cargo deny check` steps appear in the workflow run and pass.
</verification>

<success_criteria>
1. `deny.toml` at repo root, v2 schema, license allowlist locked.
2. CI runs `cargo audit` + `cargo deny check` on every push and PR.
3. Both gates pass clean on the current Cargo.lock.
4. CONTRIBUTING.md documents the gates + the allowlist-by-exception policy.
5. The threat model in this PLAN accurately reflects supply-chain mitigations now in place.
</success_criteria>

<output>
Create `.planning/phases/07-hardening-benchmarks-reproducibility/07-03-SUMMARY.md` when done.
</output>
