---
phase: 07-hardening-benchmarks-reproducibility
plan: 09
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - crates/miner-core/tests/findings_envelope_snapshot.rs
  - crates/miner-core/tests/goldens/envelope_snapshot.jsonl
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - OUT-03

must_haves:
  truths:
    - "User can run `cargo test -p miner-core --test findings_envelope_snapshot` and observe the test passes byte-equality against the checked-in golden after masking volatile fields."
    - "User can re-run the test on a second invocation and observe byte-identical masked stdout (the OUT-03 deterministic-ordering contract holds end-to-end across the Finding envelope's serialised form)."
    - "User can verify the test covers all five Finding variants currently emitted by emit-fixture (RunStart, Result, RunEnd, ScanError if applicable, DryRun if applicable)."
  artifacts:
    - path: "crates/miner-core/tests/findings_envelope_snapshot.rs"
      provides: "Hand-rolled byte-equal envelope snapshot test (NOT insta — per RESEARCH Pitfall 8)"
      contains: "include_str!"
    - path: "crates/miner-core/tests/goldens/envelope_snapshot.jsonl"
      provides: "Pinned envelope-shape golden (masked volatile fields)"
      contains: "<masked_run_id>"
  key_links:
    - from: "crates/miner-core/tests/findings_envelope_snapshot.rs"
      to: "crates/miner-core/tests/goldens/envelope_snapshot.jsonl"
      via: "include_str! at test time"
      pattern: "include_str!.*envelope_snapshot\\.jsonl"
    - from: "crates/miner-core/tests/findings_envelope_snapshot.rs"
      to: "crates/miner-core/tests/common/mod.rs"
      via: "mod common; reuses mask_volatile_fields (5-field variant)"
      pattern: "^mod common"
---

<objective>
Land the final piece of Phase 7's verification-debt closure: the locked findings-envelope snapshot test (D7-06 + ROADMAP success criterion #1). This test exercises FOUND-02 (stdout=findings discipline), FOUND-03 (locked envelope schema), and OUT-03 (deterministic output ordering for golden-file diffing).

Depends on Plan 07-01 ONLY — the envelope snapshot is only a meaningful verification if the three family goldens it sits alongside in `crates/miner-core/tests/goldens/` are REAL (not STUB). Plan 07-01 regenerated them; this plan slots `envelope_snapshot.jsonl` into the same directory as a sibling.

Per RESEARCH Pitfall 8: use the HAND-ROLLED byte-equal pattern (NOT `insta::assert_json_snapshot!`) so the test runs in CI without an `insta review` ceremony on first push. Hand-rolled mirrors the existing `cli_streams::emit_fixture_byte_identical_when_volatile_fields_masked` and reuses `common::mask_volatile_fields` (5-field variant including `produced_at_utc` per PATTERNS Pattern D).

Per D7-06: the golden's body is "the OUTPUT of running the envelope-snapshot test once and committing the resulting masked bytes" — meaning the executor runs the test once (it will fail because the golden doesn't exist yet), captures the masked stdout, writes it as `envelope_snapshot.jsonl`, and re-runs the test to confirm it now passes.

Purpose: This is the byte-determinism gate. Phase 7 ships when this test passes alongside the noise-replay regression (Plan 07-05) and the three family goldens (Plan 07-01).

Output: One new integration test file; one new golden JSONL file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-CONTEXT.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md
@.planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md
@crates/miner-cli/tests/cli_streams.rs
@crates/miner-core/tests/byte_identical_rerun.rs
@crates/miner-core/tests/common/mod.rs
@crates/miner-core/tests/scan_summary_welford.rs
@crates/miner-core/src/types.rs

<interfaces>
<!-- Pattern D: 5-field mask_volatile_fields (PATTERNS Pattern D + common/mod.rs:72-95): -->
fn mask_volatile_fields(v: &mut serde_json::Value) {
    if let serde_json::Value::Object(map) = v {
        for key in ["run_id", "started_at_utc", "ended_at_utc", "produced_at_utc"] {
            if map.contains_key(key) {
                map.insert(key.to_string(), serde_json::Value::String(format!("<masked_{key}>")));
            }
        }
        if map.contains_key("wall_clock_ms") {
            map.insert("wall_clock_ms".to_string(), serde_json::Value::from(0i64));
        }
        // ... recurse into children ...
    }
}

<!-- Pattern from cli_streams.rs:454 (canonical hand-rolled byte-equal): -->
let (out1, _, status1) = run_emit_fixture_happy();
assert_eq!(status1.code(), Some(0), "run 1 must exit 0");
let (out2, _, status2) = run_emit_fixture_happy();
assert_eq!(status2.code(), Some(0), "run 2 must exit 0");
let masked1 = mask_emit_fixture_stdout(&out1);
let masked2 = mask_emit_fixture_stdout(&out2);
assert_eq!(masked1, masked2);

<!-- Pattern from scan_summary_welford.rs:167-181 (canonical include_str! golden + provenance gate): -->
const GOLDEN_JSON: &str = include_str!("goldens/envelope_snapshot.jsonl");
// (provenance gate may NOT apply to envelope snapshot since it's not Python-derived;
//  the schema_version + scan@version act as the analogous "provenance" pins for this golden)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author findings_envelope_snapshot.rs + generate envelope_snapshot.jsonl golden + verify byte-identical re-run</name>
  <files>crates/miner-core/tests/findings_envelope_snapshot.rs, crates/miner-core/tests/goldens/envelope_snapshot.jsonl</files>
  <read_first>
    - crates/miner-cli/tests/cli_streams.rs (full file — the canonical hand-rolled byte-equal pattern at lines 323-485; copy the `mask_volatile_fields` shape if `common::mask_volatile_fields` is not accessible from a miner-core integration test)
    - crates/miner-core/tests/common/mod.rs (lines 72-95 — the 5-field mask helper this plan reuses via `mod common;`)
    - crates/miner-core/tests/byte_identical_rerun.rs (full file — sister byte-identical test for any single scan; mirror its shape for envelope-level testing)
    - crates/miner-core/tests/scan_summary_welford.rs (lines 140-220 — Pattern J Step 1 provenance gate + include_str! golden body; the envelope-snapshot test uses the same include_str! mechanism)
    - crates/miner-core/src/types.rs (the Finding enum variants — FOUND-03 schema; the test should produce at least one of each variant the emit-fixture path emits)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-PATTERNS.md §"crates/miner-core/tests/findings_envelope_snapshot.rs" + §"crates/miner-core/tests/goldens/envelope_snapshot.jsonl" + §"Pattern D" + §"Pattern E"
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-RESEARCH.md §"Pitfall 8" (insta-vs-hand-rolled rationale) + §"Pattern 2" (volatile-field masking)
    - .planning/phases/07-hardening-benchmarks-reproducibility/07-CONTEXT.md §D7-06 + §<success_criteria> bullet 1
  </read_first>
  <behavior>
    - Test 1 (`envelope_snapshot_matches_golden`): Run a deterministic emit-fixture-like scan invocation; capture stdout; parse each JSONL line; mask volatile fields via `common::mask_volatile_fields` (5-field variant); compare the resulting masked bytes against `include_str!("goldens/envelope_snapshot.jsonl")`. Byte-equal assertion.
    - Test 2 (`envelope_snapshot_byte_identical_across_runs`): Run the same emit-fixture invocation TWICE in the same test process; mask both outputs; assert mask(run1) == mask(run2). This proves OUT-03 (deterministic ordering for golden-file diffing) end-to-end across the envelope's serialised form.
    - Test 3 (`envelope_snapshot_covers_all_emitted_variants`): The golden must contain at least one envelope of each variant the chosen invocation emits. Read the golden in-test, parse each line, collect the `"kind"` (or whatever the discriminator field is — read types.rs) values into a HashSet, and assert the set contains the expected variants. Expected variants depend on the invocation chosen — at minimum: `RunStart`, `Result`, `RunEnd`. If the invocation also emits `DryRun` or `ScanError`, include those.
  </behavior>
  <action>
    Step 1 — author `crates/miner-core/tests/findings_envelope_snapshot.rs`.

    Required structure:
    - SPDX header lines 1-2 per PATTERNS Pattern B.
    - Module-doc preamble: "Locked findings-envelope snapshot test per D7-06. Hand-rolled byte-equal (not insta — see RESEARCH §Pitfall 8) so CI runs without insta-review ceremony. Compares masked stdout against the checked-in golden at tests/goldens/envelope_snapshot.jsonl."
    - `mod common;` at the top — reuses `common::mask_volatile_fields` (5-field variant).
    - `use` block including `miner_core::sweep::*` or whatever entry point produces envelopes; `serde_json::Value`; `std::collections::HashSet`.
    - Const `GOLDEN_JSONL: &str = include_str!("goldens/envelope_snapshot.jsonl");`
    - Helper function `fn run_envelope_invocation_capture_stdout() -> Vec<u8>` — chooses the canonical envelope-emitting invocation. Options:
      a) Spawn `miner emit-fixture` via `assert_cmd::Command` — uses the existing canonical fixture path. SIMPLEST option.
      b) Construct an in-memory single-job sweep + `BufferSink` + `run_sweep` invocation. More work but no subprocess spawn.
      Recommendation: option (a) — `assert_cmd::Command::cargo_bin("miner-cli")` + `args(["emit-fixture"])`. Mirror `crates/miner-cli/tests/cli_streams.rs:444-465` (`run_emit_fixture_happy` helper).
    - Helper function `fn mask_envelope_jsonl(raw: &[u8]) -> Vec<String>` — splits on newlines, parses each line as `serde_json::Value`, calls `common::mask_volatile_fields(&mut v)`, serialises back with `serde_json::to_string(&v)?`, collects into `Vec<String>` (preserves order; OUT-03 guarantees deterministic ordering).
    - The three tests from `<behavior>` above.

    Step 2 — initial test run to generate the golden.

    The golden file doesn't exist yet. First, write the test file with a placeholder reference to `goldens/envelope_snapshot.jsonl`. Create an EMPTY `crates/miner-core/tests/goldens/envelope_snapshot.jsonl` so `include_str!` doesn't fail at compile time. Run `cargo test -p miner-core --test findings_envelope_snapshot envelope_snapshot_matches_golden` — it will FAIL with a byte-equality mismatch (golden is empty; emit-fixture produces N envelopes).

    Step 3 — capture the masked stdout and write it as the golden.

    Run the same emit-fixture invocation OUTSIDE the test (e.g., `cargo run -p miner-cli -- emit-fixture > /tmp/raw.jsonl`); pipe through a small one-shot Rust helper or a manual `jq`-based masking pipeline that mirrors the in-test mask_volatile_fields logic. The CLEANEST way: add a small helper binary or use a `cargo test -- --include-ignored` mode with a `#[test] #[ignore] fn regenerate_envelope_snapshot_golden()` function that writes the masked bytes to `crates/miner-core/tests/goldens/envelope_snapshot.jsonl` when run explicitly.

    Recommended: add `#[test] #[ignore = "regen-only: writes the golden; run via `cargo test -p miner-core --test findings_envelope_snapshot -- --ignored regenerate`"] fn regenerate_envelope_snapshot_golden()` to `findings_envelope_snapshot.rs`. Its body:
    ```rust
    fn regenerate_envelope_snapshot_golden() {
        let raw = run_envelope_invocation_capture_stdout();
        let masked_lines = mask_envelope_jsonl(&raw);
        let golden_path = std::path::Path::new(env!("CARGO_MANIFEST_DIR"))
            .join("tests/goldens/envelope_snapshot.jsonl");
        std::fs::write(&golden_path, masked_lines.join("\n") + "\n")
            .expect("write golden");
        eprintln!("[regenerate] wrote {}", golden_path.display());
    }
    ```

    Run `cargo test -p miner-core --test findings_envelope_snapshot -- --ignored regenerate_envelope_snapshot_golden` to populate the golden. Then run `cargo test -p miner-core --test findings_envelope_snapshot envelope_snapshot_matches_golden` and confirm it passes.

    Step 4 — verify the byte-identical-across-runs test (`envelope_snapshot_byte_identical_across_runs`) passes. This is the OUT-03 contract: re-running the same invocation produces the same envelope bytes (after masking volatile fields).

    Step 5 — verify the variant-coverage test (`envelope_snapshot_covers_all_emitted_variants`) passes.

    HARD RULES:
    - No `println!` or `eprintln!` outside `#[ignore]`d regen helper (which is allowed since regen mode is operator-triggered).
    - The non-`#[ignore]`d tests MUST be active (no `#[ignore]` attribute) so `cargo test --workspace` exercises them per ROADMAP success criterion #1.
    - The golden file MUST end with exactly one `\n` (no trailing empty line; matches the existing JSONL goldens' convention — check `crates/miner-core/tests/goldens/stats.summary.welford.jsonl` for the trailing-newline convention).
    - Mask the 5-field variant: `run_id`, `started_at_utc`, `ended_at_utc`, `produced_at_utc`, `wall_clock_ms`.
  </action>
  <acceptance_criteria>
    - `crates/miner-core/tests/findings_envelope_snapshot.rs` exists.
    - SPDX header: `head -3 crates/miner-core/tests/findings_envelope_snapshot.rs | grep -c SPDX-License-Identifier` returns 1.
    - File uses `include_str!`: `grep -c 'include_str!.*envelope_snapshot\.jsonl' crates/miner-core/tests/findings_envelope_snapshot.rs` returns 1.
    - File uses `mod common;`: `grep -c '^mod common' crates/miner-core/tests/findings_envelope_snapshot.rs` returns 1.
    - File has at least three `#[test]` functions: `grep -c '#\[test\]' crates/miner-core/tests/findings_envelope_snapshot.rs` returns at least 3 (the three behaviour tests + optional regen helper).
    - The regen helper is `#[ignore]`d (operator-triggered only): `grep -c '#\[ignore' crates/miner-core/tests/findings_envelope_snapshot.rs` returns at least 1.
    - The byte-equal test is NOT `#[ignore]`d: the test count under `cargo test -p miner-core --test findings_envelope_snapshot` (NOT --ignored) reports at least 2 tests passing.
    - Golden file exists: `test -f crates/miner-core/tests/goldens/envelope_snapshot.jsonl`.
    - Golden is non-empty: `[ $(wc -c < crates/miner-core/tests/goldens/envelope_snapshot.jsonl) -gt 100 ]`.
    - Golden contains masked sentinel strings (volatile fields replaced): `grep -c '<masked_run_id>\|<masked_started_at_utc>\|<masked_produced_at_utc>' crates/miner-core/tests/goldens/envelope_snapshot.jsonl` returns at least 1.
    - Golden last line is properly newline-terminated: `tail -c 1 crates/miner-core/tests/goldens/envelope_snapshot.jsonl | od -c | head -1 | grep -q '\\n'`.
    - Each line of the golden is valid JSON: `awk 'NR{print > "/tmp/line-"NR".json"}' crates/miner-core/tests/goldens/envelope_snapshot.jsonl; for f in /tmp/line-*.json; do python3 -c "import json; json.load(open(\"$f\"))" || { echo "invalid JSON in $f"; exit 1; }; done`
    - `cargo test -p miner-core --test findings_envelope_snapshot` exits 0 (excluding --ignored).
    - `cargo test -p miner-core --test findings_envelope_snapshot envelope_snapshot_byte_identical_across_runs` exits 0 (the OUT-03 contract).
    - `cargo clippy -p miner-core --test findings_envelope_snapshot -- -D warnings` exits 0.
    - No `println!` outside the #[ignore]d regen helper: `awk '/#\[ignore/{ignored=1; next} /^fn /{ignored=0} /println!/{ if (!ignored) {print "println! outside ignore"; exit 1} }' crates/miner-core/tests/findings_envelope_snapshot.rs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; test -f crates/miner-core/tests/findings_envelope_snapshot.rs; head -3 crates/miner-core/tests/findings_envelope_snapshot.rs | grep -q SPDX-License-Identifier; grep -q "include_str!.*envelope_snapshot\.jsonl" crates/miner-core/tests/findings_envelope_snapshot.rs; grep -q "^mod common" crates/miner-core/tests/findings_envelope_snapshot.rs; [ "$(grep -c "#\[test\]" crates/miner-core/tests/findings_envelope_snapshot.rs)" -ge "3" ]; grep -q "#\[ignore" crates/miner-core/tests/findings_envelope_snapshot.rs; test -f crates/miner-core/tests/goldens/envelope_snapshot.jsonl; [ $(wc -c < crates/miner-core/tests/goldens/envelope_snapshot.jsonl) -gt 100 ]; grep -qE "<masked_(run_id|started_at_utc|produced_at_utc)>" crates/miner-core/tests/goldens/envelope_snapshot.jsonl; while IFS= read -r line; do echo "$line" | python3 -c "import json,sys; json.loads(sys.stdin.read())" || { echo "invalid JSON line in golden"; exit 1; }; done < crates/miner-core/tests/goldens/envelope_snapshot.jsonl; cargo test -p miner-core --test findings_envelope_snapshot && cargo clippy -p miner-core --test findings_envelope_snapshot -- -D warnings'</automated>
  </verify>
  <done>findings_envelope_snapshot.rs exists with 3+ active tests + 1 #[ignore]d regen helper; envelope_snapshot.jsonl golden is populated with masked envelope bytes; cargo test --workspace exercises the snapshot test; byte-identical-across-runs and variant-coverage tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Finding envelope schema → consumer parsers | This golden test is the contractual pin between miner's serialised envelope and downstream consumers (quant agent, future tradedesk). Drift here breaks every downstream parser silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Tampering | Schema drift bypasses the snapshot gate by accident | mitigate | Test runs in `cargo test --workspace` (NOT `#[ignore]`d for the byte-equal assertion); any schema change FAILS CI until either (a) the golden is regenerated via the `#[ignore]`d regen helper AND committed as a documented schema-evolution PR, or (b) the underlying schema-evolution log entry in docs/findings_envelope.md is added per D7-06 manual contract. |
| T-07-09-02 | Information Disclosure | Volatile fields (run_id, timestamps) leak into the golden | mitigate | 5-field mask via `common::mask_volatile_fields` replaces them with literal `<masked_*>` sentinels before byte comparison; the golden contains the sentinels, not real values. |
</threat_model>

<verification>
- `cargo test --workspace --no-fail-fast` passes (the snapshot test is now part of the standard suite).
- `cargo test -p miner-core --test findings_envelope_snapshot envelope_snapshot_matches_golden` passes.
- `cargo test -p miner-core --test findings_envelope_snapshot envelope_snapshot_byte_identical_across_runs` passes.
- `cargo test -p miner-core --test findings_envelope_snapshot envelope_snapshot_covers_all_emitted_variants` passes.
- Plan 07-01's three family goldens still pass: `cargo test -p miner-core --test scan_summary_welford --test scan_engle_granger --test scan_seas_hour_of_day` exits 0.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0.
</verification>

<success_criteria>
1. The envelope-snapshot test lands as a hand-rolled byte-equal (NOT insta) gate.
2. The golden lives alongside the three family goldens in `crates/miner-core/tests/goldens/envelope_snapshot.jsonl`.
3. Byte-identical re-run contract (OUT-03) is asserted in-test.
4. Variant-coverage assertion ensures all emit-fixture variants appear in the golden.
5. Together with Plan 07-01 (real family goldens), ROADMAP success criterion #1 is closed:
   "User can run the full golden-file regression suite (one representative scan per family,
    plus the locked findings-envelope snapshot test) and observe byte-identical JSONL across runs."
6. FOUND-02 (stdout discipline) + FOUND-03 (locked envelope schema) + OUT-03 (deterministic ordering) all exercised by this single integration test.
</success_criteria>

<output>
Create `.planning/phases/07-hardening-benchmarks-reproducibility/07-09-SUMMARY.md` when done. This is the final Phase 7 plan SUMMARY — include a section noting Phase 7 is complete and ready for `/gsd-complete-milestone` → v1.0 release ceremony.
</output>
