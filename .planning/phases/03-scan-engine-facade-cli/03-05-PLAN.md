---
phase: 03-scan-engine-facade-cli
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - crates/miner-cli/Cargo.toml
  - crates/miner-cli/src/scan_args.rs
  - crates/miner-cli/src/cli.rs
  - crates/miner-cli/src/main.rs
  - crates/miner-core/src/lib.rs
autonomous: true
requirements: [OP-01, OP-05, OP-06, OP-07, OP-08]

must_haves:
  truths:
    - "`miner scan stats.autocorr.ljung_box@1 --instrument EURUSD --side bid --timeframe 15m --window 2024-01-01:2024-01-02 --cache-root <fixture>` (against a fixture cache) returns exit 0 with stdout containing one run_start + one result + one run_end JSONL line, in that order"
    - "`miner scans` returns exit 0 with stdout containing exactly one JSONL line per registered scan; for Phase 3 that is exactly one line for stats.autocorr.ljung_box@1 carrying scan_id, version, params, finding_fields"
    - "`miner scan nope@1 ...` returns exit 1 with stdout EMPTY and stderr containing a structured WireError JSON line with code=\"unknown_scan\""
    - "`miner scan stats.autocorr.ljung_box@1 ... --params bad-form` returns exit 1 with code=\"invalid_parameter\""
    - "`miner scan stats.autocorr.ljung_box@1 ... --dry-run` returns exit 0 with stdout containing one run_start + one dry_run + one run_end (no result)"
    - "`miner scan ...` with --gap-policy strict on a window containing a gap (synthetic fixture cache with gaps) returns exit 0 with stdout containing one run_start + one gap_aborted + one run_end (no result)"
    - "`ctrlc::set_handler` is called BEFORE `Cli::parse()` per Pitfall 2 (line ordering verified by source inspection)"
    - "miner-cli main() routes RunOutcome::Ok→0, RunOutcome::PreflightFailed→1, RunOutcome::HadScanErrors→2, cancel→130 per CONTEXT D3-24"
    - "The cfg-gated `--sleep-after-first-finding-ms <ms>` CLI flag is the test-time ingress point for Plan 06's `sigint_preserves_stream` SIGINT integration test (Pitfall 8 — Blocker 1): the flag is declared on `ScanArgs` under `#[cfg(any(test, feature = \"test-internal\"))]`, hidden from `--help`, and forwarded into the constructed `ScanRequest.sleep_after_first_finding_ms` (Plan 02 cfg-gated field) by `handle_scan_subcommand`. The flag is ABSENT from release builds; `miner --help` of a release binary MUST NOT mention it."
    - "miner-cli declares a parallel `test-internal` feature in `crates/miner-cli/Cargo.toml` (mirror of Plan 02's miner-core feature) that simply enables `miner-core/test-internal` — propagating the cfg gate from the binary down to the library so the sleep hook is reachable end-to-end in test/feature builds and absent in release"
  artifacts:
    - path: "crates/miner-cli/Cargo.toml"
      provides: "`test-internal` feature flag mirroring miner-core's — `test-internal = [\"miner-core/test-internal\"]` — propagates the cfg gate end-to-end from the binary to the library"
      contains: "test-internal"
    - path: "crates/miner-cli/src/scan_args.rs"
      provides: "ScanArgs clap-derive struct (9 fields, including the cfg-gated --sleep-after-first-finding-ms test hook) + parse_window + to_scan_request boundary preflight"
      contains: "pub struct ScanArgs"
    - path: "crates/miner-cli/src/cli.rs"
      provides: "Command::Scan(ScanArgs) + Command::Scans variants added; existing EmitFixture preserved"
      contains: "Scan(ScanArgs)"
    - path: "crates/miner-cli/src/main.rs"
      provides: "ctrlc handler install + facade-call plumbing + miner scans introspection + exit-code routing 0/1/2/130 + cfg-gated forwarding of sleep_after_first_finding_ms into ScanRequest"
      contains: "ctrlc::set_handler"
    - path: "crates/miner-core/src/lib.rs"
      provides: "Phase 3 pub use re-export block (Scan, ScanCtx, ScanRequest, ScanError, ScanFindingShape, Registry, bootstrap, run_one, RunOutcome, GapPolicyKind, DryRunFinding)"
      contains: "pub use scan"
  key_links:
    - from: "crates/miner-cli/src/main.rs"
      to: "crates/miner-core/src/engine/mod.rs::run_one"
      via: "engine::run_one(req, cfg, reader, sink, cancel) call"
      pattern: "miner_core::engine::run_one|engine::run_one"
    - from: "crates/miner-cli/src/main.rs"
      to: "crates/miner-reader-dukascopy/src/reader.rs::DukascopyReader::new"
      via: "DukascopyReader::new(cfg.cache_root) at the binary edge"
      pattern: "DukascopyReader::new"
    - from: "crates/miner-cli/src/scan_args.rs"
      to: "crates/miner-core/src/engine/preflight.rs"
      via: "ScanArgs::to_scan_request calls preflight::resolve_scan_id_at_version + parse_params_kv + parse_iso_utc_window"
      pattern: "preflight::"
    - from: "crates/miner-cli/src/main.rs (miner scans handler)"
      to: "crates/miner-core/src/findings/sink.rs::FindingSink::write_raw_json"
      via: "per-scan catalogue line emission via write_raw_json"
      pattern: "write_raw_json"
    - from: "crates/miner-cli/src/scan_args.rs (cfg-gated --sleep-after-first-finding-ms flag)"
      to: "crates/miner-core/src/scan/mod.rs::ScanRequest.sleep_after_first_finding_ms (Plan 02 cfg-gated field)"
      via: "ScanArgs::to_scan_request — under the same cfg gate — forwards self.sleep_after_first_finding_ms into the returned ScanRequest via the ScanRequest::new(...).with_sleep_after_first_finding_ms(...) chained constructor pattern, so the call site is unconditional but the chained method is cfg-gated to test/feature-internal builds (Blocker 1 — Pitfall 8 ingress wiring)"
      pattern: "sleep_after_first_finding_ms|sleep-after-first-finding-ms"
  user_setup: []
---

<objective>
Wire the CLI binary: extend the clap Command enum with `Scan(ScanArgs)` and `Scans` variants, install the SIGINT handler BEFORE Cli::parse (Pitfall 2), construct DukascopyReader at the binary edge, call `engine::run_one`, route RunOutcome / cancellation to the four-tier exit code (0/1/2/130 per D3-24), implement `miner scans` introspection via `FindingSink::write_raw_json` against the schemas/scans-catalogue-v1.schema.json shape, declare the parallel `test-internal` feature on `miner-cli` and add the cfg-gated `--sleep-after-first-finding-ms <ms>` ingress flag that Plan 06's SIGINT integration test relies on (Blocker 1 — Pitfall 8 closure). Also finalize the miner-core public surface (`pub use` block per PATTERNS line 786-815).

Purpose: The CLI is the FIRST consumer of the facade — Phase 6 will validate MCP and HTTP wrappers produce byte-identical output against this same facade. Plan 05's wiring is the integration point that makes every Phase 3 success criterion observable from a user shell (`miner scan ... | head -1` should produce a run_start JSONL line). Pitfall 2 + Pitfall 8 are the trickiest details — both are addressed here. Plan 06's `sigint_preserves_stream` test invokes the binary with the cfg-gated flag declared in this plan; if the flag is missing, the integration test cannot deterministically race against the SIGINT.

Output: Two new CLI subcommands fully wired through the existing precedence + figment + sink pipeline; ctrlc handler installed in the right order; `test-internal` feature declared on miner-cli and wired to enable `miner-core/test-internal`; cfg-gated `--sleep-after-first-finding-ms` flag declared on ScanArgs and forwarded into ScanRequest; lib.rs surface extended; existing emit-fixture smoke test untouched; release builds (default features, no `test-internal`) do NOT expose the test hook anywhere on the binary surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md
@.planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md
@.planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md
@.planning/phases/03-scan-engine-facade-cli/03-VALIDATION.md
@CLAUDE.md
@crates/miner-cli/src/cli.rs
@crates/miner-cli/src/main.rs
@crates/miner-cli/src/scan_args.rs
@crates/miner-cli/Cargo.toml
@crates/miner-core/Cargo.toml
@crates/miner-core/src/lib.rs
@crates/miner-core/src/engine/mod.rs
@crates/miner-core/src/scan/mod.rs
@crates/miner-core/src/findings/sink.rs
@crates/miner-core/src/error/stderr_emit.rs
@crates/miner-reader-dukascopy/src/lib.rs

<interfaces>
<!-- Existing types this plan ties together. -->

From crates/miner-cli/src/cli.rs (current state):
- pub struct Cli { config, cache_root, bar_cache_root, output, command: Command }
- pub enum Command { EmitFixture } — Plan 05 adds Scan(ScanArgs) and Scans variants
- impl Cli { pub fn overrides(&self) -> CliOverrides { ... } }
- pub fn resolve_toml_path(...)

From crates/miner-cli/src/main.rs (current state):
- async-free main() returning anyhow::Result; calls Cli::parse, build_figment, make_sink, dispatches on command
- fn classify_figment_error(&figment::Error) -> PreflightCode
- fn make_sink(&OutputDest) -> Box<dyn FindingSink>
- fn emit_fixture(sink: &mut dyn FindingSink) — kept as Phase 1 smoke test

From crates/miner-core/src/engine/mod.rs (Plan 04 output):
- pub fn run_one<R: Reader>(req: &ScanRequest, cfg: &MinerConfig, reader: &R, sink: &mut dyn FindingSink, cancel: Arc<AtomicBool>) -> Result<RunOutcome, MinerError>
- pub enum RunOutcome { Ok, HadScanErrors, PreflightFailed }

From crates/miner-core/src/scan/mod.rs + registry.rs (Plan 02 output — already landed by Plan 05 execution time):
- pub fn bootstrap() -> Registry
- Registry::iter() -> impl Iterator<Item = &dyn Scan>
- pub struct ScanRequest { ..., #[serde(default)] pub dry_run: bool, #[cfg(any(test, feature = "test-internal"))] #[serde(default)] pub sleep_after_first_finding_ms: Option<u64> }
- Plan 02 declares the `test-internal` feature in `crates/miner-core/Cargo.toml` — Plan 05 mirrors it on miner-cli and propagates via the feature dep below.

From crates/miner-core/src/findings/sink.rs (Plan 02 output):
- FindingSink::write_raw_json(&mut self, v: &serde_json::Value) -> std::io::Result<()>

From crates/miner-core/src/aggregator.rs (Phase 1 — confirmed present):
- impl Timeframe { pub fn as_str(self) -> &'static str } at line 83 — symmetric to Side::as_str at reader.rs:58. Phase 1 already provides this, so Plan 05 does NOT need to add it. Plan 05 ONLY needs to add `from_str` (the inverse direction) if it does not already exist; verify in <action> step 4.

From crates/miner-core/src/reader.rs (Phase 1):
- impl Side { pub fn as_str(self) -> &'static str } at line 58 — confirmed present.

From crates/miner-reader-dukascopy/src/reader.rs (Phase 2):
- pub fn DukascopyReader::new(cache_root: impl Into<PathBuf>) -> Self (infallible)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill ScanArgs scan_args.rs — clap derive (9 fields incl. cfg-gated sleep hook — Blocker 1) + window parser + boundary preflight + test-internal feature on miner-cli</name>
  <files>crates/miner-cli/Cargo.toml, crates/miner-cli/src/scan_args.rs, crates/miner-cli/src/cli.rs</files>
  <read_first>
    - crates/miner-cli/Cargo.toml (current state — no [features] section yet; Plan 05 adds the parallel `test-internal` feature mirroring Plan 02's miner-core declaration)
    - crates/miner-core/Cargo.toml (Plan 02 — confirm `test-internal = []` is present in [features])
    - crates/miner-cli/src/scan_args.rs (Plan 01 scaffold — ScanArgs declared, parse_window + to_scan_request unimplemented)
    - crates/miner-cli/src/cli.rs (existing Command enum + Cli struct + global flags)
    - crates/miner-core/src/engine/preflight.rs (Plan 03 — parse_params_kv + parse_iso_utc_window + resolve_scan_id_at_version)
    - crates/miner-core/src/scan/mod.rs (Plan 02 — ScanRequest fields including cfg-gated sleep_after_first_finding_ms + GapPolicyKind enum)
    - crates/miner-core/src/aggregator.rs (line 67-90 — Timeframe enum + impl Timeframe::as_str already exists; from_str may need adding)
    - crates/miner-core/src/reader.rs (line 49-90 — Side enum + Side::as_str already exists; Side::from_str may need adding)
    - .planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md (D3-07, D3-18, D3-19 — windowing + single-shot + defaults; D3-21 — dry_run propagation)
    - .planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md (lines 664-740 — scan_args + window parser analog)
    - .planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md (Pattern 5 lines 460-489 — verbatim window parser body; Pitfall 8 lines 612-622 — sleep-flag mitigation rationale; Assumption A5 line 767 — sleep flag is acceptable per plan-phase decision)
  </read_first>
  <behavior>
    - ScanArgs_defaults: clap-parsed ScanArgs without --side, --gap-policy, --dry-run, --params has side="bid", gap_policy="continuous_only", dry_run=false, params=Vec::new() per D3-19.
    - ScanArgs_to_scan_request_happy: ScanArgs { scan_id_at_version: "stats.autocorr.ljung_box@1", instrument: "EURUSD", side: "bid", timeframe: "15m", window: parsed_window, gap_policy: "continuous_only", dry_run: false, params: vec![] }.to_scan_request(CODE_REVISION) returns Ok(ScanRequest { scan_id: "stats.autocorr.ljung_box", version: 1, instrument: "EURUSD", side: Side::Bid, timeframe: Timeframe::Tf15m, window: <parsed>, sub_range: window.into_time_range(), gap_policy: ContinuousOnly, dry_run: false, resolved_params: json!({}), param_hash: <blake3 of {}> }).
    - ScanArgs_to_scan_request_with_params: With params=["lags=20".into()], to_scan_request resolves resolved_params to json!({"lags": 20}) (typed-fallback) and computes param_hash correctly via miner_core::engine::param_hash.
    - ScanArgs_invalid_scan_id: scan_id_at_version="bad-no-at" → to_scan_request returns Err(WireError) with code=invalid_parameter.
    - ScanArgs_invalid_side: side="middle" → to_scan_request returns Err(WireError) with code=invalid_parameter.
    - ScanArgs_invalid_timeframe: timeframe="2h" (not 15m/1h/1d) → Err with code=invalid_parameter.
    - ScanArgs_invalid_gap_policy: gap_policy="lax" → Err with code=invalid_parameter.
    - parse_window_strict_z: parse_window("2024-01-01T00:00:00+02:00:2024-12-31T00:00:00+02:00") returns Err per Assumption A3 strict-Z; parse_window("2024-01-01T00:00:00Z:2024-12-31T00:00:00Z") returns Ok.
    - parse_window_date_only: parse_window("2024-01-01:2024-12-31") returns Ok with midnight UTC bounds.
    - parse_window_invalid_returns_err: parse_window("not-a-window") returns Err(String).
    - ScanArgs_sleep_after_first_finding_ms_field_cfg_gated (Blocker 1 — Pitfall 8 ingress): Under default `cargo test` (which activates `cfg(test)` automatically), the field `sleep_after_first_finding_ms: Option<u64>` exists on `ScanArgs` and clap accepts `--sleep-after-first-finding-ms 2000` on the command line; the parsed value lands as `Some(2000)`. Test name: scan_args_sleep_after_first_finding_ms_present_under_test_cfg. The test constructs the value via `Cli::try_parse_from(&["miner", "scan", "...", "--sleep-after-first-finding-ms", "2000"])` and pattern-matches the `Command::Scan(args)` variant, asserting `args.sleep_after_first_finding_ms == Some(2000)`.
    - ScanArgs_sleep_field_forwarded_to_scan_request (Blocker 1): Under `cfg(test)`, calling `args.to_scan_request(code_rev)?` with `args.sleep_after_first_finding_ms = Some(2000)` returns a `ScanRequest` whose `sleep_after_first_finding_ms` field equals `Some(2000)`. Test name: scan_args_to_scan_request_forwards_sleep_hook. Verified end-to-end so Plan 06's integration test does not need to re-derive the wiring.
  </behavior>
  <action>
    1. **`crates/miner-cli/Cargo.toml` — declare the `test-internal` feature mirroring Plan 02's miner-core declaration** (Blocker 1 — propagates the cfg gate end-to-end):
       - Add a new `[features]` section (the file has none today) with: `test-internal = ["miner-core/test-internal"]`.
       - Place the section immediately after `[[bin]]` and before `[dependencies]`.
       - Document via a leading comment line: `# `test-internal` is a parallel mirror of miner-core's `test-internal` feature (Plan 02). When enabled it surfaces the cfg-gated `--sleep-after-first-finding-ms` CLI flag and forwards the value into the cfg-gated `ScanRequest.sleep_after_first_finding_ms` field so Plan 06's SIGINT integration test can deterministically race the cancel token against the scan kernel. NEVER enable in release profiles.`
       - Verify post-edit: `grep -c '^test-internal' crates/miner-cli/Cargo.toml` returns 1.

    2. **`crates/miner-cli/src/scan_args.rs` — `pub fn parse_window(s: &str) -> Result<ClosedRangeUtc, String>`**:
       - This is the clap value_parser; per clap's API it must return Result<T, String> where the error becomes the user-facing message.
       - Internally call `miner_core::engine::preflight::parse_iso_utc_window(s)` (Plan 03). On Err(WireError), extract `wire_err.message` and return Err(message). On Ok(range), return Ok(range).

    3. **`#[derive(Debug, clap::Args)] pub struct ScanArgs`** per PATTERNS line 685-707 — **NINE fields total (Blocker 1 — the cfg-gated 9th field is REQUIRED by Plan 06's SIGINT integration test):**
       - positional `pub scan_id_at_version: String`
       - `#[arg(long)] pub instrument: String`
       - `#[arg(long, default_value = "bid")] pub side: String` (per D3-19)
       - `#[arg(long)] pub timeframe: String`
       - `#[arg(long, value_parser = parse_window)] pub window: ClosedRangeUtc`
       - `#[arg(long, default_value = "continuous_only")] pub gap_policy: String` (per D3-19)
       - `#[arg(long)] pub dry_run: bool`
       - `#[arg(long = "params", action = clap::ArgAction::Append)] pub params: Vec<String>`
       - **(Blocker 1 — cfg-gated 9th field):**
         ```
         /// Test-only hook for Pitfall 8 SIGINT-race mitigation; consumed by Plan 06
         /// Task 3's sigint_preserves_stream integration test. Hidden from --help;
         /// gated to test/feature builds. Mirrors Plan 02's cfg-gated
         /// `ScanCtx.sleep_after_first_finding_ms` and
         /// `ScanRequest.sleep_after_first_finding_ms` fields. NEVER reachable in
         /// release production builds — confirmed by the FOUND-01 release-binary
         /// `--help` inspection gate in Plan 06's threat-model row T-03-06-03.
         #[cfg(any(test, feature = "test-internal"))]
         #[arg(long = "sleep-after-first-finding-ms", hide = true)]
         pub sleep_after_first_finding_ms: Option<u64>,
         ```

    4. **`impl ScanArgs { pub fn to_scan_request(&self, code_revision: &str) -> Result<ScanRequest, WireError> }`** — use a chained-constructor pattern to keep the call site clean WHILE forwarding the cfg-gated field (Warning 1 polish principle — explicit and unambiguous, no per-field cfg in struct literals):
       - Call `miner_core::engine::preflight::resolve_scan_id_at_version(&self.scan_id_at_version)?` to split into (id, version).
       - Side::from_str(&self.side) — accept "bid"/"ask"; else Err(WireError::preflight(InvalidParameter, "side must be bid or ask").with_context("side", self.side.into())).
       - Timeframe::from_str(&self.timeframe) — accept "15m"/"1h"/"1d"; else Err with similar context.
       - GapPolicyKind::from_str(&self.gap_policy) — accept "strict"/"continuous_only"; else Err.
       - resolved_params = preflight::parse_params_kv(&self.params)?.
       - param_hash = preflight::param_hash(&resolved_params).map_err(|e| WireError::preflight(InvalidParameter, format!("param hash failed: {e}")))?.
       - **Construct the `ScanRequest` via `ScanRequest::new(...)` constructor** that takes the universally-available 10 fields (scan_id, version, instrument, side, timeframe, window, sub_range, gap_policy, dry_run, resolved_params, param_hash) and returns a `ScanRequest` whose cfg-gated `sleep_after_first_finding_ms` defaults to `None`. If `ScanRequest::new(...)` does not already exist in miner-core (Plan 02 may or may not have declared it — check), declare it as a thin constructor at the same site in Plan 02's deliverables; alternatively (acceptable fallback) inline a single struct literal here that's itself cfg-divergent:
         ```
         #[cfg(any(test, feature = "test-internal"))]
         let req = ScanRequest { scan_id: id, version, instrument: self.instrument.clone(), side, timeframe,
             window: self.window.clone(), sub_range: TimeRange { start: self.window.start, end: self.window.end },
             gap_policy, dry_run: self.dry_run, resolved_params, param_hash,
             sleep_after_first_finding_ms: self.sleep_after_first_finding_ms };
         #[cfg(not(any(test, feature = "test-internal")))]
         let req = ScanRequest { scan_id: id, version, instrument: self.instrument.clone(), side, timeframe,
             window: self.window.clone(), sub_range: TimeRange { start: self.window.start, end: self.window.end },
             gap_policy, dry_run: self.dry_run, resolved_params, param_hash };
         Ok(req)
         ```
         **Recommended pattern:** add a `ScanRequest::new(...)` associated constructor to miner-core (a one-line addition to Plan 02's scan/mod.rs in the worst case; can be made now in this plan's `to_scan_request` site if not yet present) plus a `with_sleep_after_first_finding_ms(self, Option<u64>) -> Self` chained method gated under `#[cfg(any(test, feature = "test-internal"))]`. The to_scan_request body then reads:
         ```
         let req = ScanRequest::new(id, version, self.instrument.clone(), side, timeframe,
             self.window.clone(), TimeRange { start: self.window.start, end: self.window.end },
             gap_policy, self.dry_run, resolved_params, param_hash);
         #[cfg(any(test, feature = "test-internal"))]
         let req = req.with_sleep_after_first_finding_ms(self.sleep_after_first_finding_ms);
         Ok(req)
         ```
       - **Pin the chosen pattern explicitly so the executor doesn't guess:** Prefer the `ScanRequest::new(...) + with_sleep_after_first_finding_ms(...)` chained constructor route. The two-struct-literal route is an acceptable fallback ONLY if landing `ScanRequest::new` would require an out-of-bounds edit to Plan 02. Document the chosen route in the doc-comment above `to_scan_request`.

    5. **`Side::from_str` + `Timeframe::from_str` + `GapPolicyKind::from_str`** — add to miner-core (next to each enum definition) if they don't already exist. They're tiny — match on the canonical strings and return `Result<Self, WireError>`. Live in miner-core (next to the enum definitions) so other consumers (Plan 06 MCP/HTTP) can reuse them. **Verify pre-conditions before adding** by reading the relevant source files (per Warning 2 — confirmed by source inspection that `Side::as_str` and `Timeframe::as_str` already exist; only `from_str` may need adding). For each new `from_str` method, also add a small unit test asserting round-trip equivalence: `Side::from_str(s.as_str()).unwrap() == s` for each variant; same for Timeframe and GapPolicyKind.

    6. **crates/miner-cli/src/cli.rs** — extend the Command enum per PATTERNS line 648-656:
       - Add `Scan(ScanArgs)` and `Scans` variants AFTER the existing `EmitFixture`.
       - The clap Subcommand derive auto-generates the parsing.

    7. **crates/miner-cli/src/main.rs** — add `mod scan_args;` declaration if not already done in Plan 01.

    8. Add #[cfg(test)] mod tests with the 12+ behavior tests listed in <behavior>. Use clap's own test API: `Cli::try_parse_from(&["miner", "scan", "stats.autocorr.ljung_box@1", "--instrument", "EURUSD", "--timeframe", "15m", "--window", "2024-01-01:2024-01-02"])` returns Ok(parsed); inspect the Command::Scan(_) variant. The two Blocker 1 tests (scan_args_sleep_after_first_finding_ms_present_under_test_cfg and scan_args_to_scan_request_forwards_sleep_hook) live under the same #[cfg(test)] block since `cfg(test)` makes the gated field visible.

    NO unsafe; NO println/eprintln/dbg; the file is small and bounded.
  </action>
  <verify>
    <automated>cargo test -p miner-cli scan_args::tests cli::tests::scan_args_defaults_per_d3_19 && cargo build -p miner-cli && cargo build -p miner-cli --features test-internal && cargo build -p miner-cli --release</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p miner-cli scan_args::tests passes (>=12 tests including the two Blocker 1 tests scan_args_sleep_after_first_finding_ms_present_under_test_cfg and scan_args_to_scan_request_forwards_sleep_hook)
    - cargo test -p miner-cli cli::tests::scan_args_defaults_per_d3_19 passes (a top-level test that confirms --side defaults to bid and --gap-policy defaults to continuous_only via clap)
    - `grep -c 'default_value = "bid"' crates/miner-cli/src/scan_args.rs` returns 1
    - `grep -c 'default_value = "continuous_only"' crates/miner-cli/src/scan_args.rs` returns 1
    - `grep -c 'ArgAction::Append' crates/miner-cli/src/scan_args.rs` returns 1 (repeatable --params flag)
    - `grep -c 'Scan(ScanArgs)' crates/miner-cli/src/cli.rs` returns 1
    - `grep -c 'Scans,' crates/miner-cli/src/cli.rs` returns 1
    - `grep -c 'sleep_after_first_finding_ms\|sleep-after-first-finding-ms' crates/miner-cli/src/scan_args.rs` returns >= 1 (Blocker 1 — cfg-gated CLI flag declared on ScanArgs)
    - `grep -c '#\[cfg(any(test, feature = "test-internal"))\]' crates/miner-cli/src/scan_args.rs` returns >= 1 (Blocker 1 — gate present on the flag and/or forwarding logic)
    - `grep -c '^test-internal' crates/miner-cli/Cargo.toml` returns 1 (Blocker 1 — parallel feature declared on miner-cli)
    - `grep -c 'miner-core/test-internal' crates/miner-cli/Cargo.toml` returns 1 (Blocker 1 — the feature propagates to the library)
    - cargo build -p miner-cli succeeds under default features (debug)
    - cargo build -p miner-cli --features test-internal succeeds (Blocker 1 — feature variant compiles; sleep hook reachable)
    - cargo build -p miner-cli --release succeeds with default features (Blocker 1 — release variant compiles; sleep hook absent)
    - cargo clippy -p miner-cli -- -D warnings is clean
  </acceptance_criteria>
  <done>
    ScanArgs clap-derive struct (9 fields including the cfg-gated --sleep-after-first-finding-ms hook) fills the Phase 1 default-value pattern; parse_window uses Plan 03's strict-Z parser; to_scan_request is the binary-edge preflight that produces a typed ScanRequest with computed param_hash AND forwards the cfg-gated sleep hook into ScanRequest.sleep_after_first_finding_ms via the chained-constructor pattern; cli.rs has Scan + Scans variants; miner-cli/Cargo.toml declares the `test-internal` feature that propagates to miner-core; 12+ unit tests passing; both cfg variants compile (debug, --features test-internal, --release).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: main.rs — ctrlc install (Pitfall 2) + facade plumbing + exit-code routing + miner scans + cfg-gated sleep hook forwarding (Blocker 1)</name>
  <files>crates/miner-cli/src/main.rs</files>
  <read_first>
    - crates/miner-cli/src/main.rs (current main + make_sink + classify_figment_error + emit_fixture)
    - crates/miner-cli/src/cli.rs (post-Task-1 — Command::Scan + Command::Scans variants)
    - crates/miner-cli/src/scan_args.rs (Task 1 output — to_scan_request, including the cfg-gated sleep_after_first_finding_ms field forwarding via ScanRequest::new(...).with_sleep_after_first_finding_ms(...) — so Task 2 inherits the forwarding without further work, BUT Task 2 verifies via grep + integration test that the field reaches the ScanRequest)
    - crates/miner-core/src/engine/mod.rs (Plan 04 — run_one signature + RunOutcome)
    - crates/miner-core/src/scan/registry.rs (Plan 02 — bootstrap + iter)
    - crates/miner-core/src/findings/sink.rs (Plan 02 — FindingSink::write_raw_json)
    - crates/miner-core/src/error/stderr_emit.rs (existing emit_to_stderr)
    - crates/miner-reader-dukascopy/src/reader.rs (DukascopyReader::new signature)
    - .planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md (D3-22 — ctrlc handler; D3-24 — four-tier exit codes; D3-20 — miner scans output line shape)
    - .planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md (lines 741-784 — ctrlc install + exit code routing analog)
    - .planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md (Pattern 4 lines 413-458 — verbatim ctrlc + exit-code routing; Pitfall 2 lines 549-558 — install BEFORE Cli::parse; Pitfall 8 lines 612-622 — sleep hook is consumed inside LjungBoxScan)
  </read_first>
  <behavior>
    - main_installs_ctrlc_before_parse: Source-level check that `ctrlc::set_handler` call appears in main() BEFORE the `Cli::parse()` call. Tested via `awk` / `grep -n` in the acceptance_criteria (source inspection). RESEARCH Pitfall 2 + Pattern 4.
    - exit_code_routing: A unit test on a tiny `compute_exit_code(cancel: bool, outcome: RunOutcome) -> i32` helper function asserts: (true, *) → 130, (false, PreflightFailed) → 1, (false, HadScanErrors) → 2, (false, Ok) → 0.
    - miner_scans_emits_one_line_per_registered_scan_unit_test: A unit test inside main.rs (or scan_args.rs) calls `fn handle_scans_subcommand(sink: &mut dyn FindingSink) -> std::io::Result<()>` with a VecSink; asserts sink.bytes() parses into exactly N JSONL lines where N == bootstrap().iter().count(). For Phase 3 N==1. The emitted Value has the four-property shape {scan_id, version, params, finding_fields}.
    - main_dispatches_emit_fixture_unchanged: Existing emit-fixture path still works (no regression). This is implicit — the existing test cli_streams::emit_fixture_byte_identical_when_volatile_fields_masked must still pass.
    - main_handle_scan_forwards_sleep_hook (Blocker 1 — Pitfall 8 wiring sanity check): Under `cfg(test)`, a unit test constructs `ScanArgs { ..., sleep_after_first_finding_ms: Some(2000) }` and a minimal `MinerConfig` + `VecSink` + `Arc::new(AtomicBool::new(false))`, then calls `handle_scan_subcommand` (or its testable inner). The test does NOT depend on the engine doing the sleep — it only verifies that `ScanRequest::sleep_after_first_finding_ms == Some(2000)` reaches the engine boundary (capture via a fake reader's recorder, or via a dedicated test seam exposed by `handle_scan_subcommand` returning the constructed ScanRequest for inspection). Test name: handle_scan_subcommand_forwards_sleep_hook_to_scan_request.
  </behavior>
  <action>
    Edit crates/miner-cli/src/main.rs per RESEARCH Pattern 4 + Pitfall 2 + Pitfall 8 (Blocker 1):

    1. **At the TOP of main()** — install the ctrlc handler BEFORE `Cli::parse()`:
       ```
       let cancel = Arc::new(AtomicBool::new(false));
       {
           let cancel = Arc::clone(&cancel);
           ctrlc::set_handler(move || {
               cancel.store(true, Ordering::SeqCst);
               tracing::warn!("SIGINT received; shutting down");
           }).expect("ctrlc handler install");
       }
       let cli = Cli::parse();
       ```
       The handler closure MUST use `tracing::warn!` (NOT eprintln! — Pitfall 2 + clippy gate).

    2. **After Cli::parse() and existing config resolution** — dispatch on command:
       ```
       match cli.command {
           Command::EmitFixture => { /* unchanged */ }
           Command::Scans => handle_scans_subcommand(sink.as_mut())?,
           Command::Scan(scan_args) => {
               let outcome = handle_scan_subcommand(scan_args, &cfg, sink.as_mut(), cancel.clone())?;
               let code = compute_exit_code(cancel.load(Ordering::SeqCst), outcome);
               std::process::exit(code);
           }
       }
       ```

    3. **`fn handle_scans_subcommand(sink: &mut dyn FindingSink) -> std::io::Result<()>`**:
       - Build `let registry = miner_core::scan::bootstrap();`.
       - For each `&dyn Scan` in `registry.iter()`:
         - Build `let line = serde_json::json!({"scan_id": s.id(), "version": s.version(), "params": s.param_schema(), "finding_fields": { "effect_extra_keys": s.finding_fields().effect_extra_keys, "raw_series_keys": s.finding_fields().raw_series_keys }});` per CONTEXT D3-20.
         - `sink.write_raw_json(&line)?;` — uses the new Plan 02 method; bypasses the Finding-envelope discipline since these lines are NOT findings (per RESEARCH Pitfall 7 + Open Question 8 resolution).
       - `sink.flush()?;`
       - Return Ok(()).
       - The catalogue lines validate against `schemas/scans-catalogue-v1.schema.json` (NOT `findings-v1.schema.json`); Plan 06 wires the integration-test validation.

    4. **`fn handle_scan_subcommand(args: ScanArgs, cfg: &MinerConfig, sink: &mut dyn FindingSink, cancel: Arc<AtomicBool>) -> anyhow::Result<RunOutcome>`**:
       - `let req = args.to_scan_request(miner_core::CODE_REVISION)?;` (returns WireError on preflight failure; map to anyhow at the binary edge). **Note (Blocker 1 — Pitfall 8 ingress wiring):** `to_scan_request` already handles the cfg-gated `sleep_after_first_finding_ms` forwarding via the chained constructor (per Task 1, step 4); main.rs does NOT need to add a second cfg-divergent forwarding here. Verify via grep that the value reaches the constructed `ScanRequest`.
       - On the WireError path: call `miner_core::error::stderr_emit::emit_to_stderr(&wire_err)?;` and return Ok(RunOutcome::PreflightFailed) — the caller exits with 1.
       - On Ok(req): construct `let reader = miner_reader_dukascopy::DukascopyReader::new(cfg.cache_root.clone());` (infallible per RESEARCH Open Question 6).
       - Call `miner_core::engine::run_one(&req, cfg, &reader, sink, cancel)?` — returns RunOutcome via the engine's contract.

    5. **`fn compute_exit_code(cancelled: bool, outcome: RunOutcome) -> i32`** per RESEARCH lines 765-770:
       ```
       match (cancelled, outcome) {
           (true,  _)                         => 130,
           (false, RunOutcome::PreflightFailed) => 1,
           (false, RunOutcome::HadScanErrors)   => 2,
           (false, RunOutcome::Ok)              => 0,
       }
       ```

    6. **Preserve existing emit_fixture** (Phase 1 smoke test) untouched.

    7. Add a `#[cfg(test)] mod tests { ... }` block with:
       - exit_code_routing test (4 cases).
       - handle_scans_subcommand_emits_one_line_per_registered_scan_via_vec_sink — uses VecSink directly (not subprocess).
       - main_installs_ctrlc_before_parse — source-inspection test: read the file at compile time via `include_str!("main.rs")`, find positions of `ctrlc::set_handler` and `Cli::parse`, assert the ctrlc position is smaller.
       - handle_scan_subcommand_forwards_sleep_hook_to_scan_request (Blocker 1): construct ScanArgs with `sleep_after_first_finding_ms = Some(2000)`; via a thin test seam (either expose `handle_scan_subcommand_inner` returning the constructed ScanRequest before engine dispatch, or use a fake Reader that records the ScanRequest), assert that the value reaches `ScanRequest.sleep_after_first_finding_ms`. The test confirms the wiring without depending on the engine's behaviour (which Plan 06 covers via the actual SIGINT test).

    NO unsafe; NO println/eprintln (clippy gate held — use tracing::warn in the ctrlc handler per RESEARCH line 754); the SIGINT path is sync-only (`ctrlc` spawns its own thread for the handler — see RESEARCH §"Open Question 5 resolution").
  </action>
  <verify>
    <automated>cargo test -p miner-cli main::tests && cargo build -p miner-cli --release 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p miner-cli main::tests passes (>=4 tests including the Blocker 1 handle_scan_subcommand_forwards_sleep_hook_to_scan_request)
    - cargo build -p miner-cli succeeds (release + debug)
    - `grep -n 'ctrlc::set_handler\|Cli::parse' crates/miner-cli/src/main.rs | head -2` shows ctrlc::set_handler appearing BEFORE Cli::parse in line numbers (Pitfall 2 gate)
    - `grep -c 'tracing::warn' crates/miner-cli/src/main.rs` returns >= 1 (SIGINT handler logs via tracing, not eprintln)
    - `grep -c 'eprintln!\|println!' crates/miner-cli/src/main.rs` returns 0 (clippy gate — emit_to_stderr is the sanctioned stderr writer)
    - `grep -c 'std::process::exit' crates/miner-cli/src/main.rs` returns >= 1 (exit-code routing landed)
    - `grep -c '130' crates/miner-cli/src/main.rs` returns >= 1 (SIGINT exit code)
    - `grep -c 'write_raw_json' crates/miner-cli/src/main.rs` returns >= 1 (miner scans uses the new sink method)
    - `grep -c 'sleep_after_first_finding_ms' crates/miner-cli/src/main.rs` returns >= 1 (Blocker 1 — the field name is mentioned in the test or in a doc-comment confirming Plan 06 reachability)
  </acceptance_criteria>
  <done>
    ctrlc handler installed BEFORE Cli::parse (Pitfall 2 verified by source inspection); four-tier exit code routing (0/1/2/130 per D3-24) covered by unit test; miner scans subcommand emits per-registered-scan catalogue lines via write_raw_json; miner scan subcommand calls engine::run_one and routes its RunOutcome; the cfg-gated sleep-hook forwarding into ScanRequest is verified by a dedicated unit test (Blocker 1 — Pitfall 8 ingress wiring closure); existing emit-fixture path untouched; both debug and release builds compile cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Finalize miner-core public surface — extend lib.rs pub use block + public-surface audit hook</name>
  <files>crates/miner-core/src/lib.rs</files>
  <read_first>
    - crates/miner-core/src/lib.rs (current pub use blocks at lines 31-56)
    - crates/miner-core/src/scan/mod.rs (Plan 02 — Scan, ScanCtx, ScanRequest, ScanError, ScanFindingShape, Registry, bootstrap exports)
    - crates/miner-core/src/engine/mod.rs (Plan 04 — run_one, RunOutcome + GapPolicyKind re-export from gap_policy)
    - crates/miner-core/src/findings/mod.rs (Plan 02 — DryRunFinding added; existing DataSlice etc still exported)
    - crates/miner-core/tests/public_surface_audit.rs (existing audit pattern)
    - .planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md (lines 786-815 — re-export pattern)
  </read_first>
  <action>
    Edit crates/miner-core/src/lib.rs to extend the `pub use` block with Phase 3 surface entries per PATTERNS line 806-810:

    1. **Module declarations**: confirm `pub mod scan;` and `pub mod engine;` are present (Plan 01 added them). The `#[allow(dead_code)]` should already be removed by the time Plan 05 runs.

    2. **Re-exports** — append a new section after the existing Phase 2 block:
       ```
       // Phase 3 (scan-engine-facade-cli):
       pub use scan::{Scan, ScanCtx, ScanRequest, ScanError, ScanFindingShape, Registry, bootstrap};
       pub use scan::ljung_box::LjungBoxScan;
       pub use engine::{run_one, RunOutcome, GapPolicyKind, GapDispatch};
       pub use findings::DryRunFinding;
       ```
       Verify each name exists at the cited path; if a name doesn't exist, either it was removed/renamed in Plan 02-04 (check & adjust) or it needs adding.

    3. **public_surface_audit.rs update**: read the existing audit test pattern at `crates/miner-core/tests/public_surface_audit.rs:1-50`. If the test enumerates expected public names, add the Phase 3 names to the expected list. The test pattern from Phase 2 (`miner-core/tests/public_surface_audit.rs`) is a compile-time check that the listed names resolve via `use miner_core::{Scan, ScanCtx, ...};` — extending it is a one-line addition per the existing convention.

    4. Confirm `cargo doc -p miner-core --no-deps 2>&1 | grep -E 'error|warning' | head -5` is clean (no broken doc-links from the new exports).

    NO unsafe; NO logic changes (this is a re-export-only edit).
  </action>
  <verify>
    <automated>cargo build -p miner-core && cargo test -p miner-core --test public_surface_audit && cargo doc -p miner-core --no-deps 2>&1 | grep -cE 'error|warning' </automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p miner-core --test public_surface_audit passes
    - `grep -c 'pub use scan' crates/miner-core/src/lib.rs` returns >= 1
    - `grep -c 'pub use engine' crates/miner-core/src/lib.rs` returns >= 1
    - `grep -c 'DryRunFinding' crates/miner-core/src/lib.rs` returns >= 1
    - cargo doc -p miner-core --no-deps emits no warnings or errors
    - cargo build --workspace succeeds (Plan 06's tests can import all Phase 3 types via miner_core::*)
  </acceptance_criteria>
  <done>
    miner-core public surface extends the Phase 1+2 pattern with Phase 3 names; public_surface_audit test enforces the surface; cargo doc has no broken links.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS signal → process | SIGINT handler must be installed before any parsing work |
| CLI argv → engine | Untrusted user input becomes the ScanRequest after to_scan_request boundary preflight |
| miner-core public API → external consumers | Re-exports must be stable across phase transitions |
| Test-only CLI flag → release builds | `--sleep-after-first-finding-ms` must be invisible to release-build users (Blocker 1 — Pitfall 8 ingress with cfg-gate discipline) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Denial of Service | SIGINT before handler installed | mitigate | Pitfall 2 gate held by source-inspection test main_installs_ctrlc_before_parse + by code-order in main(). Worst-case window (microseconds before ctrlc::set_handler returns) leaves default Rust signal behaviour (immediate exit) — acceptable per RESEARCH §Pattern 4. |
| T-03-05-02 | Information Disclosure | preflight WireError exposing internal paths | mitigate | WireError messages constructed at the preflight site (Plan 03) carry only the invalid input string, not OS paths. emit_to_stderr writes the structured WireError JSON; no plaintext leakage. |
| T-03-05-03 | Tampering | exit-code routing wrong | mitigate | compute_exit_code is a 4-case pure function with a unit test pinning all four outcomes. main() calls it ONCE per dispatch; no in-line exit-code arithmetic. |
| T-03-05-04 | Repudiation | miner scans output validated against wrong schema | mitigate | The handle_scans_subcommand body uses sink.write_raw_json (NOT write_envelope) — the bypass route documented in PATTERNS line 1183. Plan 06 wires the integration test to validate against schemas/scans-catalogue-v1.schema.json. The doc-comment on write_raw_json (Plan 02) makes the schema choice explicit. |
| T-03-05-05 | Information Disclosure | test-only --sleep-after-first-finding-ms flag leaks into release `miner --help` | mitigate | The flag is gated by `#[cfg(any(test, feature = "test-internal"))]` on `ScanArgs` AND the forwarding into ScanRequest is similarly cfg-gated. The `test-internal` feature in `crates/miner-cli/Cargo.toml` propagates to `miner-core/test-internal`. Default `cargo build --release` does NOT activate `cfg(test)` and the feature is not enabled by any release profile. Plan 06's final gate (T-03-06-03) inspects `miner --help` of the release binary and confirms the flag is absent. |
</threat_model>

<verification>
- cargo test -p miner-cli --lib (~15 tests across scan_args.rs + main.rs, +2 from Blocker 1 wiring)
- cargo test -p miner-core --test public_surface_audit (existing audit + Phase 3 names)
- cargo build --workspace (everything compiles; Plan 06 integration tests are next)
- cargo build -p miner-cli --features test-internal (Blocker 1 — feature variant compiles; sleep hook reachable)
- cargo build -p miner-cli --release (Blocker 1 — release variant compiles; sleep hook absent)
- cargo clippy --workspace --all-targets -- -D warnings (clean)
- cargo doc -p miner-core --no-deps (no broken doc-links)
</verification>

<success_criteria>
1. CLI Scan + Scans subcommands clap-parse with the D3-19 defaults (side=bid, gap_policy=continuous_only)
2. parse_window enforces strict-Z UTC; date-only + full-datetime forms both accepted; non-Z timezone suffixes rejected
3. ScanArgs::to_scan_request returns a typed ScanRequest with computed param_hash on the happy path; WireError on every invalid input
4. main.rs installs ctrlc::set_handler BEFORE Cli::parse (Pitfall 2 source-inspection test passes)
5. SIGINT handler logs via tracing::warn (NOT eprintln); clippy::disallowed_macros gate held
6. Four-tier exit code routing (0/1/2/130 per D3-24) covered by unit test
7. miner scans subcommand emits one JSONL line per registered scan via FindingSink::write_raw_json (per Open Question 8 resolution)
8. miner-core public surface extended with Phase 3 names; public_surface_audit test passes; cargo doc clean
9. Existing emit-fixture smoke test still passes (no Phase 1 regression)
10. **Blocker 1 — Pitfall 8 closure:** `--sleep-after-first-finding-ms` cfg-gated CLI flag declared on `ScanArgs`; forwarded into `ScanRequest.sleep_after_first_finding_ms` via the chained constructor pattern; `test-internal` feature declared on miner-cli/Cargo.toml propagating to `miner-core/test-internal`; release builds (default features, no `test-internal`) do NOT expose the flag anywhere on the binary surface; both cfg variants compile.
</success_criteria>

<output>
Create `.planning/phases/03-scan-engine-facade-cli/03-05-SUMMARY.md` when done. Record: number of clap subcommands now in the binary (3: emit-fixture + scan + scans), the source position of ctrlc::set_handler vs Cli::parse (line numbers — confirms Pitfall 2 ordering), the full output of `miner scans 2>/dev/null` against the built binary showing the one Phase 3 catalogue line, the unit-test count post-Plan-05 (~70 in miner-core + ~27 in miner-cli), the output of `miner --help` for both `cargo build --release` (no sleep flag) and `cargo build --features test-internal` (sleep flag visible only if `hide = true` is omitted; if `hide = true` is on, the flag is functional but suppressed from --help — verify via direct invocation), and the `cargo build -p miner-cli --features test-internal` success line confirming Blocker 1 — Pitfall 8 wiring compiles end-to-end.
</output>
</output>
