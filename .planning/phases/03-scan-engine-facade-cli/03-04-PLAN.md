---
phase: 03-scan-engine-facade-cli
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - crates/miner-core/src/scan/ljung_box/kernel.rs
  - crates/miner-core/src/scan/ljung_box/mod.rs
  - crates/miner-core/src/engine/mod.rs
autonomous: true
requirements: [OP-01, OP-05, OP-06, OP-08, OUT-04]

must_haves:
  truths:
    - "log_returns([1.0, e, e*e]) == vec![1.0, 1.0] (two log-returns from three closes)"
    - "biased_acf on a constant series returns NaN-or-zero at lag>=1 (denom collapses to 0); biased_acf on a fixed AR(1) seed matches a hand-computed reference within 1e-12"
    - "ljung_box_q_and_p with a known input matches the statsmodels 0.14.6 reference Q-stats and p-values within 1e-12 (the Plan 06 golden test pins byte equality on the redacted JSONL; Plan 04 unit-tests the kernel against a hand-computed reference)"
    - "LjungBoxScan::run on a deterministic BarFrame writes exactly one Finding::Result to the sink with effect.metric=='ljung_box_q', effect.value at max-lag, effect.p_value at max-lag, effect.n == returns_length, effect.extra carrying lags/q_stats/p_values/acf RawArrays, raw.series carrying returns + timestamps_ms RawArrays"
    - "engine::run_one with a Strict policy + GapManifest containing gaps emits exactly one Finding::RunStart, one Finding::GapAborted carrying the full manifest, one Finding::RunEnd; exit-mappable RunOutcome::Ok; results_emitted == 0 in RunEnd.summary"
    - "engine::run_one with ContinuousOnly + zero gaps + LjungBoxScan emits exactly one RunStart, one Result (with data_slice.gap_manifest = Some(GapManifest{gaps:vec![]})), one RunEnd; results_emitted == 1"
    - "engine::run_one with --dry-run flag in the request emits exactly one RunStart, one Finding::DryRun, one RunEnd; results_emitted == 0; the DryRunFinding carries resolved_params, planned_data_slice, estimated_findings_count"
    - "engine::run_one with cancel.load() == true at entry returns RunOutcome::Ok with zero scan-body emissions (only RunStart/RunEnd framing); the cancel-poll happens before bar-loading per RESEARCH Pattern 4"
    - "Cancellation during scan body: setting cancel = true between the RunStart write and the Scan::run call causes Scan::run's first cancellation check to return Ok(()) early; only RunStart + RunEnd emitted"
  artifacts:
    - path: "crates/miner-core/src/scan/ljung_box/kernel.rs"
      provides: "log_returns + biased_acf + ljung_box_q_and_p pure kernels with unit tests against hand-computed references"
      contains: "fn ljung_box_q_and_p"
    - path: "crates/miner-core/src/scan/ljung_box/mod.rs"
      provides: "LjungBoxScan: Scan impl with full envelope construction per D3-04"
      contains: "impl Scan for LjungBoxScan"
    - path: "crates/miner-core/src/engine/mod.rs"
      provides: "run_one facade body — preflight, framing, dry-run short-circuit, gap-policy dispatch, scan dispatch, cancel polling, RunSummary build"
      contains: "pub fn run_one"
  key_links:
    - from: "crates/miner-core/src/engine/mod.rs::run_one"
      to: "crates/miner-core/src/engine/{preflight,framing,param_hash,gap_policy}::*"
      via: "calls helpers in sequence per RESEARCH §System Architecture Diagram lines 142-191"
      pattern: "preflight::|framing::|param_hash|gap_policy::dispatch"
    - from: "crates/miner-core/src/scan/ljung_box/mod.rs::LjungBoxScan::run"
      to: "crates/miner-core/src/scan/ljung_box/kernel.rs"
      via: "log_returns + biased_acf + ljung_box_q_and_p kernel calls"
      pattern: "kernel::"
    - from: "crates/miner-core/src/scan/ljung_box/mod.rs"
      to: "crates/miner-core/src/findings/mod.rs (Finding::Result + ResultFinding + Effect + Raw + RawArray)"
      via: "envelope construction inside Scan::run"
      pattern: "Finding::Result\\("
---

<objective>
Deliver the Ljung-Box scan kernel (matching statsmodels 0.14.6 acorr_ljungbox under adjusted=False, fft=False) and the full `engine::run_one` facade that composes Plan 03's sub-modules into the single library entry point CLI/MCP/HTTP all call. This is the heart of Phase 3 — the byte-identity contract Phase 6 will validate parity against.

Purpose: After Plan 03 lands the engine sub-modules and Plan 02 locks the envelope contract, Plan 04 is the "assembly" plan — Ljung-Box becomes a real Scan impl (D3-01..D3-05), and run_one wires preflight → framing → bar loading → gap-policy dispatch → scan dispatch → framing-close into one pure-sync function. Every Phase 4 scan will land by adding a `r.register(Box::new(NewScan));` line to bootstrap() and a new file under `crates/miner-core/src/scan/<family>/<name>/`; no engine changes needed.

Output: working LjungBoxScan emitting a fully-populated `Finding::Result`; working `run_one` with strict + continuous_only + dry-run + cancel paths; >=15 in-process integration-style unit tests against a VecSink across both new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md
@.planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md
@.planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md
@.planning/phases/03-scan-engine-facade-cli/03-VALIDATION.md
@CLAUDE.md
@crates/miner-core/src/aggregator.rs
@crates/miner-core/src/cache.rs
@crates/miner-core/src/gap.rs
@crates/miner-core/src/findings/mod.rs
@crates/miner-core/src/findings/sink.rs
@crates/miner-core/src/error/codes.rs
@crates/miner-core/src/config/mod.rs
@crates/miner-core/src/reader.rs

<interfaces>
<!-- Existing types this plan composes. -->

From Plan 02/03:
- pub trait Scan: Send + Sync { id, version, param_schema, finding_fields, run(&self, &ScanCtx, &ScanRequest, &mut dyn FindingSink) -> Result<(), ScanError> }
- pub struct ScanCtx<'a> { bars: &'a BarFrame, gap_manifest: Option<&'a GapManifest>, run_id: RunId, code_revision: &'a str, cancel: Arc<AtomicBool> }
- pub struct ScanRequest { scan_id, version, instrument, side: Side, timeframe: Timeframe, window: ClosedRangeUtc, sub_range: TimeRange, gap_policy: GapPolicyKind, resolved_params: serde_json::Value, param_hash: Blake3Hex }
- pub fn build_run_start, build_run_end (Plan 03)
- pub fn param_hash (Plan 03)
- pub fn resolve_scan, parse_params_kv, parse_iso_utc_window (Plan 03)
- pub fn dispatch(manifest, requested, policy) -> GapDispatch (Plan 03)
- pub enum RunOutcome { Ok, HadScanErrors, PreflightFailed } (Plan 01 scaffold; Plan 04 may extend with Aborted/Cancelled variants if needed)

From Phase 1/2 (unchanged):
- BarCache::get_or_build<R: Reader>(&self, reader: &R, params: AggParams) -> Result<BarFrame, CacheError>
- AggParams { symbol, side, tf, range, calendar } shape (read aggregator.rs)
- GapDetector::detect<R: Reader>(reader, symbol, side, range) -> Result<GapManifest, R::Error>
- BarFrame { ts_open_utc: Vec<DateTime<Utc>>, open: Vec<f64>, high: Vec<f64>, low: Vec<f64>, close: Vec<f64>, tick_volume: Vec<f64> }
- ResultFinding { schema_version, kind tag, run_id, produced_at_utc, scan_id, scan_version, code_revision, param_hash, data_slice, effect, raw, dsr=null, fdr_q=null }
- Effect { metric, value, p_value, p_value_null, ci_95, n, extra: BTreeMap<String, RawArray> }
- Raw { series: BTreeMap<String, RawArray> }
- RawArray { dtype: Dtype, len, data: Base64Bytes }
- GapAbortedFinding (Phase 1)

From statsmodels reference (RESEARCH Code Examples lines 641-687):
- log_returns(close[]) = ln(close[t] / close[t-1])
- biased_acf(x, max_lag): denom = sum((x-mean)^2); out[0]=1; out[k] = sum((x-mean) * (x_shifted-mean)) / denom (NOT (n-k))
- ljung_box_q[h] = n * (n+2) * cumulative_sum(acf[k]^2 / (n-k)) for k in 1..=h
- p_value[h] = 1 - ChiSquared::new(h as f64).cdf(q[h-1])
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ljung-Box pure kernels — log_returns + biased_acf + ljung_box_q_and_p</name>
  <files>crates/miner-core/src/scan/ljung_box/kernel.rs</files>
  <read_first>
    - crates/miner-core/src/scan/ljung_box/kernel.rs (Plan 01 scaffold — signatures present, bodies unimplemented)
    - .planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md (Code Examples lines 641-687 — the EXACT kernel implementations to copy; note the summation order rule at line 687)
    - .planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md (D3-02 — inline log returns; D3-03 — lags default = min(10, n/5); D3-05 — golden tolerance approach)
    - .planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md (lines 240-285 — kernel pattern with #[cfg(test)] mod tests + #[inline] pure-function rule)
    - crates/miner-core/src/aggregator.rs (lines 235-258 — #[inline] private pure-function template kernel mirrors)
  </read_first>
  <behavior>
    - log_returns_basic: log_returns(&[1.0, 2.7182818284590451, 7.3890560989306495]) returns [1.0, 1.0] within 1e-12.
    - log_returns_empty: log_returns(&[]) returns vec![] (empty input → empty output).
    - log_returns_singleton: log_returns(&[42.0]) returns vec![] (one close → zero returns).
    - log_returns_length_invariant: log_returns(&close_of_n_elements) has length n-1.
    - biased_acf_lag0_is_one: biased_acf(&x, max_lag) returns out where out[0] == 1.0 (lag-0 is 1 by construction per RESEARCH line 660).
    - biased_acf_constant_series: biased_acf(&[5.0; 10], 3) — the denom is 0 (constant series has zero variance). The kernel SHOULD return [1.0, 0.0, 0.0, 0.0] (treat 0/0 as 0 to match statsmodels which special-cases constant series). Test the contract; document the choice in the function's doc-comment.
    - biased_acf_known_input: Construct a deterministic 8-element fixture x = [1.0, 2.0, 3.0, 2.0, 1.0, 2.0, 3.0, 2.0] and assert biased_acf(&x, 3) matches a hand-computed reference within 1e-12 (compute the expected values during plan-writing and embed in the test).
    - biased_acf_length_is_max_lag_plus_one: biased_acf(&x, k).len() == k + 1.
    - ljung_box_q_monotone: For a series with positive autocorrelation, ljung_box_q_and_p().0 is non-decreasing across lags (each term is a non-negative contribution).
    - ljung_box_q_and_p_known_input: Using the same 8-element x above, compute biased_acf(&x, 3), then ljung_box_q_and_p(x.len(), &acf, 3) and assert Q-stats + p-values match a statsmodels 0.14.6 reference within 1e-12. (Plan-writing precomputes these expected values via the reference; if unable to precompute at plan-time, the test uses approximate-equality with tolerance 1e-10 and a comment pointing to the Plan 06 golden test for byte-exact verification.)
    - ljung_box_p_in_unit_interval: For any Q >= 0 and df >= 1, p_value is in [0, 1].
    - ljung_box_invalid_lag_panics: ljung_box_q_and_p(n=10, &acf, 0) should panic via debug_assert (max_lag must be >= 1).
  </behavior>
  <action>
    Implement crates/miner-core/src/scan/ljung_box/kernel.rs per RESEARCH §Code Examples lines 641-687 verbatim — these are the EXACT implementations to copy, not "match the pattern":

    1. `#[inline] pub(super) fn log_returns(close: &[f64]) -> Vec<f64>`:
       ```
       close.windows(2).map(|w| (w[1] / w[0]).ln()).collect()
       ```
       Handles len 0 and len 1 naturally (returns empty Vec). Doc-comment: "log_returns[t] = ln(close[t] / close[t-1])".

    2. `#[inline] pub(super) fn biased_acf(x: &[f64], max_lag: usize) -> Vec<f64>`:
       - Verbatim from RESEARCH lines 652-666: n as f64, mean = sum/n, cent = x - mean, denom = sum(cent*cent). out[0] = 1.0. For k in 1..=max_lag: num = sum(cent[i] * cent[i+k]); out.push(num / denom).
       - Special case: if denom == 0.0 (constant series), set every lag>=1 to 0.0 instead of NaN (matches statsmodels' constant-series handling). Document the choice.

    3. `#[inline] pub(super) fn ljung_box_q_and_p(returns_n: usize, acf: &[f64], max_lag: usize) -> (Vec<f64>, Vec<f64>)`:
       - Verbatim from RESEARCH lines 670-684: n = returns_n as f64; q = vec of length max_lag; acc = 0.0. For k in 1..=max_lag: acc += acf[k] * acf[k] / (n - k as f64); q[k-1] = n * (n + 2.0) * acc. p[k-1] = 1.0 - ChiSquared::new((k as f64)).cdf(q[k-1]) via statrs::distribution::{ChiSquared, ContinuousCDF}.
       - debug_assert!(max_lag >= 1).
       - debug_assert!(acf.len() > max_lag) — caller must have provided enough ACF values.
       - Note the summation order is sequential and cumsum-style (RESEARCH line 687) — this is what makes goldens match statsmodels' np.cumsum.

    4. Add a `#[cfg(test)] mod tests { use super::*; ... }` block with the 11+ behavior tests listed above. For the "known_input" tests, EMBED the precomputed reference values as constants in the test source — the Plan 06 byte-exact golden test will catch any drift.

    Annotate the module with `#![cfg_attr(any(test, debug_assertions), allow(clippy::float_cmp))]` if needed for float-equality assertions in tests (acceptable in test context only; production code uses tolerance comparisons).

    NO unsafe; NO println/eprintln/dbg; NO async; the kernel is pure functions over &[f64].
  </action>
  <verify>
    <automated>cargo test -p miner-core --lib scan::ljung_box::kernel::tests</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p miner-core --lib scan::ljung_box::kernel::tests passes (>=11 tests)
    - `grep -c '#\[inline\]' crates/miner-core/src/scan/ljung_box/kernel.rs` returns >= 3 (one per kernel function)
    - `grep -c 'statrs::distribution::ChiSquared' crates/miner-core/src/scan/ljung_box/kernel.rs` returns >= 1
    - `grep -c 'pub(super)' crates/miner-core/src/scan/ljung_box/kernel.rs` returns >= 3 (kernels are crate-private to scan::ljung_box)
    - `grep -c 'unsafe' crates/miner-core/src/scan/ljung_box/kernel.rs` returns 0
    - cargo clippy -p miner-core -- -D warnings is clean for this file
  </acceptance_criteria>
  <done>
    Three pure kernel functions implemented per RESEARCH spec; 11+ unit tests passing including the known-input regression against precomputed references; statrs::ChiSquared::cdf wired correctly; debug-asserts guard the contracts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LjungBoxScan: Scan impl — full envelope construction per D3-04</name>
  <files>crates/miner-core/src/scan/ljung_box/mod.rs</files>
  <read_first>
    - crates/miner-core/src/scan/ljung_box/mod.rs (Plan 01 scaffold — id+version stubs only)
    - crates/miner-core/src/scan/ljung_box/kernel.rs (Task 1 output — log_returns, biased_acf, ljung_box_q_and_p)
    - crates/miner-core/src/findings/mod.rs (Effect, Raw, RawArray, Base64Bytes, Dtype, ResultFinding, DataSlice with new gap_manifest field, RunId)
    - crates/miner-core/src/findings/sink.rs (FindingSink trait + VecSink for tests)
    - crates/miner-core/src/aggregator.rs (BarFrame columnar shape)
    - .planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md (D3-01 — scan_id@version; D3-02 — inline returns; D3-03 — lags default min(10, n/5); D3-04 — full effect/raw shape lock; D3-22 — cancel polling)
    - .planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md (lines 192-285 — LjungBoxScan impl + algorithm-doc header pattern from gap.rs:157-187)
    - .planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md (Pattern 3 lines 351-411 — Scan impl sketch; Pitfall 5 lines 579-587 — NEVER call sink.flush inside Scan::run)
  </read_first>
  <behavior>
    - LjungBoxScan_id_and_version: scan.id() == "stats.autocorr.ljung_box"; scan.version() == 1.
    - LjungBoxScan_finding_fields: scan.finding_fields() returns ScanFindingShape { effect_extra_keys: &["lags", "q_stats", "p_values", "acf"], raw_series_keys: &["returns", "timestamps_ms"] }.
    - LjungBoxScan_param_schema: scan.param_schema() returns a JSON-Schema-shaped Value with `properties.lags` documented as integer (the schema is hand-rolled per RESEARCH §Pattern 3 lines 354-365).
    - LjungBoxScan_default_lags: With BarFrame of 256 closes (255 returns) and resolved_params = {}, the scan resolves lags to min(10, 255 / 5) = 10 (D3-03).
    - LjungBoxScan_explicit_lags: With resolved_params = {"lags": 5}, the scan uses 5.
    - LjungBoxScan_invalid_lags_low: With resolved_params = {"lags": 0}, Scan::run returns Err(ScanError::Compute(_)) — the engine MAY translate to PreflightCode::InvalidParameter at the boundary (Plan 04 Task 3 wires this); the scan itself just rejects via ScanError per D3-03.
    - LjungBoxScan_invalid_lags_high: With resolved_params = {"lags": 999} on a 256-close BarFrame (255 returns), Scan::run returns Err(ScanError::Compute(_)) — lags >= n is invalid.
    - LjungBoxScan_emits_one_result: On a deterministic 256-close BarFrame, Scan::run writes EXACTLY ONE Finding::Result to a VecSink — assert sink.bytes() parses into exactly one Finding::Result(_).
    - LjungBoxScan_result_envelope_shape: The emitted ResultFinding has scan_id="stats.autocorr.ljung_box", scan_version=1, effect.metric="ljung_box_q", effect.n == 255 (returns length), effect.value == q_stats[lags-1], effect.p_value == p_values[lags-1], effect.ci_95 == None, effect.extra contains exactly the keys {"lags", "q_stats", "p_values", "acf"}, raw.series contains exactly the keys {"returns", "timestamps_ms"}, dsr == None, fdr_q == None.
    - LjungBoxScan_extras_have_correct_lengths: effect.extra["q_stats"].len == lags; effect.extra["p_values"].len == lags; effect.extra["acf"].len == lags + 1 (includes lag-0).
    - LjungBoxScan_raw_series_lengths: raw.series["returns"].len == 255 (n-1); raw.series["timestamps_ms"].len == 255 (one per return; per-return timestamp == ts_open_utc[t]'s epoch milliseconds for t in 1..n).
    - LjungBoxScan_cancellation: With cancel.load() == true at entry to Scan::run, the scan returns Ok(()) early without writing any finding. Verified by setting an Arc<AtomicBool>(true) and asserting sink.bytes() is empty after the call.
    - LjungBoxScan_no_sink_flush: Scan::run NEVER calls sink.flush() (Pitfall 5). Verified by inspecting the source (`grep -c 'flush' crates/miner-core/src/scan/ljung_box/mod.rs` returns 0).
  </behavior>
  <action>
    Implement crates/miner-core/src/scan/ljung_box/mod.rs:

    1. `pub struct LjungBoxScan;` (unit struct).

    2. `impl Scan for LjungBoxScan { ... }`:
       - id() returns "stats.autocorr.ljung_box" (the &'static str per D3-17).
       - version() returns 1.
       - param_schema() returns a hand-rolled serde_json::Value per RESEARCH Pattern 3 lines 354-365: `{"$schema": "http://json-schema.org/draft-07/schema#", "type": "object", "properties": {"lags": {"type": "integer", "minimum": 1, "description": "Number of lags for the Q-statistic; defaults to min(10, n/5)"}}, "additionalProperties": false}`.
       - finding_fields() returns `ScanFindingShape { effect_extra_keys: &["lags", "q_stats", "p_values", "acf"], raw_series_keys: &["returns", "timestamps_ms"] }`.
       - run(&self, ctx, req, sink):
         a) Cancel-poll at entry per RESEARCH Pattern 4 line 397: `if ctx.cancel.load(Ordering::Relaxed) { return Ok(()); }`. Use std::sync::atomic::Ordering.
         b) Extract lags from req.resolved_params: if the Value has key "lags", parse as i64; else default to min(10, returns.len() / 5). Validate 1 <= lags < returns.len(); on invalid, return Err(ScanError::Compute(format!("invalid lags={lags} for n={n}"))).
         c) Compute returns = log_returns(&ctx.bars.close); n = returns.len(); abort if n < 2.
         d) Compute acf = biased_acf(&returns, lags); (q_stats, p_values) = ljung_box_q_and_p(n, &acf, lags).
         e) Build RawArray helpers for the four extra arrays and the two raw.series arrays. Use existing miner_core::findings::RawArray::from_f64_slice (verify the constructor exists; if not, build the Base64Bytes shape per the existing Phase 1 pattern in findings/mod.rs).
         f) For timestamps_ms: convert ctx.bars.ts_open_utc[1..] (skipping the first bar which has no return) to epoch milliseconds via DateTime::timestamp_millis. Store as RawArray with Dtype::I64.
         g) Construct Effect { metric: "ljung_box_q".to_string(), value: q_stats[lags-1], p_value: Some(p_values[lags-1]), p_value_null: None, effect_size: None, ci_95: None, n: n as u64, extra: BTreeMap from the four extras }.
         h) Construct Raw { series: BTreeMap from the two raw arrays }.
         i) Construct ResultFinding via the existing Phase 1 struct constructor; data_slice carries ctx-derived TimeRange equal to req.sub_range; data_slice.gap_manifest = ctx.gap_manifest.cloned() (the engine sets this for continuous_only; None for strict success path per D3-12).
         j) Call sink.write_envelope(&Finding::Result(result_finding))?; return Ok(()).
         k) DO NOT call sink.flush() (Pitfall 5).

    3. Add an algorithm-walk doc-comment at the top of LjungBoxScan per gap.rs:158-186 analog: numbered steps (1) log-returns, (2) biased ACF, (3) Q-stat cumulative sum, (4) chi-squared p-value, (5) envelope construction.

    4. Add a #[cfg(test)] mod tests block with the 12+ behavior tests listed above. Tests build a deterministic 256-close BarFrame in a helper fixture function `fn ar1_bar_frame_seeded(n: usize, seed: u64) -> BarFrame` (use a simple linear-congruential PRNG seeded by `seed` to produce deterministic closes; document the formula in the helper's doc-comment).

    Remove the #[allow(dead_code, unused_variables)] added in Plan 01.

    NO unsafe; NO println/eprintln/dbg; NO sink.flush; BTreeMap discipline (effect.extra and raw.series are both BTreeMap-typed per Phase 1).
  </action>
  <verify>
    <automated>cargo test -p miner-core --lib scan::ljung_box::tests</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p miner-core --lib scan::ljung_box::tests passes (>=12 tests)
    - `grep -c 'fn flush\|sink.flush' crates/miner-core/src/scan/ljung_box/mod.rs` returns 0 (Pitfall 5 gate)
    - `grep -c 'ctx.cancel.load' crates/miner-core/src/scan/ljung_box/mod.rs` returns >= 1
    - `grep -c '"ljung_box_q"' crates/miner-core/src/scan/ljung_box/mod.rs` returns >= 1
    - `grep -c 'stats.autocorr.ljung_box' crates/miner-core/src/scan/ljung_box/mod.rs` returns >= 1
    - cargo clippy -p miner-core -- -D warnings is clean for this file
  </acceptance_criteria>
  <done>
    LjungBoxScan: Scan impl is complete; cancellation polled at entry; lags default + validation per D3-03; full effect/raw envelope per D3-04; no sink.flush (Pitfall 5); 12+ unit tests passing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: engine::run_one facade — preflight, framing, dry-run, gap-policy, scan dispatch, cancellation</name>
  <files>crates/miner-core/src/engine/mod.rs</files>
  <read_first>
    - crates/miner-core/src/engine/mod.rs (Plan 01 scaffold — signature; Plan 03 added pub use gap_policy::*)
    - crates/miner-core/src/engine/{preflight,framing,param_hash,gap_policy}.rs (Plan 03 — every helper run_one calls)
    - crates/miner-core/src/scan/registry.rs (Plan 02 — Registry::get + bootstrap)
    - crates/miner-core/src/scan/ljung_box/mod.rs (Task 2 — LjungBoxScan working impl)
    - crates/miner-core/src/cache.rs (BarCache::get_or_build signature + AggParams shape)
    - crates/miner-core/src/gap.rs (GapDetector::detect signature)
    - crates/miner-core/src/findings/mod.rs (Finding::DryRun + GapAborted shapes; RunSummary + PerScanCounts)
    - crates/miner-core/src/findings/sink.rs (FindingSink + VecSink for tests)
    - crates/miner-core/src/error/codes.rs (MinerError, WireError)
    - crates/miner-core/src/config/mod.rs (MinerConfig — Plan 04 reads cache_root + bar_cache_root)
    - .planning/phases/03-scan-engine-facade-cli/03-CONTEXT.md (D3-08, D3-10, D3-11, D3-12, D3-21, D3-22, D3-23 — every emission rule)
    - .planning/phases/03-scan-engine-facade-cli/03-PATTERNS.md (lines 288-360 — run_one algorithm walk + cache.rs:519-573 analog)
    - .planning/phases/03-scan-engine-facade-cli/03-RESEARCH.md (System Architecture Diagram lines 142-191 — the EXACT 7-step run_one body)
  </read_first>
  <behavior>
    - run_one_preflight_unknown_scan: With req.scan_id == "nope", req.version == 1, the function returns Ok(RunOutcome::PreflightFailed) and writes ZERO findings to the sink. A WireError is returned via a side channel (parameter or Result variant; design decision). Actually per RESEARCH §System Architecture line 147-148: PreflightFailed has stdout empty (no RunStart), stderr gets the WireError. Design: run_one returns Result<RunOutcome, MinerError> where MinerError carries the WireError on PreflightFailed; the CLI maps MinerError to exit 1 + emit_to_stderr.
    - run_one_preflight_emits_no_run_start: When preflight fails, run_one does NOT emit a RunStart envelope. Stdout stays empty; only stderr gets the structured WireError (the caller is responsible for emitting that to stderr — Plan 05 wires this).
    - run_one_strict_with_gaps: Given a GapManifest with one gap intersecting the requested range, gap_policy=Strict, req.scan_id="stats.autocorr.ljung_box@1": run_one emits exactly one RunStart, one Finding::GapAborted carrying the full manifest (its data_slice.range == the user-requested window per CONTEXT D3-11), one RunEnd. RunEnd.summary.gap_aborted_emitted == 1, results_emitted == 0. Returns Ok(RunOutcome::Ok).
    - run_one_strict_zero_gaps: Given an empty GapManifest, Strict policy: emits one RunStart, one Result (from LjungBoxScan), one RunEnd. The Result's data_slice.gap_manifest == None per CONTEXT D3-12 ("strict provides no manifest in the success path"). RunEnd.summary.results_emitted == 1.
    - run_one_continuous_only_zero_gaps: ContinuousOnly + empty manifest: emits one RunStart, one Result, one RunEnd. The Result's data_slice.gap_manifest == Some(GapManifest { gaps: vec![] }) per CONTEXT D3-12 fast path.
    - run_one_continuous_only_with_gaps: ContinuousOnly + manifest with one gap splitting the requested range into two sub-ranges: emits one RunStart, TWO Result findings (one per sub-range), one RunEnd. Each Result's data_slice.range is the sub-range; data_slice.gap_manifest is the FULL manifest cloned into each Result. RunEnd.summary.results_emitted == 2.
    - run_one_continuous_only_gap_consumes_range: ContinuousOnly + manifest with a gap covering the requested range entirely: emits one RunStart, ZERO Result findings, one RunEnd. RunEnd.summary.results_emitted == 0. RunOutcome::Ok still (no error; just no findings).
    - run_one_dry_run: With req.dry_run == true (a field added to ScanRequest by Task 3 if not already from Plan 02; OR a parallel parameter to run_one — design decision): emits one RunStart, one Finding::DryRun, one RunEnd. The DryRunFinding carries resolved_params = req.resolved_params, planned_data_slice = the requested window, estimated_findings_count = 1 (Phase 3 LjungBox is single-shot; Phase 5's sweep dry-run will return a richer estimate). RunEnd.summary.results_emitted == 0 (Pitfall 3 invariant: DryRun does NOT increment results_emitted).
    - run_one_cancel_at_entry: With cancel = Arc::new(AtomicBool::new(true)) at the start of run_one: the function still emits a RunStart but the scan body is short-circuited; emits one RunStart, ZERO Result/GapAborted, one RunEnd. (The cancellation flag is checked AFTER preflight+framing-open per RESEARCH Pattern 4 — RunStart is always emitted for audit; the scan body is what's cancelled.)
    - run_one_cancel_mid_scan: With cancel = Arc::new(AtomicBool::new(false)) initially, then setting it to true after RunStart but before scan body: this requires a test scaffold that spawns a thread to toggle the flag. For unit testing, use a custom "cancellation-on-call" sink that toggles the flag in its write_envelope after seeing RunStart. Verify only RunStart + RunEnd appear; scan body did not emit.
    - run_one_param_hash_in_result: The emitted Result.param_hash equals param_hash(&req.resolved_params).unwrap() — verified by computing the hash independently and comparing.
    - run_one_run_id_consistency: RunStart.run_id == every Result's run_id == RunEnd.run_id (same RunId across all envelopes in one run).
    - run_one_clock_isolation: RunStart.started_at_utc <= every Result.produced_at_utc <= RunEnd.ended_at_utc (timestamps come from Utc::now() at framing/per-finding boundaries — never inside Scan::run kernel per CONTEXT D3-23).
  </behavior>
  <action>
    Implement crates/miner-core/src/engine/mod.rs::run_one per RESEARCH §System Architecture Diagram lines 142-191:

    1. Signature: `pub fn run_one<R: Reader>(req: &ScanRequest, cfg: &MinerConfig, reader: &R, sink: &mut dyn FindingSink, cancel: Arc<AtomicBool>) -> Result<RunOutcome, MinerError>`.

    2. **Step 1 — Preflight (no RunStart yet)**:
       - Build the Registry via scan::registry::bootstrap() (one allocation; cheap because static contents).
       - Call preflight::resolve_scan_id_at_version on the scan_id@version field of req (NOTE: req.scan_id and req.version are already split — confirm against Plan 02 ScanRequest field shape; if combined, split here). Or: the CLI (Plan 05) splits before calling run_one. Decision: ScanRequest carries scan_id + version separately (already done in Plan 02). run_one calls `registry.get(&req.scan_id, req.version).ok_or_else(|| MinerError::Preflight(WireError::preflight(UnknownScan, ...)))`. On Err, return Err(MinerError::Preflight(...)) WITHOUT emitting RunStart. Caller maps to exit 1.
       - Validate resolved_params against scan.param_schema() (Plan 04 skips JSON Schema validation if it's heavyweight; the typed-fallback parse in parse_params_kv plus LjungBoxScan's internal lags-range check is sufficient for Phase 3 — document this in a doc-comment). For Phase 4 scans the engine MAY add jsonschema validation against scan.param_schema() at this point.

    3. **Step 2 — Framing-open**:
       - run_id = RunId::new(); started = Utc::now() (the FIRST clock read; CONTEXT D3-23).
       - Build the RunStart via framing::build_run_start(req, run_id, started, miner_core::CODE_REVISION).
       - sink.write_envelope(&RunStart_finding)?.

    4. **Step 3 — Dry-run short-circuit**:
       - If req.dry_run (add this field to ScanRequest if not already there from Plan 02 — likely needed; check Plan 02 ScanRequest fields). If true: build a Finding::DryRun(DryRunFinding { run_id, produced_at_utc: Utc::now(), request: <reuse RunStart.request value>, resolved_params: req.resolved_params.clone(), planned_data_slice: DataSlice { range: req.window.into_time_range(), gap_manifest_ref: None, gap_manifest: None }, estimated_findings_count: 1 }). sink.write_envelope(...). Jump to Step 7.

    5. **Step 4 — Gap detection**:
       - manifest = GapDetector::detect(reader, &req.instrument, req.side, req.window).map_err(|e| MinerError::Reader(e))? — handle the reader's associated Error type via a `R::Error: Into<MinerError>` bound, OR wrap it via `MinerError::ReaderGeneric(format!("..."))`. Pick the simpler path; verify against the existing MinerError shape.
       - dispatch = gap_policy::dispatch(&manifest, req.window, req.gap_policy).

    6. **Step 5 — Dispatch result**:
       - GapDispatch::Aborted(m): build Finding::GapAborted(GapAbortedFinding { ... carries m clone, schema_version, run_id, produced_at_utc, scan_id@version, code_revision, param_hash=req.param_hash, data_slice with range=req.window.into_time_range() per CONTEXT D3-11, dsr=null, fdr_q=null }). sink.write_envelope. Increment summary.gap_aborted_emitted. Jump to Step 6.
       - GapDispatch::SubRanges(sub_ranges): For each sub_range, build a per-sub-range ScanRequest::new_for_sub_range(req, sub_range) per Pitfall 4. Cancel-poll BEFORE each sub-range. Load bars: `let bars = BarCache::new(&cfg.bar_cache_root).get_or_build(reader, AggParams { symbol: &req.instrument, side: req.side, tf: req.timeframe, range: ClosedRangeUtc { start: sub_range.start, end: sub_range.end }, calendar: <reader.trading_calendar()> }).map_err(MinerError::Cache)?`. Build ScanCtx { bars: &bars, gap_manifest: <conditional per ContinuousOnly>, run_id, code_revision: miner_core::CODE_REVISION, cancel: cancel.clone() }. Call scan.run(&ctx, &per_sub_request, sink) — if it returns Err(ScanError::Cancelled), break out of the loop cleanly. If it returns Err(ScanError::Compute(_) | Kernel(_)), construct a Finding::ScanError envelope and emit it; increment summary.errors_emitted; CONTINUE the loop (D-05: mid-run errors are findings, sweep continues). On Ok(()) from Scan::run, increment summary.results_emitted (Plan 04 trusts Scan::run to have emitted exactly one or zero results; counters increment per emission tracked via the sink — for unit testing, use a counting sink wrapper).
       - For ContinuousOnly with sub_ranges, ctx.gap_manifest = Some(&manifest); for Strict success path (zero gaps), ctx.gap_manifest = None per CONTEXT D3-12.
       - Counter discipline: Plan 04 implements a tiny "CountingSink" adapter pattern inside run_one — wrap the user-provided &mut dyn FindingSink with a counter that intercepts each emit and bumps the right summary field; pass &mut wrapper into Scan::run. After Scan::run returns, harvest counts. Document the choice in a code comment.

    7. **Step 6 — Framing-close**:
       - ended = Utc::now() (the LAST clock read).
       - Build RunEnd via framing::build_run_end(run_id, started, ended, summary).
       - sink.write_envelope(&run_end_finding)?.
       - sink.flush()? — THIS is the one and only place sink.flush is called (Pitfall 5: scans never flush; facade flushes once).

    8. **Step 7 — Return**:
       - If summary.errors_emitted > 0: Ok(RunOutcome::HadScanErrors).
       - Else: Ok(RunOutcome::Ok).

    9. Add a #[cfg(test)] mod tests block with the 12+ behavior tests listed in <behavior>. Tests build:
       - A `FakeReader` (test-only impl of Reader returning a fixed BarFrame and a configurable GapManifest) — declare in #[cfg(test)] mod fakes.
       - A `CountingSink` wrapper around VecSink that lets the test inspect the per-envelope count.
       - Deterministic ScanRequest fixtures.

    Remove any #[allow(dead_code)] from Plan 01.

    NO unsafe; NO println/eprintln (use tracing::info/warn at state transitions per cache.rs:592-598 analog); BTreeMap-only in summary.per_scan; clock reads ONLY in run_one (build_run_start and build_run_end accept clock values as parameters per Plan 03 Task 1).
  </action>
  <verify>
    <automated>cargo test -p miner-core --lib engine::tests</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p miner-core --lib engine::tests passes (>=12 tests)
    - `grep -c 'sink.flush' crates/miner-core/src/engine/mod.rs` returns 1 (exactly one flush, after RunEnd — Pitfall 5 gate)
    - `grep -c 'Utc::now' crates/miner-core/src/engine/mod.rs` returns >= 2 (started + ended; CONTEXT D3-23: clock reads only at framing boundaries inside run_one, NOT inside Scan::run kernel)
    - `grep -c 'cancel.load' crates/miner-core/src/engine/mod.rs` returns >= 2 (at entry + before each sub-range)
    - `grep -c 'preflight::resolve_scan' crates/miner-core/src/engine/mod.rs` returns >= 1
    - `grep -c 'framing::build_run' crates/miner-core/src/engine/mod.rs` returns >= 2 (build_run_start + build_run_end)
    - `grep -c 'gap_policy::dispatch' crates/miner-core/src/engine/mod.rs` returns >= 1
    - cargo clippy -p miner-core -- -D warnings is clean
    - cargo build -p miner-core (full crate compiles; Plan 05 picks up from here)
  </acceptance_criteria>
  <done>
    run_one is fully wired; every behavior test passes (preflight failure, strict-with-gaps, strict-zero-gaps, continuous_only with/without gaps, dry-run, cancel-at-entry, cancel-mid-scan, run_id consistency, clock-isolation, param_hash in result); RunSummary counts are correct per OUT-04 SC-3a..SC-3e + D3-12 + Pitfall 3 + Pitfall 5.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scan kernel → FindingSink | Scans control what bytes hit stdout; must respect Pitfall 5 (no flush) |
| Reader → engine | reader errors must propagate via MinerError without leaking unstructured strings |
| Cancel token → scan body | cooperative; Scan::run must poll between findings (Phase 4 rolling scans) — Phase 3 polls once at entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | Ljung-Box kernel correctness vs statsmodels | mitigate | Kernel unit tests assert against precomputed reference values within 1e-12; Plan 06 byte-exact golden test pins the envelope-level invariant against statsmodels 0.14.6. |
| T-03-04-02 | Repudiation | run_one summary counters incorrect | mitigate | Behavior tests assert summary.results_emitted / errors_emitted / gap_aborted_emitted per code path. Pitfall 3 (DryRun does NOT increment results_emitted) covered by run_one_dry_run test. |
| T-03-04-03 | Denial of Service | scan kernel panics on edge inputs | mitigate | debug_asserts in ljung_box_q_and_p; LjungBoxScan validates lags range and returns ScanError::Compute on out-of-bounds; n<2 returns gracefully without emitting (or returns Err — test pins the choice). |
| T-03-04-04 | Repudiation | DataSlice.gap_manifest inconsistent across policies | mitigate | run_one_continuous_only_with_gaps + run_one_strict_zero_gaps + run_one_continuous_only_zero_gaps tests pin the D3-12 + D3-10 rules separately. |
| T-03-04-05 | Tampering | Clock reads inside scan kernel | mitigate | Test run_one_clock_isolation asserts started_at_utc <= produced_at_utc <= ended_at_utc; Plan 03 Task 1 pinned that framing builders accept clock values as inputs (no internal Utc::now). |
| T-03-04-06 | Information Disclosure | param_hash time-dependent | mitigate | run_one_param_hash_in_result test asserts param_hash matches a freshly-computed hash of req.resolved_params; Pitfall 6 separation pinned. |
</threat_model>

<verification>
- cargo test -p miner-core --lib scan::ljung_box (kernel + scan impl tests, ~23 total)
- cargo test -p miner-core --lib engine (engine + sub-modules + run_one, ~37 total including Plan 03's contributions)
- cargo test -p miner-core --lib (full library test suite green)
- cargo clippy -p miner-core -- -D warnings (clean)
- cargo build -p miner-core (compiles; ready for Plan 05 CLI wiring)
</verification>

<success_criteria>
1. Three Ljung-Box pure kernels match statsmodels reference within 1e-12 on hand-computed inputs
2. LjungBoxScan::run produces a fully-populated Finding::Result with the D3-04 envelope shape (effect.metric, effect.value at max-lag, effect.extra with 4 keys, raw.series with 2 keys, n correct, dsr/fdr_q null)
3. engine::run_one composes preflight + framing + bar-loading + gap-policy + scan-dispatch + cancel-polling + RunSummary build into one synchronous function
4. The 7-step algorithm walk from RESEARCH §System Architecture is implemented step-for-step
5. All five gap-policy emission rules from CONTEXT D3-08..D3-12 produce the expected envelope shapes
6. Pitfall 3 (DryRun does NOT increment results_emitted), Pitfall 4 (sub_range discipline), Pitfall 5 (only facade flushes), Pitfall 6 (param_hash excludes run metadata) all pinned with unit tests
7. >=23 unit tests across the three tasks all green; cargo test -p miner-core --lib passes
</success_criteria>

<output>
Create `.planning/phases/03-scan-engine-facade-cli/03-04-SUMMARY.md` when done. Record: LOC added per file, full `cargo test -p miner-core --lib` output showing all tests green and counting (~50+ total post-Plan-04), one sample emitted ResultFinding JSON (with run_id + timestamps redacted) demonstrating the D3-04 envelope shape, the run_one algorithm walk in 7 numbered bullets matching RESEARCH §System Architecture Diagram.
</output>
