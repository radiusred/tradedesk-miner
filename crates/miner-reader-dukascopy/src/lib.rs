//! Phase 2: implementation forthcoming.
//!
//! Will hold the zstd-CSV reader for `<cache-root>/<SYMBOL>/<YYYY>/<MM>/<DD>_<side>.csv.zst`,
//! producing 1-minute OHLCV bars for the aggregator. Phase 1 ships only this placeholder so
//! the workspace builds cleanly and downstream crates can take a path dependency.

#[doc(hidden)]
pub fn _placeholder() {}
